---
milestone: v1.1
audited: 2026-04-21
status: passed
scores:
  requirements: 28/28
  phases: 4/4
  integration: exercised via real N=2 hardware test runs across all phases
  flows: end-to-end TPC-H SF1/SF10 + multi-GPU parquet scan + GPU↔GPU P2P round-trip + adaptive scan partitioning
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 07-p2p-direct-transfer-adaptive-scan-partitioning
    items:
      - "cucascade convert_gpu_to_gpu cross-stream race (Sirius-side converter override in place; upstream PR candidate, deferred)"
      - "Phase-5/6 regression comparisons deferred per user directive 2026-04-21 ('we don't need to run any comparisons, let's just make sure everything is working, we can optimize later')"
  - phase: 04-cucascade-bump-v1-0-re-integration
    items:
      - "TPC-H Q4 parquet intermittent flake (documented 04-01-SUMMARY.md, did not block any phase)"
deferrals_to_future_milestones:
  - "Upstream cucascade PR for convert_gpu_to_gpu cross-stream race fix"
  - "Phase-5 vs Phase-4 parquet I/O wall-clock regression comparison"
  - "Phase-6 vs Phase-5 single-GPU SF10 regression comparison"
  - "cudaDeviceDisablePeerAccess on explicit teardown (currently rely on CUDA cleanup at process exit)"
  - "Cucascade upstream fix for idisk_io_backend no file-handle cache (research pitfall P1)"
---

# Milestone v1.1 Audit: Multi-GPU Re-integration + Cucascade I/O Migration

## Status: ✅ PASSED

**Score:** 28/28 requirements satisfied across 4/4 phases.
**Integration:** exercised end-to-end on real N=2 hardware (2× RTX 6000 Ada, driver 595.58.03, CUDA 13.2).
**Full unit-test suite:** 979/979 pass (78,789,847+ assertions) as of Plan 07-04 close.

---

## Requirements Coverage (3-source cross-reference)

All 28 v1.1 requirements verified via:
- **REQUIREMENTS.md** traceability — all 28 marked `[x]`
- **Phase SUMMARY.md** `requirements-completed` frontmatter — 28 distinct REQ-IDs enumerated
- **Phase VALIDATION.md** (Phases 5, 6, 7) — hardware evidence captured on N=2 host

### Per-Phase Cross-Reference

| Phase | REQ-IDs in frontmatter | REQUIREMENTS.md state | VALIDATION.md | Status |
|-------|------------------------|-----------------------|---------------|--------|
| 04 | BUMP-01/02/03, PORT-01/02/03/04/05 (8) | all `[x]` | — *(early phase)* | ✅ satisfied |
| 05 | IO-01..11, HYG-01/02 (13) | all `[x]` | 05-06-VALIDATION.md + 05-06-MULTIGPU-VALIDATION.md | ✅ satisfied |
| 06 | MGPU-01..05 (5) | all `[x]` | 06-04-VALIDATION.md | ✅ satisfied |
| 07 | MGPU-06, MGPU-07 (2) | all `[x]` | 07-04-VALIDATION.md | ✅ satisfied |

**Orphans:** None. Every REQ-ID in REQUIREMENTS.md traceability appears in exactly one phase's `requirements-completed` frontmatter.

**Unchecked v1.0 predecessor IDs:** 7 items in REQUIREMENTS.md "v1.0 Requirements (inherited — unmerged, behavior baseline only)" section remain `[ ]` (FOUND-01, FOUND-04, FOUND-06, MEM-04, MEM-05, CUCS-01, CUCS-02). These are by-design pointers to v1.1 MGPU-* IDs that ARE checked — the cross-reference is explicit in the REQUIREMENTS.md text ("→ tracked as **MGPU-XX** in v1.1"). Not gaps.

---

## Phase Summary

### Phase 4 — cuCascade Bump + v1.0 Re-integration *(5 plans, ~5h30m)*

- Submodule bumped `942c0bf → f47de0b` (PRs #96/#100/#103/#104 absorbed)
- 23 v1.0 multi-GPU commits replayed on current `dev` (5 cherry-picks + 4 re-authored against PR #579 `downgrade_executor_config` shape)
- Push-model dispatch + `preferred_device_id` plumbing preserved
- YAML config replaces libconfig++ cleanly
- Test suite verified: 966 cases / 78.8M assertions

### Phase 5 — Cucascade-Backed Parquet I/O Migration *(6 plans, ~65m)*

- `sirius::io::cucascade_datasource` ships in `src/io/`, `supports_device_read() == false`, pinned-host pool from cucascade
- Per-GPU `idisk_io_backend` cache on `SiriusContext` under `rmm::cuda_set_device_raii`
- All `cudf::io::datasource::create(path)` call-sites migrated (parquet + iceberg + prefetched-datasource fallback)
- `grep -rnw 'datasource::create' src/` → zero hits (IO-08 gate)
- `rmm::cuda_stream_default` cleaned at `parquet_scan_task.cpp:468` + every touched file
- TPC-H SF1 correctness preserved; SF10 absolute N=2 numbers captured; regression comparisons deferred per user directive

### Phase 6 — Multi-GPU Gap Closure *(4 plans, ~60m)*

- `SiriusContext::initialize()` fails hard on `num_gpus == 0`; emits 3-line topology startup log
- Device-guard `cudaSetDevice` in both `noexcept` per-thread init callbacks (`gpu_pipeline_executor` + `downgrade_executor`)
- Compute-sanitizer memcheck `ERROR SUMMARY: 0 errors` across multi-GPU + integration test tags on N=2
- GPU↔GPU converter registered in `sirius::converter_registry::initialize()`; forward-leg round-trip passes (return leg deliberately deferred to Phase 7)
- Per-NUMA host memory spaces created via `numa_region_pinned_host_allocator`

### Phase 7 — P2P Direct Transfer + Adaptive Scan *(4 plans, ~2h)*

- **07-01:** `cudaDeviceEnablePeerAccess` loop in `SiriusContext::initialize()` + sticky-error consume pattern
- **07-02:** un-hid `[.][multi_gpu_transfer]` + `[.][mem_04_p2p_transfer]`; FNV-1a checksum integrity assertion on round-trip payloads; Sirius-side P2P converter override in `src/data/sirius_p2p_converter.cpp` works around a cross-stream race in cucascade's `convert_gpu_to_gpu`
- **07-03:** un-hid `[.][mem_05_scan_distribution]`; asymmetric-memory fixture proves 3.08× free-memory ratio produces proportional batch distribution within 10% tolerance; integration TEST_CASE in `test_gpu_execution_locality.cpp` exercises adaptive scan + P2P path
- **07-04:** phase sign-off with 979/979 tests green

---

## Cross-Phase Integration

*(Integration checker spawn skipped — integration was exercised directly via real N=2 hardware test runs at phase-close for each of Phases 5, 6, 7, culminating in the 979/979 full-suite run after Plan 07-03 that covers every cross-phase seam.)*

**Verified end-to-end flows on N=2 hardware:**

1. **Parquet scan → cucascade_datasource → cucascade io_backend → per-GPU memory**
   - Source: `src/op/scan/parquet_scan_task.cpp` (HYG-01 stream cleanup), `sirius_parquet_metadata_scan_operator.cpp`, `iceberg_scan_task.cpp`, `prefetched_data_source` fallback
   - All routes the same registry lookup in `SiriusContext::get_io_backend_for_device()`
   - Evidence: IO-11 per-GPU instance log + compute-sanitizer 0 errors on parquet-scan tags

2. **Multi-GPU task dispatch → NUMA-aware downgrade → peer-enabled GPU↔GPU transfer**
   - `task_creator` preferred_device_id → `management_eventloop` push-dispatch → per-GPU `downgrade_executor` with `preferred_numa_node` → cucascade `any_memory_space_in_tier_with_preference` → Sirius-side `sirius_p2p_converter_factory` for peer copies
   - Evidence: Plans 06-04 + 07-02 VALIDATION.md + 977-test suite pass

3. **Adaptive scan partitioning + P2P path**
   - `duckdb_scan_executor::select_target_gpu` memory-proportional distribution → per-GPU reservation → P2P converter on cross-GPU movement
   - Evidence: Plan 07-03 integration TEST_CASE + 3.08× ratio test

**Startup log audit:** All three expected log sections present in a live `[multi_gpu_foundation]` run:
- "SiriusContext: {N} GPUs, {M} NUMA nodes, GPU↔NUMA: ..."
- "SiriusContext: {X} host memory space(s) created for {Y} NUMA node(s)"
- "SiriusContext: P2P enabled {i} -> {j} (MGPU-06)" (one line per enabled pair)

---

## Tech Debt (Non-Blocking)

| Phase | Item | Type | Action |
|-------|------|------|--------|
| 07 | cucascade `convert_gpu_to_gpu` cross-stream race | Upstream bug | Sirius override in place; file upstream PR (deferred) |
| 07 | `cudaDeviceDisablePeerAccess` not called on teardown | Convention | CUDA cleans at process exit; revisit if tests grow longer |
| 05 | cucascade `pipeline_io_backend` no file-handle cache (research pitfall P1) | Upstream | File upstream if profiling shows hotspot |
| 05/06 | Phase-N vs Phase-(N-1) wall-clock regression deferrals | Measurement | Resume when optimization work kicks in |
| 04 | TPC-H Q4 parquet intermittent flake | Flake | Documented; did not block any phase |

---

## Nyquist Compliance

Skipped per `workflow.nyquist_validation` configuration (`nyquist_validation: false`).

Phases 5, 6, 7 each produced `VALIDATION.md` artifacts as hardware-evidence captures independent of Nyquist. Phase 4 did not produce a VALIDATION.md (predates the pattern) but its SUMMARY files cover equivalent evidence.

---

## Audit Verdict

**PASSED.** All 28 milestone requirements satisfied across 4 phases with hardware evidence on N=2. Tech debt items are all non-blocking; deferrals are explicit with user acknowledgement. Cross-phase integration exercised via real test runs end-to-end.

---

## ▶ Next Up

`/gsd:complete-milestone v1.1` — archive milestone and prepare for v1.2.

*Tech-debt items above should be captured in backlog / v1.2 planning — none block v1.1 closure.*

---
*Milestone v1.1 audit completed: 2026-04-21*
