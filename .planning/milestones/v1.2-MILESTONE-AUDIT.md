---
milestone: v1.2
milestone_name: Multi-GPU SQL Pipeline Fix
audited: 2026-04-27
status: tech_debt
scores:
  requirements_satisfied: 8/11 fully + 3/11 partial = 11/11 authored, 8/11 fully verified
  ship_gate_criteria: 5/6 PASS (CRIT-2 PARTIAL — pre-existing [mgpu-audit] SIGSEGV)
  phases: 3/3 plans complete (Phase 8 6/6 + 2 halted gap-closure; Phase 9 4/4; Phase 10 4/4)
  integration: 23/23 cross-phase wiring connections sound; 0 orphans; 0 missing
  flows: 4/4 e2e flows complete (1 informational gap on canonical AUDIT TEST_CASE)
phases_in_scope:
  - 08-multi-gpu-sql-pipeline-fix
  - 09-scan-task-distributor-batch-ownership-affinity
  - 10-table-function-form-gpu-execution-sigsegv-fix
gaps:
  requirements:
    - id: AUDIT-01
      status: partial
      phase: Phase 8
      claimed_by_plans: ["08-03-PLAN.md", "08-05-PLAN.md"]
      completed_by_plans: ["08-03-SUMMARY.md", "08-05-SUMMARY.md"]
      verification_status: passed (with proxy evidence)
      evidence: "Canonical AUDIT TEST_CASE crashes at test_gpu_execution_tpch_mgpu_audit.cpp:200 BEFORE the assertion block (line 245-246) due to pre-existing SIGSEGV in attach_integration_duckdb path. Proxy evidence: SF100 [mgpu-audit] log shows pipeline_task entries on both GPU 0 and GPU 1 (219 entries total, Phase 10-04-VALIDATION.md). Pre-existing failure confirmed on base branch via 10-03 git-stash test."
    - id: AUDIT-02
      status: partial
      phase: Phase 8
      claimed_by_plans: ["08-03-PLAN.md", "08-05-PLAN.md"]
      completed_by_plans: ["08-03-SUMMARY.md", "08-05-SUMMARY.md"]
      verification_status: passed (with proxy evidence)
      evidence: "Same TEST_CASE blocker as AUDIT-01. Proxy evidence: SF100 shows GPU0=42, GPU1=29 unique batch_ids (intersection=0); SF10 shows GPU0=4, GPU1=3 (intersection=0). Phase 9 disjointness REQUIRE fires successfully in tpch_q1_sf10_2gpu (99 assertions PASS)."
    - id: AUDIT-03
      status: partial
      phase: Phase 8
      claimed_by_plans: ["08-03-PLAN.md", "08-05-PLAN.md"]
      completed_by_plans: ["08-03-SUMMARY.md", "08-05-SUMMARY.md"]
      verification_status: passed (with proxy evidence)
      evidence: "AUDIT TEST_CASE has no [.] hide tag (CMakeLists.txt:369), so it IS in the default unit-tests run. The substantive regression gate (Plan 09-03 disjointness REQUIRE at line 271) fires in tpch_q1_sf10_2gpu. Pre-existing SIGSEGV at line 200 prevents the TEST_CASE itself from completing assertions — this is the technical debt."
  integration: []
  flows:
    - flow: "Canonical AUDIT TEST_CASE — [mgpu-audit] per-GPU distribution on TPC-H Q1"
      severity: informational
      breaks_at: "test_gpu_execution_tpch_mgpu_audit.cpp:200 — con->Query SET enable_duckdb_fallback before assertion block"
      attribution: "Pre-existing on base branch (confirmed via 10-03 git-stash + MCP run sequence). Path uses attach_integration_duckdb (DuckDB SF1 data), not the parquet filter translation path that Phase 10-03 fixed."
      proxy_evidence: "SF100 PROCEDURE-form (CALL gpu_execution(...)) PASSES with full per-GPU distribution captured; tpch_q1_sf10_2gpu PASSES with disjointness REQUIRE firing"
tech_debt:
  - phase: 08-multi-gpu-sql-pipeline-fix
    items:
      - "Plans 08-09 and 08-10 HALTED per 08-09-HALT.md — work intentionally inherited by Phase 9. Not a blocker; documented handoff."
  - phase: 09-scan-task-distributor-batch-ownership-affinity
    items:
      - "Plan 09-04 verdict PARTIAL — distributor proven correct at SF100 scale (CRIT-1, CRIT-4, CRIT-6 PASS); CRIT-2 ship-gate (88 SF1 + SF10 unit-test pass) deferred to Phase 10 due to TABLE_FUNCTION-form SIGSEGV. Phase 10 closed CRIT-2 fix objective."
  - phase: 10-table-function-form-gpu-execution-sigsegv-fix
    items:
      - "Plan 10-04 verdict PARTIAL — CRIT-1, CRIT-2 (target tests), CRIT-4, CRIT-6 all PASS; one pre-existing [mgpu-audit] SIGSEGV at test_gpu_execution_tpch_mgpu_audit.cpp:200 prevents 'All tests passed' on the full suite. Failure path is attach_integration_duckdb DuckDB-attach data, structurally separate from the parquet filter translation path Phase 10 fixed. Confirmed pre-existing via 10-03 git-stash + MCP base-branch run."
      - "Phase 10 lacks a standalone phase-level VERIFICATION.md; 10-04-VALIDATION.md serves dual roles. Documentation-only gap; verification work itself is complete."
nyquist:
  scanned: false
  reason: "workflow.nyquist_validation = false in config"
---

# v1.2 Milestone Audit — Multi-GPU SQL Pipeline Fix

**Audited:** 2026-04-27
**Verdict:** `tech_debt` — milestone is functionally complete; one pre-existing canonical-test failure is documented as a Phase-11 candidate.
**Branch:** `feature/single-node-multi-gpu2` (no merge to dev)

## Definition of Done (from ROADMAP)

> "TPC-H SQL queries execute correctly end-to-end on `num_gpus: 2` with pipeline tasks distributed across both GPUs, and the integration test suite catches multi-GPU regressions by default."

The six concrete success criteria from Phase 8's ROADMAP entry serve as the milestone's ship-gate.

| # | Success Criterion | Verdict | Evidence |
|---|-------------------|---------|----------|
| 1 | SF100 TPC-H Q1 num_gpus=2 returns correct results, no `cudaErrorInvalidValue`, matches num_gpus=1 baseline | **PASS** | 10-04-VALIDATION.md: 5.70s wall-clock, 4 canonical rows, byte-identical to 1-GPU baseline (5.45s) |
| 2 | MCP unit-tests exits 0 on num_gpus=2 with 22 SF1 + SF10 Q1/Q6/Q12 green; Q4 parquet flake retry-once | **PARTIAL** | 665/666 pass; the 1 failure (`[mgpu-audit]` SIGSEGV at line 200) is **pre-existing on base branch**, not a v1.2 regression. Both target tests fixed by Phase 10-03 (`filter equality parquet` 31/31, `tpch_q1_sf10_2gpu` 99/99) |
| 3 | Zero net-new `rmm::cuda_stream_default` (HYG-02) | **PASS** | grep count went 41→40 across Phases 8-10 (improvement via Phase 10-03) |
| 4 | AUDIT TEST_CASE asserts pipeline_task ≥ 5 AND scan_batch ≥ 5 on both GPUs; default unit-tests catches regressions | **PASS** (substantive); **PARTIAL** (canonical TEST_CASE blocked by pre-existing SIGSEGV) | Phase 9 Plan 09-03's `std::set_intersection` REQUIRE fires in `tpch_q1_sf10_2gpu` (GPU0=4, GPU1=3, intersection=0); SF100 shows GPU0=42, GPU1=29 disjoint distribution. Canonical AUDIT TEST_CASE itself blocked by pre-existing SIGSEGV at line 200 (before its assertion block) |
| 5 | Pattern 2 idiom grep — `cuda_set_device_raii.*source\|pack.*source_stream\|copy.*target_stream` | **PASS** | 6 matches across 4 fix sites; preserved through Phase 10 |
| 6 | SF100 `[mgpu-audit]` log captures scan_batch distribution + wall-clock | **PASS** | 219 audit entries; GPU0=42, GPU1=29; intersection=0; 5.70s |

**Ship-gate score: 5/6 PASS, 1/6 PARTIAL.** The PARTIAL is on a pre-existing failure unrelated to any v1.2 change.

## Requirements Coverage — 3-Source Cross-Reference

All 11 named REQ-IDs are assigned to Phase 8 in REQUIREMENTS.md. Phases 9 and 10 use synthetic CRIT-1/2/4/6 IDs (aliases for ROADMAP success criteria 1/2/4/6) that are NOT separate REQ-IDs.

| REQ-ID | Phase 8 SUMMARY frontmatter | Phase 8 VERIFICATION | REQUIREMENTS.md | Final Status |
|--------|-----------------------------|----------------------|-----------------|--------------|
| FIX-01 | listed (08-01) | gaps_found→**resolved** (re_verification + commits 93fea6f, ecb96c1) | `[x]` Complete | **satisfied** |
| FIX-02 | listed (08-02, 08-07) | gaps_found→**resolved** (Phase 8 residual closure 93fea6f per-GPU filter translation; Phase 10 owned_stream completes lifecycle) | `[x]` Complete | **satisfied** |
| FIX-03 | listed (08-06) | gaps_found→**resolved** | `[x]` Complete | **satisfied** (live grep 40 ≤ 41 baseline) |
| FIX-04 | listed (08-06) | gaps_found→**resolved** | `[x]` Complete | **satisfied** (MCP build exit 0 across all phase HEADs) |
| TEST-01 | listed (08-04) | resolved | `[x]` Complete | **satisfied** (`RUN_TPCH_MGPU` macro + GENERATE(1,2), 45 call sites) |
| TEST-02 | listed (08-04) | resolved | `[x]` Complete | **satisfied** (`integration-2gpu.yaml` + `acquire_integration_env_for(num_gpus)`) |
| TEST-03 | listed (08-05) | resolved | `[x]` Complete | **satisfied** (Phase 10-04 validation: 22 SF1 queries × {1,2} GPU all green; 665/666 full-suite) |
| TEST-04 | listed (08-05) | resolved | `[x]` Complete | **satisfied** (`tpch_q1_sf10_2gpu` 99/99 assertions; Q6/Q12 authored, gated by env var, env-var passthrough verified) |
| AUDIT-01 | listed (08-03, 08-05) | resolved | `[x]` Complete | **partial** — proxy evidence strong; canonical TEST_CASE blocked by pre-existing SIGSEGV |
| AUDIT-02 | listed (08-03, 08-05) | resolved | `[x]` Complete | **partial** — same as AUDIT-01 |
| AUDIT-03 | listed (08-03, 08-05) | resolved | `[x]` Complete | **partial** — TEST_CASE has no [.] hide tag (default-runnable); substantive regression gate (disjointness REQUIRE) fires |

**Coverage: 8/11 fully satisfied, 3/11 partial. No orphans. No unsatisfied. No FAIL-gate trip.** All REQ-IDs are authored and traceable; the partial trio (AUDIT-01/02/03) is documented as a known pre-existing failure, not a v1.2 regression.

## Cross-Phase Integration

The integration checker (`gsd-integration-checker`) verified 23 cross-phase connections; **0 orphans, 0 missing wiring, 1 informational gap (canonical AUDIT TEST_CASE blocker)**.

Detailed findings in `.planning/INTEGRATION-NOTES.md` not maintained as a separate file — summary captured here:

1. **Phase 8 → Phase 9** — `_batch_gpu_affinity` map (Phase 9) coexists with per-GPU stream pool `_gpu_stream_pools` (Phase 8) in `duckdb_scan_executor`; both reset together at query start. **WIRED.**
2. **Phase 9 → Phase 10** — `preferred_device_id` plumbing and `owned_stream` are structurally orthogonal (different files, no shared state). **NON-INTERFERING.**
3. **Phase 8 residual (93fea6f per-GPU filter translation) → Phase 10 (71fd623 owned_stream)** — Layered correctly. Phase 8 ensures each scalar lands on the right device; Phase 10 ensures the stream handle remains valid when those scalars are eventually freed. C++ reverse-destruction order in `translated_expression` (owned_stream declared BEFORE owned_literals) guarantees correctness. **ADDITIVE.**
4. **TEST-01/02 fixture → Phase 10 fix** — Both `GENERATE(1,2)` iterations now pass; before Phase 10, num_gpus=1 iteration crashed at `filter equality parquet`. **VERIFIED.**
5. **AUDIT-01/02/03 vs. Phase 9 disjointness REQUIRE** — The disjointness REQUIRE (line 271) is structurally OK; the pre-existing crash at line 200 prevents the canonical TEST_CASE from reaching it but the same REQUIRE does fire successfully in `tpch_q1_sf10_2gpu`. **PARTIAL functionality.**
6. **HYG-02 invariant** — Continuously satisfied across phases; improved 41→40 via Phase 10-03 explicit-stream usage replacing `rmm::cuda_stream_default`. **IMPROVED.**
7. **PROCEDURE vs TABLE_FUNCTION dichotomy** — Phase 9 hypothesized H2 (TABLE_FUNCTION-form result-materialization divergence). Phase 10-02 GDB analysis ruled H2 OUT — both forms use the same `GPUExecutionBind/GPUExecutionFunction`. The actual cause was H1 (refined): stream use-after-destroy in parquet filter translation, fixed in 71fd623. **RESOLVED.**

## Tech Debt

| Phase | Items |
|-------|-------|
| 08 | Plans 08-09, 08-10 HALTED per `08-09-HALT.md` — work intentionally inherited by Phase 9 (handoff documented; not a blocker) |
| 09 | Plan 09-04 verdict PARTIAL — distributor goal achieved (SF100 + disjointness PASS); ship-gate CRIT-2 deferred to Phase 10 (closed there) |
| 10 | Plan 10-04 verdict PARTIAL — Phase-10 fix objective met; one pre-existing `[mgpu-audit]` SIGSEGV remains (Phase 11 candidate); no standalone `10-VERIFICATION.md` (10-04-VALIDATION.md serves dual roles — documentation-only gap) |

## Phase 8 Re-Verification Note

Phase 8's `08-VERIFICATION.md` carries `status: gaps_found` with `re_verification: true` and `resolved.residual_closed: true`. The re-verification block credits commits **93fea6f** (sirius — per-GPU filter translation) and **ecb96c1** (cucascade pinned-memory bump). Per `08-11-DIAGNOSIS.md`, the original residual fix-site was NOT in `convert_host_parquet_to_gpu_with_prefetched_data_source` — it was in `sirius_physical_parquet_scan` translating the DuckDB filter expression once at plan-construction time and binding scalars to the planner's current device. The fix builds one translated filter tree per configured GPU at plan time and selects per-task at converter time. Phase 10-03 then closed the stream-lifecycle gap on top of that infrastructure (`owned_stream` in `translated_expression`).

## Open Issue (Phase 11 Candidate)

**`[mgpu-audit]` per-GPU distribution AUDIT TEST_CASE SIGSEGV at line 200**

- File: `test/cpp/integration/test_gpu_execution_tpch_mgpu_audit.cpp:200`
- Path: `con->Query("SET enable_duckdb_fallback = false;")` in `attach_integration_duckdb` block
- Confirmed pre-existing on base branch via 10-03 `git stash; MCP run; git stash pop`
- Out of scope for any v1.2 change (orthogonal to parquet filter translation path)
- AUDIT-01/02/03 are substantively satisfied via SF100 + SF10 proxy evidence in 10-04-VALIDATION.md
- Recommendation: scope a Phase 11 to investigate the `attach_integration_duckdb` SIGSEGV; estimated < 50 LOC fix similar to Phase 10-03 in approach

## Routing

Verdict is `tech_debt`: all 11 REQ-IDs authored, 8/11 fully satisfied + 3/11 partial via proxy evidence, 5/6 ROADMAP success criteria PASS, 1 pre-existing failure documented. The user can choose:

- **Complete v1.2 now** — `/gsd:complete-milestone v1.2` (accepts the documented Phase-11 candidate)
- **Close the residual first** — `/gsd:plan-milestone-gaps` to scope a Phase 11 for the `[mgpu-audit]` SIGSEGV before milestone close

---

*Audited 2026-04-27 against branch HEAD `9275cbf`*
