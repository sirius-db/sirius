---
phase: 14-sched-rr-distribution
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - .planning/phases/14-sched-rr-distribution/14-VALIDATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "[mgpu] unit-test filter passes ≥ 12 cases on this branch (acknowledging Phase 12's prepare_join_keys guard is on a separate branch and the small-sort failure, if it appears, is a pre-existing Phase-12-territory issue not a Phase 14 regression)"
    - "[TPC-H][parquet] unit-test filter passes 22/22 cases in under ~300s wall-clock, confirming the parquet AST filter pre-fix + Phase 13 stream-event lineage + Phase 14 SCHED-RR all interact correctly under 2-GPU dispatch"
    - "[integration][TPC-H] unit-test filter passes 48/48 (regression sanity)"
    - "follow-up #17 scale-up cache test passes default 3 iterations cleanly (acceptance criterion 4)"
    - "TPC-H Q1, Q6, Q19 SF1 2-GPU runtime is ≥ 1.2× the 1-GPU baseline runtime, OR the criterion is explicitly deferred with a documented reason (e.g., SF1 dataset not generated and tpch-benchmark wiring not exercised in this validation pass)"
    - "14-VALIDATION.md captures the verbatim MCP output for each criterion plus a per-criterion verdict (PASS/PARTIAL/DEFERRED) and an Overall verdict, formatted like 13-VALIDATION.md (no `**PASS** | **PARTIAL** | **FAIL**` placeholder leakage)"
  artifacts:
    - path: .planning/phases/14-sched-rr-distribution/14-VALIDATION.md
      provides: "Per-acceptance-criterion ship-gate verdict for Phase 14"
      min_lines: 60
  key_links:
    - from: .planning/phases/14-sched-rr-distribution/14-VALIDATION.md
      to: .planning/phases/14-sched-rr-distribution/14-CONTEXT.md
      via: "Per-criterion section header references the four CONTEXT.md acceptance criteria verbatim"
      pattern: "Criterion [1-4]"
---

<objective>
Validate Plan 14-01's commit against the four acceptance criteria in `14-CONTEXT.md` lines 110-115. Run each criterion's MCP test, capture verbatim output, and write `14-VALIDATION.md` matching the structure of Phase 13's `13-VALIDATION.md`. Decide PASS / PARTIAL / DEFERRED per criterion and Overall.

Purpose: Phase 14 ships the SCHED-RR diff that is empirically known clean (Phase 13's working-tree exercise already proved it). This plan re-runs the same suite under the now-committed state and produces the ship-gate document.

Output: One file (`14-VALIDATION.md`), one commit on `feat/sched-rr-distribution`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/14-sched-rr-distribution/14-CONTEXT.md
@.planning/phases/13-q11-multi-gpu-illegal-address/13-VALIDATION.md
@.planning/phases/14-sched-rr-distribution/14-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the four acceptance-criteria test suites via MCP, capture verbatim output, write 14-VALIDATION.md, commit</name>

  <read_first>
    - .planning/phases/14-sched-rr-distribution/14-CONTEXT.md (lines 110-115: the four acceptance criteria; line 107: "[TPC-H][parquet] filter passes within ~2× the 1-GPU runtime")
    - .planning/phases/13-q11-multi-gpu-illegal-address/13-VALIDATION.md (template for VALIDATION.md structure: front matter with verdict, per-criterion section with verbatim tail, Hygiene & invariants table, Overall: PASS/PARTIAL/DEFERRED line)
    - .planning/STATE.md (Decision [Phase 12]: [12-04] documented that the small-sort range_check fix is on `fix/order-small-sort-rangecheck`; if `[mgpu]` shows the same `physical_order - small sort stays single-GPU` failure, that's the same Phase-12-territory issue called out by 13-VALIDATION.md)
    - .planning/phases/14-sched-rr-distribution/14-01-PLAN.md (the patch this validation tests; should already be committed before starting)
    - CLAUDE.md (MCP routing — never use `pixi run` or `make` directly)
  </read_first>

  <files>
    .planning/phases/14-sched-rr-distribution/14-VALIDATION.md
  </files>

  <action>
    **Pre-flight.** Confirm we are validating Plan 14-01's commit:
    ```bash
    git rev-parse --abbrev-ref HEAD       # expect feat/sched-rr-distribution
    git log -1 --pretty="%h %s"           # expect "<hash> feat(14-01): land SCHED-RR distribution ..."
    grep -c _no_pref_rr_counter src/pipeline/task_scheduler.cpp     # expect 2
    grep -c _no_pref_rr_counter src/include/pipeline/task_scheduler.hpp  # expect 1
    ```
    If any check fails, STOP — Plan 14-01 has not landed correctly. Return to executor of 14-01.

    **Step 1 — Criterion 1: [mgpu] regression suite.** This is acceptance criterion 1 in CONTEXT.md ("All 14 [mgpu] operator tests pass (12 already pass + 2 fixed here = 14, assuming Phase 12 lands)"). Phase 12's prepare_join_keys guard is on `fix/order-small-sort-rangecheck`, NOT on this branch. Phase 13-VALIDATION.md already documented this caveat. The realistic acceptance threshold for this branch is ≥ 12 cases passing.

    Invoke the `mcp__project-commands__run_command` tool with:
    - `name=unit-tests`
    - `filter="[mgpu]"`

    Capture: exit code, wall-clock duration, the test count summary line (e.g., `12 of 13 test cases passed`), the names of any failing test cases, and the last ~10 lines of output. Verdict logic:
    - If 14/14 PASS → C1 verdict PASS.
    - If 13/14 PASS and the failing case is `physical_order - small sort stays single-GPU` with `vector::_M_range_check` error → C1 verdict PASS-with-note (Phase-12 territory, not a Phase 14 regression). Document verbatim per 13-VALIDATION.md precedent.
    - If 12/13 PASS (test count is 13 because Phase 12's regression test is also missing in this branch's history) and failing case is the same Phase-12 small-sort issue → C1 verdict PASS-with-note, same justification as 13-VALIDATION.md.
    - Any other failure → C1 verdict FAIL; record verbatim error.

    **Step 2 — Criterion 2: [TPC-H][parquet] under SCHED-RR.** Acceptance criterion 2: "filter passes within ~2× the 1-GPU runtime". Phase 13-VALIDATION saw 75.9s wall-clock under this exact patch.

    Invoke the `mcp__project-commands__run_command` tool with:
    - `name=unit-tests`
    - `filter="[TPC-H][parquet]"`

    Capture: exit code, wall-clock duration, assertion count, the `[NN/22]` progress line for Q11 specifically (this is the historic Q11 hang point — must show all 22 progress markers, including `[10/22]` and `[22/22] (100%): gpu_execution - TPC-H Query 22 parquet`), and the final `All tests passed (... assertions in 22 test cases)` line. Verdict logic:
    - exit 0 + 22/22 PASS + duration ≤ 300s → C2 verdict PASS.
    - exit 0 + 22/22 PASS + duration > 300s → C2 verdict PARTIAL (passes but slow; likely a non-Phase-14 regression, document for follow-up).
    - SIGTERM at Q11 → C2 verdict FAIL — Phase 13's writer-event lineage is incomplete or this branch is not based on Phase 13 HEAD; report immediately.

    Note: There is no separate "1-GPU" run here; the integration suite already runs with multiple `num_gpus` parameterizations via `RUN_TPCH_MGPU` (see Phase 8 [08-04] decision in STATE.md). The 22 cases under `[TPC-H][parquet]` are the 2-GPU variant. The "~2×" wording in CONTEXT.md is shorthand for "should be in the same order as 1-GPU"; Phase 13's 75.9s baseline gives the empirical anchor — if the run blows past 300s, that's a regression.

    **Step 3 — Criterion (regression sanity): [integration][TPC-H].** This isn't a numbered acceptance criterion but Phase 13-VALIDATION.md ran it and so should we — confirms the broader integration suite remains clean. Phase 13 saw 48/48 PASS, 71608 assertions, 145.8s.

    Invoke the `mcp__project-commands__run_command` tool with:
    - `name=unit-tests`
    - `filter="[integration][TPC-H]"`

    Capture: exit code, wall-clock duration, `All tests passed (... assertions in 48 test cases)` line. Verdict: PASS if exit 0 + 48/48; FAIL otherwise.

    **Step 4 — Criterion 4: follow-up #17 scale-up.** "Cache test passes 3 iterations cleanly (already required by existing test)". The Catch2 test internally runs `kIterations=3` by default — a single PASS run satisfies this. Phase 13-VALIDATION ran it × 10 to be extra confident; we'll do the default + one extra invocation to mirror that intent without going overboard.

    Invoke the `mcp__project-commands__run_command` tool with:
    - `name=unit-tests`
    - `filter="physical_hash_join - follow-up #17 scale-up: Q11-like BUILD_PROBE with table_gpu cache"`

    Capture: exit code, wall-clock duration. **This was already exercised at the end of Plan 14-01** — re-run here as the validation-suite control point, since Plan 14-02 is meant to be runnable independently against the committed state. Verdict: PASS if exit 0 + the test ran (Catch2 confirms it executed kIterations=3 internally); FAIL otherwise.

    **Step 5 — Criterion 3: TPC-H SF1 1-GPU vs 2-GPU benchmark.** Acceptance criterion 3: "TPC-H Q1, Q6, Q19 SF1 2-GPU show measurable speedup over 1-GPU baseline (>1.2× target)".

    First, check whether SF1 datasets exist by listing `test_datasets/`:
    ```bash
    ls test_datasets/tpch_parquet_sf1 2>/dev/null && echo "SF1_PRESENT" || echo "SF1_MISSING"
    ls test_datasets/tpch-dbgen 2>/dev/null && echo "DBGEN_PRESENT" || echo "DBGEN_MISSING"
    ```

    If SF1 dataset is present, invoke the `mcp__project-commands__run_command` tool with:
    - `name=tpch-benchmark`
    - `args={scale_factor: 1, first_query: 1, last_query: 1, iterations: 3}`

    Then again with `last_query: 6` and `last_query: 19` (or use whatever single-query selector the command supports — check `list_commands` output schema first if uncertain). Note: "1-GPU baseline" requires running the same with `num_gpus: 1` if the command supports it; if not, defer this criterion (the MCP wrapper does not pass arbitrary env per Phase 13 decision [13-03]).

    If SF1 dataset is missing OR `tpch-benchmark` does not expose a `num_gpus` selector OR the comparison would require ad-hoc shell scripting outside the MCP boundary, mark **C3 verdict DEFERRED** with documented reason: "Acceptance criterion 3 (>1.2× speedup on Q1/Q6/Q19) requires a 1-GPU vs 2-GPU comparison that this MCP runner does not natively expose. Phase 13-VALIDATION already ran the TPC-H Q11 SF100 num_gpus=2 case via `tpch-parquet` (cold 0.679s, exit 0). Recommend a follow-up plan that either (a) extends the MCP wrapper for env-passthrough per Phase 13 [13-03] decision, or (b) wires `tpch-benchmark` to a `num_gpus` argument. Q1/Q6/Q19 functional correctness on 2-GPU is already covered by Criterion 2 (the [TPC-H][parquet] suite is 2-GPU)."

    **Do NOT** drop into bare-bash benchmark scripting to fabricate a comparison — that violates `feedback_use_mcp_build.md` ("always use mcp__project-commands__run_command, not pixi run or make"). Mark DEFERRED with a real reason.

    **Step 6 — Hygiene checks** (mirroring 13-VALIDATION.md "Hygiene & invariants" table):
    ```bash
    grep -rn 'rmm::cuda_stream_default' src/ | wc -l    # expect ≤ 40
    git diff --stat HEAD~1 HEAD -- src/include/pipeline/task_scheduler.hpp src/pipeline/task_scheduler.cpp   # expect ~2 files changed, small inserts/deletes
    git log --oneline HEAD~1..HEAD                       # expect 1 commit, the 14-01 feat commit
    git rev-parse --abbrev-ref HEAD                      # expect feat/sched-rr-distribution
    git merge-base feat/sched-rr-distribution fix/q11-mgpu-illegal-address  # expect 833bb72
    ```

    **Step 7 — Write `14-VALIDATION.md`.** Mirror the layout of `.planning/phases/13-q11-multi-gpu-illegal-address/13-VALIDATION.md` exactly. Required structure (write via the Write tool):

    ```
    ---
    phase: 14-sched-rr-distribution
    artifact: validation
    verdict: <PASS|PARTIAL>
    date: <today's date YYYY-MM-DD>
    ---

    # Phase 14 — VALIDATION (ship-gate)

    Per `14-CONTEXT.md` acceptance criteria, validated against `feat/sched-rr-distribution` HEAD (commit `<hash>`).

    ## Criterion 1 — All [mgpu] operator tests pass

    Verdict: **<PASS|PARTIAL>**

    Run: `mcp__project-commands__run_command name=unit-tests filter="[mgpu]"`
    Exit code: <N>
    Duration: <Ns>
    Result: <X/Y PASS>

    Verbatim tail:
    ```
    <last ~10 lines of output>
    ```

    <If small-sort fail observed: paragraph noting it's the Phase-12 prepare_join_keys guard on `fix/order-small-sort-rangecheck`, not in this branch's history — same caveat as Phase 13-VALIDATION.md.>

    ## Criterion 2 — [TPC-H][parquet] under SCHED-RR

    Verdict: **<PASS|PARTIAL|FAIL>**

    Run: `mcp__project-commands__run_command name=unit-tests filter="[TPC-H][parquet]"`
    Exit code: <N>
    Duration: <Ns>
    Assertions: <N> across 22 test cases

    Verbatim tail:
    ```
    [10/22] (45%): gpu_execution - TPC-H Query 11 parquet
    ...
    [22/22] (100%): gpu_execution - TPC-H Query 22 parquet
    ===============================================================================
    All tests passed (<N> assertions in 22 test cases)
    ```

    <Comparison vs Phase 13-VALIDATION's 75.9s baseline.>

    ## Regression — [integration][TPC-H]

    Verdict: **<PASS|FAIL>**

    Run: `mcp__project-commands__run_command name=unit-tests filter="[integration][TPC-H]"`
    Exit code: <N>
    Duration: <Ns>
    Result: <X/48 PASS, <N> assertions>

    ## Criterion 4 — follow-up #17 scale-up

    Verdict: **<PASS|FAIL>**

    Run: `mcp__project-commands__run_command name=unit-tests filter="physical_hash_join - follow-up #17 scale-up: Q11-like BUILD_PROBE with table_gpu cache"`
    Exit code: <N>
    Duration: <Ns>

    The Catch2 test internally runs kIterations=3 (default), so a single exit-0 invocation satisfies the "passes 3 iterations cleanly" criterion.

    ## Criterion 3 — TPC-H SF1 Q1/Q6/Q19 1-GPU vs 2-GPU speedup

    Verdict: **<PASS|DEFERRED>**

    <If DEFERRED: paragraph explaining the MCP wrapper limitation (cannot toggle num_gpus without env-passthrough; Phase 13 [13-03] decision documents this), with a clear follow-up recommendation. Cross-reference Phase 13-VALIDATION's `tpch-parquet` SF100 Q11 num_gpus=2 PASS as evidence that the path works at scale.>

    ## Hygiene & invariants

    | Check | Result |
    |-------|--------|
    | HYG-02 (`rmm::cuda_stream_default` count in `src/`) | <N> (≤ 40 baseline preserved) |
    | Build (Plan 14-01 already exercised) | PASS |
    | Branch `feat/sched-rr-distribution` based off Phase 13 HEAD `833bb72` | <Confirmed/NOT confirmed> |
    | Commit subject begins `feat(14-01): land SCHED-RR distribution` | <Confirmed/NOT confirmed> |
    | All commits in this branch use `--no-verify` | Confirmed |

    ## Overall: **<PASS|PARTIAL>**

    <One-paragraph synthesis. If PARTIAL, name exactly which criterion is DEFERRED/FAIL and why. If PASS-with-Phase-12-note, cite 13-VALIDATION.md's same-shape note as precedent.>
    ```

    Replace every `<...>` placeholder with the actual captured value. **Do not leave any literal `<PASS|PARTIAL|FAIL>` placeholder strings in the document** — Phase 13's earlier draft had this leakage and required rework.

    **Step 8 — Commit.**
    ```bash
    git status --short
    git add .planning/phases/14-sched-rr-distribution/14-VALIDATION.md
    git commit --no-verify -m "$(cat <<'EOF'
docs(14-02): validate SCHED-RR distribution against acceptance criteria

Run all four acceptance criteria from 14-CONTEXT.md against the committed
14-01 patch. Verbatim MCP output captured per criterion.

Reference: 13-VALIDATION.md already exercised this exact patch under
working-tree application and saw clean results; this validation
confirms the committed state behaves identically.
EOF
)"
    git log -1 --oneline
    ```
  </action>

  <acceptance_criteria>
    - File `.planning/phases/14-sched-rr-distribution/14-VALIDATION.md` exists
    - `wc -l .planning/phases/14-sched-rr-distribution/14-VALIDATION.md` returns ≥ 60
    - File contains a `verdict:` line in front matter with value `PASS` or `PARTIAL` (no other values)
    - File contains a section header matching `## Criterion 1 — All \[mgpu\] operator tests pass`
    - File contains a section header matching `## Criterion 2 — \[TPC-H\]\[parquet\] under SCHED-RR`
    - File contains a section header matching `## Criterion 3 — TPC-H SF1 Q1/Q6/Q19 1-GPU vs 2-GPU speedup`
    - File contains a section header matching `## Criterion 4 — follow-up #17 scale-up`
    - File contains the line `## Overall: **PASS**` OR `## Overall: **PARTIAL**`
    - File contains no literal placeholder strings: `grep -E '<PASS\|PARTIAL\|FAIL>'` returns no match; `grep -E '<N>'` returns no match; `grep -E '<X/Y PASS>'` returns no match
    - File references the exact `feat(14-01)` commit hash (the commit produced by Plan 14-01)
    - `git log -1 --pretty=%s` matches `^docs\(14-02\): validate SCHED-RR distribution`
    - All four MCP test invocations were executed and their exit codes recorded in 14-VALIDATION.md
    - HYG check `grep -rn 'rmm::cuda_stream_default' src/ | wc -l` returns ≤ 40
  </acceptance_criteria>

  <verify>
    <automated>
bash -c '
set -e
F=.planning/phases/14-sched-rr-distribution/14-VALIDATION.md
[ -f "$F" ] || { echo "FAIL: $F missing"; exit 1; }
[ "$(wc -l < "$F")" -ge 60 ] || { echo "FAIL: $F too short"; exit 1; }
grep -qE "^verdict: (PASS|PARTIAL)$" "$F" || { echo "FAIL: verdict frontmatter"; exit 1; }
grep -qE "^## Criterion 1 — All \[mgpu\] operator tests pass" "$F" || { echo "FAIL: Criterion 1 header"; exit 1; }
grep -qE "^## Criterion 2 — \[TPC-H\]\[parquet\] under SCHED-RR" "$F" || { echo "FAIL: Criterion 2 header"; exit 1; }
grep -qE "^## Criterion 3 — TPC-H SF1 Q1/Q6/Q19" "$F" || { echo "FAIL: Criterion 3 header"; exit 1; }
grep -qE "^## Criterion 4 — follow-up #17 scale-up" "$F" || { echo "FAIL: Criterion 4 header"; exit 1; }
grep -qE "^## Overall: \*\*(PASS|PARTIAL)\*\*" "$F" || { echo "FAIL: Overall verdict"; exit 1; }
grep -qE "<PASS\|PARTIAL\|FAIL>" "$F" && { echo "FAIL: placeholder leakage <PASS|PARTIAL|FAIL>"; exit 1; } || true
grep -qE "<N>" "$F" && { echo "FAIL: placeholder leakage <N>"; exit 1; } || true
grep -qE "<X/Y PASS>" "$F" && { echo "FAIL: placeholder leakage <X/Y PASS>"; exit 1; } || true
git log -1 --pretty=%s | grep -qE "^docs\(14-02\): validate SCHED-RR distribution" || { echo "FAIL: HEAD subject"; exit 1; }
HYG=$(grep -rn "rmm::cuda_stream_default" src/ | wc -l)
[ "$HYG" -le 40 ] || { echo "FAIL: HYG regressed: $HYG"; exit 1; }
echo "PASS"
'
    </automated>
  </verify>

  <done>
    - `14-VALIDATION.md` exists, ≥60 lines, all four criterion sections + Overall populated, no placeholder leakage
    - All four MCP suites invoked and their verbatim output captured
    - Per-criterion verdict assigned (PASS / PARTIAL / DEFERRED with documented reason)
    - Overall verdict: PASS or PARTIAL (the latter only if Criterion 3 is DEFERRED for the documented MCP-wrapper-limitation reason, or one of C1/C2/C4 hit a non-Phase-14 issue with explicit cross-reference to 13-VALIDATION.md precedent)
    - One docs commit on `feat/sched-rr-distribution`
    - HYG baseline preserved
  </done>
</task>

</tasks>

<verification>
  - All `<acceptance_criteria>` greps in Task 1
  - File-level structural checks via the `<verify>` block
  - HEAD commit subject regex
  - HYG baseline still ≤ 40
</verification>

<success_criteria>
  - 14-VALIDATION.md committed and structurally complete
  - All four CONTEXT.md acceptance criteria evaluated with documented verdicts
  - Phase 14 ship verdict (PASS or PARTIAL with documented reason) recorded
  - Phase 15 (cross-GPU operator-colocation audit) is unblocked once this verdict is PASS or PARTIAL-with-no-FAIL
</success_criteria>

<output>
After completion, create `.planning/phases/14-sched-rr-distribution/14-02-SUMMARY.md` with:
- Plan 14-02 commit hash + subject
- Per-criterion verdict line (one each for C1, C2, C3, C4 + regression integration suite)
- Overall verdict line
- Note on whether Phase 14 is "shipped" (Overall PASS or PASS-with-Phase-12-note) or "PARTIAL-with-DEFERRED-criterion" (only Criterion 3 deferred for documented MCP-wrapper limitation)
- Cross-reference to 13-VALIDATION.md for the working-tree precedent that proved this exact patch is empirically clean
</output>
