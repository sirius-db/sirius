---
phase: 08-multi-gpu-sql-pipeline-fix
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/include/data/sirius_converter_registry.hpp
  - src/data/sirius_host_to_gpu_converter.hpp
  - src/data/sirius_host_to_gpu_converter.cpp
  - src/sirius_extension.cpp
  - CMakeLists.txt
autonomous: false

requirements: [FIX-02]

must_haves:
  truths:
    - "FIX-02 probe task runs TPC-H Q1/Q3/Q6/Q12 at SF1 on num_gpus=2 AFTER FIX-01 lands; captures any residual cudaErrorInvalidValue from cuda_memcpy.cu OR confirms clean"
    - "Probe verdict is recorded in .planning/phases/08-multi-gpu-sql-pipeline-fix/08-02-PROBE.md with explicit PASS/FAIL and stack trace if FAIL"
    - "IF probe PASSES: no override code authored; plan SUMMARY documents the audit sites covered (src/pipeline/ + src/op/scan/) and states FIX-02 is discharged by FIX-01's scope"
    - "IF probe FAILS on cached-scan / materialized_columns path: a new file pair src/data/sirius_host_to_gpu_converter.{hpp,cpp} is authored mirroring Pattern 2 (src/data/sirius_p2p_converter.cpp), registered via sirius_converter_registry unregister+register pattern, wired from sirius_extension.cpp after the existing MGPU-06 P2P override registration"
    - "Whichever branch is taken, the probe queries PASS on num_gpus=2 at the end of this plan (no cudaErrorInvalidValue, no cuda_memcpy.cu in log)"
    - "Zero net-new rmm::cuda_stream_default introduced"
    - "MCP build exits 0 after any added files compile"
  artifacts:
    - path: ".planning/phases/08-multi-gpu-sql-pipeline-fix/08-02-PROBE.md"
      provides: "Probe verdict + evidence (commands + log excerpts)"
      contains: "Probe verdict"
    - path: "src/data/sirius_host_to_gpu_converter.cpp"
      provides: "(CONDITIONAL — only if probe FAILS) Pattern 2 host→gpu factory body"
      contains: "sirius_host_fast_to_gpu_factory"
    - path: "src/include/data/sirius_converter_registry.hpp"
      provides: "(CONDITIONAL) unregister + register for host_data_representation -> gpu_table_representation"
      contains: "host_data_representation"
  key_links:
    - from: "Probe verdict"
      to: "Branch selection (A: doc-only; B: author override)"
      via: ".planning/phases/08-multi-gpu-sql-pipeline-fix/08-02-PROBE.md"
      pattern: "Probe (PASS|FAIL)"
    - from: "(if B) sirius_extension.cpp converter-registry init call"
      to: "register host→gpu override"
      via: "call site after existing MGPU-06 P2P override (sirius_extension.cpp:1053-ish)"
      pattern: "register_converter<.*host_data_representation.*gpu_table_representation>"
---

<objective>
Discharge FIX-02: audit every cross-device CUDA memcpy call-site in `src/pipeline/` and `src/op/` that might share the same shape as the v1.1 root-cause bug, and — if the probe demonstrates concrete failure on a path not closed by FIX-01 — author a Sirius-side `host_data_representation → gpu_table_representation` converter override mirroring Pattern 2.

This plan is INTENTIONALLY BIMODAL because the research's Open Question 1 ("Does cucascade's `convert_host_fast_to_gpu` actually race on 2-GPU?") can only be answered empirically after FIX-01 lands. A planner cannot know at plan-phase time whether a new override is needed; the plan must include the probe + conditional branch in-band.

Purpose: Close any remaining cross-device stream-correctness surface that FIX-01 did not cover. Keep the cucascade submodule pin (`f47de0b`) unchanged — all fixes stay Sirius-side. Per Pitfall 5 (scope creep), add NO preemptive overrides; only fix where probe surfaces a failure.

Output: `.planning/phases/08-multi-gpu-sql-pipeline-fix/08-02-PROBE.md` unconditionally. Optionally (branch B) `src/data/sirius_host_to_gpu_converter.{hpp,cpp}`, updated `src/include/data/sirius_converter_registry.hpp`, updated `src/sirius_extension.cpp` registration site, updated `CMakeLists.txt` (add new .cpp).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-multi-gpu-sql-pipeline-fix/08-RESEARCH.md
@.planning/phases/08-multi-gpu-sql-pipeline-fix/08-01-SUMMARY.md
@.planning/phases/08-multi-gpu-sql-pipeline-fix/08-01-REPRODUCTION.md
@.planning/phases/07-p2p-direct-transfer-adaptive-scan-partitioning/07-02-SUMMARY.md
@src/data/sirius_p2p_converter.cpp
@src/include/data/sirius_converter_registry.hpp
@src/sirius_extension.cpp

<interfaces>
<!-- sirius_converter_registry existing pattern for override registration -->
<!-- Source: src/include/data/sirius_converter_registry.hpp:69-82 (EXISTING MGPU-06 P2P override): -->

```cpp
// EXISTING pattern for gpu->gpu (Plan 07-02):
auto const removed = instance_->unregister_converter<
  cucascade::gpu_table_representation,
  cucascade::gpu_table_representation>();
if (!removed) { spdlog::warn("sirius: gpu->gpu converter not previously registered"); }
instance_->register_converter<
  cucascade::gpu_table_representation,
  cucascade::gpu_table_representation>(&sirius::data::sirius_p2p_converter_factory);
spdlog::info("sirius: MGPU-06 P2P converter override registered");
```

<!-- Pattern 2 reference (source of truth for factory body shape): -->
<!-- src/data/sirius_p2p_converter.cpp:33-126 VERBATIM — planner must mirror for host→gpu direction -->
<!-- Key lines reproduced in 08-RESEARCH.md "Pattern 2" section — use that as CONCRETE template. -->

<!-- cucascade converter target (FIX-02 probe target): -->
<!-- cucascade/src/data/representation_converter.cpp:820-856 — convert_host_fast_to_gpu -->
<!-- This is the default registered converter for host_data_representation -> gpu_table_representation. -->
<!-- Sets device_guard at L837; consumes CALLER's stream parameter (which is correct) for -->
<!-- reconstruct_column + batch.flush + cudf::table ctor. Race hypothesis: batch.flush at L851 -->
<!-- under caller's stream may observe a prior scan-task stream completion boundary. -->

<!-- BatchCopyAccumulator + reconstruct_column API visibility check (Tertiary concern): -->
<!-- Plan task must verify via grep whether these are in cucascade public headers -->
<!-- (cucascade/include/) before depending on them; if private, use pack/unpack shape instead. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run FIX-02 probe — execute TPC-H Q1/Q3/Q6/Q12 on num_gpus=2 and record verdict</name>
  <files>.planning/phases/08-multi-gpu-sql-pipeline-fix/08-02-PROBE.md</files>
  <read_first>
    - .planning/phases/08-multi-gpu-sql-pipeline-fix/08-01-SUMMARY.md (confirm FIX-01 landed)
    - .planning/phases/08-multi-gpu-sql-pipeline-fix/08-01-REPRODUCTION.md (initial Q1 outcome)
    - .planning/phases/08-multi-gpu-sql-pipeline-fix/08-RESEARCH.md sections "Open Questions → Q1" and "Common Pitfalls → Pitfall 2"
    - .planning/milestones/v1.1-E2E-VERIFICATION.md (v1.1 failure reproduction commands; Q3/Q6/Q12 failure stacks)
    - test/cpp/integration/test_gpu_execution_tpch.cpp lines 3229-4130 (TPC-H TEST_CASE names for Q1, Q3, Q6, Q12 — needed to construct test invocation)
  </read_first>
  <action>
    Step 1 — preconditions:
    - `mcp__project-commands__run_command build` exits 0.
    - Confirm 08-01 landed via `grep -c '_gpu_stream_pools' src/include/op/scan/duckdb_scan_executor.hpp` >= 1.

    Step 2 — prepare 2-GPU probe config. Create `$TMPDIR/sirius-fix02-probe.yaml` by copying `test/cpp/integration/integration.yaml` and changing `num_gpus` to `2`.

    Step 3 — run the probe. Execute the Catch2 tests for Q1, Q3, Q6, Q12 with `SIRIUS_CONFIG_FILE` overridden to the 2-GPU config and `SIRIUS_LOG_DIR` pointing at `$TMPDIR/fix02-probe-log`. Use `grep -nE 'TEST_CASE.*[qQ](01|1|03|3|06|6|12)\b' test/cpp/integration/test_gpu_execution_tpch.cpp | head -20` to identify exact TEST_CASE tags / names. Invocation pattern:

    ```
    SIRIUS_LOG_LEVEL=info SIRIUS_LOG_DIR=$TMPDIR/fix02-probe-log \
    SIRIUS_CONFIG_FILE=$TMPDIR/sirius-fix02-probe.yaml \
      build/release/extension/sirius/test/cpp/sirius_unittest \
        "tpch_q1" "tpch_q3" "tpch_q6" "tpch_q12"
    ```

    (Exact TEST_CASE names TBD from grep; if tagged, use `[tpch]` with a narrower tag. Pass all four in one invocation if possible; fall back to four separate invocations if needed.)

    ADDITIONAL probe target — exercise the materialized_columns / cached-scan path explicitly. The caching path goes through `convert_host_fast_to_gpu` (cucascade) rather than `convert_host_parquet_to_gpu_with_prefetched_data_source`. Identify a TEST_CASE that exercises materialized caching (grep for `materialized_columns|cache|cached_scan` in test_gpu_execution_tpch.cpp). If no such TEST_CASE exists, add a manual SQL invocation:

    ```
    # Run same query TWICE so the second invocation hits the cache path
    for i in 1 2; do
      SIRIUS_LOG_LEVEL=info SIRIUS_LOG_DIR=$TMPDIR/fix02-probe-log \
      SIRIUS_CONFIG_FILE=$TMPDIR/sirius-fix02-probe.yaml \
        build/release/extension/sirius/test/cpp/sirius_unittest "tpch_q1"
    done
    ```

    Step 4 — analyze logs:
    - `grep -cE 'cudaErrorInvalidValue' $TMPDIR/fix02-probe-log/*.log`
    - `grep -cE 'cuda_memcpy\.cu' $TMPDIR/fix02-probe-log/*.log` (file-match only per Pitfall 2; do not hard-code :42)
    - `grep -cE '\[mgpu-audit\] scan_batch assigned to GPU 1' $TMPDIR/fix02-probe-log/*.log` (sanity: distribution happened)
    - `grep -cE '\[mgpu-audit\] scan_batch assigned to GPU 0' $TMPDIR/fix02-probe-log/*.log` (sanity: other GPU got batches too)

    Step 5 — write `.planning/phases/08-multi-gpu-sql-pipeline-fix/08-02-PROBE.md` with:
    - **Section: Probe Commands** (exact invocations)
    - **Section: Per-query results** (4 rows: Q1, Q3, Q6, Q12 — each with PASS/FAIL, grep counts, result-comparison delta vs num_gpus=1 baseline if PASS)
    - **Section: Probe Verdict** — one of:
      - `Probe PASS` — all 4 queries completed, 0 cudaErrorInvalidValue, 0 cuda_memcpy.cu errors. FIX-02 is discharged by FIX-01's scope; no override needed.
      - `Probe FAIL` — cudaErrorInvalidValue observed on paths [list]; paste the stack trace with file:line; materialized_columns path (if tested) observed error Y/N.
    - **Section: Audit sites covered** — explicitly list: (a) `src/op/scan/duckdb_scan_executor.cpp` (closed by FIX-01), (b) `src/pipeline/pipeline_executor.cpp` + `src/include/pipeline/batch_lock_utils.hpp:48-125` lock_or_prepare_batch (caller's stream is target-bound; no bug shape here), (c) `src/op/scan/duckdb_scan_task.cpp:444-610 make_data_batch` (host-only; safe), (d) `src/op/scan/cpu_source_task.cpp:86-304` (host-only; safe).
    - **Section: Branch decision** — "Proceed to Task 2: doc-only" OR "Proceed to Task 3: author override".

    HARDWARE GATE: If the execution host is single-GPU, DO NOT claim probe PASS or FAIL. Record `Probe DEFERRED — single-GPU host; probe re-runs on verification host during 08-06`. Branch decision defers to 08-06. Task 2 (doc-only) is still written conditionally-correct, acknowledging the deferral.
  </action>
  <verify>
    <automated>test -f .planning/phases/08-multi-gpu-sql-pipeline-fix/08-02-PROBE.md &amp;&amp; grep -qE 'Probe (PASS|FAIL|DEFERRED)' .planning/phases/08-multi-gpu-sql-pipeline-fix/08-02-PROBE.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/08-multi-gpu-sql-pipeline-fix/08-02-PROBE.md` exists
    - File contains exactly one verdict line matching `^Probe (PASS|FAIL|DEFERRED)` (section header OR bullet)
    - File contains audit-sites-covered section enumerating src/op/scan/duckdb_scan_executor.cpp, src/include/pipeline/batch_lock_utils.hpp, src/op/scan/duckdb_scan_task.cpp, src/op/scan/cpu_source_task.cpp
    - File contains branch-decision line indicating which of Task 2 (doc-only) or Task 3 (override) executes next
  </acceptance_criteria>
  <done>Probe verdict recorded with explicit PASS/FAIL/DEFERRED plus audit-sites-covered enumeration plus branch decision.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Branch decision checkpoint — confirm Task 3 path</name>
  <files>.planning/phases/08-multi-gpu-sql-pipeline-fix/08-02-PROBE.md</files>
  <read_first>
    - .planning/phases/08-multi-gpu-sql-pipeline-fix/08-02-PROBE.md (the probe evidence from Task 1)
  </read_first>
  <decision>Based on Task 1's probe verdict, which branch does FIX-02 take?</decision>
  <context>
    The probe determines whether cucascade's built-in `convert_host_fast_to_gpu` races on 2-GPU paths that FIX-01 did not cover. This is Open Question 1 from the research; it can only be answered empirically. Review `.planning/phases/08-multi-gpu-sql-pipeline-fix/08-02-PROBE.md` before selecting.
  </context>
  <options>
    <option id="branch-A">
      <name>Branch A: Doc-only (Probe PASS)</name>
      <pros>No new code = no new risk; fastest path to W3 (tests).</pros>
      <cons>Relies on FIX-01 closing every cross-device path; requires probe to have exercised materialized_columns / cached-scan path (not just parquet cold-scan).</cons>
    </option>
    <option id="branch-B">
      <name>Branch B: Author Sirius-side host→gpu override (Probe FAIL)</name>
      <pros>Closes the specific failure surface with a proven pattern (Pattern 2 from Plan 07-02); keeps cucascade pin unchanged.</pros>
      <cons>~150 lines new code + CMake edit + registration wiring; marginal risk on BatchCopyAccumulator/reconstruct_column public API visibility (Tertiary-confidence item in research).</cons>
    </option>
    <option id="branch-C">
      <name>Branch C: DEFERRED — single-GPU host, re-run probe on verification host during 08-06</name>
      <pros>Honest about hardware availability; avoids false-positive PASS.</pros>
      <cons>FIX-02 stays open until 08-06; if 08-06 discovers failure, must return to this plan and exercise Branch B.</cons>
    </option>
  </options>
  <action>
    Pause execution. Present the probe verdict from 08-02-PROBE.md to the user. Do NOT proceed to Task 3 until the user replies with an explicit branch selection. If Branch A or C is selected, skip Task 3 entirely (no source edits). If Branch B is selected, proceed to Task 3 and author the override.
  </action>
  <verify>
    <automated>grep -qE 'Probe (PASS|FAIL|DEFERRED)' .planning/phases/08-multi-gpu-sql-pipeline-fix/08-02-PROBE.md</automated>
  </verify>
  <done>User has explicitly selected branch-A, branch-B, or branch-C; execution continues per that choice.</done>
  <resume-signal>Select: branch-A, branch-B, or branch-C (plus any notes on which TEST_CASEs to re-run if branch-C).</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: (Branch B ONLY) Author Sirius-side host→gpu converter override and register it</name>
  <files>src/data/sirius_host_to_gpu_converter.hpp, src/data/sirius_host_to_gpu_converter.cpp, src/include/data/sirius_converter_registry.hpp, src/sirius_extension.cpp, CMakeLists.txt</files>
  <read_first>
    - .planning/phases/08-multi-gpu-sql-pipeline-fix/08-02-PROBE.md (MUST read — confirms Branch B was selected; contains stack trace pointing to exact failure site)
    - src/data/sirius_p2p_converter.cpp FULL file (Pattern 2 source of truth — this override mirrors its shape verbatim but for host→gpu direction)
    - src/include/data/sirius_converter_registry.hpp FULL file (registration pattern to mirror)
    - src/sirius_extension.cpp lines 1030-1080 (existing converter-registry initialization block — shows where MGPU-06 P2P override is registered; new override goes right after)
    - cucascade/src/data/representation_converter.cpp lines 820-856 (the function we're overriding — reference shape for what must happen in our body)
    - cucascade/include/cucascade/data/cpu_data_representation.hpp (VERIFY at read-time whether `BatchCopyAccumulator` + `reconstruct_column` are public symbols; if not, use pack/unpack shape fallback described in action below)
    - .planning/phases/08-multi-gpu-sql-pipeline-fix/08-RESEARCH.md "Pattern 2" section (exact code template)
  </read_first>
  <action>
    PRECONDITION: this task ONLY runs if the decision checkpoint (Task 2) selected branch-B. If branch-A or branch-C was selected, SKIP this task entirely (autonomous:false plan; execute-plan will respect the user's checkpoint choice).

    Step 1 — verify cucascade symbol visibility. Run:
    ```
    grep -nE 'class BatchCopyAccumulator|reconstruct_column' cucascade/include/
    ```
    - If BOTH symbols are in public headers: use the research's "Pattern 2 example" body directly (see 08-RESEARCH.md "Pattern 2: Sirius-Side Converter Override" code block).
    - If NOT in public headers: use the fallback PACK/UNPACK shape (mirrors sirius_p2p_converter_factory's cudf::pack → cudf::unpack pattern, substituting `cudaMemcpyHostToDevice` for `cudaMemcpyPeerAsync`). The fallback body is structurally simpler — it packs the source host table via manual column serialization to a flat buffer, H2D-copies under target device guard + target stream, then unpacks on GPU. See `src/data/sirius_p2p_converter.cpp` L64-125 for the pack/copy/unpack shape.

    Step 2 — create header `src/data/sirius_host_to_gpu_converter.hpp`:
    ```cpp
    // src/data/sirius_host_to_gpu_converter.hpp
    #pragma once
    #include <cucascade/data/idata_representation.hpp>
    #include <cucascade/memory/memory_space.hpp>
    #include <rmm/cuda_stream_view.hpp>
    #include <memory>

    namespace sirius::data {

    // FIX-02 (v1.2) Sirius-side override for host_data_representation -> gpu_table_representation.
    // Replaces cucascade's built-in convert_host_fast_to_gpu for paths that exhibited
    // cudaErrorInvalidValue on num_gpus=2 (see .planning/phases/08-multi-gpu-sql-pipeline-fix/08-02-PROBE.md).
    //
    // Mirrors Pattern 2 shape from sirius_p2p_converter_factory — acquires a target-bound
    // stream from target_memory_space, performs all allocations + H2D copies on that stream
    // under rmm::cuda_set_device_raii for target_device_id.
    std::unique_ptr<cucascade::idata_representation> sirius_host_fast_to_gpu_factory(
      cucascade::idata_representation& source,
      const cucascade::memory::memory_space* target_memory_space,
      rmm::cuda_stream_view stream);

    }  // namespace sirius::data
    ```

    Step 3 — create implementation `src/data/sirius_host_to_gpu_converter.cpp` with the factory body per Pattern 2. Use the 08-RESEARCH.md "Pattern 2" code block directly if cucascade symbols are public, else use pack/unpack fallback. Include sticky `cudaGetLastError()` consume after any `cuda*` call (HYG decision from v1.1). Explicit stream construction only — NO `rmm::cuda_stream_default`.

    Key invariants the body MUST satisfy (verified via acceptance_criteria):
    - Opens with `rmm::cuda_set_device_raii target_guard{rmm::cuda_device_id{target_device_id}}` (target-first, because we're writing GPU-side data on target device).
    - Acquires stream via `target_memory_space->acquire_stream()` — does NOT consume caller's `stream` parameter for cudf work (the whole point of this override).
    - Synchronizes target_stream before returning (so caller sees finished data).
    - Returns `std::make_unique<cucascade::gpu_table_representation>(...)` with the new cudf::table moved in.

    Step 4 — wire into `src/include/data/sirius_converter_registry.hpp`:
    - Open the file; find the existing MGPU-06 P2P unregister+register block (around lines 69-82 per research).
    - IMMEDIATELY AFTER that block (same initialize() function), add:
      ```cpp
      // FIX-02 (v1.2): host->gpu override for cross-device stream-correctness on multi-GPU.
      auto const host_removed = instance_->unregister_converter<
        cucascade::host_data_representation,
        cucascade::gpu_table_representation>();
      if (!host_removed) {
        spdlog::warn("sirius: host->gpu converter not previously registered (FIX-02)");
      }
      instance_->register_converter<
        cucascade::host_data_representation,
        cucascade::gpu_table_representation>(&sirius::data::sirius_host_fast_to_gpu_factory);
      spdlog::info("sirius: FIX-02 host->gpu converter override registered");
      ```
    - Add `#include "data/sirius_host_to_gpu_converter.hpp"` (or the correct relative path from this header's location) at the top.

    Step 5 — verify `sirius_extension.cpp` registration. The existing converter-registry init call is invoked at extension load. Since we added the new unregister+register INSIDE the same registry::initialize() function (Step 4), `sirius_extension.cpp` needs NO edit UNLESS the registry is initialized via a different entry point. Confirm via `grep -n 'converter_registry::get\\(\\).initialize\\|converter_registry::initialize' src/sirius_extension.cpp`.

    Step 6 — CMakeLists.txt wiring. Add `src/data/sirius_host_to_gpu_converter.cpp` to the Sirius sources list. Use `grep -n 'sirius_p2p_converter.cpp' CMakeLists.txt` to find the right list; append the new file alphabetically below it.

    Step 7 — MCP build-gate: `mcp__project-commands__run_command build` must exit 0. Fix any compile errors. The likely errors:
    - Missing include in registry header → add it.
    - `BatchCopyAccumulator` not public → fall back to pack/unpack shape (redo Step 3 body).
    - `cucascade::host_data_representation` not visible → confirm include path matches cucascade's actual header layout.

    Step 8 — re-run probe from Task 1 (same commands, same `$TMPDIR/sirius-fix02-probe.yaml`) to confirm the override closes the bug. Update `.planning/phases/08-multi-gpu-sql-pipeline-fix/08-02-PROBE.md` with a "Post-Override Re-Probe" section showing the now-clean log counts.

    HYG invariants:
    - `grep -n 'rmm::cuda_stream_default' src/data/sirius_host_to_gpu_converter.{hpp,cpp}` returns 0.
    - Every stream is explicit `rmm::cuda_stream` or `target_memory_space->acquire_stream()`.
    - Every `cuda*` call followed by `cudaGetLastError()` consume (matches Pattern 2 idiom).
  </action>
  <verify>
    <automated>test ! -f src/data/sirius_host_to_gpu_converter.cpp || (grep -q 'sirius_host_fast_to_gpu_factory' src/data/sirius_host_to_gpu_converter.cpp &amp;&amp; grep -q 'register_converter<.*host_data_representation.*gpu_table_representation>' src/include/data/sirius_converter_registry.hpp &amp;&amp; grep -q 'sirius_host_to_gpu_converter.cpp' CMakeLists.txt)</automated>
  </verify>
  <acceptance_criteria>
    - (Branch B only) File `src/data/sirius_host_to_gpu_converter.hpp` exists and declares `sirius_host_fast_to_gpu_factory` in `namespace sirius::data`
    - (Branch B only) File `src/data/sirius_host_to_gpu_converter.cpp` exists, contains `sirius_host_fast_to_gpu_factory` body, includes `rmm::cuda_set_device_raii` usage, and does NOT contain `rmm::cuda_stream_default`
    - (Branch B only) `grep -nE 'register_converter<\s*cucascade::host_data_representation.*cucascade::gpu_table_representation' src/include/data/sirius_converter_registry.hpp` returns >= 1 match
    - (Branch B only) `grep -c 'sirius_host_to_gpu_converter.cpp' CMakeLists.txt` returns >= 1
    - (Branch B only) `grep -rn 'rmm::cuda_stream_default' src/data/sirius_host_to_gpu_converter.hpp src/data/sirius_host_to_gpu_converter.cpp` returns NO matches
    - (Branch B only) `mcp__project-commands__run_command build` exits 0
    - (Branch B only) `.planning/phases/08-multi-gpu-sql-pipeline-fix/08-02-PROBE.md` contains a "Post-Override Re-Probe" section showing 0 cudaErrorInvalidValue in the re-probe log
    - (Branch A or C) This task exits early without creating new files (verify: `ls src/data/sirius_host_to_gpu_converter.* 2>/dev/null` returns nothing)
  </acceptance_criteria>
  <done>(Branch B) Host→gpu override authored, registered, built, and re-probe confirms clean. (Branch A or C) Task skipped; probe document is the only artifact.</done>
</task>

</tasks>

<verification>
- `.planning/phases/08-multi-gpu-sql-pipeline-fix/08-02-PROBE.md` exists and contains a `Probe PASS|FAIL|DEFERRED` verdict plus audit-sites-covered enumeration.
- IF Branch B taken: `grep -rnE 'sirius_host_fast_to_gpu_factory' src/data/ src/include/data/` returns >= 2 matches (declaration + registration); `mcp__project-commands__run_command build` exits 0; re-probe log shows 0 cudaErrorInvalidValue.
- IF Branch A: probe log already shows 0 cudaErrorInvalidValue for Q1/Q3/Q6/Q12 at num_gpus=2 AND on at least one materialized_columns / cache-hit invocation.
- IF Branch C: single-GPU deferral note in PROBE.md explicitly defers to 08-06 on verification host; no new source files authored.
- In ALL branches: `grep -rn 'rmm::cuda_stream_default' src/data/sirius_host_to_gpu_converter.*` returns NO matches (file absent in A/C, clean in B).
</verification>

<success_criteria>
FIX-02 is satisfied when:
1. Audit document `.planning/phases/08-multi-gpu-sql-pipeline-fix/08-02-PROBE.md` enumerates all candidate cross-device memcpy sites in src/pipeline/ and src/op/ with their verdict (covered by FIX-01 / safe / needs-override).
2. Empirical probe on 2-GPU host produces either a PASS (all 4 queries clean, plus materialized-columns variant) or a FAIL that is then closed by the Branch B override.
3. If Branch B was exercised, the new override file mirrors Pattern 2 exactly (device guard, target stream, sync, HYG-clean).
4. MCP build exits 0.
5. cucascade submodule is UNTOUCHED (verify: `git status cucascade/` is clean).
</success_criteria>

<output>
After completion, create `.planning/phases/08-multi-gpu-sql-pipeline-fix/08-02-SUMMARY.md` documenting:
- Probe verdict (PASS / FAIL / DEFERRED)
- Which branch was taken (A / B / C)
- If B: file list added + lines-of-code approx; exact registration site in converter_registry
- Audit sites enumerated
- Open items handed off (if C: defer to 08-06; if B: nothing; if A: nothing)
</output>
