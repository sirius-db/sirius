---
phase: 08-multi-gpu-sql-pipeline-fix
plan: 06
type: execute
wave: 5
depends_on: ["08-01", "08-02", "08-03", "08-04", "08-05"]
files_modified:
  - .planning/phases/08-multi-gpu-sql-pipeline-fix/08-06-VALIDATION.md
autonomous: false
requirements: [FIX-03, FIX-04]

must_haves:
  truths:
    - "FIX-03 verified: `grep -rn 'rmm::cuda_stream_default' src/` on the phase-8 HEAD returns zero net-new matches compared to the phase-7 baseline (ideally 0 total, per STATE.md note that baseline is already 0)"
    - "FIX-04 verified: `mcp__project-commands__run_command build` exits 0 on a fresh build"
    - "ROADMAP criterion 1 validated on N=2 hardware: SF100 Q1 runs on num_gpus=2 against /datasets/tpch_parquet_sf100/lineitem.parquet, completes without cudaErrorInvalidValue, result matches num_gpus=1 baseline"
    - "ROADMAP criterion 6 validated: VALIDATION.md records full [mgpu-audit] log showing scan_batch distribution on BOTH GPUs + wall-clock timings"
    - "Pattern 2 idiom is grep-verifiable in pipeline code (ROADMAP criterion 5): cuda_set_device_raii + source-bound construction + target-bound copy shape present in the post-fix src/"
    - "All Phase 8 plans (08-01 through 08-05) are committed and summarized"
  artifacts:
    - path: ".planning/phases/08-multi-gpu-sql-pipeline-fix/08-06-VALIDATION.md"
      provides: "Final ship-gate evidence document"
      contains: "SF100 Q1"
  key_links:
    - from: "Phase 8 code changes"
      to: "ROADMAP Phase 8 success criteria 1-6"
      via: "explicit criterion-by-criterion table in VALIDATION.md"
      pattern: "Criterion [1-6]"
---

<objective>
Close FIX-03 + FIX-04 and execute the final ROADMAP Phase 8 ship gate (criteria 1 + 6 — SF100 Q1 on N=2 hardware with [mgpu-audit] evidence). This is a VALIDATION plan: no new production code, no new tests. It is the phase's acceptance documentation.

This plan is INTERACTIVE (`autonomous: false`) because:
1. ROADMAP criterion 1 requires running on N=2 hardware (2 × RTX 6000 Ada). Per 08-RESEARCH.md "Environment Availability", the worktree host may be single-GPU; a user-coordinated hand-off to the verification host is required.
2. The 22.8 GB SF100 lineitem dataset must be present at `/datasets/tpch_parquet_sf100/lineitem.parquet` on the execution host; the user confirms availability.
3. Pitfall 6 (Ada + Sapphire Rapids silent corruption) may require user judgment if results differ subtly from 1-GPU baseline.

Purpose: Produce the `08-06-VALIDATION.md` file that records Phase 8's ship evidence. This is the artifact ROADMAP criterion 6 explicitly requires. Also confirms HYG-02 (FIX-03) stayed clean across the phase.

Output: One markdown file — `.planning/phases/08-multi-gpu-sql-pipeline-fix/08-06-VALIDATION.md`. No source edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-multi-gpu-sql-pipeline-fix/08-RESEARCH.md
@.planning/phases/08-multi-gpu-sql-pipeline-fix/08-01-SUMMARY.md
@.planning/phases/08-multi-gpu-sql-pipeline-fix/08-02-SUMMARY.md
@.planning/phases/08-multi-gpu-sql-pipeline-fix/08-03-SUMMARY.md
@.planning/phases/08-multi-gpu-sql-pipeline-fix/08-04-SUMMARY.md
@.planning/phases/08-multi-gpu-sql-pipeline-fix/08-05-SUMMARY.md
@.planning/phases/08-multi-gpu-sql-pipeline-fix/08-05-RUN.md
@.planning/milestones/v1.1-E2E-VERIFICATION.md
@.planning/phases/07-p2p-direct-transfer-adaptive-scan-partitioning/07-04-VALIDATION.md
@src/op/scan/duckdb_scan_executor.cpp
@src/pipeline/pipeline_executor.cpp

<interfaces>
<!-- ROADMAP Phase 8 success criteria (verbatim — each MUST be addressed in VALIDATION.md): -->
<!-- 1. SF100 TPC-H Q1 on num_gpus: 2 returns correct results (/datasets/tpch_parquet_sf100/lineitem.parquet, no cudaErrorInvalidValue, matches num_gpus=1 baseline). SF10 smoke variant passes first. -->
<!-- 2. mcp unit-tests exits 0 with 2-GPU parameterization; all 22 SF1 + Q1/Q6/Q12 SF10 green. -->
<!-- 3. grep -rn 'rmm::cuda_stream_default' src/ returns zero net-new. -->
<!-- 4. Catch2 TEST_CASE asserts pipeline_task >= 5 AND scan_batch >= 5 on BOTH GPUs. -->
<!-- 5. Pattern 2 idiom grep-verifiable: cuda_set_device_raii.*source|pack.*source_stream|copy.*target_stream. -->
<!-- 6. SF100 Q1 run recorded in .planning/phases/08-*/*-VALIDATION.md with [mgpu-audit] log + wall-clock. -->

<!-- Precedent for VALIDATION.md format: -->
<!-- .planning/phases/07-p2p-direct-transfer-adaptive-scan-partitioning/07-04-VALIDATION.md -->
<!-- (same phase family; same ship-gate shape) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run FIX-03 HYG sweep — grep for rmm::cuda_stream_default across src/</name>
  <files>.planning/phases/08-multi-gpu-sql-pipeline-fix/08-06-VALIDATION.md</files>
  <read_first>
    - .planning/phases/08-multi-gpu-sql-pipeline-fix/08-RESEARCH.md "FIX-03" row (research baseline: 0 matches on phase-7 HEAD)
    - .planning/phases/08-multi-gpu-sql-pipeline-fix/08-01-SUMMARY.md (confirm what files FIX-01 touched)
    - .planning/phases/08-multi-gpu-sql-pipeline-fix/08-02-SUMMARY.md (confirm whether Branch B authored new files)
    - .planning/phases/08-multi-gpu-sql-pipeline-fix/08-03-SUMMARY.md (audit log extension files)
  </read_first>
  <action>
    Step 1 — run the HYG sweep across src/ (production code only, not test/):
    ```
    grep -rn 'rmm::cuda_stream_default' src/
    ```

    Step 2 — expected outcome: ZERO matches (per 08-RESEARCH.md FIX-03 row, the phase-7 HEAD baseline was already 0). If matches appear:
    - Check whether the match is in a file modified by Phase 8 (08-01, 08-02, 08-03). If yes, this is a NET-NEW introduction — FAIL. Go back to the plan that introduced it, remove the offending line, replace with explicit stream.
    - If the match is in a file NOT modified by Phase 8, record it as a pre-existing baseline item in VALIDATION.md with a note explaining; do NOT block on it.

    Step 3 — also run the ancillary sweep for the HYG invariant — verify every new stream construction is under a device guard. Grep for each new stream site:
    ```
    grep -n 'rmm::cuda_stream\b' src/op/scan/duckdb_scan_executor.cpp src/data/sirius_host_to_gpu_converter.cpp 2>/dev/null
    ```
    For each new site, verify (manual read) that `rmm::cuda_set_device_raii` is in scope at the construction point.

    Step 4 — Start writing `.planning/phases/08-multi-gpu-sql-pipeline-fix/08-06-VALIDATION.md`. Initial sections:

    ```markdown
    # Phase 8 Validation — Multi-GPU SQL Pipeline Fix

    **Validated:** <DATE>
    **Host:** <HOSTNAME> (<GPU_INFO>)
    **Branch:** <BRANCH@HEAD>
    **Datasets:** /datasets/tpch_parquet_sf1, sf10, sf100 (confirmed present)

    ## FIX-03 — Zero Net-New rmm::cuda_stream_default

    Command: `grep -rn 'rmm::cuda_stream_default' src/`
    Result: <N matches — ideally 0>
    Delta vs phase-7 baseline: <+0 expected>
    Per-file breakdown (if any): <paste>

    ## ROADMAP Criterion 3: PASS / FAIL
    ```

    Step 5 — commit the partial VALIDATION.md so far. It will be extended by Tasks 2 and 3.
  </action>
  <verify>
    <automated>test -f .planning/phases/08-multi-gpu-sql-pipeline-fix/08-06-VALIDATION.md &amp;&amp; grep -qE 'FIX-03.*PASS|FIX-03.*FAIL|ROADMAP Criterion 3' .planning/phases/08-multi-gpu-sql-pipeline-fix/08-06-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'rmm::cuda_stream_default' src/` returns 0 matches in files modified by Phase 8 (08-01/02/03's files_modified)
    - `.planning/phases/08-multi-gpu-sql-pipeline-fix/08-06-VALIDATION.md` exists
    - File records exact grep command, exit code, match count
    - File contains a FIX-03 verdict (PASS if 0 net-new, FAIL with remediation plan otherwise)
  </acceptance_criteria>
  <done>FIX-03 HYG sweep recorded; VALIDATION.md scaffolded.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: FIX-04 clean-build verification via MCP + Pattern 2 idiom grep for ROADMAP criterion 5</name>
  <files>.planning/phases/08-multi-gpu-sql-pipeline-fix/08-06-VALIDATION.md</files>
  <read_first>
    - .planning/phases/08-multi-gpu-sql-pipeline-fix/08-06-VALIDATION.md (the partial file from Task 1)
    - src/op/scan/duckdb_scan_executor.cpp (confirm FIX-01 post-edit state shows Pattern 2 idiom)
    - src/data/sirius_p2p_converter.cpp (reference idiom)
    - .planning/phases/08-multi-gpu-sql-pipeline-fix/08-02-SUMMARY.md (whether sirius_host_to_gpu_converter.cpp was authored)
    - ROADMAP.md lines 29-40 (criteria 3, 4, 5 verbatim text)
  </read_first>
  <action>
    Step 1 — clean build verification. Remove any stale build artifacts, then rebuild:
    ```
    rm -rf build
    mcp__project-commands__run_command build
    ```
    Exit code MUST be 0. Record the exit code in VALIDATION.md.

    Step 2 — verify ROADMAP criterion 5 (Pattern 2 idiom grep-verifiable). The criterion says:
    > `grep -rnE 'cuda_set_device_raii.*source|pack.*source_stream|copy.*target_stream' src/pipeline/` or equivalent returns the Pattern 2 idiom

    Run:
    ```
    grep -rnE 'cuda_set_device_raii.*(target|source)' src/op/scan/duckdb_scan_executor.cpp src/data/sirius_p2p_converter.cpp src/data/sirius_host_to_gpu_converter.cpp 2>/dev/null
    ```
    Expected: >= 2 matches in FIX-01's duckdb_scan_executor.cpp (per-GPU pool construction guard + dispatch-site guard), >= 2 in sirius_p2p_converter.cpp (existing Pattern 2 precedent), and IF Branch B of 08-02 was taken: >= 1 in sirius_host_to_gpu_converter.cpp.

    Step 3 — append to VALIDATION.md:

    ```markdown
    ## FIX-04 — Clean Build

    Command: `rm -rf build && mcp__project-commands__run_command build`
    Exit code: 0
    Build time: <T seconds>

    ## ROADMAP Criterion 4: PASS (build clean)

    ## ROADMAP Criterion 5: Pattern 2 Idiom Verification

    Command: `grep -rnE 'cuda_set_device_raii.*(target|source)' src/op/scan/duckdb_scan_executor.cpp src/data/sirius_p2p_converter.cpp src/data/sirius_host_to_gpu_converter.cpp`
    Match count: <N>
    Breakdown by file: <paste>

    Verdict: PASS (idiom present in fix sites).
    ```

    Step 4 — also cross-reference the 08-05-RUN.md results for unit-tests criterion (criterion 2 in the ROADMAP numbering — actually it's criterion 2 in the 6-criteria list). Append:

    ```markdown
    ## ROADMAP Criterion 2: mcp unit-tests exits 0 with 2-GPU parameterization

    Reference: .planning/phases/08-multi-gpu-sql-pipeline-fix/08-05-RUN.md
    Verdict: <PASS / DEFERRED to Task 3 if 08-05-RUN.md records DEFERRED due to single-GPU host>
    ```
  </action>
  <verify>
    <automated>grep -qE 'FIX-04.*exit.*0|Exit code.*:\s*0' .planning/phases/08-multi-gpu-sql-pipeline-fix/08-06-VALIDATION.md &amp;&amp; grep -qE 'Criterion 5.*PASS|Pattern 2 Idiom.*PASS' .planning/phases/08-multi-gpu-sql-pipeline-fix/08-06-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `rm -rf build && mcp__project-commands__run_command build` exits 0
    - VALIDATION.md records clean build exit code 0
    - VALIDATION.md records ROADMAP criterion 5 verdict with grep output evidence (>= 2 matches in the combined grep across fix sites)
    - VALIDATION.md references 08-05-RUN.md for criterion 2 verdict
  </acceptance_criteria>
  <done>FIX-04 clean build confirmed; ROADMAP criteria 4, 5 recorded in VALIDATION.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: USER-COORDINATED N=2 hardware validation — SF10 smoke + SF100 Q1 ship-gate run</name>
  <files>.planning/phases/08-multi-gpu-sql-pipeline-fix/08-06-VALIDATION.md</files>
  <read_first>
    - .planning/phases/08-multi-gpu-sql-pipeline-fix/08-06-VALIDATION.md (contains sections from Tasks 1 + 2)
    - .planning/phases/08-multi-gpu-sql-pipeline-fix/08-05-RUN.md (whether unit-tests ran on 2-GPU host already)
    - .planning/phases/08-multi-gpu-sql-pipeline-fix/08-RESEARCH.md "Environment Availability" + "Common Pitfalls → Pitfall 6" (Ada + Sapphire Rapids silent-corruption risk)
    - ROADMAP.md lines 29-40 (criteria 1 + 6 verbatim)
    - .planning/milestones/v1.1-E2E-VERIFICATION.md (v1.1 verification-host baseline — same host should produce comparable numbers)
  </read_first>
  <what-built>
    Phase 8 fix stack (08-01 FIX-01 + 08-02 conditional FIX-02 + 08-03 AUDIT log extension + 08-04 TEST infra + 08-05 test cases) is landed and the local unit-tests pass (single-GPU or 2-GPU — whichever host the executor runs on). Pending: the ROADMAP criterion 1 + 6 ship gate which REQUIRES N=2 hardware (2 × RTX 6000 Ada) AND the 22.8 GB SF100 lineitem parquet dataset AT /datasets/tpch_parquet_sf100/lineitem.parquet.
  </what-built>
  <action>
    Pause execution. Present Tasks 1 + 2 evidence (FIX-03 + FIX-04 + criterion 5) from VALIDATION.md to the user. Do NOT proceed until the user either (a) returns with the completed criterion 1 + 6 evidence pasted into VALIDATION.md, or (b) explicitly defers with "verification host unavailable". The checkpoint's `<how-to-verify>` block below is the script the user runs on the verification host.
  </action>
  <how-to-verify>
    Prerequisites the user must confirm or arrange:
    1. SSH / remote access to the v1.1 verification host (2 × RTX 6000 Ada, 49140 MiB each, driver 595.58.03, CUDA 13.2) OR equivalent N=2 host.
    2. `/datasets/tpch_parquet_sf10/lineitem.parquet` accessible on that host.
    3. `/datasets/tpch_parquet_sf100/lineitem.parquet` accessible on that host (22.8 GB).
    4. This Phase 8 feature branch available at that host (`git fetch && git checkout <phase-8-branch>`).

    Step 1 — the user OR claude (if MCP can remote to verification host) runs:
    ```
    cd <repo> && git status   # confirm clean tree on phase 8 branch
    rm -rf build && mcp__project-commands__run_command build
    ```
    Build must exit 0 on the verification host (same as Task 2 on worktree host).

    Step 2 — SF10 smoke variant (prerequisite per criterion 1):
    ```
    SIRIUS_TEST_SF10_PATH=/datasets/tpch_parquet_sf10 \
    SIRIUS_LOG_DIR=$HOME/sirius-logs/phase8-sf10-smoke \
      build/release/extension/sirius/test/cpp/sirius_unittest "[tpch_sf10]"
    ```
    Expected: Q1 / Q6 / Q12 SF10 2-GPU variants PASS. If they fail, halt — do not proceed to SF100.

    Step 3 — Full unit-tests on verification host:
    ```
    SIRIUS_TEST_SF10_PATH=/datasets/tpch_parquet_sf10 \
      mcp__project-commands__run_command unit-tests
    ```
    Expected: exit 0; 22 TPC-H SF1 queries × 2 num_gpus = 44 assertions pass (modulo Q4 retry); [mgpu-audit] TEST_CASE passes with `>= 5 per GPU` unique counts; SF10 smoke variants pass.

    Step 4 — SF100 Q1 SHIP GATE (criterion 1 + 6):
    Create a standalone driver script `$HOME/phase8-sf100-q1.sh`:
    ```
    #!/usr/bin/env bash
    set -euxo pipefail
    export SIRIUS_LOG_DIR=$HOME/sirius-logs/phase8-sf100-q1
    export SIRIUS_LOG_LEVEL=info
    mkdir -p $SIRIUS_LOG_DIR
    CONFIG=$HOME/phase8-sf100-2gpu.yaml
    cat > $CONFIG <<'YAML'
    # Copy integration.yaml content here but set num_gpus: 2
    YAML
    export SIRIUS_CONFIG_FILE=$CONFIG
    time pixi run duckdb -c "
      LOAD 'build/release/extension/sirius/sirius.duckdb_extension';
      CREATE OR REPLACE VIEW lineitem AS
        SELECT * FROM read_parquet('/datasets/tpch_parquet_sf100/lineitem.parquet');
      CALL gpu_execution('
        SELECT l_returnflag, l_linestatus,
               sum(l_quantity), sum(l_extendedprice), avg(l_discount)
          FROM lineitem
         WHERE l_shipdate <= DATE ''1998-12-01'' - INTERVAL 90 DAY
         GROUP BY l_returnflag, l_linestatus
         ORDER BY l_returnflag, l_linestatus
      ');
    " | tee $HOME/phase8-sf100-q1.output
    ```
    (Exact YAML content copied from test/cpp/integration/integration.yaml with num_gpus: 2.)

    Run BOTH num_gpus=1 (baseline) and num_gpus=2 (fix verification) variants of SF100 Q1. Time both. Results must match bitwise (modulo float tolerance).

    Step 5 — capture evidence for VALIDATION.md:
    - Wall-clock from `time` output for both 1-GPU and 2-GPU runs
    - Full [mgpu-audit] log section: `grep '\[mgpu-audit\]' $SIRIUS_LOG_DIR/*.log`
    - Per-GPU scan_batch count: `grep '\[mgpu-audit\] scan_batch assigned to GPU 0' $SIRIUS_LOG_DIR/*.log | wc -l` and `GPU 1` variant
    - Per-GPU pipeline_task count: same pattern with pipeline_task
    - Any cudaErrorInvalidValue occurrence: `grep cudaErrorInvalidValue $SIRIUS_LOG_DIR/*.log` MUST return empty
    - Result-set diff between 1-GPU and 2-GPU runs (must be empty / within tolerance)

    Step 6 — user appends final section to `.planning/phases/08-multi-gpu-sql-pipeline-fix/08-06-VALIDATION.md`:

    ```markdown
    ## ROADMAP Criterion 1 + 6 — SF100 Q1 Ship Gate

    **Host:** <hostname>, 2 × RTX 6000 Ada, driver <X>, CUDA <Y>
    **Dataset:** /datasets/tpch_parquet_sf100/lineitem.parquet (<SIZE>, <ROWS>)

    ### SF10 prerequisite (Q1/Q6/Q12)
    | Query | num_gpus=1 | num_gpus=2 | Result match |
    |-------|------------|------------|--------------|
    | Q1 | <t1> s | <t2> s | bitwise / <delta> |
    | Q6 | <t1> s | <t2> s | bitwise / <delta> |
    | Q12 | <t1> s | <t2> s | bitwise / <delta> |

    ### SF100 Q1 ship run
    | num_gpus | wall-clock | exit | cudaErrorInvalidValue | result rows | match to 1-GPU |
    |---------|-----------|------|----------------------|-------------|----------------|
    | 1 | <T1> s | 0 | 0 | <R> | baseline |
    | 2 | <T2> s | 0 | 0 | <R> | bitwise / <delta> |

    ### [mgpu-audit] distribution (num_gpus=2)
    Unique task_id on GPU 0: <count>
    Unique task_id on GPU 1: <count>
    Unique batch_id on GPU 0: <count>
    Unique batch_id on GPU 1: <count>
    Full log excerpt:
    ```
    [paste of grep '[mgpu-audit]' output, up to ~40 lines]
    ```

    **Criterion 1 verdict:** PASS (or FAIL with details)
    **Criterion 6 verdict:** PASS (VALIDATION.md captures all required evidence)
    ```

    Step 7 — Pitfall 6 check. If 1-GPU vs 2-GPU SF100 Q1 aggregates differ: suspect Ada+Sapphire Rapids silent corruption. Run `lscpu | grep "Model name"` on the host; if Xeon Sapphire Rapids family, document in VALIDATION.md as a known-platform risk and attach to phase retrospective for v1.3 follow-up; do NOT block the ship gate if the delta is within the `compare_gpu_vs_cpu` float tolerance.
  </how-to-verify>
  <verify>
    <automated>grep -qE 'Criterion 1.*PASS|Criterion 1.*DEFERRED|Criterion 1.*FAIL' .planning/phases/08-multi-gpu-sql-pipeline-fix/08-06-VALIDATION.md &amp;&amp; grep -qE 'Criterion 6.*PASS|Criterion 6.*DEFERRED|Criterion 6.*FAIL' .planning/phases/08-multi-gpu-sql-pipeline-fix/08-06-VALIDATION.md</automated>
  </verify>
  <done>User appended criterion 1 + 6 evidence (PASS on verification host) OR explicitly deferred with "verification host unavailable" written into VALIDATION.md; Phase 8 ship gate resolved either way (PASS or BLOCKED-on-hardware).</done>
  <resume-signal>Type "validated" and paste the completed Criterion 1 + 6 section content (or file diff) for review. Alternatively: "deferred — verification host unavailable; milestone ship blocked pending hardware" OR "failed — [details]" for remediation.</resume-signal>
</task>

</tasks>

<verification>
- `.planning/phases/08-multi-gpu-sql-pipeline-fix/08-06-VALIDATION.md` exists.
- VALIDATION.md contains sections for all 6 ROADMAP criteria with explicit PASS/FAIL/DEFERRED verdicts.
- FIX-03 verdict (grep of src/ for rmm::cuda_stream_default = 0 net-new) recorded.
- FIX-04 verdict (clean build exit 0) recorded.
- Criterion 5 evidence (Pattern 2 idiom grep) recorded.
- Criterion 1 + 6 evidence (SF100 Q1 run on N=2) either recorded as PASS or explicitly DEFERRED with clear handoff to the user.
- `grep -rn 'rmm::cuda_stream_default' src/` returns 0 matches in Phase 8 files (or documented baseline).
</verification>

<success_criteria>
Phase 8 ship gate satisfied when:
1. FIX-03 HYG sweep: 0 net-new `rmm::cuda_stream_default` in src/ (recorded in VALIDATION.md).
2. FIX-04: MCP build exits 0 after `rm -rf build` (recorded in VALIDATION.md).
3. ROADMAP criterion 1: SF100 Q1 on num_gpus=2 PASSES on N=2 verification host, result bitwise-matches num_gpus=1 baseline. OR explicitly DEFERRED with hardware-unavailability note handed back to the user.
4. ROADMAP criterion 2: 08-05-RUN.md referenced for unit-tests exit 0.
5. ROADMAP criterion 4: 08-05-RUN.md referenced for audit TEST_CASE per-GPU count verdict.
6. ROADMAP criterion 5: Pattern 2 idiom grep produces >= 2 matches in fix sites.
7. ROADMAP criterion 6: VALIDATION.md captures SF100 Q1 [mgpu-audit] log + wall-clock.
8. cucascade submodule is unchanged (`git status cucascade/` clean).
</success_criteria>

<output>
After completion, create `.planning/phases/08-multi-gpu-sql-pipeline-fix/08-06-SUMMARY.md` AND `.planning/phases/08-multi-gpu-sql-pipeline-fix/08-SUMMARY.md` (phase-level summary).

`08-06-SUMMARY.md` documents:
- Which ROADMAP criteria are recorded as PASS in VALIDATION.md
- Which (if any) are DEFERRED due to hardware availability
- Final FIX-03 count (0 / N)
- Final FIX-04 build time

`08-SUMMARY.md` (phase-level) documents the full Phase 8 outcome:
- All 11 REQ-IDs closed (traceability table with each REQ-ID's plan-of-record)
- Plan-by-plan summary (one line per 08-01..08-06 summary)
- Any deferred items (e.g., upstream cucascade PR = OUT OF SCOPE, not deferred)
- Milestone v1.2 readiness verdict: SHIP / BLOCK
- Handoff to v1.3 (or STATE.md update) if applicable
</output>
