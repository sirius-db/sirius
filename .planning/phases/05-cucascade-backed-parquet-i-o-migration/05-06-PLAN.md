---
phase: 05-cucascade-backed-parquet-i-o-migration
plan: 06
type: execute
wave: 4
depends_on: ["05-04", "05-05"]
files_modified:
  - .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-06-VALIDATION.md
  - .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-06-MULTIGPU-VALIDATION.md
  - .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-SUMMARY.md
autonomous: false
requirements: [IO-08, IO-09, IO-10, IO-11, HYG-02]
must_haves:
  truths:
    - "Project-wide grep `grep -rnw 'datasource::create' src/` returns 0 hits (IO-08 global gate)"
    - "Across every file modified by Phase 5, `grep cuda_stream_default <file>` returns 0 hits (HYG-02 sweep)"
    - "TPC-H SF1 all 22 queries pass post-migration with status identical to the Plan 01 baseline (IO-09)"
    - "TPC-H SF10 parquet-scan wall-clock is measured post-migration; regression vs the Phase 4 kvikio baseline is either (a) ≤ 30% or (b) > 30% with an upstream cucascade issue filed and documented in the phase summary (IO-10)"
    - "compute-sanitizer memcheck run on 2+ GPU host reports zero NEW 'invalid device' or 'context mismatch' errors vs the Phase 4 baseline (IO-11; Phase 4 deferred GPU1→GPU0 converter return-leg failures are accepted as pre-existing)"
    - "Multi-GPU evidence artifact (05-06-MULTIGPU-VALIDATION.md) exists BEFORE the human sign-off checkpoint so the checkpoint task is purely a review-and-decide step, not a mix of autonomous work + human verification"
    - "Phase 5 SUMMARY.md exists with all 13 requirement IDs (IO-01..11, HYG-01, HYG-02) marked as closed with evidence links"
  artifacts:
    - path: ".planning/phases/05-cucascade-backed-parquet-i-o-migration/05-06-VALIDATION.md"
      provides: "Validation evidence artifact for autonomous checks — grep gate outputs, SF1 diff vs baseline, adapter unit tests, full regression suite"
      contains: "IO-08 Global Grep Gate"
    - path: ".planning/phases/05-cucascade-backed-parquet-i-o-migration/05-06-MULTIGPU-VALIDATION.md"
      provides: "Multi-GPU evidence artifact for the human-verify checkpoint — compute-sanitizer log excerpt, per-backend cudaGetDevice readback, SF10 regression measurement, decision matrix; written autonomously by Task 2a so the checkpoint (Task 2b) is pure review-and-decide"
      contains: "IO-11 compute-sanitizer"
    - path: ".planning/phases/05-cucascade-backed-parquet-i-o-migration/05-SUMMARY.md"
      provides: "Phase 5 rollup summary mirroring 04-SUMMARY.md structure — requirements table, commits landed, deferrals, next-phase prep"
      contains: "Phase 5 Outcome"
  key_links:
    - from: "Phase 5 VALIDATION.md"
      to: "IO-08 global gate"
      via: "grep -rnw 'datasource::create' src/"
      pattern: "0 hits"
    - from: "Task 2a (auto) → 05-06-MULTIGPU-VALIDATION.md"
      to: "Task 2b (checkpoint:human-verify) reading the artifact"
      via: "artifact-mediated hand-off — autonomous work completes first, human reviews artifact and decides"
      pattern: "MULTIGPU-VALIDATION"
    - from: "05-SUMMARY.md"
      to: "ROADMAP + REQUIREMENTS update trigger (orchestrator-driven after checkpoint approval)"
      via: "phase-exit summary"
      pattern: "Requirements Satisfied"
---

<objective>
Close Phase 5 by validating all migrations at phase-scope (not per-plan) and recording evidence. Scope:

1. **IO-08 global grep gate**: project-wide `grep -rnw 'datasource::create' src/` returns 0 hits. Individual plans asserted file-level clean; this validates the combined state.
2. **HYG-02 sweep**: across every file Phase 5 touched, `grep cuda_stream_default` returns 0 hits. List each file in the validation artifact.
3. **IO-09 correctness re-verification**: re-run TPC-H SF1 and diff per-query pass/fail against the Plan 01 baseline. Any regression is a Phase 5 blocker.
4. **IO-10 performance measurement**: run TPC-H SF10 parquet scan wall-clock, compare to the Phase 4 kvikio-backed baseline. Regression ≤ 30% is pass. > 30% is documented in the phase summary with a filed upstream cucascade issue reference.
5. **IO-11 multi-GPU validation**: run `compute-sanitizer --tool memcheck` over the test suite on a ≥2 GPU host. Confirm zero NEW "invalid device" / "context mismatch" errors vs the Phase 4 baseline. Also confirm the per-backend `cudaGetDevice()` readback log lines (from Plan 03 Task 2) show distinct device IDs when multiple GPUs are configured.
6. **Phase summary**: write `05-SUMMARY.md` mirroring the `04-SUMMARY.md` template — requirements table, commits landed, deferrals, next-phase prep for Phase 6.

**Structure rationale:** Task 2 is split into Task 2a (auto — collect all multi-GPU evidence into an artifact) and Task 2b (checkpoint:human-verify — read the artifact and sign off). This keeps the checkpoint pure: restart semantics are clean (Task 2a can be re-run deterministically; Task 2b reads whatever artifact exists), and the human reviewer gets a single file to read rather than mixed streaming output.

Purpose: Wave 4 — phase-level validation consuming all Wave 3 outputs.
Output: Autonomous validation artifact + multi-GPU evidence artifact + phase summary; ROADMAP + STATE updates handled by orchestrator post-phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-cucascade-backed-parquet-i-o-migration/05-CONTEXT.md
@.planning/phases/05-cucascade-backed-parquet-i-o-migration/05-RESEARCH.md
@.planning/phases/05-cucascade-backed-parquet-i-o-migration/05-01-SUMMARY.md
@.planning/phases/05-cucascade-backed-parquet-i-o-migration/05-01-BASELINE.md
@.planning/phases/05-cucascade-backed-parquet-i-o-migration/05-02-SUMMARY.md
@.planning/phases/05-cucascade-backed-parquet-i-o-migration/05-03-SUMMARY.md
@.planning/phases/05-cucascade-backed-parquet-i-o-migration/05-04-SUMMARY.md
@.planning/phases/05-cucascade-backed-parquet-i-o-migration/05-05-SUMMARY.md
@.planning/phases/04-cucascade-bump-v1-0-re-integration/04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run grep gates + HYG-02 sweep + SF1 correctness + write validation artifact</name>
  <files>.planning/phases/05-cucascade-backed-parquet-i-o-migration/05-06-VALIDATION.md</files>
  <read_first>
    - .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-01-BASELINE.md (Plan 01 SF1 baseline — the pre-migration comparison source)
    - .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-04-SUMMARY.md (Plan 04 SF1 post-migration status)
    - .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-05-SUMMARY.md
    - .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-CONTEXT.md §"Testing & Validation"
    - .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-RESEARCH.md §"HYG-02 Scope Audit" (exact file list)
  </read_first>
  <action>
    Create `.planning/phases/05-cucascade-backed-parquet-i-o-migration/05-06-VALIDATION.md` with the following sections. Collect each piece of evidence BEFORE writing the document, then populate.

    **Section 1 — IO-08 Global Grep Gate:**
    Run `grep -rnw 'datasource::create' src/` and capture stdout. Expected: zero matches. Record the exact command and output. If ANY hit appears, STOP — it's a Plan 04 or Plan 05 regression. Fix the residual hit(s) by migrating them to `sirius::io::cucascade_datasource` using the same pattern before proceeding.

    Also run `grep -rnw 'cudf::io::datasource::create' src/` for belt-and-suspenders. Expected: zero matches.

    **Section 2 — HYG-02 Sweep across Phase 5 Modified Files:**
    Build the file list from the combined `files_modified` fields of Plans 01-05's frontmatter. Expected list:
    - `src/include/io/cucascade_datasource.hpp` (new, Plan 01)
    - `src/io/cucascade_datasource.cpp` (new, Plan 02)
    - `test/cpp/io/test_cucascade_datasource.cpp` (new, Plan 02)
    - `src/include/sirius_context.hpp` (Plan 03)
    - `src/sirius_context.cpp` (Plan 03)
    - `src/op/scan/parquet_scan_task.cpp` (Plan 04)
    - `src/include/op/scan/parquet_scan_task.hpp` (Plan 04 — Approach C member)
    - `src/creator/task_creator.cpp` (Plan 04 — Approach C plumbing)
    - `src/op/scan/sirius_parquet_metadata_scan_operator.cpp` (Plan 05)
    - `src/op/scan/iceberg_scan_task.cpp` (Plan 05)
    - `CMakeLists.txt` (Plan 01)

    For each file: run `grep -c 'cuda_stream_default' <file>` and record the count. Every count MUST be 0. The validation document includes the full list with per-file counts in a table.

    **Section 3 — IO-09 SF1 Correctness Re-verification:**
    Invoke `mcp__project-commands__run_command(build)` — exit 0.
    Run `build/release/test/unittest --test-dir . test/sql/tpch-sirius.test` (or the MCP equivalent). Exit code captured. Per-query pass/fail parsed.
    Diff per-query status against `05-01-BASELINE.md`. The diff table goes in the validation document with columns: `Query | Baseline | Post-Migration | Match?`. Any mismatch is a blocker — stop and fix before advancing the plan.

    If TPC-H Q4 flakes (known per STATE.md), retry once. Document both attempts.

    **Section 4 — Adapter Unit Tests Re-run:**
    Run `build/release/extension/sirius/test/cpp/sirius_unittest "[io_backend][cucascade_datasource]"` and capture exit code + test-case + assertion counts. Expected: exit 0, ≥7 TEST_CASEs ran, all passed.

    **Section 5 — Existing Regression Suites:**
    Full unit-tests run: `mcp__project-commands__run_command(unit-tests)`. Must PASS (with Q4 flake retry if needed). Test-case + assertion totals should meet or exceed the Phase 4 Plan 04-05 totals (966 test cases / ~78.8M assertions) because Phase 5 added 7+ cases.

    **Validation Document Template:**
    ```markdown
    # Phase 5 Validation Evidence

    **Validated:** {ISO-8601 timestamp}
    **Sirius HEAD:** {git rev-parse HEAD}

    ## 1. IO-08 Global Grep Gate

    Command: `grep -rnw 'datasource::create' src/`
    Result: {0 hits | N hits — list them}
    Status: {PASS | FAIL}

    Command: `grep -rnw 'cudf::io::datasource::create' src/`
    Result: {0 hits | N hits}
    Status: {PASS | FAIL}

    ## 2. HYG-02 Sweep

    | File | cuda_stream_default count |
    |------|---------------------------|
    | src/include/io/cucascade_datasource.hpp | 0 |
    | src/io/cucascade_datasource.cpp | 0 |
    | ... {every file from Phase 5's combined files_modified list} ... |
    Overall status: {PASS | FAIL}

    ## 3. IO-09 SF1 Correctness

    Command: `build/release/test/unittest --test-dir . test/sql/tpch-sirius.test`
    Exit code: {0 | nonzero}
    Runtime: {seconds}

    ### Per-Query Diff vs Plan 01 Baseline

    | Query | Baseline | Post-Migration | Match? |
    |-------|----------|----------------|--------|
    | Q1 | PASS | PASS | YES |
    | ... through Q22 ... |

    Q4 flake attempts: {1 or 2 — describe}

    Status: {PASS (all match) | FAIL (regressions: list)}

    ## 4. Adapter Unit Tests

    Command: `build/release/extension/sirius/test/cpp/sirius_unittest "[io_backend][cucascade_datasource]"`
    Exit code: 0
    TEST_CASEs ran: {N}
    Assertions: {N}
    Status: PASS

    ## 5. Full Unit-Tests Regression

    Command: `mcp__project-commands__run_command(unit-tests)`
    Exit code: 0
    Total test cases: {N} (>= 966 baseline from Phase 4)
    Total assertions: {N}
    Q4 retry needed: {yes/no}
    Status: PASS
    ```

    Save the document, then commit (via `node gsd-tools.cjs commit` per the standard pattern — orchestrator handles git).

    **Stop condition:** If any of Sections 1-5 show FAIL/regression, surface the specific failure in the final message, do NOT proceed to Task 2a (multi-GPU evidence collection), fix the underlying plan's output, re-run Task 1.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-06-VALIDATION.md &amp;&amp; grep -q "IO-08 Global Grep Gate" .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-06-VALIDATION.md &amp;&amp; grep -q "HYG-02 Sweep" .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-06-VALIDATION.md &amp;&amp; grep -q "IO-09 SF1 Correctness" .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-06-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/05-cucascade-backed-parquet-i-o-migration/05-06-VALIDATION.md` exists
    - Document contains the literal section headers "IO-08 Global Grep Gate", "HYG-02 Sweep", "IO-09 SF1 Correctness", "Adapter Unit Tests", "Full Unit-Tests Regression"
    - IO-08 section records "0 hits" for the `grep -rnw 'datasource::create' src/` command
    - HYG-02 section contains a per-file table with 0 counts for every file in Phase 5's modified-files list (≥ 9 files)
    - IO-09 section has a per-query Q1..Q22 diff table with "Match?" column — every row says YES
    - Adapter unit tests section records exit 0 and ≥ 7 TEST_CASEs
    - Full unit-tests section records exit 0 and total test-case count ≥ 966 (Phase 4 baseline)
    - If any section reports FAIL, the document explicitly describes the failure and the fix lands in the appropriate preceding plan before this plan re-runs
  </acceptance_criteria>
  <done>Validation artifact complete, all grep gates PASS, SF1 correctness confirmed vs baseline, all existing unit tests + adapter unit tests PASS.</done>
</task>

<task type="auto">
  <name>Task 2a: Multi-GPU evidence collection — compute-sanitizer + SF10 + write 05-06-MULTIGPU-VALIDATION.md artifact</name>
  <files>.planning/phases/05-cucascade-backed-parquet-i-o-migration/05-06-MULTIGPU-VALIDATION.md</files>
  <read_first>
    - .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-06-VALIDATION.md (Task 1 output — confirms autonomous gates passed before running multi-GPU work)
    - .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-01-BASELINE.md (Phase 4 HEAD fingerprint for SF10 baseline comparison; also TPC-H SF1 state for sanity)
    - .planning/phases/04-cucascade-bump-v1-0-re-integration/04-SUMMARY.md §"Hidden-tag explicit invocation on N=2 GPU verification host" (the Phase 4 baseline for multi-GPU error shapes that are already-known-failing; Phase 5 must NOT introduce new shapes beyond these)
    - .planning/STATE.md Blockers / Concerns section (pre-existing deferrals)
    - .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-CONTEXT.md §"Testing & Validation" (CONTEXT-locked compute-sanitizer + cudaGetDevice readback requirements)
  </read_first>
  <action>
    This task is PURELY autonomous — collects evidence, writes artifact. Human review happens in Task 2b. If the N≥2 GPU host is unavailable, record that fact in the artifact and let Task 2b decide whether to defer or reject; do NOT block on hardware.

    **Step 1 — IO-11 compute-sanitizer run on N≥2 GPU host:**
    ```bash
    # List hidden tests to confirm tag presence (diagnostic only).
    build/release/extension/sirius/test/cpp/sirius_unittest --list-tests 2>/dev/null | head -50 > /tmp/phase5-testlist.txt

    # Run the SF1 test suite under compute-sanitizer memcheck.
    compute-sanitizer --tool memcheck --require-cuda-init \
      build/release/test/unittest --test-dir . test/sql/tpch-sirius.test \
      > /tmp/phase5-sanitizer.log 2>&1
    echo "sanitizer exit: $?" >> /tmp/phase5-sanitizer.log
    ```
    Parse the log:
    - Count `========= Invalid` lines referencing "invalid device" or "context mismatch"
    - Extract `SiriusContext: io_backend created for GPU` lines — count them and record each line's (device_id, cudaGetDevice readback) pair
    - Capture the last 100 lines of the log verbatim for the artifact

    Compare against Phase 4 baseline error shapes documented in `.planning/phases/04-cucascade-bump-v1-0-re-integration/04-SUMMARY.md §"Hidden-tag explicit invocation on N=2 GPU verification host"`. Classify each error line as either `pre-existing` (shape matches Phase 4 baseline — known-failing GPU1→GPU0 converter return-leg deferral) or `NEW` (blocker — Phase 5 regression).

    **Step 2 — IO-10 SF10 performance measurement:**

    Baseline: use the Phase 4 HEAD commit's TPC-H SF10 parquet-scan timing. If not already recorded in any prior summary, capture it by checking out Phase 4 HEAD (before any Phase 5 edits) in a clean worktree and running:
    ```
    python3 test/tpch_performance/generate_test_data.py 10
    python3 test/tpch_performance/performance_test.py 10
    ```

    Then switch back to the Phase 5 head and run the same SF10 performance test. Capture wall-clock for parquet-scan-dominated queries (Q1, Q3, Q6 are typically scan-bound).

    Compute: `regression_pct = (post_migration_time - baseline_time) / baseline_time * 100`.

    **Step 3 — Write the multi-GPU evidence artifact** at `.planning/phases/05-cucascade-backed-parquet-i-o-migration/05-06-MULTIGPU-VALIDATION.md` with the following template:

    ```markdown
    # Phase 5 Multi-GPU Validation Evidence

    **Captured:** {ISO-8601 timestamp}
    **Verification host:** {hostname}
    **GPUs:** {nvidia-smi -L output — one line per GPU}
    **Sirius HEAD:** {git rev-parse HEAD}

    ## IO-11 compute-sanitizer memcheck

    **Command:** `compute-sanitizer --tool memcheck --require-cuda-init build/release/test/unittest --test-dir . test/sql/tpch-sirius.test`
    **Log file:** `/tmp/phase5-sanitizer.log`
    **Sanitizer exit code:** {0 | nonzero}

    ### Per-Backend cudaGetDevice Readback (IO-11 audit)

    | device_id (target) | cudaGetDevice readback | Match? |
    |--------------------|-----------------------|--------|
    | 0 | 0 | YES |
    | 1 | 1 | YES |
    | ... |

    Expected: N rows where N = number of GPUs on verification host. Each readback MUST equal target device_id.

    ### Error Classification

    | Line # | Error type | Shape | Classification |
    |--------|-----------|-------|----------------|
    | {line}  | invalid device | ... | pre-existing (Phase 4 GPU1→GPU0 return-leg deferral) |
    | {line} | context mismatch | ... | NEW — Phase 5 regression BLOCKER |

    **NEW error count:** {0 | N}
    **Status:** {PASS — no new errors | FAIL — N new errors require fix}

    ### Sanitizer Log Excerpt (last 100 lines)

    ```
    {verbatim excerpt}
    ```

    ## IO-10 SF10 Performance Measurement

    ### Baseline (Phase 4 HEAD)
    - Commit: {phase-4 HEAD SHA}
    - Command: `python3 test/tpch_performance/performance_test.py 10`
    - Wall-clock per scan-bound query: {Q1: X ms, Q3: Y ms, Q6: Z ms}

    ### Post-migration (Phase 5 HEAD)
    - Commit: {current HEAD SHA}
    - Wall-clock per scan-bound query: {Q1: X' ms, Q3: Y' ms, Q6: Z' ms}

    ### Regression

    | Query | Baseline (ms) | Post (ms) | Δ (%) |
    |-------|---------------|-----------|-------|
    | Q1 | {baseline} | {post} | {pct} |
    | Q3 | ... | ... | ... |
    | Q6 | ... | ... | ... |

    **Aggregate regression_pct:** {value}%

    ### Decision Matrix

    | regression_pct | Outcome |
    |----------------|---------|
    | ≤ 30% | IO-10 PASS; no escalation needed |
    | 30% < pct ≤ 50% | IO-10 PASS with escalation — file upstream cucascade issue, phase ships with documented caveat |
    | > 50% | Stop and discuss — may indicate per-read open/close cost (research P1) or pinned-buffer contention worse than anticipated |

    **Applied decision:** {value} falls into bucket `{≤30% | 30-50% | >50%}` → `{PASS | PASS+escalate | STOP}`

    ## Recommendation for Task 2b Checkpoint

    Based on the evidence above, the recommendation to the human reviewer is:
    - **IO-11:** {CLEAN | PASS with pre-existing deferrals | BLOCKER — N new errors}
    - **IO-10:** {PASS | PASS with upstream-issue escalation | STOP}
    - **Overall:** {approve | approve with note | reject}

    ## Environment / Availability Notes

    {If N≥2 GPU host was unavailable, document here; Task 2b decides whether to defer to a follow-up plan or reject.}
    ```

    **Step 4 — Commit the artifact** via the standard `node gsd-tools.cjs commit` flow (orchestrator-driven). Do NOT make a human-facing decision here — Task 2a's output is the artifact; Task 2b decides.

    Do NOT proceed to Task 2b until the artifact is written and committed. If any step fails to produce evidence (e.g., compute-sanitizer unavailable, SF10 dataset generation failed), document the gap in the artifact's "Environment / Availability Notes" section; Task 2b handles the decision.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-06-MULTIGPU-VALIDATION.md &amp;&amp; grep -q "IO-11 compute-sanitizer" .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-06-MULTIGPU-VALIDATION.md &amp;&amp; grep -q "IO-10 SF10 Performance" .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-06-MULTIGPU-VALIDATION.md &amp;&amp; grep -q "Per-Backend cudaGetDevice" .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-06-MULTIGPU-VALIDATION.md &amp;&amp; grep -q "Decision Matrix" .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-06-MULTIGPU-VALIDATION.md &amp;&amp; grep -q "Recommendation for Task 2b" .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-06-MULTIGPU-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/05-cucascade-backed-parquet-i-o-migration/05-06-MULTIGPU-VALIDATION.md` exists and is committed
    - Document contains all required sections: "IO-11 compute-sanitizer memcheck", "Per-Backend cudaGetDevice Readback", "Error Classification", "Sanitizer Log Excerpt", "IO-10 SF10 Performance Measurement", "Regression", "Decision Matrix", "Recommendation for Task 2b Checkpoint", "Environment / Availability Notes"
    - Sanitizer log path (`/tmp/phase5-sanitizer.log` or equivalent) recorded in the artifact
    - Per-backend cudaGetDevice readback table has N rows (N = GPU count on verification host), each row has target device_id matching readback
    - Error classification table categorizes every `invalid device` / `context mismatch` line from the sanitizer log as `pre-existing` or `NEW`
    - If NEW error count > 0, the artifact flags it as BLOCKER and the document recommendation is `reject`
    - SF10 regression table has per-query and aggregate regression_pct values (numeric, even if ≤ 0)
    - Decision matrix applies the regression_pct to the correct bucket
    - "Recommendation for Task 2b Checkpoint" section contains a clear approve/approve-with-note/reject suggestion
    - If the verification host is not N≥2 GPU, "Environment / Availability Notes" documents the gap explicitly
    - No human-facing decision made by this task — artifact is evidence-only; Task 2b is the decision point
  </acceptance_criteria>
  <done>Multi-GPU evidence artifact exists with sanitizer + SF10 + decision matrix; artifact is ready for human review in Task 2b; no decision made by autonomous step.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2b: Human sign-off checkpoint — review 05-06-MULTIGPU-VALIDATION.md and approve/reject Phase 5</name>
  <files>(no file modifications — checkpoint reads Task 2a's artifact and captures the human response)</files>
  <read_first>
    - .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-06-VALIDATION.md (Task 1 output — autonomous gate status)
    - .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-06-MULTIGPU-VALIDATION.md (Task 2a output — REQUIRED; if missing, Task 2a did not complete and this checkpoint cannot proceed)
    - .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-01-BASELINE.md
    - .planning/phases/04-cucascade-bump-v1-0-re-integration/04-SUMMARY.md §"Hidden-tag explicit invocation on N=2 GPU verification host"
    - .planning/STATE.md Blockers / Concerns section
  </read_first>
  <action>
    Present the human reviewer with a concise summary of Task 2a's evidence, highlight the recommendation, and pause for sign-off. DO NOT proceed to Task 3 until the human response is received.

    The autonomous work is complete — this task's role is PURELY review-and-decide. If Task 2a's artifact has not been written or committed, halt with an error message and surface Task 2a as the blocker; do not re-run Task 2a from here.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-06-MULTIGPU-VALIDATION.md &amp;&amp; test -f .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-06-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Task 2a's artifact `.planning/phases/05-cucascade-backed-parquet-i-o-migration/05-06-MULTIGPU-VALIDATION.md` exists on disk at the time this checkpoint is shown
    - Task 1's artifact `.planning/phases/05-cucascade-backed-parquet-i-o-migration/05-06-VALIDATION.md` exists on disk
    - Human response recorded verbatim, captured by Task 3's phase SUMMARY Outcome section
    - If response is `reject`, execution halts before Task 3 writes the SUMMARY — the plan surfaces the rejection reason and halts
    - Response text matches one of the resume-signal options below (or a reject variant)
  </acceptance_criteria>
  <done>Human has read Task 2a's artifact and typed one of `approved`, `approved — ship with {note}`, or `reject — {reason}`. Response is captured for Task 3 to embed verbatim.</done>
  <what-built>
    Claude has completed ALL autonomous Phase 5 work:

    - All 7 `datasource::create` / filepath `source_info` call sites migrated to `sirius::io::cucascade_datasource` (Plans 04 + 05)
    - Per-GPU `idisk_io_backend` cache on `SiriusContext` with device-guarded construction (Plan 03)
    - HYG-01 fixed: explicit stream in `filter_row_groups_with_stats` (Plan 04)
    - HYG-02 guards green across all modified files (Task 1)
    - TPC-H SF1 correctness confirmed identical to Plan 01 baseline (Task 1)
    - Adapter unit tests pass (7+ TEST_CASEs; Task 1)
    - Full unit-tests regression green (Task 1)
    - Multi-GPU evidence collected autonomously: compute-sanitizer log, per-backend cudaGetDevice readback, SF10 regression measurement, decision-matrix classification (Task 2a)

    Now the human reviewer needs to sign off. All evidence is in the two artifacts — this checkpoint is pure review-and-decide, no autonomous work remaining.
  </what-built>
  <how-to-verify>
    Read both artifacts (`05-06-VALIDATION.md` and `05-06-MULTIGPU-VALIDATION.md`) and respond with your decision.

    **Review priorities (from Task 2a's "Recommendation for Task 2b Checkpoint" section):**

    1. **IO-11 — Multi-GPU safety:** Open `05-06-MULTIGPU-VALIDATION.md` § "Error Classification" table. Any row marked NEW is a Phase 5 regression; `pre-existing` rows match Phase 4's documented GPU1→GPU0 deferrals and are acceptable. Check the "Per-Backend cudaGetDevice Readback" table — each row's readback MUST equal target device_id (if not, Plan 03's `rmm::cuda_set_device_raii` guard is broken).

    2. **IO-10 — Performance regression:** Open `05-06-MULTIGPU-VALIDATION.md` § "SF10 Performance Measurement". Decision buckets:
       - ≤ 30%: PASS outright
       - 30% < pct ≤ 50%: PASS if you're comfortable filing an upstream cucascade issue to track the regression
       - > 50%: Probably reject — investigate before shipping

    3. **Overall Phase 5 sign-off:** Cross-reference both artifacts. Confirm:
       - All 13 Phase-5 requirements have evidence (IO-01..11 + HYG-01/02)
       - No NEW regressions vs Phase 4 baseline (IO-11 clean + IO-10 within budget)
       - Any deferrals (TPC-H Q4 flake, file-handle caching upstream issue, SF10 upstream issue) are scoped to future phases with explicit markers

    **Respond with one of:**
    - `approved` — Phase 5 ships as-is. Task 3 writes the phase SUMMARY with all 13 requirements CLOSED.
    - `approved — ship with {issue/deferral note}` — Phase 5 ships with a documented caveat (e.g., "ship with SF10 upstream cucascade issue #NNN filed; 35% regression accepted"). Task 3 records the note verbatim in the SUMMARY Outcome section.
    - `reject — {reason}` — specific fixes required. Execution halts; the upstream plan responsible for the issue is revisited. Task 3 does NOT run.

    **Environment note:** If Task 2a reported that the N≥2 GPU host was unavailable, the artifact's "Environment / Availability Notes" will flag it. In that case, you can either (a) reject and require Task 2a to be re-run on a proper host before sign-off, or (b) approve-with-note and defer IO-11 multi-GPU verification to a follow-up plan — your judgement call.
  </how-to-verify>
  <resume-signal>Type "approved" or "approved — ship with {note}" to proceed to Task 3 phase summary writing, or "reject — {reason}" to halt and loop back to the failing upstream plan.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write Phase 5 SUMMARY.md (mirrors 04-SUMMARY.md template)</name>
  <files>.planning/phases/05-cucascade-backed-parquet-i-o-migration/05-SUMMARY.md</files>
  <read_first>
    - .planning/phases/04-cucascade-bump-v1-0-re-integration/04-SUMMARY.md (template to mirror — same frontmatter shape, same section layout)
    - .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-06-VALIDATION.md (Task 1 evidence artifact)
    - .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-06-MULTIGPU-VALIDATION.md (Task 2a evidence artifact — REQUIRED for IO-10 + IO-11 evidence rows)
    - .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-01-SUMMARY.md through 05-05-SUMMARY.md (per-plan summaries from Plans 01-05)
    - .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-CONTEXT.md
    - .planning/ROADMAP.md (Phase 5 Success Criteria section — mirror those into the Requirements Satisfied table)
    - .planning/REQUIREMENTS.md (IO-01..11 + HYG-01/02 wording — copy verbatim into the evidence table)
    - `/tmp/phase5-sanitizer.log` (from Task 2a)
    - The checkpoint response from Task 2b (captured in the Task 2b message log — embed verbatim)
  </read_first>
  <action>
    Write `.planning/phases/05-cucascade-backed-parquet-i-o-migration/05-SUMMARY.md` mirroring the structure of `04-SUMMARY.md`. Required sections in order:

    1. **Frontmatter** — YAML block at the top with:
       - `phase: 05-cucascade-backed-parquet-i-o-migration`
       - `status: COMPLETE`
       - `subsystem: io+scan+hygiene`
       - `tags:` include `[cucascade-io, parquet-migration, kvikio-removal, multi-gpu-safety, hyg-01, hyg-02, phase-exit]`
       - `requires: ["04-cucascade-bump-v1-0-re-integration"]`
       - `provides:` list — cucascade-backed parquet I/O adapter, per-GPU backend cache on SiriusContext, HYG-01 + HYG-02 hygiene, 13/13 Phase 5 requirements cleared
       - `affects:` — Phase 6 (consumes per-GPU backend plumbing for topology discovery + device safety work), Phase 7 (adaptive scan / P2P leverages the backend infrastructure)
       - `tech-stack.added:` — sirius::io::cucascade_datasource, cucascade::io_backend_registry on SiriusContext, per-GPU idisk_io_backend cache
       - `tech-stack.patterns:` — "Custom cudf::io::datasource subclass reporting supports_device_read() == false to force host-staging through pinned memory", "Per-GPU backend construction under rmm::cuda_set_device_raii to bind streams + pinned buffers to the correct CUDA context", "Planning-time datasource picks first GPU backend deterministically (metadata-only reads are context-neutral)", "Approach-C plumbing: task_creator seeds parquet_scan_task_global_state with gpu_io_backends map", "Approach-A plumbing: iceberg helpers extended to accept backend parameter"
       - `key-files.created:` — src/include/io/cucascade_datasource.hpp, src/io/cucascade_datasource.cpp, test/cpp/io/test_cucascade_datasource.cpp, all six per-plan SUMMARY files, 05-01-BASELINE.md, 05-06-VALIDATION.md, 05-06-MULTIGPU-VALIDATION.md, this file
       - `key-files.modified:` — src/include/sirius_context.hpp, src/sirius_context.cpp, src/op/scan/parquet_scan_task.cpp, src/include/op/scan/parquet_scan_task.hpp, src/creator/task_creator.cpp (Approach C plumbing), src/op/scan/sirius_parquet_metadata_scan_operator.cpp, src/op/scan/iceberg_scan_task.cpp, CMakeLists.txt
       - `key-decisions:` — verbatim quotes of the major decisions (no remote URI support, one backend per GPU, std::launch::async for async reads, stack-local adapters for iceberg, pinned host buffer via cudaMallocHost, Approach C for parquet / Approach A for iceberg, SF10 escalation path if >30%)
       - `plans:` — array of 6 plans with id, title, commits, requirements, outcome
       - `requirements-completed: [IO-01, IO-02, IO-03, IO-04, IO-05, IO-06, IO-07, IO-08, IO-09, IO-10, IO-11, HYG-01, HYG-02]`
       - `duration: {total time}` started / completed ISO-8601 dates

    2. **Body — Phase 5 Outcome paragraph** (≤120 words): one-paragraph rollup stating all 7 call sites migrated, per-GPU backend cache operational, SF1 correctness preserved, SF10 regression value ({X}%), compute-sanitizer memcheck clean of new errors, HYG-01 + HYG-02 sweep complete, Phase 5 ships. Include the exact Task 2b checkpoint response verbatim.

    3. **Phase 5 Outcome** header: PASS | PASS with deferrals | FAIL (match the Task 2b checkpoint signal).

    4. **Requirements Satisfied** table with columns: `REQ-ID | Description | Evidence | Where proved`. One row per requirement (13 rows):
       - IO-01..11 — copy description from REQUIREMENTS.md; evidence: specific grep command outputs or test results; where proved: specific plan number + task (e.g., "Plan 05-02 Task 1", "Plan 05-04 Task 2")
       - IO-10 evidence cites `05-06-MULTIGPU-VALIDATION.md § SF10 Performance Measurement`
       - IO-11 evidence cites `05-06-MULTIGPU-VALIDATION.md § IO-11 compute-sanitizer memcheck`
       - HYG-01 — `grep -c cuda_stream_default src/op/scan/parquet_scan_task.cpp` = 0; Plan 04 Task 1
       - HYG-02 — full file-set sweep from 05-06-VALIDATION.md Section 2; Plan 06 Task 1

    5. **Commits Landed** section — list Phase 5 commits in most-recent-first order with the commit shape breakdown (mirror Phase 4's shape).

    6. **Deviations from Plan** (if any). Examples:
       - If Approach C or Approach A needed to be deviated from (should NOT happen — both are frontmatter-locked), document why.
       - If TPC-H Q4 flake was investigated as part of Phase 5 scope but remains unresolved, note the finding and re-scope to Phase 6 or leave open.

    7. **TODO Markers Added for Future Phases** — table:
       - `TODO(MGPU-03)` from Phase 4 still at test_downgrade_executor.cpp:813 (re-confirm it's still present — Phase 5 does not remove it)
       - If file-handle caching upstream issue filed: record URL/ID
       - If SF10 upstream cucascade issue filed: record URL/ID

    8. **Test Results** — full-unit-tests + TPC-H SF1 + adapter unit tests + compute-sanitizer + SF10 measurement, all with exit codes + counts (cross-reference both VALIDATION artifacts for specifics).

    9. **Cucascade API Usage Notes** — what parts of cucascade PR #96 Phase 5 consumed: `idisk_io_backend`, `io_backend_registry`, `register_builtin_io_backends`, `create_default_backend()`.

    10. **Open Questions Resolved** (from 05-RESEARCH.md):
        - OQ-1 (Preferred-device plumbing): resolved — Approach C for parquet_scan_task (task_creator seeds global_state), Approach A for iceberg (helper signatures extended)
        - OQ-2 (Planning-time device selection): resolved — first available device with explanatory comment
        - OQ-3 (cucascade size API): resolved — use std::filesystem::file_size at adapter construction
        - OQ-4 (TPC-H Q4 flake): {status}
        - OQ-5 (device_read override): resolved — base-class CUDF_FAIL correct; not overridden
        - OQ-6 (kvikio residue post-migration): resolved — grep gate IO-08 proves zero Sirius-source use

    11. **Issues Encountered Across the Phase** — any flakes, sandbox drift, unexpected findings.

    12. **Next Phase Prep (Phase 6)** — starting state for Multi-GPU Gap Closure work:
        - Per-GPU `idisk_io_backend` cache on SiriusContext is the substrate MGPU-01 (topology discovery) + MGPU-03 (device-guard enforcement) plug into
        - HYG-01 removed one `cuda_stream_default`; remaining hits in `src/legacy/` + `src/cuda/` are frozen-path hygiene debt (out of Phase 6 scope but documented)
        - Phase 4 hidden-test regressions (GPU1→GPU0 converter return leg) still pending Phase 6 (MGPU-03) + Phase 7 (MGPU-06)
        - TPC-H Q4 flake: {fixed/unchanged/scoped to future}

    13. **Self-Check** — checklist mirroring 04-SUMMARY.md's self-check:
        - All 6 per-plan summaries exist (05-01..05-06) — CONFIRMED
        - 05-SUMMARY.md at `.planning/phases/05-cucascade-backed-parquet-i-o-migration/` — FOUND (this file)
        - All 13 requirement IDs present in Requirements Satisfied table with evidence — CONFIRMED
        - `grep -rnw 'datasource::create' src/` = 0 — CONFIRMED
        - HYG-02 sweep clean across all modified files — CONFIRMED
        - SF1 per-query diff matches Plan 01 baseline — CONFIRMED
        - IO-11 multi-GPU validation performed on N≥2 host with zero NEW errors vs Phase 4 baseline (evidence in 05-06-MULTIGPU-VALIDATION.md) — CONFIRMED
        - IO-10 SF10 regression measurement within budget (evidence in 05-06-MULTIGPU-VALIDATION.md) — CONFIRMED
        - Phase 5 sign-off checkpoint response (Task 2b) recorded verbatim — CONFIRMED

    After writing, commit via the standard `node gsd-tools.cjs commit` flow (orchestrator handles this).
  </action>
  <verify>
    <automated>test -f .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-SUMMARY.md &amp;&amp; grep -q "Phase 5 Outcome" .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-SUMMARY.md &amp;&amp; grep -q "Requirements Satisfied" .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-SUMMARY.md &amp;&amp; grep -cE "IO-0[1-9]|IO-1[01]|HYG-0[12]" .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-SUMMARY.md | awk '$1 >= 13 { exit 0 } { exit 1 }' &amp;&amp; grep -q "Next Phase Prep" .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-SUMMARY.md &amp;&amp; grep -q "05-06-MULTIGPU-VALIDATION" .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/05-cucascade-backed-parquet-i-o-migration/05-SUMMARY.md` exists
    - Frontmatter declares `status: COMPLETE`
    - Frontmatter includes `requirements-completed` list of exactly 13 IDs: IO-01, IO-02, IO-03, IO-04, IO-05, IO-06, IO-07, IO-08, IO-09, IO-10, IO-11, HYG-01, HYG-02
    - Body contains section header "Phase 5 Outcome"
    - Body contains section header "Requirements Satisfied" with table rows referencing each of the 13 requirement IDs (grep count ≥ 13)
    - Body contains section header "Test Results" with specific exit codes and counts (not placeholders)
    - Body contains section header "Next Phase Prep" referencing Phase 6 (MGPU-01..05)
    - Body contains section header "Self-Check" with line-by-line CONFIRMED markers
    - If SF10 regression > 30%, body documents the upstream cucascade issue reference (URL or tracking ID)
    - If Task 2b checkpoint responded with "approved — ship with {note}", the note is recorded verbatim in the Outcome section
    - File references BOTH `05-06-VALIDATION.md` AND `05-06-MULTIGPU-VALIDATION.md` as evidence artifacts (links or relative paths)
  </acceptance_criteria>
  <done>Phase 5 SUMMARY written with full evidence trail (including multi-GPU artifact reference); all 13 requirements documented with plan-level references; ready for ROADMAP + REQUIREMENTS update (orchestrator-driven).</done>
</task>

</tasks>

<verification>
Phase-5 Plan 06 completion:
1. `05-06-VALIDATION.md` exists with all 5 sections (IO-08 gate, HYG-02 sweep, SF1 diff, adapter tests, full regression).
2. `05-06-MULTIGPU-VALIDATION.md` exists with compute-sanitizer log classification, per-backend cudaGetDevice readback, SF10 regression measurement, decision matrix, and recommendation for checkpoint — written autonomously by Task 2a.
3. Task 2b checkpoint is pure review-and-decide — it reads Task 2a's artifact and captures the human response; no autonomous work interleaved.
4. IO-08 global grep returns 0 hits; HYG-02 sweep shows 0 across all modified files.
5. SF1 per-query diff matches Plan 01 baseline 22/22.
6. IO-11 multi-GPU compute-sanitizer run performed; no NEW errors beyond Phase 4 baseline; per-backend device readback log entries confirm per-GPU context pinning.
7. IO-10 SF10 measurement captured; regression either ≤30% (PASS) or documented with upstream issue (PASS with escalation).
8. `05-SUMMARY.md` written with all 13 Phase 5 requirements marked closed with evidence (including multi-GPU artifact reference).
9. Human Task 2b checkpoint signal recorded verbatim in the SUMMARY Outcome.
</verification>

<success_criteria>
- All 13 Phase-5 requirements (IO-01..11, HYG-01, HYG-02) closed with evidence.
- Phase 5 ships with PASS (or PASS with documented upstream issue for SF10).
- Autonomous work (Task 1 + Task 2a) and human sign-off (Task 2b) are cleanly separated — restart semantics are clean.
- Validation artifacts + phase summary ready for orchestrator to drive ROADMAP + REQUIREMENTS updates.
- No NEW multi-GPU regressions beyond Phase 4's documented deferrals.
</success_criteria>

<output>
This is the phase exit plan — no per-plan SUMMARY is written (the phase-level SUMMARY IS the artifact). The orchestrator commits the VALIDATION + MULTIGPU-VALIDATION + SUMMARY files after checkpoint sign-off and triggers the ROADMAP + STATE updates.
</output>
