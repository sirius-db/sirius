---
phase: 15-mgpu-operator-colocation-audit
plan: 04
type: execute
wave: 4
depends_on: ["15-01", "15-02", "15-03"]
files_modified:
  - .planning/phases/15-mgpu-operator-colocation-audit/15-VALIDATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Each of the 3 acceptance criteria from 15-CONTEXT.md lines 86-88 is evaluated with verbatim evidence: PASS, PARTIAL, or DEFERRED with rationale."
    - "C1 (every site has INVARIANT comment OR patch) is grep-validated against the 11-site list from 15-CONTEXT.md + the per-site verdict table from 15-AUDIT-LOG.md."
    - "C2 (mgpu_stress_test runs N iterations of [mgpu] tests cleanly) is validated by re-running the [mgpu_stress] suite via MCP."
    - "C3 (docs/super-sirius/ covers the per-task-device contract) is grep-validated."
    - "Regression check: full [mgpu] + [integration][TPC-H] suites still match Phase 14 baselines (12-13/13 [mgpu], 48/48 [integration][TPC-H]) — Phase 15 introduces no new failures."
    - "HYG-02 baseline preserved at <= 40."
    - "15-VALIDATION.md mirrors 14-VALIDATION.md's structure (one section per criterion, verbatim MCP outputs, single 'Overall' verdict at the bottom — no leakage of '**PASS** | **PARTIAL**' placeholder template syntax in the final file)."
  artifacts:
    - path: ".planning/phases/15-mgpu-operator-colocation-audit/15-VALIDATION.md"
      provides: "Phase 15 ship-gate validation report. Mirrors 14-VALIDATION.md's structure: one section per acceptance criterion, verbatim MCP run output, regression checks, hygiene & invariants table, single Overall verdict at the bottom."
      min_lines: 100
      contains: "Overall:"
      contains_also: "Criterion 1"
      contains_also_2: "Criterion 2"
      contains_also_3: "Criterion 3"
  key_links:
    - from: ".planning/phases/15-mgpu-operator-colocation-audit/15-VALIDATION.md"
      to: ".planning/phases/15-mgpu-operator-colocation-audit/15-AUDIT-LOG.md"
      via: "Criterion 1 cites the per-site verdict table from the audit log"
      pattern: "15-AUDIT-LOG.md"
    - from: ".planning/phases/15-mgpu-operator-colocation-audit/15-VALIDATION.md"
      to: ".planning/phases/14-sched-rr-distribution/14-VALIDATION.md"
      via: "Regression baselines are taken from Phase 14's validation report (12-13/13 [mgpu], 48/48 [integration][TPC-H])"
      pattern: "14-VALIDATION.md"
---

<objective>
Validate Phase 15 against all 3 acceptance criteria from `15-CONTEXT.md` lines 86-88, write `15-VALIDATION.md` with verbatim MCP outputs per criterion, and emit the Overall verdict that lets the orchestrator close v1.3.

Purpose: a ship-gate. Each criterion gets a real MCP run (not a static analysis), the verdict is one of PASS / PARTIAL / DEFERRED with documented rationale, and the validation file mirrors `14-VALIDATION.md`'s structure so the orchestrator can compare them side-by-side.

Output: 1 new file `15-VALIDATION.md` (~150-300 lines), 1 commit. No source changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/15-mgpu-operator-colocation-audit/15-CONTEXT.md
@.planning/phases/15-mgpu-operator-colocation-audit/15-01-SUMMARY.md
@.planning/phases/15-mgpu-operator-colocation-audit/15-02-SUMMARY.md
@.planning/phases/15-mgpu-operator-colocation-audit/15-03-SUMMARY.md
@.planning/phases/15-mgpu-operator-colocation-audit/15-AUDIT-LOG.md
@.planning/phases/14-sched-rr-distribution/14-VALIDATION.md

<interfaces>
<!-- Validation file structure exemplar — copy 14-VALIDATION.md's shape verbatim. -->

The 14-VALIDATION.md structure to mirror (verified by reading it):

```markdown
{frontmatter-start}
phase: <phase>
artifact: validation
verdict: <PASS | PARTIAL | BLOCKED>
date: <YYYY-MM-DD>
{frontmatter-end}

# Phase <N> — VALIDATION (ship-gate)

Per `<N>-CONTEXT.md` lines XX-YY acceptance criteria, validated against ...

## Criterion 1 — <name>

Verdict: **<PASS | PARTIAL | DEFERRED>**

Run: `<MCP command>`
Exit code: <N>
Duration: <S>s
Result: <summary>

Verbatim tail:
```
<verbatim output>
```

<commentary>

## Criterion 2 — <name>

(same structure)

## Criterion 3 — <name>

(same structure)

## Regression — <suite name>

(same structure)

## Hygiene & invariants

| Check | Result |
|-------|--------|
| <name> | <value> |

## Overall: **<PASS | PARTIAL | BLOCKED>**

<final paragraph reconciling all criteria>
```

The verdict frontmatter goes one level deeper than just PASS/PARTIAL/BLOCKED — Phase 14 used `verdict: PASS` even though Criterion 3 was DEFERRED, because the deferral was for an MCP-wrapper limitation, not a code issue. Phase 15's frontmatter verdict should follow the same logic: PASS if all 3 criteria are PASS or contain only documented deferrals/PARTIALs that are not Phase-15 source-code issues.

**Verbatim MCP-output convention:** copy the tail (last 20-30 lines) into a triple-backticked code block. Don't summarize — the verbatim block is the audit trail.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-run all validation MCP commands and capture verbatim outputs</name>
  <read_first>
    - .planning/phases/15-mgpu-operator-colocation-audit/15-CONTEXT.md (acceptance criteria at lines 86-88)
    - .planning/phases/15-mgpu-operator-colocation-audit/15-AUDIT-LOG.md (per-site verdicts from Wave 1)
    - .planning/phases/15-mgpu-operator-colocation-audit/15-01-SUMMARY.md (Wave 1 outcome)
    - .planning/phases/15-mgpu-operator-colocation-audit/15-02-SUMMARY.md (Wave 2 outcome — stress test verdict)
    - .planning/phases/15-mgpu-operator-colocation-audit/15-03-SUMMARY.md (Wave 3 outcome — doc verdict)
    - .planning/phases/14-sched-rr-distribution/14-VALIDATION.md (regression baselines)
  </read_first>
  <files>
    None modified — this task only runs MCP commands and captures their output for Task 2's writeup.
  </files>
  <action>
    Execute these 5 MCP/bash invocations in order. Capture each command's exit code, duration, and tail output verbatim.

    **Run 1 — Criterion 1 grep validation (Bash, no MCP):**
    ```
    cd /home/felipe/sirius/.worktrees/ws-9aa781df-6d8c-4395-9329-737a67e8e272
    grep -c 'INVARIANT (SCHED-RR contract)' \
      src/op/sirius_physical_concat.cpp \
      src/op/sirius_physical_top_n.cpp \
      src/op/sirius_physical_ungrouped_aggregate.cpp \
      src/op/sirius_physical_sort_sample.cpp \
      src/op/sirius_physical_sort_partition.cpp \
      src/op/sirius_physical_table_scan.cpp \
      src/op/sirius_physical_order.cpp \
      src/op/sirius_physical_merge_sort.cpp \
      src/op/sirius_physical_nested_loop_join.cpp
    grep -c 'NEEDS-PATCH' .planning/phases/15-mgpu-operator-colocation-audit/15-AUDIT-LOG.md
    ```
    Acceptance:
    - Total INVARIANT comment count across the 9 files >= 11
    - NEEDS-PATCH count in audit log = 0 (or, if non-zero, MUST be surfaced + Wave 1 should have already escalated; this is a final consistency check)

    **Run 2 — Criterion 2 stress test (MCP):**
    Run via the `mcp__project-commands__run_command` tool with `name=unit-tests filter="[mgpu_stress]"`.
    Acceptance:
    - exit 0 within 30 minutes
    - all assertions pass (no `require_gpu_matches_cpu` divergence)
    - if PARTIAL/budget-exceeded, capture the iteration count completed and classify

    **Run 3 — Criterion 3 doc grep (Bash):**
    ```
    grep -ri 'per-task-device contract' docs/super-sirius/
    grep -ri 'SCHED-RR' docs/super-sirius/
    grep -c 'lock_or_prepare_batch' docs/super-sirius/pipeline-execution.md
    ```
    Acceptance:
    - First grep returns ≥ 1 match (the new section).
    - Second grep returns ≥ 3 matches (heading + body + README link).
    - Third grep returns ≥ 1 match.

    **Run 4 — Regression [mgpu] (MCP):**
    Run via the `mcp__project-commands__run_command` tool with `name=unit-tests filter="[mgpu]"`.
    Acceptance: same exit code + same single Phase-12-territory failure as Phase 14 baseline (12/13 PASS with the `vector::_M_range_check` failure on `physical_order - small sort stays single-GPU`). NOTE: with Plan 15-02's stress test added, the test case count may now be 14 (= 13 existing + 1 new mgpu_stress) but `[mgpu]` filter pulls in the stress test too, which extends runtime. Re-baseline accordingly.

    **Run 5 — Regression [integration][TPC-H] (MCP):**
    Run via the `mcp__project-commands__run_command` tool with `name=unit-tests filter="[integration][TPC-H]"`.
    Acceptance: 48/48 PASS (matches Phase 14 baseline 71608 assertions).

    **Run 6 — HYG-02 (Bash):**
    ```
    grep -rn 'rmm::cuda_stream_default' src/ | wc -l
    ```
    Acceptance: ≤ 40.

    Capture all 6 outputs into scratch — Task 2 will paste verbatim into the validation report.
  </action>
  <verify>
    <automated>true</automated>
    <!-- Verification is encoded in Task 2's writeup which checks each captured output. -->
  </verify>
  <done>
    Six command outputs captured (5 MCP/bash + 1 grep). Each has: exit code, duration, tail (last 20-30 lines), summary.

    Per-criterion provisional verdicts drafted:
    - Criterion 1: <PASS | PARTIAL | DEFERRED>
    - Criterion 2: <PASS | PARTIAL | DEFERRED>
    - Criterion 3: <PASS | PARTIAL | DEFERRED>
    - Regression [mgpu]: <PASS | PARTIAL>
    - Regression [integration][TPC-H]: <PASS | PARTIAL>
    - HYG-02: <number>

    Overall verdict computed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write 15-VALIDATION.md and commit</name>
  <read_first>
    - .planning/phases/14-sched-rr-distribution/14-VALIDATION.md (full file — copy structure)
    - .planning/phases/13-q11-multi-gpu-illegal-address/13-VALIDATION.md if it exists (for additional structure cross-reference)
    - All scratch outputs from Task 1
  </read_first>
  <files>
    .planning/phases/15-mgpu-operator-colocation-audit/15-VALIDATION.md (new)
  </files>
  <action>
    **Step 2.A — Write the validation file** with this exact structure:

    ```markdown
    ---
    phase: 15-mgpu-operator-colocation-audit
    artifact: validation
    verdict: <PASS | PARTIAL>
    date: 2026-04-30  # or whatever today's date is when you run this
    ---

    # Phase 15 — VALIDATION (ship-gate)

    Per `15-CONTEXT.md` lines 86-88 acceptance criteria, validated against `audit/mgpu-operator-colocation` HEAD <commit-hash> (the 15-03 docs commit).

    Plan dependencies:
    - 15-01 (audit + invariant comments) — `<commit-hash>` `audit(15-01): comment-only INVARIANT ...`
    - 15-02 (stress test + setter) — `<commit-hash>` `feat(15-02): ...` + `<commit-hash>` `test(15-02): ...`
    - 15-03 (documentation) — `<commit-hash>` `docs(15-03): ...`

    ## Criterion 1 — Every site has an INVARIANT comment OR a patch

    Verdict: **<PASS | PARTIAL>**

    Run: bash grep against the 11-site list from 15-CONTEXT.md + audit-log NEEDS-PATCH count
    Exit code: 0
    Result: <N> INVARIANT comments across 9 operator files; <M> NEEDS-PATCH in audit log

    Verbatim:
    ```
    <Run 1 output>
    ```

    Per-site verdict table (copied from 15-AUDIT-LOG.md):

    | # | File | Line | Verdict |
    |---|------|------|---------|
    | 1 | src/op/sirius_physical_concat.cpp | 193 | <V> |
    | ... |
    | 11 | src/op/sirius_physical_nested_loop_join.cpp | 415 | <V> |

    Summary: <N> SAFE / <M> NEEDS-PATCH / <U> UNCLEAR. <commentary>

    ## Criterion 2 — mgpu_stress_test runs cleanly

    Verdict: **<PASS | PARTIAL>**

    Run: `mcp__project-commands__run_command name=unit-tests filter="[mgpu_stress]"`
    Exit code: <N>
    Duration: <S>s
    Result: <X>/<Y> PASS, <assertions> assertions

    Verbatim tail:
    ```
    <Run 2 output last 20 lines>
    ```

    <commentary on iteration count, query corpus, any PARTIAL classification>

    ## Criterion 3 — docs/super-sirius/ covers the per-task-device contract

    Verdict: **<PASS>**

    Run: bash grep against docs/super-sirius/
    Exit code: 0
    Result: <N> matches for "per-task-device contract", <M> matches for "SCHED-RR"

    Verbatim:
    ```
    <Run 3 output>
    ```

    <commentary on which doc files contain it, which sections, README cross-link verified>

    ## Regression — [mgpu] suite

    Verdict: **<PASS-with-Phase-12-note | PARTIAL>**

    Run: `mcp__project-commands__run_command name=unit-tests filter="[mgpu]"`
    Exit code: <N>
    Duration: <S>s
    Result: <X>/<Y> PASS

    Verbatim tail:
    ```
    <Run 4 output>
    ```

    Comparison vs Phase 14 baseline (`14-VALIDATION.md` Criterion 1): same single Phase-12-territory failure (`physical_order - small sort stays single-GPU`, `vector::_M_range_check`). Plus the new `mgpu_stress` test which is part of the `[mgpu]` filter — counts updated accordingly. NEW failures count: <N> (must be 0 for PASS).

    ## Regression — [integration][TPC-H] suite

    Verdict: **<PASS>**

    Run: `mcp__project-commands__run_command name=unit-tests filter="[integration][TPC-H]"`
    Exit code: <N>
    Duration: <S>s
    Result: <X>/<Y> PASS, <assertions> assertions

    Verbatim tail:
    ```
    <Run 5 output>
    ```

    Comparison vs Phase 14 baseline (48/48, 71608 assertions): <same | new>.

    ## Hygiene & invariants

    | Check | Result |
    |-------|--------|
    | HYG-02 (`rmm::cuda_stream_default` count in `src/`) | <N> (≤ 40 baseline preserved) |
    | Plan 15-01 source diff (9 operator files + 15-AUDIT-LOG.md) | comment-only |
    | Plan 15-02 source diff (task_scheduler.hpp + test_mgpu_stress.cpp + CMakeLists.txt) | <lines> |
    | Plan 15-03 source diff (docs only) | doc-only |
    | Build (Plans 15-01 + 15-02 already exercised; doc-only commit doesn't need rebuild) | PASS — exit 0 |
    | Branch `audit/mgpu-operator-colocation` HEAD | `<commit-hash>` |
    | Branch base | `feat/sched-rr-distribution @ 0ee3166` (Phase 14 HEAD) |
    | All commits in this branch use `--no-verify` | <yes/no — verify via `git log --pretty=full` if unsure> |

    ## Overall: **<PASS | PARTIAL>**

    <Final reconciliation paragraph: list each criterion's verdict, note any caveats (e.g., the Phase-12-territory regression which is documented and not a Phase 15 issue, same as Phase 14 caveat), and state the v1.3 ship verdict.>

    Phase 15 closes v1.3 (Multi-GPU Distribution). All operator-colocation sites have verified upstream-protection comments; the SCHED-RR distribution is empirically correct under counter-offset rotation; the per-task-device contract is documented for future maintainers.
    ```

    **Step 2.B — Self-check the file** before committing:
    - No `**PASS** | **PARTIAL**` placeholder syntax leaks (search for the literal string and replace with the actual verdict).
    - All `<...>` placeholders filled in with real values.
    - Verbatim blocks are pasted from the actual MCP run captures, NOT paraphrased.
    - Per-criterion verdicts add up to the Overall verdict consistently.

    Run via Bash tool:
    ```
    grep -F '**PASS** | **PARTIAL**' .planning/phases/15-mgpu-operator-colocation-audit/15-VALIDATION.md
    grep -F '<' .planning/phases/15-mgpu-operator-colocation-audit/15-VALIDATION.md
    ```
    Both must return empty (or only false positives like `<commit-hash>` in literal git output blocks). If real placeholders remain, fix them before committing.

    **Step 2.C — Commit:**
    ```
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(15-04): validate Phase 15 against acceptance criteria" --files .planning/phases/15-mgpu-operator-colocation-audit/15-VALIDATION.md
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/15-mgpu-operator-colocation-audit/15-VALIDATION.md &amp;&amp; grep -E '^## Overall: \*\*(PASS|PARTIAL)\*\*$' .planning/phases/15-mgpu-operator-colocation-audit/15-VALIDATION.md &amp;&amp; ! grep -F '**PASS** | **PARTIAL**' .planning/phases/15-mgpu-operator-colocation-audit/15-VALIDATION.md &amp;&amp; git log --oneline -1 | grep -F 'docs(15-04)'</automated>
  </verify>
  <done>
    - `15-VALIDATION.md` exists, mirrors 14-VALIDATION.md structure (one section per acceptance criterion + regressions + hygiene + Overall).
    - All 3 acceptance criteria from 15-CONTEXT.md lines 86-88 evaluated.
    - All MCP outputs verbatim (not paraphrased).
    - Overall verdict is one of PASS / PARTIAL (not BLOCKED — Wave 1 would have already blocked if NEEDS-PATCH appeared).
    - No template placeholder syntax leaks.
    - Single commit `docs(15-04): ...` exists on branch.
  </done>
</task>

</tasks>

<verification>
- All 3 acceptance criteria from 15-CONTEXT.md lines 86-88 have been evaluated with verbatim MCP evidence.
- `15-VALIDATION.md` exists and is well-formed.
- Phase 15 ships PASS (or documented PARTIAL).
- Regression suites match Phase 14 baselines.
- HYG-02 ≤ 40.
- Branch `audit/mgpu-operator-colocation` is ready for PR / merge.
</verification>

<success_criteria>
- 1 file created: `15-VALIDATION.md`
- 1 commit: `docs(15-04): validate Phase 15 against acceptance criteria`
- Overall verdict line `^## Overall: \*\*(PASS|PARTIAL)\*\*$` exists in the file.
- v1.3 milestone is ready to close (orchestrator's call after this report).
</success_criteria>

<output>
After completion, create `.planning/phases/15-mgpu-operator-colocation-audit/15-04-SUMMARY.md` documenting:
- Final verdicts per criterion
- Total wall-clock for all MCP runs
- Branch/commit pointers
- v1.3 ship recommendation (PASS / PARTIAL / BLOCKED)
- Note: 15-VALIDATION.md is the canonical artifact; 15-04-SUMMARY.md is the plan-execution metadata.
</output>
