---
phase: 04-cucascade-bump-v1-0-re-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/creator/task_creator.cpp
  - src/include/creator/task_creator.hpp
  - src/pipeline/pipeline_executor.cpp
  - src/pipeline/gpu_pipeline_executor.cpp
  - src/include/pipeline/gpu_pipeline_task.hpp
  - src/include/pipeline/sirius_pipeline_task_states.hpp
  - src/op/scan/duckdb_scan_executor.cpp
  - src/include/op/scan/duckdb_scan_executor.hpp
  - test/cpp/integration/test_gpu_execution_locality.cpp
  - test/cpp/config/test_context.cpp
  - CMakeLists.txt
  - src/sirius_context.cpp
autonomous: false
requirements:
  - PORT-01
  - PORT-02
  - PORT-04

must_haves:
  truths:
    - "Push-model dispatch plumbing (preferred_device_id on local+global state) is present in pipeline task-state headers"
    - "task_creator computes a data-locality score and maps NUMA nodes to GPUs"
    - "management_eventloop / pipeline_executor routes tasks by preferred_device_id (push-model)"
    - "At-capacity preferred task waits on that executor (no fallback-to-another-executor path) — user-decision-locked in STATE.md"
    - "duckdb_scan_executor.select_target_gpu distributes scan batches across GPUs"
    - "Integration test test_gpu_execution_locality.cpp compiles and exposes [data_locality] tag"
    - "test/cpp/config/test_context.cpp exposes the [multi_gpu_foundation] tag against dev's YAML fixtures"
    - "No DuckDB vocabulary types (LogicalType::INTEGER/BIGINT/VARCHAR) were re-introduced by the cherry-picks"
  artifacts:
    - path: "src/include/pipeline/gpu_pipeline_task.hpp"
      provides: "get_preferred_device_id() accessor"
      contains: "preferred_device_id"
    - path: "src/include/pipeline/sirius_pipeline_task_states.hpp"
      provides: "preferred_device_id on both local and global state"
      contains: "preferred_device_id"
    - path: "src/creator/task_creator.cpp"
      provides: "compute_data_locality_score + NUMA->GPU mapping"
      contains: "compute_data_locality_score"
    - path: "src/pipeline/pipeline_executor.cpp"
      provides: "preferred_device_id routing in management_eventloop with wait_on_preferred_device semantics"
      contains: "wait_on_preferred_device"
    - path: "src/op/scan/duckdb_scan_executor.cpp"
      provides: "select_target_gpu for cross-GPU scan distribution"
      contains: "select_target_gpu"
    - path: "test/cpp/integration/test_gpu_execution_locality.cpp"
      provides: "[data_locality] integration tests"
      contains: "[data_locality]"
  key_links:
    - from: "src/pipeline/pipeline_executor.cpp"
      to: "src/include/pipeline/sirius_pipeline_task_states.hpp"
      via: "preferred_device_id lookup on local_state then global_state"
      pattern: "preferred_device_id"
    - from: "src/op/scan/duckdb_scan_executor.cpp"
      to: "src/creator/task_creator.cpp"
      via: "select_target_gpu feeds locality score computation"
      pattern: "select_target_gpu|compute_data_locality_score"
---

<objective>
Cherry-pick the 5 code-carrying v1.0 commits that do NOT touch the PR #579 downgrade rewrite. This lands the push-model task dispatch plumbing (`preferred_device_id` on local + global state), the `task_creator` locality score, the `management_eventloop` routing loop, the `duckdb_scan_executor` cross-GPU batch distribution, and the integration test file. PR #579 collision work is isolated to Plan 03.

Purpose: These 5 commits have MODERATE or TRIVIAL conflict classifications per research section 2 — they are line-level / hunk-level conflicts resolvable without re-authoring. Doing them as a contiguous block keeps authorship preserved and builds incremental confidence before the hard re-authoring work in Plan 03.

Output: 5 cherry-pick commits on the current branch preserving v1.0 authorship (+ possibly 1 small fixup commit for `e1dab76` per research OQ-1), each followed by a green `build` check. A checkpoint:human-verify task (Task 3b) gates the management_eventloop rewrite before it's committed, because the "wait on preferred GPU instead of falling back" behavior is a user-decision-locked invariant (STATE.md). Full unit-tests are run at the end of the block.

**autonomous=false** (Task 3b is a blocking human-verify checkpoint). This flip from the prior revision is required because revision W6 identified that an auto-executor could silently replace "wait" semantics with "fallback" semantics when dev's task-queue shape diverges from v1.0's. Gating the commit via human review preserves the locked decision.

Commits in scope (chronological order per research section 5):
  1. `3777645` — test(01-02): multi-GPU foundation validation tests (test_context.cpp)
  2a. `59bc284` PLUMBING PORTION — test_states + task + task_creator hunks (4 trivial files)
  2b. `59bc284` STRUCTURAL PORTION — sirius_context.cpp carve-out (HARD — downgrade_executor hunks deferred to Plan 03)
  3. `dd9264b` — feat(02-01): management_eventloop routes tasks by preferred_device_id (2 files; GATED BY CHECKPOINT for wait-semantics)
  4. (optional) `e1dab76` — merge-fixup on src/sirius_context.cpp — include only if delta is novel
  5. `7f18e66` — feat(02-02): distribute scan batches across GPUs (duckdb_scan_executor.*)
  6. `2e6ba26` — test(02-02): integration tests for data-locality (new file test_gpu_execution_locality.cpp + CMakeLists)

Commits deferred to Plan 03 (touch the PR #579 landmine and need re-authoring, NOT cherry-pick):
  - `dd86dd0` — feat(01-01): NUMA-aware downgrade (touches downgrade_executor.{hpp,cpp}, downgrade_task.{hpp,cpp}, sirius_context.cpp)
  - `c5a3d8e` — test(01-03): downgrade + multi-GPU-transfer tests (test_downgrade_executor.cpp)
  - `ec2399e` — test(03-01): NUMA downgrade ordering tests (test_downgrade_executor.cpp)
  - `0d99cde` — test(03-01): MEM-04 P2P + MEM-05 scan distribution tests (test_downgrade_executor.cpp)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-cucascade-bump-v1-0-re-integration/04-CONTEXT.md
@.planning/phases/04-cucascade-bump-v1-0-re-integration/04-RESEARCH.md
@.planning/phases/04-cucascade-bump-v1-0-re-integration/04-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Interfaces each cherry-pick introduces or depends on. Executor should not explore the -->
<!-- codebase for these — they are the contracts being re-authored onto dev's shape. -->

### From `59bc284` (preferred_device_id plumbing):

`src/include/pipeline/sirius_pipeline_task_states.hpp` gains:
  - `std::optional<int> preferred_device_id` field on `gpu_pipeline_task_local_state` (per-task override, wins over global)
  - `std::optional<int> preferred_device_id` field on `gpu_pipeline_task_global_state` (pipeline-level default)

`src/include/pipeline/gpu_pipeline_task.hpp` gains:
  - `std::optional<int> get_preferred_device_id() const` on `gpu_pipeline_task` (local wins over global)

`src/creator/task_creator.cpp` gains:
  - `double compute_data_locality_score(const task& t, int device_id, const system_topology_info& topo)` — higher score = better target GPU for this task based on input residency + NUMA proximity

### From `dd9264b` (push-model dispatch in pipeline_executor):

`src/pipeline/pipeline_executor.cpp` management_eventloop gains routing block:
  After popping a task, inspect `task->get_preferred_device_id()`. If present, route to that executor. If no preference, keep round-robin. If preferred executor is at capacity, **WAIT on it rather than falling back** (v1.0 Phase 02-01 decision, locked in STATE.md decisions). The wait-semantics is gated by Task 3b's human-verify checkpoint.

### From `7f18e66` (cross-GPU scan distribution):

`src/include/op/scan/duckdb_scan_executor.hpp` gains:
  - `int select_target_gpu(const std::vector<memory_space*>& gpu_spaces)` — weighted distribution by free memory on each GPU's space
  - `std::vector<memory_space*> gpu_memory_spaces_` private member

### From `2e6ba26` (new integration test file):

`test/cpp/integration/test_gpu_execution_locality.cpp` is NEW — Catch2 tags:
  - `[data_locality]` — visible, runs on single-GPU hosts
  - `[.][data_locality][multi_gpu]` — hidden (leading dot), requires N>=2 GPUs, WARN+return on single-GPU

CMakeLists.txt addition: file path appended to TEST_SOURCES.

### From `3777645` (multi_gpu_foundation tests):

`test/cpp/config/test_context.cpp` gains TEST_CASEs tagged `[multi_gpu_foundation]` and `[.][multi_gpu_foundation]`. dev PR #565 rewrote this file's fixtures for YAML config; v1.0's env-var + config-file fixtures must be translated to the YAML test pattern now in the file.

### Dev surface (PR #565, PR #564/#626/#628) the cherry-picks must adapt to:

- `src/sirius_context.cpp` — reshaped by PR #565 (YAML config). v1.0's 56-line addition (P2P loop, gpu_numa_node pass-through) from commit `59bc284` lands here. CRITICAL: v1.0 also modifies this file's downgrade_executor construction which is PR #579 territory — that portion is DEFERRED TO PLAN 03. During this plan's cherry-pick of `59bc284`, discard the downgrade_executor hunks and commit only the P2P-loop + gpu_numa_node-passthrough portion. Leave a `TODO(04-03)` marker where the downgrade construction would be touched.
- `src/include/pipeline/gpu_pipeline_task.hpp` — 11 dev refactors (PR #564, #628, #626, #643, #561, #540, #590, #531, 4e44ebe, #596, #092bcf5). Mostly whitespace + namespace drift.
- `src/include/pipeline/sirius_pipeline_task_states.hpp` — same 11 refactors. v1.0's preferred_device_id field additions are primitive-typed (std::optional<int>) and orthogonal to sirius-native type changes.

### Pre-commit pipeline:
Every commit runs clang-format + codespell + cmake-format hooks. Expect whitespace fixups in cherry-picked hunks. The *semantic* diff is what matters for attribution (research R7).

### Merge-fixup inspection command (revision W5):
Per research OQ-1: the correct command to determine if `e1dab76` merge-fixup introduced
novel `src/sirius_context.cpp` content is:
  `git diff 59bc284 81d4c75 -- src/sirius_context.cpp`
NOT `git show e1dab76 -- src/sirius_context.cpp` (which only shows conflict-resolution markers per research OQ-1).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cherry-pick 3777645 (multi_gpu_foundation tests against dev YAML fixtures)</name>
  <files>test/cpp/config/test_context.cpp</files>
  <read_first>
    - .planning/phases/04-cucascade-bump-v1-0-re-integration/04-RESEARCH.md section 2, test_context.cpp row (MODERATE test-rewrite)
    - Current `test/cpp/config/test_context.cpp` (post PR #565 YAML rewrite) — understand the new fixture pattern
    - `git show 3777645:test/cpp/config/test_context.cpp` — v1.0 test bodies + env-var/config-file fixtures to translate
  </read_first>
  <action>
    Step 1: Attempt the cherry-pick: `git cherry-pick 3777645`

    Step 2: Expected outcome: conflict in `test/cpp/config/test_context.cpp` because dev's PR #565 rewrote the file's fixtures (env-var + libconfig -> YAML config loader).

    Step 3: Resolve by keeping dev's YAML fixture infrastructure and ADDING v1.0's new TEST_CASEs tagged `[multi_gpu_foundation]` and `[.][multi_gpu_foundation]`. Translation rules:
      - v1.0's env-var setup -> use dev's YAML test-config pattern (likely a temp YAML file or a `SIRIUS_CONFIG` env var pointing at a test fixture)
      - v1.0's assertions on `topology_discovery()`, `reservation_manager_configurator`, `memory_manager`, `converter_registry` — these APIs are stable across the drift (verified research section 2 "Surprise-audit" — PR #643 doesn't touch these types); assertions need no change
      - v1.0's `[.][multi_gpu_foundation]` hidden TEST_CASE uses WARN+return; keep as-is (Catch2 v2 compatible per research section 4)

    Step 4: For the hidden multi-GPU TEST_CASE, ensure the WARN+return idiom is preserved:
      - Check cudaGetDeviceCount returns >=2, otherwise WARN("skipping: requires >=2 GPUs") and return.

    Step 5: Run pre-commit hooks on the resolved file:
      `pre-commit run --files test/cpp/config/test_context.cpp`
    Accept the clang-format auto-fixups.

    Step 6: Complete the cherry-pick:
      `git add test/cpp/config/test_context.cpp`
      `git cherry-pick --continue`
    Commit message preserved from `3777645`; if resolution was substantive (fixture translation changed the diff meaningfully), amend to add `(rebased)` suffix per research section 5 protocol.

    Step 7: Build check: `mcp__project-commands__run_command("build")` must exit 0. Most likely failures: missing include or renamed symbol. Fix in-line; amend.
  </action>
  <verify>
    <automated>grep -cE '\[multi_gpu_foundation\]' test/cpp/config/test_context.cpp | awk '$1 &gt;= 1 {exit 0} {exit 1}' &amp;&amp; mcp__project-commands__run_command("build")</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --format='%s'` shows the commit subject from `3777645` (possibly with `(rebased)` suffix)
    - `grep -cE '\[multi_gpu_foundation\]' test/cpp/config/test_context.cpp` returns at least 2 (visible + hidden TEST_CASEs)
    - `grep -n 'WARN.*skipping.*requires' test/cpp/config/test_context.cpp` finds the Catch2 v2 skip idiom in the `[.][multi_gpu_foundation]` case
    - `mcp__project-commands__run_command("build")` exits 0
    - `grep -c libconfig test/cpp/config/test_context.cpp` returns 0 (no re-introduction of libconfig symbols)
  </acceptance_criteria>
  <done>Commit 3777645 cherry-picked; conflict resolved by translating v1.0 env-var fixtures to dev YAML test pattern; [multi_gpu_foundation] tag is present; build is green.</done>
</task>

<task type="auto">
  <name>Task 2a: Cherry-pick 59bc284 plumbing portion — test_states + task + task_creator (4 trivial files)</name>
  <files>src/include/pipeline/sirius_pipeline_task_states.hpp, src/include/pipeline/gpu_pipeline_task.hpp, src/creator/task_creator.cpp, src/include/creator/task_creator.hpp</files>
  <read_first>
    - .planning/phases/04-cucascade-bump-v1-0-re-integration/04-RESEARCH.md section 2 rows for the 4 plumbing files (MODERATE/TRIVIAL)
    - `git show 59bc284 --stat` — see the 5-file shape; Task 2a handles 4 of 5
    - Current `src/include/pipeline/sirius_pipeline_task_states.hpp` and `gpu_pipeline_task.hpp` — understand PR #564/#626/#628/#643 shape drift
    - Current `src/creator/task_creator.cpp` — understand PR #573 operator_data refactor
  </read_first>
  <action>
    Per revision W3: Task 2 (59bc284 cherry-pick) is split into 2a (plumbing) + 2b (sirius_context.cpp structural carve-out) so the HARD structural work is reviewable separately from the TRIVIAL/MODERATE plumbing hunks.

    Step 1: Start the cherry-pick with `-n` (no-commit) so we can stage only the plumbing files:
      `git cherry-pick -n 59bc284`

      If `-n` makes resolution harder (merge state bookkeeping confusing), alternative approach:
      do a regular `git cherry-pick 59bc284`, resolve all 5 conflicts (including the carve-out
      for sirius_context.cpp), but unstage `sirius_context.cpp` before committing. That leaves
      Task 2b to pick up a dirty-tree remnant for sirius_context.cpp.

      For the split-commit discipline to hold: DO NOT include sirius_context.cpp in Task 2a's
      commit. It goes in Task 2b.

    Step 2: Expected conflicts on the 4 plumbing files (per research section 2):
      - `src/include/pipeline/sirius_pipeline_task_states.hpp` (MODERATE): insert v1.0's `std::optional<int> preferred_device_id` field in the right location (both local and global state)
      - `src/include/pipeline/gpu_pipeline_task.hpp` (MODERATE): add `get_preferred_device_id()` accessor
      - `src/creator/task_creator.cpp` (MODERATE): add `compute_data_locality_score` + NUMA-to-GPU mapping; watch for PR #573 operator_data refactor that may have moved the insertion point
      - `src/include/creator/task_creator.hpp` (TRIVIAL): add declaration

    Step 3: For each conflict:
      1. Keep dev's surrounding code (sirius-native types, updated method signatures, PR #573 operator_data shape).
      2. Insert v1.0's added fields/methods/functions at the correct location in dev's shape.
      3. Add `#include <optional>` to any header that gains `std::optional<int>` but doesn't already have it.

    Step 4: Run pre-commit on the 4 plumbing files:
      `pre-commit run --files src/include/pipeline/sirius_pipeline_task_states.hpp src/include/pipeline/gpu_pipeline_task.hpp src/creator/task_creator.cpp src/include/creator/task_creator.hpp`

    Step 5: Stage ONLY the 4 plumbing files (not sirius_context.cpp):
      `git add src/include/pipeline/sirius_pipeline_task_states.hpp src/include/pipeline/gpu_pipeline_task.hpp src/creator/task_creator.cpp src/include/creator/task_creator.hpp`

      Verify staging: `git diff --cached --name-only` must list exactly those 4 files.
      Verify sirius_context.cpp is NOT staged: `git diff --cached --name-only | grep -q sirius_context.cpp` must fail (grep returns 1).

    Step 6: Commit with original 59bc284 authorship but message annotated for the split:
      ```
      git commit -c 59bc284 -m "feat(02-01): add preferred_device_id + compute locality score (plumbing portion; sirius_context.cpp carved out to Task 2b per 04-plan revision W3)

      Original commit: 59bc284 feat(02-01): add preferred_device_id + compute locality score
      Split rationale: sirius_context.cpp has a HARD structural carve-out (downgrade_executor hunks
      deferred to Plan 03) that merits separate reviewable commit per W3.

      PORT-04"
      ```
      (Use `git commit --author="$(git log -1 --format='%an <%ae>' 59bc284)"` if `-c` UX is awkward.)

    Step 7: Build check: `mcp__project-commands__run_command("build")` must exit 0.
      The 4 plumbing files alone should compile — they add fields/methods but don't change
      constructor signatures. If build fails:
      - Missing `#include <optional>` → add to modified header
      - `system_topology_info` API drift → use `config_.get_hw_topology()` pattern from dev
  </action>
  <verify>
    <automated>grep -c 'preferred_device_id' src/include/pipeline/sirius_pipeline_task_states.hpp | awk '$1 &gt;= 2 {exit 0} {exit 1}' &amp;&amp; grep -q 'get_preferred_device_id' src/include/pipeline/gpu_pipeline_task.hpp &amp;&amp; grep -qE 'compute_data_locality_score|data_locality_score' src/creator/task_creator.cpp &amp;&amp; mcp__project-commands__run_command("build")</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'preferred_device_id' src/include/pipeline/sirius_pipeline_task_states.hpp` returns >= 2 (local + global)
    - `grep -q 'get_preferred_device_id' src/include/pipeline/gpu_pipeline_task.hpp` (accessor exists)
    - `grep -qE 'compute_data_locality_score|data_locality_score' src/creator/task_creator.cpp` (function exists)
    - `git log -1 --format='%s'` shows a subject referencing `feat(02-01): add preferred_device_id` (plumbing portion annotation acceptable)
    - `git diff HEAD~1 HEAD --name-only` lists ONLY the 4 plumbing files — `sirius_context.cpp` is NOT in the diff
    - `mcp__project-commands__run_command("build")` exits 0
  </acceptance_criteria>
  <done>4 plumbing files from 59bc284 are committed as a dedicated plumbing commit; preferred_device_id fields + get_preferred_device_id accessor + compute_data_locality_score present; sirius_context.cpp NOT included (deferred to Task 2b); build is green.</done>
</task>

<task type="auto">
  <name>Task 2b: Cherry-pick 59bc284 structural carve-out — sirius_context.cpp (P2P loop + gpu_numa_node passthrough, downgrade hunks DEFERRED)</name>
  <files>src/sirius_context.cpp</files>
  <read_first>
    - Task 2a output (4 plumbing files now committed with preferred_device_id plumbing in place)
    - `git show 59bc284 -- src/sirius_context.cpp` — identify which hunks touch downgrade_executor construction (DEFER) vs. P2P loop / gpu_numa_node passthrough (KEEP)
    - Current `src/sirius_context.cpp` — understand PR #565 (YAML config) + PR #579 (downgrade_executor reshape) current shape
    - .planning/phases/04-cucascade-bump-v1-0-re-integration/04-RESEARCH.md section 2, sirius_context.cpp row (HARD structural) + section 5 "Handling HARD conflicts" protocol
  </read_first>
  <action>
    Per revision W3: this task handles the HARD structural carve-out in sirius_context.cpp as a separate reviewable commit from Task 2a's plumbing.

    Step 1: Determine current repo state post-Task 2a:
      - If Task 2a used `git cherry-pick -n` and `sirius_context.cpp` is still in the working tree with conflict markers: proceed to Step 2.
      - If Task 2a used the alternative approach (`git cherry-pick 59bc284` full resolution, unstage sirius_context.cpp): `sirius_context.cpp` should have resolved content but be unstaged. Proceed to Step 2.
      - If neither (Task 2a cleanly excluded sirius_context.cpp): manually apply the relevant
        hunks from `git show 59bc284 -- src/sirius_context.cpp`, keeping only P2P-loop + gpu_numa_node passthrough.

    Step 2: Carve-out resolution — inspect each hunk in v1.0's sirius_context.cpp diff:
      * KEEP: P2P-enablement loop (loop over `topology_discovery().gpus` calling `cudaDeviceCanAccessPeer` + `cudaDeviceEnablePeerAccess`)
      * KEEP: `gpu_numa_node` pass-through from `topo.gpus[i].numa_node` if fed to something that is NOT downgrade_executor (e.g., pipeline state, locality tables)
      * DEFER: any hunk that calls `downgrade_executor(... gpu_numa_node ...)` — remove v1.0's hunk; leave dev's current downgrade_executor construction unchanged. This is explicitly in scope for Plan 03.

    Step 3: For each KEEP hunk:
      1. Keep dev's surrounding code (PR #565 YAML plumbing, PR #579 downgrade construction).
      2. Insert v1.0's P2P loop and gpu_numa_node-passthrough logic at the correct location in dev's shape.

    Step 4: Add inline `// TODO(04-03): route preferred_numa_node through downgrade_executor_config (PR #579 shape)` marker immediately adjacent to the (dev-shaped, untouched) downgrade_executor construction site. This gives Plan 03 a grep-discoverable integration point.

    Step 5: Run pre-commit on sirius_context.cpp:
      `pre-commit run --files src/sirius_context.cpp`

    Step 6: Stage and commit:
      ```
      git add src/sirius_context.cpp
      git commit -m "feat(02-01): P2P loop + gpu_numa_node passthrough in SiriusContext (59bc284 carve-out; downgrade_executor hunks deferred to 04-03)

      Carves out the HARD structural portion of v1.0 59bc284 touching src/sirius_context.cpp:
      - KEEP: P2P-enablement loop over topology_discovery().gpus
      - KEEP: gpu_numa_node passthrough (feeds into pipeline state, not downgrade_executor)
      - DEFER: downgrade_executor construction hunks — Plan 03 re-authors these onto dev's
        PR #579 downgrade_executor_config shape.

      TODO(04-03) marker placed at the downgrade_executor construction site for Plan 03 discovery.

      Original intent: 59bc284 feat(02-01): add preferred_device_id + compute locality score
      Carve-out: sirius_context.cpp is the HARD conflict row; split per 04-plan revision W3 to
      make the structural translation separately reviewable from the TRIVIAL plumbing hunks.

      PORT-04"
      ```

    Step 7: Inspect `e1dab76` (merge-fixup) per research OQ-1 (revision W5 — corrects the diff command):
      `git diff 59bc284 81d4c75 -- src/sirius_context.cpp`
      (NOT `git show e1dab76 -- src/sirius_context.cpp`, which only shows merge-resolution markers per research OQ-1.)
      - If diff is empty or just conflict markers with no semantic delta: SKIP.
      - If the diff shows novel sirius_context.cpp content NOT in `59bc284`:
          - Apply the novel portion (still carving out any downgrade_executor-touching hunks)
          - Create a small amendment commit: `fix(02-01): include e1dab76 merge-resolution delta`

    Step 8: Build check: `mcp__project-commands__run_command("build")` must exit 0.
    Anticipated failures + fixes:
      - Missing `#include <optional>` -> add to modified header
      - `system_topology_info` API drift -> use `config_.get_hw_topology()` pattern from dev

    Step 9: Surface the carve-out risk in the structured return (per revision W3):
      - Enumerate which hunks were KEPT from v1.0 59bc284 (P2P loop, gpu_numa_node passthrough)
      - Enumerate which hunks were DEFERRED (any touching downgrade_executor construction)
      - Note that Plan 03 Task 2 must resolve the TODO(04-03) marker
  </action>
  <verify>
    <automated>grep -nE 'downgrade_executor\(' src/sirius_context.cpp | grep -v 'itask_executor' &amp;&amp; grep -q 'TODO(04-03)' src/sirius_context.cpp &amp;&amp; mcp__project-commands__run_command("build")</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'downgrade_executor\(' src/sirius_context.cpp` shows dev's current (PR #579) shape; v1.0's 3-arg form with gpu_numa_node has NOT been reintroduced
    - `grep -q 'TODO(04-03)' src/sirius_context.cpp` finds the marker for Plan 03
    - `grep -qE 'cudaDeviceCanAccessPeer|cudaDeviceEnablePeerAccess' src/sirius_context.cpp` (P2P loop preserved)
    - `mcp__project-commands__run_command("build")` exits 0
    - `git log -1 --format='%s'` begins with `feat(02-01): P2P loop + gpu_numa_node passthrough` (or `fix(02-01): include e1dab76` if e1dab76 delta was applied)
    - Structured return documents KEEP vs. DEFER hunks per Step 9
  </acceptance_criteria>
  <done>sirius_context.cpp has v1.0 P2P loop + gpu_numa_node passthrough committed as a dedicated carve-out commit; downgrade_executor construction is UNCHANGED from dev (PR #579 shape) with TODO(04-03) marker; e1dab76 inspection done per corrected diff command; build is green.</done>
</task>

<task type="auto">
  <name>Task 3a: Cherry-pick dd9264b (management_eventloop push-model routing) — prepare for human-verify</name>
  <files>src/pipeline/pipeline_executor.cpp, src/pipeline/gpu_pipeline_executor.cpp</files>
  <read_first>
    - .planning/phases/04-cucascade-bump-v1-0-re-integration/04-RESEARCH.md section 2 rows for both files (MODERATE)
    - `git show dd9264b --stat` — 2 files, ~30 lines (+19/-11 on pipeline_executor, -7 on gpu_pipeline_executor)
    - `git show dd9264b -- src/pipeline/pipeline_executor.cpp` — the management_eventloop rewrite
    - Current `src/pipeline/pipeline_executor.cpp` — post PR #579 task-request plumbing shape
    - Current `src/pipeline/gpu_pipeline_executor.cpp` — post PR #573 operator_data shape
    - STATE.md decision: "at-capacity preferred task waits on that executor rather than falling back"
  </read_first>
  <action>
    Step 1: Attempt cherry-pick: `git cherry-pick dd9264b`

    Step 2: Expected conflicts (MODERATE per research section 2):
      - `src/pipeline/pipeline_executor.cpp`: v1.0 adds preferred_device_id routing in management_eventloop; dev's PR #579 changed task-request plumbing shape. Push-model intent (pop task first, route by preferred_device_id) survives; anchor to dev's new task-request type.
      - `src/pipeline/gpu_pipeline_executor.cpp`: v1.0 removes a `task_request_publisher.send()` call (5-7 lines); PR #573 reshaped the surrounding region. Semantic intent ("don't send pull signal for GPU tasks in push-model") is still valid — find dev's equivalent of the send() call and remove/disable.

    Step 3: Resolution pattern:
      1. Keep dev's task-request type shape (PR #579) and operator_data API (PR #573).
      2. Find the task-pop point in management_eventloop (after task-queue.pop or equivalent).
      3. After pop, call `task->get_preferred_device_id()` and route accordingly.
      4. In `gpu_pipeline_executor.cpp`, remove the `task_request_publisher.send()` call at the v1.0-equivalent location on dev's shape. If dev's PR #579 refactor removed the call already, skip v1.0's hunk here.

    Step 4: CRITICAL INVARIANT (revision W6 — structural predicate gate):
      v1.0 Phase 02-01 decision "at-capacity task waits on preferred GPU rather than falling back
      to another GPU" must be preserved. This is a user-decision-locked behavior (STATE.md).

      MANDATORY: introduce a sentinel symbol in the routing code that proves the wait-semantics:
        - Use a function/method name or comment containing `wait_on_preferred_device` or
          `wait_for_preferred_executor` at the at-capacity wait point.
      The verify gate below uses `grep -q 'wait_on_preferred_device\|wait_for_preferred_executor'`
      to make the invariant structurally checkable. Do NOT replace with `fallback_to_any_executor`
      or similar — the decision is locked.

      If dev's task-queue shape does NOT support a "wait on this executor's slot" pattern and
      fallback-to-another-executor is the only compilable option:
      - STOP. Do NOT cherry-pick --continue.
      - Surface as a BLOCKER in the structured return with the specific dev task-queue API that
        lacks wait-semantics. The human-verify checkpoint in Task 3b will then drive the
        resolution.

    Step 5: Run pre-commit: `pre-commit run --files src/pipeline/pipeline_executor.cpp src/pipeline/gpu_pipeline_executor.cpp`

    Step 6: `git add` and `git cherry-pick --continue`. Amend with `(rebased)` if substantive.

    Step 7: Build check: `mcp__project-commands__run_command("build")` must exit 0.
    DO NOT proceed to Task 4 yet — the human-verify gate in Task 3b must approve the
    wait-semantics before the plan moves forward.
  </action>
  <verify>
    <automated>grep -q 'preferred_device_id' src/pipeline/pipeline_executor.cpp &amp;&amp; grep -qE 'wait_on_preferred_device|wait_for_preferred_executor' src/pipeline/pipeline_executor.cpp &amp;&amp; mcp__project-commands__run_command("build")</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE 'get_preferred_device_id\(\)|preferred_device_id' src/pipeline/pipeline_executor.cpp` (routing logic in management_eventloop)
    - `grep -qE 'wait_on_preferred_device|wait_for_preferred_executor' src/pipeline/pipeline_executor.cpp` (STRUCTURAL INVARIANT: wait-semantics sentinel — revision W6)
    - `grep -nE 'task_request_publisher.*send|pull.*request' src/pipeline/gpu_pipeline_executor.cpp` shows v1.0 intent honored (send() call removed or comment explaining why dev's shape already omits it)
    - Reading the preferred_device_id routing region confirms: at-capacity preferred task waits on that executor (no fallback-to-different-executor path)
    - `mcp__project-commands__run_command("build")` exits 0
    - `git log -1 --format='%s'` shows commit subject from `dd9264b` (possibly `(rebased)`)
  </acceptance_criteria>
  <done>management_eventloop routes tasks by preferred_device_id with wait-semantics (structural sentinel `wait_on_preferred_device` present); push-model dispatch active; at-capacity preferred task waits on that executor (no fallback); build is green; awaiting Task 3b human sign-off.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3b: Human verification of wait-on-preferred-device semantics (STATE.md locked decision)</name>
  <files>(human review of Task 3a's commit; no file modifications in this task)</files>
  <read_first>
    - Task 3a output (the dd9264b cherry-pick commit)
    - STATE.md decision entry: "at-capacity preferred task waits on that executor rather than falling back"
    - .planning/phases/04-cucascade-bump-v1-0-re-integration/04-CONTEXT.md "Preserved v1.0 patterns" section (push-model dispatch)
  </read_first>
  <action>
    Pause execution and present Task 3a's commit for human review. This checkpoint exists per
    revision W6 because the "wait on preferred GPU instead of falling back" behavior is a
    user-decision-locked invariant — an auto-executor could rationalize a fallback path when
    dev's task-queue shape diverges from v1.0's, silently violating the decision.

    Surface:
      - `git show HEAD -- src/pipeline/pipeline_executor.cpp src/pipeline/gpu_pipeline_executor.cpp`
      - Highlight the at-capacity branch: does it WAIT on the preferred executor, or does it
        fall back to a different executor?
      - Confirm the structural sentinel `wait_on_preferred_device` or `wait_for_preferred_executor`
        is present at the wait point (this is also enforced by Task 3a's verify gate, but the
        human should verify it semantically).
      - Enumerate any dev task-queue API choices the executor made that differ from v1.0's
        direct `executor->wait_for_slot()` (or similar).

    Wait for user approval before proceeding to Task 4.
  </action>
  <what-built>
    management_eventloop push-model dispatch with preferred_device_id routing:
    - After task pop, `get_preferred_device_id()` is consulted
    - If preferred executor has capacity: route there
    - If preferred executor is AT CAPACITY: WAIT on that executor (via `wait_on_preferred_device`
      or equivalent sentinel) — NOT fallback to a different executor
    - If no preference: dev's existing round-robin/default routing
    - `gpu_pipeline_executor::task_request_publisher.send()` removed at v1.0-equivalent site
      (push-model: don't send pull signals for GPU tasks)

    This is Task 3a's commit `dd9264b (rebased)` and is now awaiting sign-off that the
    wait-semantics correctly express v1.0's locked decision on dev's PR #579 task-queue shape.
  </what-built>
  <how-to-verify>
    1. `git show HEAD -- src/pipeline/pipeline_executor.cpp` — inspect the routing block after
       task pop. Verify:
       - `task->get_preferred_device_id()` is consulted
       - If preferred but at capacity: the code WAITS (not falls back)
       - Sentinel symbol present: grep for `wait_on_preferred_device` or `wait_for_preferred_executor`

    2. `git show HEAD -- src/pipeline/gpu_pipeline_executor.cpp` — verify the
       `task_request_publisher.send()` call is removed (push-model discipline) OR a comment
       explains why dev's shape already omits it.

    3. Cross-reference STATE.md's decision:
       "At-capacity preferred task waits on that executor rather than falling back to another"
       Does Task 3a's commit preserve this exactly?

    4. If dev's task-queue API does NOT expose a wait-on-executor-slot primitive, verify
       Task 3a used a suitable substitute (e.g., a condition variable on the executor, a
       retry-with-same-target loop, etc.) that still semantically blocks until the preferred
       executor has capacity rather than dispatching to a different executor.

    5. If all looks correct: type "approved" to proceed to Task 4.

    6. If the wait-semantics is not preserved: describe the issue and request a specific
       remediation (e.g., "rewrite the at-capacity branch to use `executor->wait()` instead of
       `next_any_executor()`"). Do NOT approve until the invariant is preserved.
  </how-to-verify>
  <verify>
    <automated>MISSING — human-verify checkpoint. Automated guard is in Task 3a's verify gate (`grep -qE 'wait_on_preferred_device|wait_for_preferred_executor' src/pipeline/pipeline_executor.cpp`). This task pauses for human semantic review of whether the sentinel's presence actually reflects the locked wait-semantics behavior.</automated>
  </verify>
  <done>Human reviewed Task 3a's dd9264b commit, confirmed the wait-on-preferred-device semantics are preserved on dev's PR #579 task-queue shape, and typed "approved" to resume execution.</done>
  <resume-signal>Type "approved" to proceed to Task 4, or describe the specific wait-semantics issue observed in the routing block.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Cherry-pick 7f18e66 (distribute scan batches across GPUs)</name>
  <files>src/op/scan/duckdb_scan_executor.cpp, src/include/op/scan/duckdb_scan_executor.hpp</files>
  <read_first>
    - .planning/phases/04-cucascade-bump-v1-0-re-integration/04-RESEARCH.md section 2 rows for both files (.cpp MODERATE, .hpp TRIVIAL)
    - `git show 7f18e66 --stat` — 2 files / ~77 lines
    - `git show 7f18e66 -- src/op/scan/duckdb_scan_executor.cpp` — see select_target_gpu implementation
    - Current `src/op/scan/duckdb_scan_executor.cpp` — understand PR #570 (hive partitions), PR #643 (sirius-native types), PR #571 (metadata scan), PR #363 (row group pruning) refactors
  </read_first>
  <action>
    Step 1: Attempt cherry-pick: `git cherry-pick 7f18e66`

    Step 2: Expected conflicts (MODERATE .cpp, TRIVIAL .hpp). Per research section 2, v1.0's `select_target_gpu` method is orthogonal to hive-partition filtering + row-group pruning; the new method should land cleanly even if existing method modifications drift line-for-line.

    Step 3: Resolution pattern for `.cpp`:
      1. Keep dev's sirius-native types (`logical_type`, `type_id`), hive-partition code, metadata-scan support, row-group pruning.
      2. Add v1.0's `select_target_gpu` method at a sensible location (typically before or after the scan-task-creation method).
      3. If v1.0's hunks conflict with PR #570's partition filtering in the scan creation method, re-apply v1.0's changes conceptually: after task creation, set `task->preferred_device_id = select_target_gpu(gpu_memory_spaces_)`.
      4. Ensure `gpu_memory_spaces_` field (or equivalent `vector<memory_space*>`) is initialized in the executor constructor. Populate from `SiriusContext::memory_manager_->get_memory_spaces_for_tier(Tier::GPU)`.

    Step 4: Resolution for `.hpp` (TRIVIAL):
      - Add declaration of `select_target_gpu`.
      - Add `std::vector<memory_space*> gpu_memory_spaces_` member.

    Step 5: Run pre-commit: `pre-commit run --files src/op/scan/duckdb_scan_executor.cpp src/include/op/scan/duckdb_scan_executor.hpp`

    Step 6: `git add` both files, `git cherry-pick --continue`.

    Step 7: Build check: `mcp__project-commands__run_command("build")` must exit 0. Likely failures: missing include for memory_space vector; signature mismatch with sirius-native types (unlikely — select_target_gpu uses primitive int + memory_space*).
  </action>
  <verify>
    <automated>grep -q 'select_target_gpu' src/op/scan/duckdb_scan_executor.cpp &amp;&amp; grep -q 'select_target_gpu' src/include/op/scan/duckdb_scan_executor.hpp &amp;&amp; mcp__project-commands__run_command("build")</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'select_target_gpu' src/include/op/scan/duckdb_scan_executor.hpp` (declaration present)
    - `grep -q 'select_target_gpu' src/op/scan/duckdb_scan_executor.cpp` (definition present)
    - `grep -q 'gpu_memory_spaces' src/include/op/scan/duckdb_scan_executor.hpp` (vector member exists)
    - `grep -nE 'LogicalType::(INTEGER|BIGINT|VARCHAR)' src/op/scan/duckdb_scan_executor.cpp src/include/op/scan/duckdb_scan_executor.hpp` returns 0 hits (PORT-02)
    - `mcp__project-commands__run_command("build")` exits 0
    - `git log -1 --format='%s'` shows commit subject from `7f18e66`
  </acceptance_criteria>
  <done>select_target_gpu implemented in duckdb_scan_executor, distributes scan batches proportionally across GPU memory spaces; build is green; no DuckDB vocabulary re-introduced.</done>
</task>

<task type="auto">
  <name>Task 5: Cherry-pick 2e6ba26 (integration test file + CMakeLists wiring)</name>
  <files>test/cpp/integration/test_gpu_execution_locality.cpp, CMakeLists.txt</files>
  <read_first>
    - .planning/phases/04-cucascade-bump-v1-0-re-integration/04-RESEARCH.md section 2 rows (NEW file = no conflict; CMakeLists TRIVIAL) + section 4 test tags correction
    - `git show 2e6ba26 --stat` — 2-file shape
    - `git show 2e6ba26 -- test/cpp/integration/test_gpu_execution_locality.cpp` — actual Catch2 tags used (`[data_locality]` per research section 4 — NOT `[test_gpu_execution_locality]` as ROADMAP.md incorrectly states)
    - Current `CMakeLists.txt` TEST_SOURCES block — find insertion point for new test file
  </read_first>
  <action>
    Step 1: Attempt cherry-pick: `git cherry-pick 2e6ba26`

    Step 2: Expected conflicts:
      - `test/cpp/integration/test_gpu_execution_locality.cpp`: NEW file, NO conflict possible.
      - `CMakeLists.txt`: TRIVIAL — v1.0 adds one line to TEST_SOURCES list. Dev has 14 CMakeLists touches; layout may have shifted. Hand-place the new entry inside the actual TEST_SOURCES block (not some other list).

    Step 3: If CMakeLists conflict, resolve by appending `test/cpp/integration/test_gpu_execution_locality.cpp` to the current TEST_SOURCES list (grouped under integration/ if dev organizes that way). CRITICAL (revision W8): verify inclusion is inside the `TEST_SOURCES` block, not just anywhere in CMakeLists.txt — a loose mention in a comment or another list doesn't wire the file into the build.

    Step 4: Run pre-commit: `pre-commit run --files test/cpp/integration/test_gpu_execution_locality.cpp CMakeLists.txt`

    Step 5: `git add` both files, `git cherry-pick --continue`.

    Step 6: Because CMakeLists.txt changed, CMake may auto-reconfigure on next build; if not, force it:
      `mcp__project-commands__run_command("cmake-release")`
      `mcp__project-commands__run_command("build")`

    Step 7: Build check — must exit 0. Expected failures + fixes:
      - test_gpu_execution_locality.cpp includes `pipeline/gpu_pipeline_task.hpp` + `sirius_pipeline_task_states.hpp` — both have preferred_device_id from Task 2a. Test body sets preferred_device_id (primitive int) -> should compile cleanly.
      - If the test uses `SiriusContext::initialize` and dev's signature changed, update the test fixture accordingly (likely small change to pass YAML config).

    Step 8: Verify correct Catch2 tag (research section 4 corrected this):
      `grep -nE '\[data_locality\]' test/cpp/integration/test_gpu_execution_locality.cpp`
      must return >= 2 (one visible `[data_locality]`, one hidden `[.][data_locality][multi_gpu]`).
  </action>
  <verify>
    <automated>test -f test/cpp/integration/test_gpu_execution_locality.cpp &amp;&amp; grep -A20 'TEST_SOURCES' CMakeLists.txt | grep -q 'test_gpu_execution_locality.cpp' &amp;&amp; grep -cE '\[data_locality\]' test/cpp/integration/test_gpu_execution_locality.cpp | awk '$1 &gt;= 2 {exit 0} {exit 1}' &amp;&amp; mcp__project-commands__run_command("build")</automated>
  </verify>
  <acceptance_criteria>
    - `test/cpp/integration/test_gpu_execution_locality.cpp` exists on disk
    - `grep -A20 'TEST_SOURCES' CMakeLists.txt | grep -q 'test_gpu_execution_locality.cpp'` (revision W8: wired into TEST_SOURCES block specifically, not just mentioned somewhere in CMakeLists.txt)
    - `grep -cE '\[data_locality\]' test/cpp/integration/test_gpu_execution_locality.cpp` returns >= 2 (visible + hidden)
    - `grep -cE '\[\.\]\[data_locality\]\[multi_gpu\]' test/cpp/integration/test_gpu_execution_locality.cpp` returns >= 1 (hidden multi-GPU case)
    - `grep -n 'WARN.*skipping.*requires' test/cpp/integration/test_gpu_execution_locality.cpp` finds the Catch2 v2 skip idiom in the hidden case
    - `mcp__project-commands__run_command("build")` exits 0 (sirius_unittest rebuilt with new test)
    - `git log -1 --format='%s'` shows commit subject from `2e6ba26`
  </acceptance_criteria>
  <done>test_gpu_execution_locality.cpp present and wired into the TEST_SOURCES block of CMakeLists; [data_locality] and [.][data_locality][multi_gpu] tags exist; new test compilation unit built cleanly.</done>
</task>

<task type="auto">
  <name>Task 6: Full unit-test suite run + PORT-02 grep gate</name>
  <files>(verification only — no file modifications)</files>
  <read_first>
    - .planning/phases/04-cucascade-bump-v1-0-re-integration/04-RESEARCH.md section 6 "Validation Strategy"
    - .planning/phases/04-cucascade-bump-v1-0-re-integration/04-RESEARCH.md section 4 — test tags + WARN+return convention
  </read_first>
  <action>
    Step 1: Run full unit test suite: `mcp__project-commands__run_command("unit-tests")` must exit 0.

    Expected test behavior on single-GPU host:
      - `[multi_gpu_foundation]` visible: passes (primitive-typed assertions)
      - `[.][multi_gpu_foundation]` hidden: not invoked by default
      - `[data_locality]` visible: passes (preferred_device_id default + precedence)
      - `[.][data_locality][multi_gpu]` hidden: not invoked by default
      - Pre-existing tests ([downgrade], [reservation], [converter]): still pass

    Step 2: Run PORT-02 grep gate (no DuckDB vocabulary reintroduced):
      `grep -rnE 'LogicalType::(INTEGER|BIGINT|VARCHAR)' src/creator/ src/include/creator/ src/pipeline/ src/include/pipeline/ src/op/scan/duckdb_scan_executor.cpp src/include/op/scan/duckdb_scan_executor.hpp test/cpp/integration/test_gpu_execution_locality.cpp test/cpp/config/test_context.cpp`
    Must return 0 hits.

    Step 3: Verify commit count:
      `git log --oneline dev..HEAD | wc -l` — should be >= 7 accounting for the split:
        1 (bump from 04-01) + 1 (Task 1: 3777645) + 1 (Task 2a: plumbing) + 1 (Task 2b: sirius_context carve-out) + 1 (Task 3a: dd9264b) + 1 (Task 4: 7f18e66) + 1 (Task 5: 2e6ba26) = 7 minimum, plus optional e1dab76 fixup.

    Step 4: Structured return enumerates:
      - Which commits cherry-picked cleanly (no conflict)
      - Which commits required hunk-level resolution (what was kept/discarded)
      - The Task 2a/2b split outcome (both commits landed? authorship preserved where possible?)
      - Whether e1dab76 was included or skipped (with rationale per research OQ-1 using corrected `git diff 59bc284 81d4c75` command)
      - Task 3b human-verify outcome (approved? revisions requested?)
      - Any `TODO(04-03)` markers left for Plan 03 (expected in src/sirius_context.cpp from Task 2b)
      - Grep gate result (PORT-02)
      - Full unit-test run result

    Step 5: Known expected-failures (NOT regressions):
      - If a `[downgrade]` test fails because it was ADDED by a Plan 03 commit (c5a3d8e, ec2399e, 0d99cde) that hasn't been picked yet: that's correct — they ARE in scope for Plan 03.
      - If a `[downgrade]` test that is PRE-EXISTING on dev fails: that IS a regression from this plan's cherry-picks. Triage immediately. Most likely cause: preferred_device_id threading bug. Run the specific test to find the failing assertion; fix in-line via amendment to the responsible cherry-pick.
  </action>
  <verify>
    <automated>mcp__project-commands__run_command("unit-tests") &amp;&amp; test $(grep -rnE 'LogicalType::(INTEGER|BIGINT|VARCHAR)' src/creator/ src/include/creator/ src/pipeline/ src/include/pipeline/ src/op/scan/duckdb_scan_executor.cpp src/include/op/scan/duckdb_scan_executor.hpp test/cpp/integration/test_gpu_execution_locality.cpp test/cpp/config/test_context.cpp 2>/dev/null | wc -l) -eq 0 &amp;&amp; test $(git log --oneline dev..HEAD | wc -l) -ge 7</automated>
  </verify>
  <acceptance_criteria>
    - `mcp__project-commands__run_command("unit-tests")` exits 0
    - `grep -rnE 'LogicalType::(INTEGER|BIGINT|VARCHAR)' ...` on the 10 files above returns 0 hits (PORT-02 gate)
    - `git log --oneline dev..HEAD | wc -l` >= 7 (1 bump + 6 cherry-pick-derived commits after the 2a/2b split)
    - Structured return includes the enumeration from Step 4
    - No pre-existing tests regressed
  </acceptance_criteria>
  <done>Full unit-test suite passes after Plan 02's cherry-picks; PORT-02 grep gate is clean; commit count aligns with the plan's intended shape (post-2a/2b split).</done>
</task>

</tasks>

<verification>
Plan-level exit gate:
1. `git log --oneline dev..HEAD | wc -l` >= 7 (1 bump + 1 3777645 + 2 from 59bc284 split (2a+2b) + 1 dd9264b + 1 7f18e66 + 1 2e6ba26; plus optional e1dab76 fixup)
2. All cherry-pick commits have their original v1.0 author preserved where the split allowed (Task 2a/2b use `git commit -c 59bc284` authorship)
3. `mcp__project-commands__run_command("build")` exits 0
4. `mcp__project-commands__run_command("unit-tests")` exits 0
5. `grep -rnE 'LogicalType::(INTEGER|BIGINT|VARCHAR)' ...` on the 10 files returns 0 hits (PORT-02)
6. `grep -qE 'wait_on_preferred_device|wait_for_preferred_executor' src/pipeline/pipeline_executor.cpp` (revision W6 structural invariant)
7. `grep -A20 'TEST_SOURCES' CMakeLists.txt | grep -q 'test_gpu_execution_locality.cpp'` (revision W8 tightened gate)
8. A `TODO(04-03)` marker exists in `src/sirius_context.cpp` indicating where Plan 03 must thread preferred_numa_node through downgrade_executor_config
9. Task 3b human-verify checkpoint was approved
</verification>

<success_criteria>
- PORT-01 (partial — code-carrying cherry-picks done, split into 7 commits): the 5 "trivial/moderate" commits' effects are on the branch with authorship preserved
- PORT-02: no DuckDB vocabulary types reintroduced; grep gate passes
- PORT-04 (partial — plumbing side): preferred_device_id on local + global state; management_eventloop routes by preference with wait-semantics structurally gated; task_creator computes locality score; duckdb_scan_executor distributes batches. Remaining PORT-04 work (the NUMA-downgrade branch) is Plan 03.
- Build + unit-tests green throughout the cherry-pick batch
- Plan 03 integration points (`TODO(04-03)` markers) are in place
- Human-verify checkpoint (Task 3b) approved the wait-on-preferred-device semantics
</success_criteria>

<output>
After completion, create `.planning/phases/04-cucascade-bump-v1-0-re-integration/04-02-SUMMARY.md` documenting:
- Each commit's cherry-pick outcome (clean / hunk-resolved / carved-out / rebased-message / split-2a-2b)
- Whether e1dab76 was included (yes/no + rationale using the corrected `git diff 59bc284 81d4c75` command)
- List of `TODO(04-03)` markers left in source files (with file:line references)
- Task 3b human-verify outcome (approved commit SHA or revision history)
- Final commit count on branch
- Build + unit-test run outcomes
- Any assertions or behavior translations that deviate from v1.0 intent, with rationale
</output>
</output>
