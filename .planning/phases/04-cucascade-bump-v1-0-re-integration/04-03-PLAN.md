---
phase: 04-cucascade-bump-v1-0-re-integration
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/include/downgrade/downgrade_executor.hpp
  - src/downgrade/downgrade_executor.cpp
  - src/include/downgrade/downgrade_task.hpp
  - src/downgrade/downgrade_task.cpp
  - src/sirius_context.cpp
  - test/cpp/downgrade/test_downgrade_executor.cpp
autonomous: false
requirements:
  - PORT-01
  - PORT-02
  - PORT-04

must_haves:
  truths:
    - "downgrade_executor accepts a preferred_numa_node preference per request (or per executor) and honors it when dispatching downgrade work"
    - "Downgrade dispatch uses cucascade's any_memory_space_in_tier_with_preference with the NUMA preference populated from hw_topology"
    - "SiriusContext threads gpu_numa_node from config_.get_hw_topology() into the downgrade_executor_config (dev's PR #579 shape)"
    - "Downgrade test file compiles against dev's POD downgrade_task + request-queue shape while preserving v1.0 test intent (NUMA ordering, basic lifecycle, GPU-to-GPU transfer via converter, MEM-04 P2P, MEM-05 scan distribution)"
    - "No references to v1.0's removed itask / downgrade_task_global_state / downgrade_task_local_state class shapes remain"
  artifacts:
    - path: "src/include/downgrade/downgrade_executor.hpp"
      provides: "downgrade_executor_config with preferred_numa_node field (or equivalent)"
      contains: "preferred_numa_node"
    - path: "src/downgrade/downgrade_executor.cpp"
      provides: "NUMA-aware dispatch using any_memory_space_in_tier_with_preference"
      contains: "any_memory_space_in_tier_with_preference"
    - path: "src/sirius_context.cpp"
      provides: "gpu_numa_node populated into downgrade_executor_config at construction"
      contains: "preferred_numa_node"
    - path: "test/cpp/downgrade/test_downgrade_executor.cpp"
      provides: "[downgrade][numa_aware_downgrade] + [.][multi_gpu_transfer] test tags"
      contains: "numa_aware_downgrade"
  key_links:
    - from: "src/sirius_context.cpp"
      to: "src/downgrade/downgrade_executor.cpp"
      via: "downgrade_executor_config carries preferred_numa_node derived from hw_topology"
      pattern: "preferred_numa_node"
    - from: "src/downgrade/downgrade_executor.cpp"
      to: "cucascade::memory::any_memory_space_in_tier_with_preference"
      via: "strategy selection for downgrade target memory_space"
      pattern: "any_memory_space_in_tier_with_preference"
---

<objective>
Re-author the NUMA-aware downgrade + related tests from v1.0 onto dev's new `downgrade_request` queue + POD `downgrade_task` shape (PR #579). This is NOT a cherry-pick — v1.0's diffs target types/classes that were replaced on dev, so the research (section 2 "CRITICAL (rewrite)" rows, section 5 "Handling HARD / CRITICAL conflicts" protocol) explicitly mandates re-authoring v1.0's INTENT against dev's new file shape.

The four v1.0 commits this plan expresses the intent of (without naive cherry-pick):
  - `dd86dd0` — feat(01-01): NUMA-aware downgrade, multi-device sync, P2P enablement (5 files including downgrade_executor.{hpp,cpp}, downgrade_task.{hpp,cpp}, sirius_context.cpp)
  - `c5a3d8e` — test(01-03): basic downgrade-executor lifecycle + [.][multi_gpu_transfer] GPU-to-GPU via converter tests
  - `ec2399e` — test(03-01): NUMA downgrade ordering tests (numa_aware_downgrade_executor_passes_numa_node, downgrade_executor_default_numa_node_is_nullopt)
  - `0d99cde` — test(03-01): MEM-04 P2P + MEM-05 scan distribution tests

Each commit targets code/test shapes that no longer exist on dev. The re-authoring produces separate commits with messages like:
  - `feat(01-01): NUMA-aware downgrade preference (re-authored onto dev #579)`
  - `test(01-03): downgrade-executor lifecycle + GPU-to-GPU transfer (re-authored onto dev #579)`
  - `test(03-01): NUMA downgrade ordering (re-authored onto dev #579)`
  - `test(03-01): MEM-04 P2P + MEM-05 scan distribution (re-authored onto dev #579)`

Purpose: PR #579 is the single largest risk in Phase 4 (research R1 — "HIGH likelihood, HIGH impact"). The v1.0 NUMA-aware downgrade intent must be preserved; the dev downgrade_request architecture must also be preserved (it ships improvements Sirius depends on). The combined outcome is the v1.0 feature running on dev's shape.

Output: 4 re-authored commits on the branch. Full unit-tests pass including `[downgrade]` and `[downgrade][numa_aware_downgrade]` tags. Autonomous=false because this plan contains a `checkpoint:human-verify` task (Task 5) to confirm the behavioral translation BEFORE committing the re-authored downgrade_executor change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-cucascade-bump-v1-0-re-integration/04-CONTEXT.md
@.planning/phases/04-cucascade-bump-v1-0-re-integration/04-RESEARCH.md
@.planning/phases/04-cucascade-bump-v1-0-re-integration/04-01-SUMMARY.md
@.planning/phases/04-cucascade-bump-v1-0-re-integration/04-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Dev's PR #579 shape (MUST be preserved) vs. v1.0's shape (INTENT to translate). -->
<!-- Executor should NOT try to "cherry-pick" these; the v1.0 diff has no valid anchor on dev. -->

### Dev's downgrade_executor shape (PR #579, pre-port) — SOURCE OF TRUTH

`src/include/downgrade/downgrade_executor.hpp` on dev currently:
  - `downgrade_executor` no longer inherits from `itask_executor` (PR #579 removed the base class)
  - Constructor's first parameter is `exec::downgrade_executor_config` (dev's new config struct) — NOT `exec::thread_pool_config`
  - Internal model: `downgrade_request` queue architecture where the executor receives requests and processes them on worker threads
  - No `downgrade_task_global_state` / `downgrade_task_local_state` inner classes (those were v1.0's model; dev replaced them)

`src/include/downgrade/downgrade_task.hpp` on dev currently:
  - Plain struct (POD): `struct downgrade_task { ... fields ... };` — NOT `class downgrade_task : public itask`
  - No `_global_state` / `_local_state` members
  - Used as a value type passed through the request queue

`src/downgrade/downgrade_executor.cpp` on dev currently:
  - ~385 lines (promise/future/request-queue machinery)
  - Has a function like `downgrade_executor::process_requests()` or equivalent that pulls a request and executes the downgrade

### v1.0 INTENT to preserve (the behavior, NOT the diff)

From v1.0 commit `dd86dd0`:
  - `downgrade_executor` took an additional `std::optional<int> gpu_numa_node` constructor arg
  - This numa_node was passed into the downgrade dispatch path
  - At the dispatch point, v1.0 called cucascade's `any_memory_space_in_tier_with_preference(Tier::HOST, *gpu_numa_node)` to pick a NUMA-local host space for the downgrade target
  - In `SiriusContext::initialize`, for each GPU executor, `gpu_numa_node` was populated from `config_.get_hw_topology().gpus[i].numa_node`

### Re-authored shape on dev's file geometry

A. `src/include/downgrade/downgrade_executor.hpp`:
   Find or add the `exec::downgrade_executor_config` struct definition (possibly in a different header under `src/include/exec/` — check the include graph). Add field:
   ```cpp
   struct downgrade_executor_config {
     /* ... existing dev fields ... */
     std::optional<int> preferred_numa_node;   // NEW: NUMA-local host memory preference
   };
   ```
   No constructor signature change on `downgrade_executor` itself — the config struct carries the preference.

B. `src/downgrade/downgrade_executor.cpp`:
   In `process_requests()` (or wherever dev's downgrade dispatch selects a memory_space), consult `config_.preferred_numa_node`. If set, call:
   ```cpp
   auto target_space = cucascade::memory::any_memory_space_in_tier_with_preference(
       memory_manager_, cucascade::Tier::HOST, *config_.preferred_numa_node);
   ```
   If unset, keep dev's current selection logic (round-robin, first-available, or whatever is there).

C. `src/include/downgrade/downgrade_task.hpp`:
   NO changes needed if the POD struct already carries the memory_space* fields the dispatch needs. v1.0's NUMA work happens at executor-dispatch time, not per-task.

D. `src/downgrade/downgrade_task.cpp`:
   NO changes expected for the NUMA-locality intent. If v1.0's original diff touched this file for `any_memory_space_in_tier_with_preference`, that logic now lives in `downgrade_executor.cpp` (see B).

E. `src/sirius_context.cpp`:
   At `downgrade_executor` construction (wherever it is on dev), populate `downgrade_executor_config::preferred_numa_node` from `config_.get_hw_topology().gpus[device_id].numa_node`. Replace the `TODO(04-03)` marker left by Plan 02 with the actual plumbing.

### Test file re-authoring (test/cpp/downgrade/test_downgrade_executor.cpp)

v1.0 added TEST_CASEs tagged:
  - `[downgrade][numa_aware_downgrade]` — `numa_aware_downgrade_executor_passes_numa_node`, `downgrade_executor_default_numa_node_is_nullopt`
  - `[downgrade_executor]` — basic lifecycle tests
  - `[.][multi_gpu_transfer]` — `gpu_to_gpu_transfer_via_converter` (hidden, requires N>=2 GPUs)

These must be re-authored against dev's new downgrade_request / POD downgrade_task shape:
  - OLD (v1.0): directly instantiate `downgrade_task_global_state`, pass into `downgrade_executor::submit(...)`
  - NEW: construct a `downgrade_request` (or whatever dev calls it), submit it, wait on the promise/future
  - Assertions about NUMA node routing now inspect `downgrade_executor_config.preferred_numa_node` at construction time AND verify (via a mock/spy memory_manager or via the cucascade strategy output) that the chosen memory_space has the expected NUMA id

### Catch2 v2 + WARN+return for multi-GPU tests

All `[.][multi_gpu_transfer]` tests follow the Catch2 v2 skip idiom per research section 4:
```cpp
int count = 0; cudaGetDeviceCount(&count);
if (count < 2) { WARN("skipping: requires >=2 GPUs"); return; }
```

### Critical pitfall

Research section 2 calls these rows CRITICAL (rewrite):
  - `src/include/downgrade/downgrade_task.hpp`
  - `src/downgrade/downgrade_task.cpp`
  - `test/cpp/downgrade/test_downgrade_executor.cpp`

"CRITICAL (rewrite)" means: v1.0's diff TARGETS A TYPE THAT DOES NOT EXIST on dev. Do NOT attempt `git cherry-pick dd86dd0 c5a3d8e ec2399e 0d99cde` and try to resolve conflicts. The conflict markers will be meaningless. Instead, author new diffs that express v1.0's INTENT against dev's shape.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map dev's current downgrade architecture to v1.0's intent (READ-ONLY discovery)</name>
  <files>(read-only — no modifications; produces a mapping document for Tasks 2-5)</files>
  <read_first>
    - Current `src/include/downgrade/downgrade_executor.hpp` — identify the `downgrade_executor_config` struct (or find where it's defined if in a different header)
    - Current `src/downgrade/downgrade_executor.cpp` — identify `process_requests()` (or dev's equivalent dispatch function) and the memory_space selection point
    - Current `src/include/downgrade/downgrade_task.hpp` — confirm it is a POD struct
    - Current `src/sirius_context.cpp` — find where `downgrade_executor` is constructed (there should be a `TODO(04-03)` marker nearby from Plan 02)
    - Current `test/cpp/downgrade/test_downgrade_executor.cpp` — understand dev's test fixture pattern for the request-queue architecture
    - `git show dd86dd0` (for v1.0 NUMA intent)
    - `git show c5a3d8e` (for v1.0 test intent)
    - `git show ec2399e` (for NUMA ordering test intent)
    - `git show 0d99cde` (for MEM-04 P2P + MEM-05 scan distribution test intent)
  </read_first>
  <action>
    Produce a concrete mapping table (record in the Plan 03 summary for audit). For each element of v1.0 intent, identify the equivalent point in dev's shape.

    Required mapping artifacts (minimum):

    1. Where is `downgrade_executor_config` defined on dev?
       - File:line reference
       - Current fields list

    2. Where does dev's downgrade dispatch pick a memory_space?
       - File:line reference (function name + containing class)
       - Current selection logic (round-robin? first-available? explicit?)

    3. Where is `downgrade_executor` constructed in `SiriusContext`?
       - File:line reference
       - Current arguments (dev's shape)
       - Location of the `TODO(04-03)` marker from Plan 02

    4. Dev's `downgrade_request` submission API:
       - Function signature
       - How a caller waits for completion (promise/future, callback, sync?)

    5. Dev's test fixture for downgrade_executor:
       - How a downgrade_request is constructed in tests
       - How assertions wait for completion
       - Any mock/spy patterns used

    6. Where does `any_memory_space_in_tier_with_preference` currently get called on dev (if at all)?
       - `grep -rn 'any_memory_space_in_tier_with_preference' src/ test/`
       - If 0 hits: this is new-to-Sirius-but-existing-in-cucascade; Task 2 introduces the call.
       - If >0 hits: study existing usage for pattern consistency.

    Do NOT modify any files in this task. Produce the mapping in a scratch note for Tasks 2-5 to consume. If any required discovery item (e.g., `downgrade_executor_config` struct) can't be found on dev, the architecture may differ more than research §2 indicated — STOP and surface this in the structured return before Task 2.
  </action>
  <verify>
    <automated>test -f src/include/downgrade/downgrade_executor.hpp &amp;&amp; test -f src/downgrade/downgrade_executor.cpp &amp;&amp; grep -rln 'downgrade_executor_config' src/ | head -1</automated>
  </verify>
  <acceptance_criteria>
    - All 6 mapping items above are documented with concrete file:line references
    - `grep -rn 'downgrade_executor_config' src/` returns at least 1 hit (the struct definition exists somewhere on dev)
    - `grep -n 'TODO(04-03)' src/sirius_context.cpp` returns at least 1 hit (Plan 02's marker is in place)
    - Dev's equivalent of v1.0's `process_requests`-dispatch point is identified with line number
    - If dev's architecture diverges from research §2's prediction in any material way (e.g., no `downgrade_executor_config` struct, no request queue), this is flagged as a blocker in the structured return
  </acceptance_criteria>
  <done>Concrete mapping from v1.0 intent to dev's shape is documented, with file:line references for each re-authoring target in Tasks 2-5.</done>
</task>

<task type="auto">
  <name>Task 2: Re-author NUMA-aware downgrade on dev's PR #579 shape (preferred_numa_node via config struct)</name>
  <files>src/include/downgrade/downgrade_executor.hpp, src/downgrade/downgrade_executor.cpp, src/sirius_context.cpp</files>
  <read_first>
    - Task 1's mapping output (from the structured return / in-memory)
    - `git show dd86dd0 -- src/sirius_context.cpp src/include/downgrade/downgrade_executor.hpp src/downgrade/downgrade_executor.cpp` — v1.0's original intent
    - .planning/phases/04-cucascade-bump-v1-0-re-integration/04-RESEARCH.md section 5 "Handling HARD / CRITICAL conflicts" — protocol for re-authoring
    - cucascade docs for `any_memory_space_in_tier_with_preference` (research CUCASCADE-IO.md or cucascade's `memory/strategy.hpp`)
  </read_first>
  <action>
    Step 1: Add `preferred_numa_node` to `downgrade_executor_config`:
      Locate the struct (from Task 1 mapping). Add field:
      ```cpp
      std::optional<int> preferred_numa_node;
      ```
      Add `#include <optional>` if not already present.

    Step 2: Wire the preference into dev's downgrade dispatch:
      In `src/downgrade/downgrade_executor.cpp`, find the memory_space selection point
      identified in Task 1 mapping (item 2). Adapt dispatch:
      ```cpp
      memory_space* target_space = nullptr;
      if (config_.preferred_numa_node.has_value()) {
        target_space = cucascade::memory::any_memory_space_in_tier_with_preference(
            *memory_manager_, cucascade::Tier::HOST, *config_.preferred_numa_node);
      }
      if (!target_space) {
        // fallback: dev's existing selection path (unchanged)
        target_space = /* dev's existing selection */;
      }
      ```
      The exact call signature of `any_memory_space_in_tier_with_preference` comes from cucascade — look it up via grep or the cucascade header (likely at `cucascade/memory/strategy.hpp` or similar). If the function does not exist in cucascade at f47de0b, STOP and surface as a blocker (indicates cucascade API moved or was renamed since v1.0 research).

    Step 3: Populate `preferred_numa_node` in `SiriusContext::initialize`:
      Find the `TODO(04-03)` marker from Plan 02. Replace it with:
      ```cpp
      downgrade_executor_config dcfg;
      /* ... existing dev field population ... */
      dcfg.preferred_numa_node = config_.get_hw_topology().gpus[device_id].numa_node;
      // Construct downgrade_executor with dcfg (dev's existing constructor pattern)
      ```
      Remove the `TODO(04-03)` marker (leave only the implementation).

    Step 4: Build check: `mcp__project-commands__run_command("build")` — must exit 0.
      Likely failures:
      - `any_memory_space_in_tier_with_preference` namespace mismatch — fix by correcting namespace (check cucascade header)
      - Missing include — add the cucascade header
      - `config_.get_hw_topology().gpus[device_id].numa_node` field name drift — look up dev's actual field via grep

    Step 5: Run pre-commit on the 3 files:
      `pre-commit run --files src/include/downgrade/downgrade_executor.hpp src/downgrade/downgrade_executor.cpp src/sirius_context.cpp`

    Step 6: Commit — this is the first re-authored commit; message format per research section 5:
      ```
      git commit -m "feat(01-01): NUMA-aware downgrade preference (re-authored onto dev #579)

      Express v1.0 commit dd86dd0's intent (NUMA-local host-memory preference for
      GPU->HOST downgrade) against dev's PR #579 architecture:
      - Add preferred_numa_node to exec::downgrade_executor_config
      - downgrade_executor dispatch consults the preference and calls
        cucascade::memory::any_memory_space_in_tier_with_preference
      - SiriusContext populates the preference from hw_topology().gpus[i].numa_node

      Original intent: dd86dd0 feat(01-01): NUMA-aware downgrade, multi-device sync, P2P
      Re-authored: the v1.0 diff targeted downgrade_executor(itask_executor)
      + class downgrade_task(itask), both of which PR #579 replaced. The NUMA
      preference now rides the new exec::downgrade_executor_config struct rather
      than a constructor arg. Behavior is preserved: preferred_numa_node drives
      any_memory_space_in_tier_with_preference selection.

      PORT-04"
      ```
  </action>
  <verify>
    <automated>grep -q 'preferred_numa_node' src/include/downgrade/downgrade_executor.hpp &amp;&amp; grep -q 'any_memory_space_in_tier_with_preference' src/downgrade/downgrade_executor.cpp &amp;&amp; grep -q 'preferred_numa_node' src/sirius_context.cpp &amp;&amp; ! grep -q 'TODO(04-03)' src/sirius_context.cpp &amp;&amp; mcp__project-commands__run_command("build")</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'preferred_numa_node' src/include/downgrade/downgrade_executor.hpp` (or wherever `downgrade_executor_config` lives per Task 1 mapping)
    - `grep -q 'any_memory_space_in_tier_with_preference' src/downgrade/downgrade_executor.cpp`
    - `grep -q 'preferred_numa_node' src/sirius_context.cpp` (preference populated from hw_topology)
    - `grep -n 'TODO(04-03)' src/sirius_context.cpp` returns 0 hits (Plan 02's marker is resolved)
    - `grep -nE 'downgrade_executor.*itask_executor|class downgrade_task.*public itask' src/include/downgrade/` returns 0 hits (v1.0's deleted inheritance shape was NOT re-introduced)
    - `mcp__project-commands__run_command("build")` exits 0
    - `git log -1 --format='%s'` begins with `feat(01-01): NUMA-aware downgrade preference (re-authored`
  </acceptance_criteria>
  <done>NUMA preference threaded through dev's downgrade_executor_config; dispatch uses cucascade's any_memory_space_in_tier_with_preference; SiriusContext populates the preference from hw_topology; build is green; Plan 02's TODO marker is resolved.</done>
</task>

<task type="auto">
  <name>Task 3: Re-author c5a3d8e tests — basic downgrade lifecycle + [.][multi_gpu_transfer] GPU-to-GPU</name>
  <files>test/cpp/downgrade/test_downgrade_executor.cpp</files>
  <read_first>
    - Task 1 mapping item 5 (dev's test fixture pattern)
    - `git show c5a3d8e -- test/cpp/downgrade/test_downgrade_executor.cpp` — v1.0 test bodies
    - Current `test/cpp/downgrade/test_downgrade_executor.cpp` — dev's post-#579 test fixture pattern (how requests are submitted, how assertions wait)
    - cucascade test patterns from existing `[downgrade]` tests on dev
  </read_first>
  <action>
    Step 1: For each v1.0 TEST_CASE from c5a3d8e, identify dev equivalent API:
      - Basic lifecycle: v1.0 tested `downgrade_executor` start + submit one task + observe
        completion. Dev equivalent: construct executor with `downgrade_executor_config`, submit a
        `downgrade_request`, wait on the returned promise/future/callback.
      - `[.][multi_gpu_transfer]` GPU-to-GPU via converter: v1.0 tested that transferring a
        data_batch from GPU 0 to GPU 1 via cucascade converter produces correct data.
        Dev equivalent: nearly unchanged — this test depends on converter registry, not
        on the downgrade_task class shape directly. Port the test body as-is, adjusting any
        constructor signature drift.

    Step 2: Add the re-authored TEST_CASEs to `test/cpp/downgrade/test_downgrade_executor.cpp`:
      - Preserve Catch2 tags: `[downgrade_executor]` for visible, `[.][multi_gpu_transfer]` for hidden
      - Preserve WARN+return idiom for the hidden case
      - Translate v1.0's `downgrade_task_global_state` / `downgrade_task_local_state` constructions into dev's `downgrade_request` constructions (from Task 1 mapping item 4)

    Step 3: Run pre-commit: `pre-commit run --files test/cpp/downgrade/test_downgrade_executor.cpp`

    Step 4: Build check: `mcp__project-commands__run_command("build")` — must exit 0. If the test file compiles, the type-translation was successful.

    Step 5: Commit:
      ```
      git commit -m "test(01-03): downgrade-executor lifecycle + GPU-to-GPU transfer (re-authored onto dev #579)

      Translate v1.0 commit c5a3d8e's tests onto dev's POD downgrade_task +
      downgrade_request queue architecture:
      - [downgrade_executor]: construct with dev's downgrade_executor_config; submit
        a downgrade_request; wait on the completion signal; assert final memory_space tier.
      - [.][multi_gpu_transfer] gpu_to_gpu_transfer_via_converter: unchanged test body
        (converter registry test, not downgrade-class dependent).

      Original intent: c5a3d8e test(01-03): NUMA-aware downgrade and GPU-to-GPU transfer tests.
      Re-authored: v1.0 tests constructed downgrade_task_global_state / downgrade_task_local_state
      directly — those inner classes were replaced by PR #579. Tests now use the
      downgrade_request submission API. Intent (lifecycle + GPU->GPU transfer correctness) is
      preserved.

      PORT-04, PORT-05"
      ```

    Step 6: Run the specific test tags to verify:
      (via binary direct invocation if available, otherwise via full unit-tests)
      `./build/release/extension/sirius/test/cpp/sirius_unittest "[downgrade_executor]"` — must pass.
      `./build/release/extension/sirius/test/cpp/sirius_unittest "[.][multi_gpu_transfer]"` — on
      single-GPU host should WARN+return cleanly; on N>=2 host should exercise real path.
  </action>
  <verify>
    <automated>grep -q 'downgrade_executor' test/cpp/downgrade/test_downgrade_executor.cpp &amp;&amp; grep -cE '\[\.\]\[multi_gpu_transfer\]' test/cpp/downgrade/test_downgrade_executor.cpp | awk '$1 &gt;= 1 {exit 0} {exit 1}' &amp;&amp; mcp__project-commands__run_command("build")</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '\[downgrade_executor\]' test/cpp/downgrade/test_downgrade_executor.cpp` returns >= 1 (basic lifecycle test)
    - `grep -cE '\[\.\]\[multi_gpu_transfer\]' test/cpp/downgrade/test_downgrade_executor.cpp` returns >= 1 (hidden GPU-to-GPU test)
    - `grep -n 'WARN.*skipping.*requires' test/cpp/downgrade/test_downgrade_executor.cpp` finds the WARN+return idiom for the hidden case
    - `grep -nE 'downgrade_task_global_state|downgrade_task_local_state|downgrade_task.*:.*itask' test/cpp/downgrade/test_downgrade_executor.cpp` returns 0 hits (removed v1.0 shape was NOT re-introduced)
    - `mcp__project-commands__run_command("build")` exits 0
    - `git log -1 --format='%s'` begins with `test(01-03): downgrade-executor lifecycle`
  </acceptance_criteria>
  <done>Basic downgrade-executor lifecycle test + hidden [.][multi_gpu_transfer] GPU-to-GPU test re-authored against dev's request-queue shape; tests compile; build green.</done>
</task>

<task type="auto">
  <name>Task 4: Re-author ec2399e + 0d99cde tests — NUMA ordering + MEM-04 P2P + MEM-05 scan distribution</name>
  <files>test/cpp/downgrade/test_downgrade_executor.cpp</files>
  <read_first>
    - `git show ec2399e -- test/cpp/downgrade/test_downgrade_executor.cpp` — v1.0 NUMA ordering tests
    - `git show 0d99cde -- test/cpp/downgrade/test_downgrade_executor.cpp` — v1.0 MEM-04 + MEM-05 tests
    - Current `test/cpp/downgrade/test_downgrade_executor.cpp` post Task 3 (now has the c5a3d8e re-authored tests)
    - `src/downgrade/downgrade_executor.cpp` post Task 2 (now has preferred_numa_node handling)
  </read_first>
  <action>
    This task batches two v1.0 commits (ec2399e + 0d99cde) into one re-authored change because
    they both add TEST_CASEs to the same file and both depend on Task 2's preferred_numa_node
    plumbing.

    Step 1: Translate v1.0 ec2399e TEST_CASEs:
      - `numa_aware_downgrade_executor_passes_numa_node`: v1.0 constructed a
        downgrade_executor with gpu_numa_node=7 (or similar), then asserted some internal
        state about memory_space selection. Dev equivalent: construct `downgrade_executor_config`
        with `preferred_numa_node = 7`, construct executor, submit a request that triggers a
        memory_space selection, assert the chosen memory_space has numa_id=7 (via a mock/spy on
        the memory_manager or by inspecting the cucascade strategy output).
      - `downgrade_executor_default_numa_node_is_nullopt`: construct `downgrade_executor_config`
        without setting `preferred_numa_node`, assert the field is nullopt (trivially true since
        std::optional defaults to nullopt). Also assert the dispatch path does NOT call
        `any_memory_space_in_tier_with_preference` (verify via spy or by asserting the dispatch
        takes the fallback branch).

      Both preserve tag `[downgrade][numa_aware_downgrade]`.

    Step 2: Translate v1.0 0d99cde TEST_CASEs (MEM-04 P2P + MEM-05 scan distribution):
      - `[.][mem_04_p2p_transfer]` (or v1.0's actual tag — check the git show): `cudaMemcpyPeerAsync`
        P2P transfer test. Dev equivalent: same test body, but the actual P2P implementation
        is DEFERRED to Phase 7 (MGPU-06). For Phase 4, this test should assert the EXISTING
        host-staged transfer path works; add a TODO comment referencing MGPU-06 for the real
        P2P test.
      - `[.][mem_05_scan_distribution]` (or v1.0's actual tag): adaptive scan partitioning by
        GPU memory. Same caveat — adaptive scan is MGPU-07 (Phase 7). For Phase 4, re-author
        the test to assert the `select_target_gpu` function from Plan 02 exists and returns a
        valid GPU index given a list of memory_spaces. Actual memory-proportional distribution
        behavior validation is Phase 7.

      Both preserve `[.]` prefix (hidden tags; WARN+return on single-GPU).

    Step 3: Add `// TODO(MGPU-06)` / `// TODO(MGPU-07)` markers inside the MEM-04 / MEM-05 test
    bodies where the Phase 7 work would expand the assertion set. This makes Phase 7's planner
    job trivial — they grep for the marker.

    Step 4: Run pre-commit: `pre-commit run --files test/cpp/downgrade/test_downgrade_executor.cpp`

    Step 5: Build check: `mcp__project-commands__run_command("build")` — must exit 0.

    Step 6: Commit (two commits per research section 5 "preserve attribution per commit",
    OR one combined commit if the two v1.0 commits are tightly interdependent — executor's
    discretion). If two commits:
      ```
      git commit -m "test(03-01): NUMA downgrade ordering (re-authored onto dev #579)

      Translate v1.0 ec2399e onto dev's downgrade_executor_config:
      - numa_aware_downgrade_executor_passes_numa_node: verifies the config field
        flows into any_memory_space_in_tier_with_preference at dispatch time.
      - downgrade_executor_default_numa_node_is_nullopt: verifies the optional
        defaults and the fallback selection path runs.

      Original intent: ec2399e test(03-01): NUMA downgrade ordering tests
      Re-authored: assertions now target downgrade_executor_config.preferred_numa_node
      rather than the removed downgrade_task_global_state constructor arg.

      PORT-05"

      git commit -m "test(03-01): MEM-04 P2P + MEM-05 scan distribution placeholders (re-authored)

      Translate v1.0 0d99cde: test stubs for the Phase 7 P2P + adaptive-scan work.
      For Phase 4 these tests verify:
      - host-staged GPU->GPU transfer (MEM-03 baseline) still works
      - select_target_gpu exists and returns a valid index
      TODO(MGPU-06/07) markers identify where Phase 7 will expand the assertions.

      PORT-05"
      ```

    Step 7: Verify the Catch2 tag for NUMA ordering: `grep -nE '\[downgrade\]\[numa_aware_downgrade\]' test/cpp/downgrade/test_downgrade_executor.cpp`
    must return at least 2 hits (one per TEST_CASE).
  </action>
  <verify>
    <automated>grep -cE '\[downgrade\]\[numa_aware_downgrade\]' test/cpp/downgrade/test_downgrade_executor.cpp | awk '$1 &gt;= 2 {exit 0} {exit 1}' &amp;&amp; grep -q 'TODO(MGPU-0' test/cpp/downgrade/test_downgrade_executor.cpp &amp;&amp; mcp__project-commands__run_command("build")</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '\[downgrade\]\[numa_aware_downgrade\]' test/cpp/downgrade/test_downgrade_executor.cpp` returns >= 2 (the two TEST_CASEs from ec2399e)
    - `grep -nE 'TODO\(MGPU-0[67]\)' test/cpp/downgrade/test_downgrade_executor.cpp` returns >= 1 hit for each of MGPU-06 and MGPU-07 (markers for Phase 7 to expand)
    - Assertions reference `preferred_numa_node` (not v1.0's removed `gpu_numa_node` constructor param)
    - `mcp__project-commands__run_command("build")` exits 0
    - At least one commit matches `test(03-01): NUMA downgrade ordering` and at least one matches `test(03-01): MEM-04 P2P + MEM-05 scan distribution`
  </acceptance_criteria>
  <done>NUMA downgrade ordering tests + MEM-04/MEM-05 placeholder tests re-authored against dev's downgrade_executor_config + POD downgrade_task; TODO markers identify Phase 7 expansion points; build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Human verification of re-authored NUMA-downgrade behavior</name>
  <files>(human review of commits from Tasks 2-4; no file modifications in this task)</files>
  <read_first>
    - `git log --oneline dev..HEAD` — full commit sequence from Plans 01+02+03
    - The 4 re-authored commits from Tasks 2-4
  </read_first>
  <action>
    Pause execution and present the re-authored NUMA-downgrade work (Tasks 2-4) to the human user for sign-off. Surface:
      - `git log --oneline HEAD~4..HEAD` — the 4 re-authored commits
      - `git show HEAD~3..HEAD` — the combined diffs
      - Results of running the re-authored test tags explicitly
      - Confirmation that no v1.0 deleted shapes were re-introduced
    Wait for the user resume signal before proceeding to Plan 04.
  </action>
  <what-built>
    Re-authored NUMA-aware downgrade on dev's PR #579 shape:
    - `preferred_numa_node` field added to `downgrade_executor_config` (dev's config struct)
    - `downgrade_executor` dispatch consults the field and calls `cucascade::memory::any_memory_space_in_tier_with_preference`
    - `SiriusContext::initialize` populates `preferred_numa_node` from `config_.get_hw_topology().gpus[i].numa_node`
    - Tests translated from v1.0's removed `downgrade_task_global_state` shape to dev's `downgrade_request` queue
    - `[downgrade][numa_aware_downgrade]` tests exercise the config flow
    - `[.][multi_gpu_transfer]` + `[.][mem_04_p2p_transfer]` + `[.][mem_05_scan_distribution]` hidden tests exist with WARN+return on single-GPU
    - TODO(MGPU-06/07) markers identify Phase 7 expansion points
  </what-built>
  <how-to-verify>
    1. Review `git log --oneline dev..HEAD` — confirm 4 new commits (1 feat, 3 test) with `(re-authored onto dev #579)` suffixes.
    2. Review `git show HEAD~3..HEAD` — verify the re-authored diffs look correct:
       - `preferred_numa_node` is on the config struct, not a constructor arg
       - No `class downgrade_task : public itask` or `downgrade_task_global_state` / `downgrade_task_local_state` re-appeared
       - `any_memory_space_in_tier_with_preference` call site has sensible fallback for nullopt case
    3. Run the re-authored tests explicitly:
       ```
       ./build/release/extension/sirius/test/cpp/sirius_unittest "[downgrade_executor],[downgrade][numa_aware_downgrade]"
       ```
       Expect: all pass (these are visible tags, not hidden).
    4. If on N>=2 GPU host, additionally run:
       ```
       ./build/release/extension/sirius/test/cpp/sirius_unittest "[.][multi_gpu_transfer]"
       ```
       Expect: real test body runs; assertions pass.
    5. Confirm behavioral intent preserved: the user's decision from STATE.md ("NUMA-aware downgrade via cucascade `any_memory_space_in_tier_with_preference`") is honored in the re-authored dispatch.
    6. Confirm no regression: `mcp__project-commands__run_command("unit-tests")` exits 0 end-to-end.

    If any of the above fails, do NOT proceed to Plan 04. Surface the issue so it can be fixed or rolled back.
  </how-to-verify>
  <verify>
    <automated>MISSING — human-verify checkpoint. Automated gates live in Plan 05 Task 1 (full unit-tests) and Plan 05 Task 2 (structural grep + explicit hidden-tag invocation). This task pauses for human sign-off on the behavioral translation quality.</automated>
  </verify>
  <done>Human reviewed the 4 re-authored commits, confirmed v1.0 intent is preserved on dev PR #579 shape, and typed "approved" to resume execution of Plan 04.</done>
  <resume-signal>Type "approved" to proceed to Plan 04, or describe specific issues observed in the re-authored commits / test results.</resume-signal>
</task>

</tasks>

<verification>
Plan-level exit gate:
1. 4 re-authored commits present on branch with `(re-authored onto dev #579)` suffixes (Tasks 2, 3, 4)
2. `grep -q 'preferred_numa_node' src/include/downgrade/downgrade_executor.hpp` (or wherever the config struct lives)
3. `grep -q 'any_memory_space_in_tier_with_preference' src/downgrade/downgrade_executor.cpp`
4. `grep -n 'TODO(04-03)' src/sirius_context.cpp` returns 0 hits
5. `grep -nE 'downgrade_task_global_state|downgrade_task_local_state|class downgrade_task.*:.*itask' src/ test/` returns 0 hits (dead v1.0 shape not re-introduced)
6. `mcp__project-commands__run_command("build")` exits 0
7. `mcp__project-commands__run_command("unit-tests")` exits 0 with `[downgrade]`, `[downgrade][numa_aware_downgrade]` tags passing
8. Human verification checkpoint (Task 5) approved
</verification>

<success_criteria>
- PORT-01 completed: all 10 code-carrying v1.0 commits' INTENT is on the branch (5 cherry-picked in Plan 02, 4 re-authored in Plan 03, 1 optional e1dab76 fixup). Actual commit count on branch >= 10 counting from dev.
- PORT-02: no DuckDB vocabulary types reintroduced; v1.0's removed class shapes (`itask` base, `downgrade_task_global_state/_local_state`) absent.
- PORT-04 completed: push-model dispatch plumbing (Plan 02) + NUMA-aware downgrade (Plan 03) both in place. The `[downgrade][numa_aware_downgrade]` tests pass.
- Build and unit-tests green.
- Human has verified the behavioral translation captures v1.0 intent (checkpoint task 5).
- TODO markers exist for MGPU-06 (Phase 7 P2P) and MGPU-07 (Phase 7 adaptive scan) — Phase 7 planner has a trivial grep-based discovery path.
</success_criteria>

<output>
After completion, create `.planning/phases/04-cucascade-bump-v1-0-re-integration/04-03-SUMMARY.md` documenting:
- Task 1 mapping artifacts (v1.0 intent -> dev shape reference table)
- The 4 re-authored commit SHAs with messages
- Any decisions made about v1.0 test bodies (what was kept as-is vs. adapted vs. deferred to Phase 7)
- The Task 5 human-verification outcome (approved / revised)
- Any `TODO(MGPU-0N)` markers added, with file:line
- Confirmation that cucascade `any_memory_space_in_tier_with_preference` exists at f47de0b (from Task 2 verification)
</output>
