---
phase: 04-cucascade-bump-v1-0-re-integration
plan: 05
type: execute
wave: 5
depends_on:
  - 04-04
files_modified: []
autonomous: false
requirements:
  - PORT-05

must_haves:
  truths:
    - "Full unit-test suite passes end-to-end after all Phase 4 changes"
    - "v1.0 visible test tags ([multi_gpu_foundation], [data_locality], [downgrade_executor], [downgrade][numa_aware_downgrade]) pass on single-GPU host"
    - "v1.0 hidden test tags ([.][multi_gpu_foundation], [.][multi_gpu_transfer], [.][data_locality][multi_gpu]) compile and WARN+return on single-GPU, exercise real logic on N>=2 GPU host"
    - "Phase-level structural grep gates pass: zero LogicalType::INTEGER|BIGINT|VARCHAR, zero libconfig, zero v1.0 deleted class shapes"
    - "git log shows the expected commit shape (1 bump + 5 cherry-picks + 4 re-authored + 0-2 config/fixup commits)"
  artifacts:
    - path: "build/release/extension/sirius/test/cpp/sirius_unittest"
      provides: "Full unit-test binary exercising all ported test tags"
    - path: ".planning/phases/04-cucascade-bump-v1-0-re-integration/04-SUMMARY.md"
      provides: "Phase-level rollup summary"
  key_links:
    - from: "git log dev..HEAD"
      to: "ROADMAP.md Phase 4 success criteria"
      via: "Commit list satisfies all 5 success-criteria items"
      pattern: "BUMP-0[1-3]|PORT-0[1-5]"
---

<objective>
Final v1.0 test suite gate for Phase 4. Runs the full unit-test suite end-to-end, executes the hidden multi-GPU-gated tests explicitly (WARN+return on single-GPU hosts), runs all Phase-level structural grep gates, and produces the phase summary. If hardware with N>=2 GPUs is available, this plan also exercises the hidden `[.][multi_gpu_foundation]`, `[.][multi_gpu_transfer]`, `[.][data_locality][multi_gpu]` tests for real multi-GPU validation.

Purpose: isolate the exit-gate verification from the code-change plans so success criteria are independently reviewable. PORT-05 explicitly calls for multi-GPU test suites to pass; this plan is the gate.

Output: a green test run + documented structural-grep-gate results + the phase-level SUMMARY.md rolled up from all sub-plan summaries.

This plan is autonomous=false because it contains a `checkpoint:human-verify` task (Task 3) for the final phase sign-off — the user decides whether to ship the phase based on the consolidated test + gate results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-cucascade-bump-v1-0-re-integration/04-CONTEXT.md
@.planning/phases/04-cucascade-bump-v1-0-re-integration/04-RESEARCH.md
@.planning/phases/04-cucascade-bump-v1-0-re-integration/04-01-SUMMARY.md
@.planning/phases/04-cucascade-bump-v1-0-re-integration/04-02-SUMMARY.md
@.planning/phases/04-cucascade-bump-v1-0-re-integration/04-03-SUMMARY.md
@.planning/phases/04-cucascade-bump-v1-0-re-integration/04-04-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Test tags that PORT-05 requires. Sourced from research section 4 (corrected tag table) -->

### Visible tags (run by default via `unit-tests`)

| Tag | Source | Test file | What it exercises |
|-----|--------|-----------|-------------------|
| `[multi_gpu_foundation]` | 3777645 | test/cpp/config/test_context.cpp | topology_discovery, reservation_manager_configurator, memory_manager, converter_registry |
| `[data_locality]` | 2e6ba26 | test/cpp/integration/test_gpu_execution_locality.cpp | preferred_device_id defaults, precedence, NUMA-to-GPU mapping, locality-score, proportional distribution |
| `[downgrade_executor]` | c5a3d8e (re-authored) | test/cpp/downgrade/test_downgrade_executor.cpp | lifecycle on dev's downgrade_request queue |
| `[downgrade][numa_aware_downgrade]` | ec2399e (re-authored) | test/cpp/downgrade/test_downgrade_executor.cpp | preferred_numa_node defaults + passthrough |

### Hidden tags (leading `[.]`; require explicit tag invocation on binary)

| Tag | Source | WARN+return on single-GPU? | N>=2 behavior |
|-----|--------|------|-----------------|
| `[.][multi_gpu_foundation]` | 3777645 | YES | Tests 2-GPU topology config |
| `[.][multi_gpu_transfer]` | c5a3d8e (re-authored) | YES | GPU 0 -> GPU 1 via cucascade converter |
| `[.][data_locality][multi_gpu]` | 2e6ba26 | YES | Scan batches distributed across 2+ GPUs |
| `[.][mem_04_p2p_transfer]` (stub) | 0d99cde (re-authored) | YES | Phase 4 checks baseline; Phase 7 expands |
| `[.][mem_05_scan_distribution]` (stub) | 0d99cde (re-authored) | YES | Phase 4 checks baseline; Phase 7 expands |

### Catch2 v2 hidden-tag convention

Hidden (leading `[.]`) tags require EXPLICIT invocation on the test binary:
```
./build/release/extension/sirius/test/cpp/sirius_unittest "[.][multi_gpu_foundation]"
```
On a single-GPU host, the test body starts with:
```cpp
int count = 0; cudaGetDeviceCount(&count);
if (count < 2) { WARN("skipping: requires >=2 GPUs"); return; }
```
— which Catch2 v2 records as a PASS (the WARN is informational).

### Structural grep gates (PORT-02, PORT-03, and plan-03 dead-class check)

```bash
# PORT-02 — no DuckDB vocabulary re-introduced
grep -rnE 'LogicalType::(INTEGER|BIGINT|VARCHAR)' src/ test/   # must be 0 hits

# PORT-03 — no libconfig
grep -rn 'libconfig' src/ test/                                 # must be 0 hits

# Plan-03 dead-class check — v1.0's removed shapes did not reappear
grep -rnE 'downgrade_task_global_state|downgrade_task_local_state|class downgrade_task.*:.*itask' src/ test/   # must be 0 hits

# BUMP-01 — cucascade pin
git -C cucascade rev-parse HEAD   # must be f47de0bb7bcaddd55081a9c4bc584627532d1ef9

# PORT-01 — commit count
git log --oneline dev..HEAD | wc -l   # must be >= 10 (1 bump + 9-11 code/test commits)

# PORT-04 — key symbols exist in expected files
grep -c 'preferred_device_id' src/include/pipeline/sirius_pipeline_task_states.hpp  # >= 2
grep -q 'get_preferred_device_id' src/include/pipeline/gpu_pipeline_task.hpp
grep -qE 'compute_data_locality_score|data_locality_score' src/creator/task_creator.cpp
grep -q 'preferred_device_id' src/pipeline/pipeline_executor.cpp
grep -q 'preferred_numa_node' src/include/downgrade/downgrade_executor.hpp
grep -q 'any_memory_space_in_tier_with_preference' src/downgrade/downgrade_executor.cpp
grep -q 'select_target_gpu' src/op/scan/duckdb_scan_executor.cpp
```

### Phase summary consolidation

The phase summary `.planning/phases/04-cucascade-bump-v1-0-re-integration/04-SUMMARY.md`
rolls up:
  - 04-01-SUMMARY: bump outcome
  - 04-02-SUMMARY: cherry-pick outcomes (5 commits + e1dab76)
  - 04-03-SUMMARY: re-authored commits + Task 1 mapping + human verification
  - 04-04-SUMMARY: PORT-03 verification + pre-commit fixups
  - Plus this plan's results (Tasks 1-2)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full unit-test suite run + visible-tag gate</name>
  <files>(verification only — no file modifications)</files>
  <read_first>
    - All sub-plan summaries (04-01-SUMMARY through 04-04-SUMMARY)
    - .planning/phases/04-cucascade-bump-v1-0-re-integration/04-RESEARCH.md section 4 test tags + section 6 validation strategy
  </read_first>
  <action>
    Step 1: Full suite run via MCP:
      `mcp__project-commands__run_command("unit-tests")`
      Exit 0 required. This runs all visible tags including (but not limited to):
        - `[multi_gpu_foundation]` (visible)
        - `[data_locality]` (visible)
        - `[downgrade_executor]` (visible)
        - `[downgrade][numa_aware_downgrade]` (visible)
      Plus all pre-existing tests on dev.

    Step 2: Inspect the test-log directory for any WARN messages or skipped tests:
      Log path per CLAUDE.md: `build/release/extension/sirius/test/cpp/log/`
      Look for entries matching "WARN" or "skipped" — normal if tests called WARN+return
      on single-GPU host for hidden-tag tests that ran (but if the full suite runs them,
      they're visible; this section really only matters if a visible test called WARN).

    Step 3: Record exit code + any noted warnings into the structured return for consolidation
    into the phase summary.

    Step 4: If the suite fails:
      - Identify the failing test name + exact assertion
      - Determine the source commit (was it a Plan 02 cherry-pick, a Plan 03 re-authoring,
        or a pre-existing dev test?)
      - Triage:
        * If failure is in a Plan 02/03 commit and the error is a simple symbol/include fix:
          amend the responsible commit (using `git commit --amend --no-edit` after staging the fix)
        * If failure is in a pre-existing dev test: this is a regression — probably caused by
          a behavioral drift in preferred_device_id routing or downgrade_executor_config.
          Investigate which Plan 02/03 commit introduced the change; surface as a blocker.
      - Do NOT proceed to Task 2 until the full suite is green.
  </action>
  <verify>
    <automated>mcp__project-commands__run_command("unit-tests")</automated>
  </verify>
  <acceptance_criteria>
    - `mcp__project-commands__run_command("unit-tests")` exits 0
    - Pre-existing tests on dev still pass (no regression)
    - Visible tags added by Phase 4 (`[multi_gpu_foundation]`, `[data_locality]`, `[downgrade_executor]`, `[downgrade][numa_aware_downgrade]`) all report success
    - Log directory does NOT contain new unexpected WARN messages for visible tests
    - Exit code + duration recorded in structured return
  </acceptance_criteria>
  <done>Full unit-test suite passes end-to-end on the post-Phase-4 branch; all visible v1.0-ported tags are green.</done>
</task>

<task type="auto">
  <name>Task 2: Hidden-tag explicit invocation + structural grep gates</name>
  <files>(verification only)</files>
  <read_first>
    - .planning/phases/04-cucascade-bump-v1-0-re-integration/04-RESEARCH.md section 4 (hidden tags list) + section 6 (grep gates)
    - Hardware inventory: run `nvidia-smi -L | wc -l` or `cudaGetDeviceCount` via a small test binary to determine GPU count
  </read_first>
  <action>
    Step 1: Determine GPU count:
      Preferred: run a trivial CUDA call. Simplest: `nvidia-smi -L | wc -l`. If 0 or 1 GPUs
      available, hidden tests will WARN+return. If >=2, they exercise real multi-GPU paths.

    Step 2: Explicit hidden-tag invocation (runs even on single-GPU; WARN+return is a pass):
      ```
      ./build/release/extension/sirius/test/cpp/sirius_unittest "[.][multi_gpu_foundation]"
      ./build/release/extension/sirius/test/cpp/sirius_unittest "[.][multi_gpu_transfer]"
      ./build/release/extension/sirius/test/cpp/sirius_unittest "[.][data_locality][multi_gpu]"
      ./build/release/extension/sirius/test/cpp/sirius_unittest "[.][mem_04_p2p_transfer]"
      ./build/release/extension/sirius/test/cpp/sirius_unittest "[.][mem_05_scan_distribution]"
      ```
      Each invocation must exit 0. Log for each invocation should either:
      - On single-GPU: contain "WARN: skipping: requires >=2 GPUs" and exit 0
      - On N>=2: exercise the real test body and exit 0 if assertions pass

      If the MCP wrapper doesn't expose tag-filtered runs (per research section 4), invoke the
      binary directly via a new MCP-wrapped command if available, OR record the intent and
      gate this as a "best-effort" verification that runs where the tooling permits.

    Step 3: Run all structural grep gates. Each must return 0 hits (or satisfy the predicate):
      ```
      # PORT-02
      grep -rnE 'LogicalType::(INTEGER|BIGINT|VARCHAR)' src/ test/   # must == 0

      # PORT-03
      grep -rn 'libconfig' src/ test/                                 # must == 0

      # Dead v1.0 class shapes
      grep -rnE 'downgrade_task_global_state|downgrade_task_local_state|class downgrade_task.*:.*itask' src/ test/   # must == 0

      # BUMP-01
      git -C cucascade rev-parse HEAD | grep -qx 'f47de0bb7bcaddd55081a9c4bc584627532d1ef9'   # must match

      # PORT-01 commit count
      [ $(git log --oneline dev..HEAD | wc -l) -ge 10 ]   # must be true

      # PORT-04 key symbols
      [ $(grep -c 'preferred_device_id' src/include/pipeline/sirius_pipeline_task_states.hpp) -ge 2 ]
      grep -q 'get_preferred_device_id' src/include/pipeline/gpu_pipeline_task.hpp
      grep -qE 'compute_data_locality_score|data_locality_score' src/creator/task_creator.cpp
      grep -q 'preferred_device_id' src/pipeline/pipeline_executor.cpp
      grep -q 'preferred_numa_node' src/include/downgrade/downgrade_executor.hpp
      grep -q 'any_memory_space_in_tier_with_preference' src/downgrade/downgrade_executor.cpp
      grep -q 'select_target_gpu' src/op/scan/duckdb_scan_executor.cpp
      ```

    Step 4: Aggregate results for phase summary:
      - GPU count on test host
      - Each hidden-tag invocation's exit code + log summary (WARN+return vs. real run)
      - Each structural grep gate's hit count (or pass/fail)
      - Total commit count `dev..HEAD`
      - Cucascade submodule HEAD

    Step 5: If any gate FAILS:
      - PORT-02/PORT-03 grep hit: identify offending file + line; fix in-place; amend
        responsible commit or create a new fix commit.
      - Dead class shape found: investigate which file/commit; remove the re-introduction.
      - BUMP-01 pin mismatch: `git -C cucascade rev-parse HEAD` returns wrong SHA — Plan 01
        was incomplete or a subsequent commit accidentally bumped cucascade further. Fix the
        pin.
      - Commit count < 10: indicates one of Plans 02/03 commits was missed. Cross-reference
        against the expected commit list (1 bump + 5 cherry-picks + 4 re-authored +
        optionally 1 e1dab76 fixup + optionally 1 YAML fix + optionally 1 style fixup).
      - PORT-04 symbol missing: the corresponding Plan 02/03 task did not land correctly.

      Each failure is a blocker for Task 3 (human verification).
  </action>
  <verify>
    <automated>test $(grep -rnE 'LogicalType::(INTEGER|BIGINT|VARCHAR)' src/ test/ 2>/dev/null | wc -l) -eq 0 &amp;&amp; test $(grep -rn 'libconfig' src/ test/ 2>/dev/null | wc -l) -eq 0 &amp;&amp; test $(grep -rnE 'downgrade_task_global_state|downgrade_task_local_state' src/ test/ 2>/dev/null | wc -l) -eq 0 &amp;&amp; git -C cucascade rev-parse HEAD | grep -qx 'f47de0bb7bcaddd55081a9c4bc584627532d1ef9' &amp;&amp; test $(git log --oneline dev..HEAD | wc -l) -ge 10 &amp;&amp; grep -q 'preferred_numa_node' src/include/downgrade/downgrade_executor.hpp</automated>
  </verify>
  <acceptance_criteria>
    - All hidden tags invoked explicitly (5 tags listed in action Step 2); each exits 0
    - On single-GPU host: hidden tag logs contain `WARN: skipping: requires >=2 GPUs` (or equivalent) for each hidden test
    - On N>=2 host (if available): hidden tag logs show real test bodies executing and passing
    - Structural grep gates:
      * `LogicalType::(INTEGER|BIGINT|VARCHAR)` grep -> 0 hits
      * `libconfig` grep -> 0 hits
      * `downgrade_task_global_state|downgrade_task_local_state` grep -> 0 hits
      * cucascade HEAD = `f47de0bb7bcaddd55081a9c4bc584627532d1ef9`
      * `git log --oneline dev..HEAD | wc -l` >= 10
    - PORT-04 symbol presence: every grep in Step 3 "PORT-04 key symbols" section returns a hit
    - All results captured in structured return for inclusion in phase summary
  </acceptance_criteria>
  <done>Hidden tags run explicitly (WARN+return on single-GPU); structural grep gates all pass; PORT-04 key symbols present; commit count meets >=10 threshold.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 4 final sign-off — human reviews consolidated results and approves shipment</name>
  <files>(human review of all Phase 4 commits and summaries; no file modifications in this task)</files>
  <read_first>
    - Tasks 1 + 2 results (from structured return)
    - All sub-plan summaries (04-01 through 04-04)
    - .planning/ROADMAP.md Phase 4 "Success Criteria" section (the 5-item list)
  </read_first>
  <action>
    Pause execution and present the consolidated Phase 4 results to the human user for phase-level sign-off. Surface:
      - `git log --oneline dev..HEAD` — the full Phase 4 commit sequence
      - Task 1 full unit-tests exit code + duration
      - Task 2 structural grep gate results (all 5 gates)
      - Task 2 hidden-tag explicit invocation results (single-GPU WARN+return or N>=2 real runs)
      - Cross-reference each ROADMAP Phase 4 Success Criteria item with its evidence
    Wait for user approval before proceeding to Task 4 (phase summary writing).
  </action>
  <what-built>
    Phase 4 end state:
    - cucascade submodule bumped to f47de0b (BUMP-01, BUMP-02, BUMP-03 proven via Plan 01)
    - 5 v1.0 cherry-picks with authorship preserved (Plan 02)
    - 4 re-authored commits against dev's PR #579 shape (Plan 03)
    - PORT-03 YAML config coverage proven (Plan 04)
    - Full unit-test suite passes; hidden tags compile + WARN+return on single-GPU (Task 1+2)
    - All structural grep gates pass (Task 2)
    - Phase 4 success criteria items 1-5 from ROADMAP.md all verifiable

    Ready to merge this phase's work as the stable base for Phase 5 (cucascade-backed parquet I/O migration).
  </what-built>
  <how-to-verify>
    1. Open `.planning/ROADMAP.md` and read the Phase 4 "Success Criteria" 5-item list. Cross-check each item against the Plan 05 Task 2 results:
       - Item 1 (BUMP-01, BUMP-02): Plan 01 + submodule HEAD + build exit 0
       - Item 2 (BUMP-03): Plan 01 5x test-run pass
       - Item 3 (PORT-01, PORT-02): Commit count + LogicalType grep
       - Item 4 (PORT-03): libconfig grep
       - Item 5 (PORT-04, PORT-05): preferred_device_id + test tag results

    2. Review `git log --oneline dev..HEAD` — does the commit list tell a coherent story?
       - 1 bump commit (chore(cucascade))
       - 5 cherry-picks (feat + test from v1.0, author preserved)
       - ~4 re-authored commits ((re-authored onto dev #579) suffix)
       - optional: e1dab76 fixup, config fix, style fixup

    3. Inspect `git log --pretty=format:'%an %s' dev..HEAD` — v1.0 commit authorship preserved on the cherry-picked commits?

    4. Run an ad-hoc sanity query via the built extension (optional but highly recommended):
       ```sql
       LOAD 'build/release/extension/sirius/sirius.duckdb_extension';
       CALL gpu_execution('SELECT 1');
       ```
       Expect: runs and returns 1. If this crashes, something is structurally broken even
       though unit-tests passed — triage before sign-off.

    5. Multi-GPU hardware available? If yes, run a 2-GPU scan test:
       ```sql
       LOAD 'build/release/extension/sirius/sirius.duckdb_extension';
       CALL gpu_execution('SELECT COUNT(*) FROM parquet_scan(''path/to/tpch/lineitem.parquet'')');
       ```
       Verify in logs that both GPUs participated (preferred_device_id routing active).

    6. If all of the above look good: type "approved" to mark Phase 4 complete. The orchestrator will proceed to phase transition.

    7. If issues remain: list them — specific commits to amend, gaps to close, or follow-up tasks needed.
  </how-to-verify>
  <verify>
    <automated>MISSING — human-verify checkpoint. Automated gates are in Task 1 (full unit-tests exits 0) and Task 2 (all structural grep gates satisfied). This task pauses for human review and phase-level sign-off.</automated>
  </verify>
  <done>Human reviewed the Phase 4 commit sequence, test results, and grep-gate results; cross-checked against ROADMAP Phase 4 Success Criteria; typed "approved" to sign off on the phase.</done>
  <resume-signal>Type "approved" to mark Phase 4 complete and enable `/gsd:transition` to Phase 5, or describe specific issues that block phase sign-off.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Write phase-level summary rolling up Plans 01-05</name>
  <files>.planning/phases/04-cucascade-bump-v1-0-re-integration/04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/04-cucascade-bump-v1-0-re-integration/04-01-SUMMARY.md
    - .planning/phases/04-cucascade-bump-v1-0-re-integration/04-02-SUMMARY.md
    - .planning/phases/04-cucascade-bump-v1-0-re-integration/04-03-SUMMARY.md
    - .planning/phases/04-cucascade-bump-v1-0-re-integration/04-04-SUMMARY.md
    - Task 1 + Task 2 results from structured return
    - Task 3 human-verification outcome
    - .planning/ROADMAP.md Phase 4 Success Criteria (for checklist alignment)
  </read_first>
  <action>
    Step 1: Create `.planning/phases/04-cucascade-bump-v1-0-re-integration/04-SUMMARY.md` using the summary template. Required sections:

      ## Phase 4 Outcome (single line): PASS | BLOCKED | PARTIAL

      ## Requirements Satisfied
        Table mapping every BUMP-* and PORT-* requirement to its evidence
        (command output, grep result, test name).

      ## Commits Landed
        `git log --oneline dev..HEAD` output with annotations:
        - Which commits are cherry-picks (author preserved)
        - Which are re-authored
        - Which are config / style / fixup commits

      ## Deviations from v1.0 Intent
        Enumerate decisions made in Plan 03 (re-authoring) that deviated from v1.0's literal
        diff but preserved its intent, with rationale.

      ## TODO Markers Added for Future Phases
        - TODO(MGPU-06) markers (Phase 7 P2P)
        - TODO(MGPU-07) markers (Phase 7 adaptive scan)
        - Any TODO(HYG-*) markers (Phase 5 stream cleanup) if introduced

      ## Test Results
        - Full unit-tests: PASS/FAIL + duration
        - Hidden tags invocation results (WARN+return on single-GPU, or real run on N>=2)
        - Structural grep gates (all pass)

      ## Cucascade Bump Notes
        - Previous pin: 942c0bf
        - New pin: f47de0b
        - Observations from bump: any warnings, behavior changes noticed (e.g., PR #103's added stream.synchronize in convert_to — noted as potential Phase 5 perf signal)

      ## Open Questions Resolved
        Cross-reference research OQ-1 through OQ-5:
        - OQ-1 (e1dab76 fixup): included? skipped?
        - OQ-2 (YAML config coverage): verified
        - OQ-3 (PR #103 perf impact): deferred to Phase 5 measurement
        - OQ-4 (hidden test explicit invocation): done in Task 2
        - OQ-5 (Catch2 SKIP): confirmed WARN+return convention used

      ## Next Phase Prep
        Phase 5 (cucascade-backed parquet I/O) starting state:
        - cucascade pin: f47de0b (idisk_io_backend available)
        - preferred_device_id plumbing in place (needed for per-GPU backend resolution)
        - Per-GPU memory spaces instantiated (needed for backend construction under rmm::cuda_set_device_raii)
        - HYG-01 / HYG-02 cuda_stream_default hygiene debt: NOT fixed in Phase 4 (per CONTEXT.md stream-discipline guidance); deferred to Phase 5.

    Step 2: Commit the summary:
      ```
      git commit -m "docs(04): phase summary

      Rolls up Plans 01-05 outcomes, commit-level annotations, test results,
      TODO markers for Phases 5+7, and open-question resolutions.

      Phase 4 closes with cucascade bumped (f47de0b), v1.0 multi-GPU code
      re-integrated on top of dev's 47-commit drift, and the full unit-test
      suite green."
      ```
  </action>
  <verify>
    <automated>test -f .planning/phases/04-cucascade-bump-v1-0-re-integration/04-SUMMARY.md &amp;&amp; grep -qE 'Phase 4 Outcome|Requirements Satisfied|Commits Landed' .planning/phases/04-cucascade-bump-v1-0-re-integration/04-SUMMARY.md &amp;&amp; git log -1 --format='%s' | grep -q '^docs(04): phase summary'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/04-cucascade-bump-v1-0-re-integration/04-SUMMARY.md` exists on disk
    - File contains all 8 required sections from action Step 1 (checkable via grep)
    - Every requirement ID (BUMP-01, BUMP-02, BUMP-03, PORT-01, PORT-02, PORT-03, PORT-04, PORT-05) appears in the "Requirements Satisfied" table with evidence
    - A commit exists with message starting `docs(04): phase summary`
    - File referenced in the phase's final structured return so the orchestrator's `/gsd:transition` can consume it
  </acceptance_criteria>
  <done>Phase 4 rollup summary written; commit landed; ready for `/gsd:transition` to mark Phase 4 complete in ROADMAP.md and STATE.md.</done>
</task>

</tasks>

<verification>
Phase-level exit gate (final):
1. `mcp__project-commands__run_command("unit-tests")` exits 0 (Task 1)
2. All structural grep gates pass (Task 2)
3. All 5 hidden tags invoked explicitly exit 0 (Task 2)
4. Human sign-off captured (Task 3)
5. `.planning/phases/04-cucascade-bump-v1-0-re-integration/04-SUMMARY.md` exists and covers all BUMP-* / PORT-* requirements (Task 4)
</verification>

<success_criteria>
- PORT-05 satisfied: v1.0 test suites ([multi_gpu_foundation], [data_locality], [downgrade][numa_aware_downgrade]) are green (visible) or WARN+return safely (hidden)
- All 8 Phase 4 requirements (BUMP-01..03, PORT-01..05) traceable to evidence in 04-SUMMARY.md
- Human has approved Phase 4 as shippable
- Phase 4 end state is the stable base Phase 5 will build on
</success_criteria>

<output>
After completion, `.planning/phases/04-cucascade-bump-v1-0-re-integration/04-SUMMARY.md` is the authoritative rollup. STATE.md will be updated by `/gsd:transition` based on this file.
</output>
