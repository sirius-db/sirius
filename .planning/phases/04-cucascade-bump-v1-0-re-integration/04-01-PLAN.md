---
phase: 04-cucascade-bump-v1-0-re-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cucascade
  - .gitmodules
autonomous: true
requirements:
  - BUMP-01
  - BUMP-02
  - BUMP-03

must_haves:
  truths:
    - "cucascade submodule HEAD is pinned to f47de0b (origin/main target pin)"
    - "Sirius builds cleanly against the bumped cuCascade surface with exit 0"
    - "Pre-existing cucascade integration tests ([downgrade], [reservation], [converter]) pass post-bump with zero new flakes across 5 consecutive runs"
  artifacts:
    - path: "cucascade"
      provides: "Submodule pin updated to f47de0b"
      contains: "f47de0bb7bcaddd55081a9c4bc584627532d1ef9"
    - path: "build/release/extension/sirius/sirius.duckdb_extension"
      provides: "Green build against bumped cucascade"
  key_links:
    - from: "CMakeLists.txt"
      to: "cuCascade::cucascade"
      via: "target_link_libraries"
      pattern: "cuCascade::cucascade"
---

<objective>
Bump the cuCascade submodule from `942c0bf` to `f47de0b` (origin/main) and prove the Sirius build + pre-existing cucascade integration tests survive the bump with no new warnings or flakes. This isolates the cucascade upgrade from the v1.0 code re-integration — the bump must stand on its own so any downstream failure is unambiguously attributable to the port, not the submodule upgrade.

Purpose: PR #96 (`idisk_io_backend`, `io_backend_registry`) ships in f47de0b and is a hard prerequisite for Phase 5's parquet I/O migration. PR #100/#103/#104 are absorbed transitively; none break Sirius's current call sites (see research §3).

Output: One commit on `feature/single-node-multi-gpu2` that moves the submodule pin, with validated build + 5x test-run stability as the gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-cucascade-bump-v1-0-re-integration/04-CONTEXT.md
@.planning/phases/04-cucascade-bump-v1-0-re-integration/04-RESEARCH.md
@CLAUDE.md
@CMakeLists.txt

<interfaces>
<!-- cuCascade public surface that matters for the bump. Absorbed from research §3. -->
<!-- PR #96 additions are purely additive; Sirius does not call them in Phase 4. -->
<!-- PR #103 adds a `stream.synchronize()` inside `data_batch::convert_to` (load-bearing for correctness). -->

From cucascade (f47de0b) — files touched by bump range 942c0bf..f47de0b:

PR #96 "File downgrade" — NEW public headers (additive, not called in Phase 4):
  - cucascade/memory/disk_memory_space_config.hpp — new config struct
  - cucascade/io/idisk_io_backend.hpp — abstract interface (pure virtual)
  - cucascade/io/io_backend_registry.hpp — factory registry
  - cucascade/data/disk_data_representation.hpp — DISK-tier representation
  - cucascade/io/disk_file_format.hpp

PR #100 "memory_space underflow fix" — internal only.

PR #103 "Sync stream before destroying GPU representation" — in data_batch::convert_to:
  Behavior change: the function now synchronizes the caller's stream before the old
  representation destructs when either source or target tier is GPU. Sirius calls
  convert_to in 30+ sites (aggregation, order, grouped-agg-merge, top-n). Expected to
  be correct + possibly slightly slower per call — measurable perf delta is a Phase 5
  benchmark, not a Phase 4 gate.

PR #104 "Drop hard NVML link" — cucascade's CUCASCADE_PUBLIC_LINK_LIBS no longer
  contains NVML. Sirius never linked NVML directly. CMakeLists.txt line 321
  links `cuCascade::cucascade` which re-exports the (now smaller) public link set.
  Verified: grep -nE 'nvml|NVML|NVIDIA_ML' CMakeLists.txt returns 0 hits today.

From .ai-helper/commands.yaml — MCP command reference:
  - run_command("build")         → make -j$(nproc)
  - run_command("unit-tests")    → ./build/release/extension/sirius/test/cpp/sirius_unittest --abort
  - run_command("clean")         → rm -rf build
  - run_command("cmake-release") → cmake --preset release
  - run_command("pre-commit")    → pre-commit run --all-files
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump cucascade submodule to f47de0b and commit</name>
  <files>cucascade, .gitmodules</files>
  <read_first>
    - .planning/phases/04-cucascade-bump-v1-0-re-integration/04-RESEARCH.md (§3 "cuCascade Bump Impact" — understand what's in the bump range before doing it)
    - .gitmodules (verify cucascade submodule entry exists and path)
    - CMakeLists.txt lines 300–330 (confirm `cuCascade::cucascade` target name + link wiring)
  </read_first>
  <action>
    Step 1: Capture the current cucascade HEAD for rollback reference:
      `git -C cucascade rev-parse HEAD` — expected `942c0bf...` (record the full SHA).

    Step 2: Fetch cucascade origin and check out the target pin:
      `git -C cucascade fetch origin`
      `git -C cucascade checkout f47de0bb7bcaddd55081a9c4bc584627532d1ef9`
      (If `f47de0b` does not resolve exactly, fetch `origin/main` and resolve via
       `git -C cucascade rev-parse origin/main` — the research locked this as the target.
       If `origin/main` has advanced past `f47de0b`, pause and surface the drift via the
       structured return. Do NOT pick a newer pin without explicit user confirmation.)

    Step 3: Stage only the submodule pointer + .gitmodules (if modified):
      From the superproject worktree root (NOT inside cucascade/):
        `git add cucascade`
      If `.gitmodules` changed (unlikely — URL shouldn't have moved), `git add .gitmodules` too.

    Step 4: Do NOT run pre-commit yet — this commit is purely a submodule pointer change and
    pre-commit's C++ hooks can't run against the submodule's internal file set. Commit directly:

      git commit -m "chore(cucascade): bump submodule 942c0bf -> f47de0b (origin/main)

      Brings in:
      - PR #96: idisk_io_backend / io_backend_registry / disk_data_representation
        (prerequisite for Phase 5 parquet I/O migration; additive to Sirius)
      - PR #100: memory_space underflow fix (internal bugfix)
      - PR #103: stream sync before destroying GPU representation in data_batch::convert_to
        (correctness fix for stream-ordered GPU work)
      - PR #104: drop hard NVML link (Sirius never linked NVML directly — no-op)

      BUMP-01"

    Step 5: Verify the commit landed:
      `git -C cucascade rev-parse HEAD` must print `f47de0bb7bcaddd55081a9c4bc584627532d1ef9`.
      `git log -1 --format='%s'` must begin with `chore(cucascade): bump`.
  </action>
  <verify>
    <automated>git -C cucascade rev-parse HEAD | grep -q '^f47de0bb7bcaddd55081a9c4bc584627532d1ef9$' &amp;&amp; git log -1 --format='%s' | grep -q '^chore(cucascade): bump'</automated>
  </verify>
  <acceptance_criteria>
    - `git -C cucascade rev-parse HEAD` outputs exactly `f47de0bb7bcaddd55081a9c4bc584627532d1ef9` (BUMP-01)
    - `git log -1 --format='%H %s'` shows a single new commit with subject starting `chore(cucascade): bump`
    - `git status` in the superproject reports a clean working tree (no stray files from the submodule checkout)
    - The commit body mentions PR #96, PR #100, PR #103, PR #104 so the audit trail is self-describing
    - Previous HEAD (for rollback) was captured in task-execution logs; record it in the structured return
  </acceptance_criteria>
  <done>cucascade submodule is pinned to f47de0b on a single fresh commit; previous pin recorded; superproject working tree is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Full clean rebuild against bumped cucascade and prove zero new warnings</name>
  <files>build/release/extension/sirius/sirius.duckdb_extension</files>
  <read_first>
    - .planning/phases/04-cucascade-bump-v1-0-re-integration/04-RESEARCH.md §3 — check predicted bump impact per Sirius translation unit
    - CLAUDE.md "Build System" section — confirms `mcp__project-commands__run_command` is the only build path
    - .ai-helper/commands.yaml — exact MCP commands (build, clean, cmake-release)
  </read_first>
  <action>
    The bump brings in a new cucascade version that is a compiled dependency of Sirius. A clean
    rebuild is required (not incremental) because cucascade is a compile-time dependency and
    CMake submodule timestamp detection is unreliable for this case.

    Step 1: Clean previous build artifacts:
      `mcp__project-commands__run_command("clean")`

    Step 2: Reconfigure CMake:
      `mcp__project-commands__run_command("cmake-release")`
      Expected: exit 0. Watch for any CMake errors relating to `cuCascade::cucascade` target
      resolution or link-lib changes from PR #104 (NVML drop). None are expected per research §3.

    Step 3: Full build (captures clean-slate warnings):
      `mcp__project-commands__run_command("build")`
      Expected: exit 0. Build log must be captured and scanned for new warnings in Sirius
      translation units — specifically:
        - src/sirius_extension.cpp
        - src/sirius_context.cpp
        - src/data/host_parquet_representation_converters.cpp
        - src/data/host_parquet_representation.cpp
      These are the primary cucascade-header consumers (identified in research §3).

    Step 4: Cross-reference warning-count:
      Redirect build output to build.log:
        `mcp__project-commands__run_command("build") > /tmp/build-bump.log 2>&amp;1` (or via the
        MCP command's built-in log capture).
      Count warnings in Sirius TUs: `grep -cE 'warning:' /tmp/build-bump.log` should match the
      pre-bump baseline (0 or the same count as on current `dev` HEAD — run once on the
      bump-parent commit to establish the baseline if unsure).

    Step 5: If any NEW warning appears in a Sirius TU, treat as a blocker:
      - If the warning is in a cucascade header included by Sirius: note in structured return
        as "file upstream bug"; do not suppress locally.
      - If the warning is in Sirius source code: investigate; most likely a deprecation from
        PR #100's memory_space API tightening. Fix in-place only if trivial; otherwise pause
        for user decision via structured return.

    No source-file changes are expected in this task. If any are required to resolve new
    warnings, they are in-scope for BUMP-02 and should be committed with
    `fix(cucascade-bump): <description>` referencing BUMP-02.
  </action>
  <verify>
    <automated>mcp__project-commands__run_command("build") returns exit 0 AND the build log contains 0 new "error:" lines AND 0 new "warning:" lines in src/sirius_*.cpp / src/data/host_parquet_*.cpp TUs vs. the pre-bump baseline</automated>
  </verify>
  <acceptance_criteria>
    - `mcp__project-commands__run_command("clean")` exits 0
    - `mcp__project-commands__run_command("cmake-release")` exits 0 with no `cuCascade::cucascade` target-not-found error and no warning about dropped NVML link
    - `mcp__project-commands__run_command("build")` exits 0
    - Build artifact `build/release/extension/sirius/sirius.duckdb_extension` exists (verified via `ls`)
    - Warning count in Sirius translation units (src/sirius_extension.cpp, src/sirius_context.cpp, src/data/host_parquet_representation_converters.cpp, src/data/host_parquet_representation.cpp) matches pre-bump baseline; if diff is non-zero, each new warning is documented in the structured return (BUMP-02)
  </acceptance_criteria>
  <done>A clean full build against the bumped cucascade succeeds with zero new warnings in Sirius translation units; the loadable extension artifact is on disk.</done>
</task>

<task type="auto">
  <name>Task 3: Run cucascade integration test tags 5x back-to-back and confirm zero new flakes</name>
  <files>build/release/extension/sirius/test/cpp/log/</files>
  <read_first>
    - .planning/phases/04-cucascade-bump-v1-0-re-integration/04-RESEARCH.md §4 (test invocation) + §6 (validation strategy)
    - CLAUDE.md "Testing" section — unit-test binary path and Catch2 tag filtering
  </read_first>
  <action>
    BUMP-03 requires `[downgrade]`, `[reservation]`, `[converter]` tags to pass 5x back-to-back.
    The MCP wrapper exposes a whole-suite runner; tag-filtered runs require direct binary
    invocation. The unit-tests command runs the full suite including the three tagged groups.

    Step 1: Run the full unit suite 5 times via MCP:
      for i in 1 2 3 4 5:
        `mcp__project-commands__run_command("unit-tests")`
      Each run uses `--abort` so the first failure aborts — absence-of-abort is the pass signal.

    Step 2: If the MCP wrapper supports tag-filtering (verify via .ai-helper/commands.yaml; if
    not, the full-suite run already covers the three tagged groups, just slower), additionally
    run the binary directly to get a tag-scoped confidence gate:
      `./build/release/extension/sirius/test/cpp/sirius_unittest "[downgrade],[reservation],[converter]"`
      (5 repetitions; capture exit code each run.)

    Step 3: Collect test results. Each run must:
      - Exit 0
      - Emit `All tests passed` or equivalent Catch2 success banner
      - Not produce new failures vs. the pre-bump baseline (if the pre-bump baseline has known
        skipped/hidden tests — e.g., `[.][multi_gpu_*]` tags — those are NOT expected to be
        present yet since v1.0 code has not been ported; their presence is a Plan 02/03/05
        concern, not BUMP-03)

    Step 4: If any of the 5 runs fails:
      - Capture the failing test name + output from `build/release/extension/sirius/test/cpp/log/`
      - Determine if this is a pre-existing flake (check if the same test fails on a run from
        the bump-parent commit — `git stash && git checkout HEAD~1 && make && unit-tests` or
        similar; then restore)
      - If pre-existing: log in structured return as "pre-existing flake, not caused by bump"
      - If new: this is a BUMP-03 failure; bisect between 942c0bf and f47de0b using the
        cucascade commit list (d4f9050, 2f4d4f0, 0b3f49e, 9833849, 8f51996, 5e3a637, f47de0b)
        and file an upstream cucascade issue with the offending SHA

    Step 5: Record test-run exit codes + summary line in the structured return for
    phase-summary bookkeeping.
  </action>
  <verify>
    <automated>for i in 1 2 3 4 5; do mcp__project-commands__run_command("unit-tests") || exit 1; done; echo "5/5 runs passed"</automated>
  </verify>
  <acceptance_criteria>
    - `mcp__project-commands__run_command("unit-tests")` exits 0 on all 5 consecutive runs
    - Zero test failures newly appear after the bump (compared against pre-bump baseline on HEAD~1)
    - No runs deadlock or timeout (full-suite runtime is ~5 min per research §4 — each run completes well within the MCP command timeout)
    - Test logs under `build/release/extension/sirius/test/cpp/log/` show the `[downgrade]`, `[reservation]`, `[converter]` tagged tests all reporting success (BUMP-03)
    - Structured return includes the exit codes of all 5 runs + the tested-tags confirmation
  </acceptance_criteria>
  <done>5 consecutive full unit-test runs pass post-bump; cucascade integration tests ([downgrade], [reservation], [converter]) are proven stable; any flake is quarantined as pre-existing or filed upstream.</done>
</task>

</tasks>

<verification>
Phase-level exit gate for Plan 01:
1. `git -C cucascade rev-parse HEAD` = `f47de0bb7bcaddd55081a9c4bc584627532d1ef9`
2. `git log --oneline dev..HEAD | wc -l` >= 1 (the bump commit exists on the current branch)
3. `mcp__project-commands__run_command("build")` exits 0 with no new warnings in Sirius TUs (vs. pre-bump baseline)
4. `mcp__project-commands__run_command("unit-tests")` exits 0 on 5 consecutive runs
5. Loadable extension artifact exists: `ls -la build/release/extension/sirius/sirius.duckdb_extension`
</verification>

<success_criteria>
- BUMP-01 satisfied: submodule pin is f47de0b, verifiable via `git -C cucascade rev-parse HEAD`
- BUMP-02 satisfied: clean build succeeds with zero new warnings in Sirius TUs
- BUMP-03 satisfied: 5 consecutive full unit-test runs pass with no new failures; `[downgrade]`, `[reservation]`, `[converter]` tags stable
- Phase is rollback-safe: reverting the single bump commit (`git reset --hard HEAD~1`) returns to a known-green state
- No v1.0 code has been ported yet — any behavior change is strictly attributable to the cucascade bump itself
</success_criteria>

<output>
After completion, create `.planning/phases/04-cucascade-bump-v1-0-re-integration/04-01-SUMMARY.md` documenting:
- Previous cucascade pin SHA (for rollback reference)
- New pin SHA (f47de0b)
- Build command outputs (clean, cmake-release, build exit codes)
- 5 test-run exit codes
- Any new warnings observed in Sirius TUs (even if resolved in-line)
- Any tests flagged as pre-existing flakes (with bisection evidence)
</output>
