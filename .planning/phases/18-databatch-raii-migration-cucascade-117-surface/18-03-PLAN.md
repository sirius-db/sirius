---
phase: 18-databatch-raii-migration-cucascade-117-surface
plan: 03
type: execute
wave: 3
depends_on:
  - 18-02
files_modified:
  - src/op/sirius_physical_table_scan.cpp
  - src/op/sirius_physical_hash_join.cpp
  - src/op/sirius_physical_concat.cpp
  - src/op/sirius_physical_top_n.cpp
  - src/op/sirius_physical_grouped_aggregate_merge.cpp
  - src/op/sirius_physical_ungrouped_aggregate.cpp
  - src/op/sirius_physical_merge_sort.cpp
  - src/op/sirius_physical_nested_loop_join.cpp
autonomous: true
requirements:
  - DB-02
  - DB-03
must_haves:
  truths:
    - "All 8 stateful operator .cpp files use RAII accessors (to_read_only / to_mutable) — no batch->get_data() except through accessor; no batch_state::task_created / in_transit / processing references."
    - "All FSM-pop sites use pop_next_data_batch(partition_idx) (no state arg); all pop_data_batch_by_id calls use 2-arg form."
    - "Phase 17 TODO comments in sirius_physical_table_scan.cpp at lines 86, 129, 147, 164 are resolved (TODO marker removed)."
    - "P1 lock-scope discipline preserved: no accessor held across a function call that takes another accessor on the same batch."
  artifacts:
    - path: src/op/sirius_physical_table_scan.cpp
      provides: "RAII migration of most-edited operator (6 get_data + 1 FSM-pop)"
    - path: src/op/sirius_physical_hash_join.cpp
      provides: "RAII migration with 4 pop_data_batch_by_id 2-arg + 3 FSM pops"
    - path: src/op/sirius_physical_nested_loop_join.cpp
      provides: "RAII migration with 4 pop_data_batch_by_id 2-arg"
  key_links:
    - from: src/op/sirius_physical_*.cpp (8 files)
      to: cucascade::read_only_data_batch + cucascade::mutable_data_batch
      via: "to_read_only() / to_mutable() accessor invocation"
      pattern: "to_read_only\\(\\)|to_mutable\\(\\)"
---

<objective>
DB-02 + DB-03 (operators with FSM-pop or pop_data_batch_by_id 3-arg surface): Migrate the 8 stateful operator .cpp files. After this plan, error count drops to single digits and remaining errors are isolated to read-only operators (plan 18-04) and tests (plan 18-05).

Purpose: These 8 operators were called out in PR #739 as touch-points but use FSM-pop or 3-arg `pop_data_batch_by_id` patterns that PR #739's recipe is incompatible with (P7). Apply Recipe R6 (FSM-pop → `pop_next_data_batch`), R7 (3-arg → 2-arg pop-by-id), and R1 (read-only single-shot for `get_data()` reads).
Output: 8 operator files migrated; build error count drops to single digits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-CONTEXT.md
@.planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-RESEARCH.md
@.planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-02-SUMMARY.md
@cucascade/include/cucascade/data/data_batch.hpp
@cucascade/include/cucascade/data/data_repository.hpp
@src/include/pipeline/batch_lock_utils.hpp
@src/include/op/sirius_physical_operator.hpp
@src/include/data/convertible_data_batch.hpp
@src/op/sirius_physical_table_scan.cpp
@src/op/sirius_physical_hash_join.cpp
@src/op/sirius_physical_concat.cpp
@src/op/sirius_physical_top_n.cpp
@src/op/sirius_physical_grouped_aggregate_merge.cpp
@src/op/sirius_physical_ungrouped_aggregate.cpp
@src/op/sirius_physical_merge_sort.cpp
@src/op/sirius_physical_nested_loop_join.cpp
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate sirius_physical_table_scan.cpp (most-edited; 6 get_data + 1 FSM-pop + Phase 17 TODOs)</name>
  <files>src/op/sirius_physical_table_scan.cpp</files>
  <read_first>
    - .planning/phases/17-sirius-origin-dev-merge-base-layer/17-build-output.log (anchor to ACTUAL post-Phase-17 build error sites in sirius_physical_table_scan.cpp; line numbers below may have shifted after dev-merge auto-merges — use this log to confirm current sites before editing)
    - src/op/sirius_physical_table_scan.cpp (Phase 17 TODOs at lines 86, 129, 147, 164; FSM-pop at line 87; 6 get_data sites; line 167 uses 2-arg make_data_batch which is HYG-02 site — verify all line numbers with grep against 17-build-output.log)
    - src/include/pipeline/batch_lock_utils.hpp (post-18-01 helpers)
    - cucascade/include/cucascade/data/data_repository.hpp (pop_next_data_batch signature)
    - .planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-RESEARCH.md (Recipe R1, R2, R6 + "Sirius use — pop_data_batch FSM removal (R6)" code example + Pitfall 4 (2-arg make_data_batch))
  </read_first>
  <action>
    Migrate all sites in `src/op/sirius_physical_table_scan.cpp`.

    Specific edits (verify line numbers with grep before editing — they may have shifted since RESEARCH.md was authored):

    1. **FSM-pop at line ~87** — Recipe R6:
       ```cpp
       // BEFORE: auto batch = port_ptr->repo->pop_data_batch(::cucascade::batch_state::task_created);
       // AFTER:  auto batch = port_ptr->repo->pop_next_data_batch(/* partition_idx */ 0);
       ```
       Confirm the `while(true) { ... if (!batch) break; ... }` accumulator loop shape is preserved (RESEARCH.md "Sirius use — pop_data_batch FSM removal (R6)" code example shows the canonical shape).

    2. **6 `get_data()` sites** — Recipe R1/R2 depending on access:
       - Inside the accumulator loop (line ~88-95): size-estimate read — Recipe R2:
         ```cpp
         uint64_t batch_bytes = 0;
         {
           auto ro = batch->to_read_only();
           if (ro.get_data()) { batch_bytes = ro.get_data()->get_size_in_bytes(); }
         }  // ro destroyed
         ```
       - Other sites (in `execute(...)` body or elsewhere): if the surrounding code already holds a `mutable_data_batch` from `prepare_for_processing`'s vector, access via `acc.get_data()->cast<...>()`. If not, take a `to_read_only()` accessor scoped to the access block.

    3. **2-arg `make_data_batch` at line ~167** — Pitfall 4 / HYG-02 site #1:
       ```cpp
       // BEFORE: sirius::make_data_batch(std::move(table), *batch->get_memory_space());
       // AFTER:  sirius::make_data_batch(std::move(table), *ro.get_memory_space(), stream);
       ```
       The `stream` argument must be the operator's actual execution stream (passed in from `execute()` or held as a member). DO NOT pass `rmm::cuda_stream_default` or `rmm::cuda_stream_view{}`.

    4. **Phase 17 TODO comments at lines 86, 129, 147, 164** — these are markers placed during Phase 17 indicating "Phase 18 must touch this site". Remove the TODO comment lines after the migration is applied (the migration ITSELF resolves the TODO).

    P1 discipline: Each `to_read_only()` is scoped to its access block. The `mutable_data_batch` from `prepare_for_processing`'s vector (held by the caller, not this file) lives across `execute()`; this file MUST NOT take a second accessor on the same batch while operating from that vector.

    HYG-02: zero new `rmm::cuda_stream_default`; pass actual stream to `make_data_batch`.
  </action>
  <acceptance_criteria>
    - `grep -E "pop_data_batch\(.*task_created|pop_data_batch\(.*in_transit" src/op/sirius_physical_table_scan.cpp` returns zero hits.
    - `grep -c "pop_next_data_batch" src/op/sirius_physical_table_scan.cpp` returns at least 1.
    - `grep -E "TODO.*Phase 17|TODO.*Phase 18" src/op/sirius_physical_table_scan.cpp` returns zero hits.
    - `grep -E "make_data_batch\([^,]+,\s*\*[^,)]+\)" src/op/sirius_physical_table_scan.cpp` returns zero hits (no 2-arg form).
    - `grep -c "rmm::cuda_stream_default" src/op/sirius_physical_table_scan.cpp` returns 0.
    - `grep -E "->get_data\(\)|\.get_data\(\)" src/op/sirius_physical_table_scan.cpp` — every match preceded (within 5 lines) by a `to_read_only()` or accessor reference.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "pop_next_data_batch" src/op/sirius_physical_table_scan.cpp</automated>
  </verify>
  <done>table_scan operator migrated; FSM-pop replaced; 2-arg make_data_batch fixed; TODOs cleared.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate hash_join + nested_loop_join + concat (8 pop_data_batch_by_id 3-arg sites)</name>
  <files>src/op/sirius_physical_hash_join.cpp, src/op/sirius_physical_nested_loop_join.cpp, src/op/sirius_physical_concat.cpp</files>
  <read_first>
    - src/op/sirius_physical_hash_join.cpp (1 get_data + 3 FSM pops + 4 pop_data_batch_by_id 3-arg)
    - src/op/sirius_physical_nested_loop_join.cpp (4 pop_data_batch_by_id 3-arg at lines 320-331)
    - src/op/sirius_physical_concat.cpp (2 get_data + 2 pop_data_batch_by_id 3-arg)
    - src/include/pipeline/batch_lock_utils.hpp (post-18-01)
    - cucascade/include/cucascade/data/data_repository.hpp (pop_data_batch_by_id 2-arg + pop_next_data_batch signatures)
    - .planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-RESEARCH.md (Recipe R6 + R7 + R1)
  </read_first>
  <action>
    Three operator files share the `pop_data_batch_by_id(id, state, partition)` 3-arg pattern (Recipe R7). Migrate all three together for atomic semantics.

    **For all 3 files: 8 total `pop_data_batch_by_id(id, state, partition)` 3-arg sites** — Recipe R7:
    ```cpp
    // BEFORE: input_batch.push_back(default_port->repo->pop_data_batch_by_id(
    //                  left_batch_id, cucascade::batch_state::task_created, partition_idx));
    // AFTER:  input_batch.push_back(default_port->repo->pop_data_batch_by_id(
    //                  left_batch_id, partition_idx));
    ```
    Drop the state argument. The state-transition side-effect is gone; caller takes accessor when ready.

    **`sirius_physical_hash_join.cpp` — 3 FSM-pop sites + 1 get_data + 4 pop-by-id**:
    1. 3 FSM-pop sites — Recipe R6: `pop_data_batch(state)` → `pop_next_data_batch(partition)`.
    2. 1 `get_data()` site — Recipe R1: scoped `to_read_only()` accessor.
    3. 4 pop-by-id sites — Recipe R7 (drop state arg).

    **`sirius_physical_nested_loop_join.cpp` — 4 pop-by-id at lines 320-331**:
    All 4 are Recipe R7 (drop state arg). No `get_data()` calls in this file per RESEARCH.md.

    **`sirius_physical_concat.cpp` — 2 get_data + 2 pop-by-id**:
    1. 2 `get_data()` — Recipe R1: scoped `to_read_only()` accessor.
    2. 2 pop-by-id — Recipe R7 (drop state arg).

    P1 discipline (especially relevant for hash_join with build/probe ports): the `mutable_data_batch` vector returned by `prepare_for_processing` is held by `gpu_pipeline_task::run_pipeline_task_round` (post-18-02). Inside `hash_join::execute`, accessing the vector's accessors via `acc.get_data()->cast<...>()` is safe — they're already locked. DO NOT take a second `to_read_only()` accessor on a batch that's already in the `mutable` vector (would deadlock).

    Sibling-batch reads (e.g., probe port reading from a separate `data_batch` that's NOT in the build port's `mutable_data_batch` vector): use a scoped `to_read_only()` accessor.

    HYG-02: zero new `rmm::cuda_stream_default`.
  </action>
  <acceptance_criteria>
    - `grep -E "pop_data_batch_by_id\([^,]+,[^,]+,[^,)]+\)" src/op/sirius_physical_hash_join.cpp src/op/sirius_physical_nested_loop_join.cpp src/op/sirius_physical_concat.cpp` returns zero hits (3-arg form gone).
    - `grep -E "pop_data_batch\(.*task_created|pop_data_batch\(.*in_transit" src/op/sirius_physical_hash_join.cpp src/op/sirius_physical_nested_loop_join.cpp src/op/sirius_physical_concat.cpp` returns zero hits.
    - `grep -c "pop_next_data_batch\|pop_data_batch_by_id" src/op/sirius_physical_hash_join.cpp` returns at least 7 (3 FSM-pop sites replaced + 4 pop-by-id stay but as 2-arg).
    - `grep -E "->get_data\(\)|\.get_data\(\)" src/op/sirius_physical_hash_join.cpp src/op/sirius_physical_concat.cpp` — every match preceded by an accessor.
    - `grep -c "rmm::cuda_stream_default" src/op/sirius_physical_hash_join.cpp src/op/sirius_physical_nested_loop_join.cpp src/op/sirius_physical_concat.cpp` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -cE "pop_data_batch_by_id\([^,]+,[^,]+,[^,)]+\)" src/op/sirius_physical_hash_join.cpp src/op/sirius_physical_nested_loop_join.cpp src/op/sirius_physical_concat.cpp</automated>
  </verify>
  <done>8 pop-by-id 3-arg sites converted to 2-arg; hash_join FSM-pops replaced; get_data sites use accessors.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Migrate top_n + grouped_aggregate_merge + ungrouped_aggregate + merge_sort (4 FSM-pop sites)</name>
  <files>src/op/sirius_physical_top_n.cpp, src/op/sirius_physical_grouped_aggregate_merge.cpp, src/op/sirius_physical_ungrouped_aggregate.cpp, src/op/sirius_physical_merge_sort.cpp</files>
  <read_first>
    - src/op/sirius_physical_top_n.cpp (2 get_data + 1 FSM-pop)
    - src/op/sirius_physical_grouped_aggregate_merge.cpp (1 get_data at line 209 mutable-output write site + 1 FSM-pop)
    - src/op/sirius_physical_ungrouped_aggregate.cpp (2 get_data + 1 FSM-pop)
    - src/op/sirius_physical_merge_sort.cpp (1 FSM-pop at line 66)
    - src/include/pipeline/batch_lock_utils.hpp
    - cucascade/include/cucascade/data/data_repository.hpp
    - .planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-RESEARCH.md (Recipe R1 + R6; "src/op/sirius_physical_grouped_aggregate_merge.cpp ... line 209 is a mutable-output write site")
  </read_first>
  <action>
    Four operator files; each has 1 FSM-pop and varying numbers of `get_data()` reads.

    **For all 4 files: 4 total FSM-pop sites** — Recipe R6:
    ```cpp
    // BEFORE: auto batch = repo->pop_data_batch(::cucascade::batch_state::task_created);
    // AFTER:  auto batch = repo->pop_next_data_batch(/* partition_idx */ 0);
    ```

    **`sirius_physical_top_n.cpp`** — 2 `get_data()` sites + 1 FSM-pop. Both reads → Recipe R1 (scoped `to_read_only()`).

    **`sirius_physical_grouped_aggregate_merge.cpp`** — 1 `get_data()` at line ~209 (write site per RESEARCH.md: "merged->get_data()->cast<>... is a mutable-output write site (output of merge)"). The `merged` batch is freshly produced by the merge; verify it lives outside the `mutable_data_batch` vector. Apply Recipe R3 if it's a write:
    ```cpp
    auto mut = merged->to_mutable();
    auto* output = mut.get_data()->cast<cucascade::gpu_table_representation>();
    // ... write into output ...
    ```
    Plus 1 FSM-pop → Recipe R6.

    **`sirius_physical_ungrouped_aggregate.cpp`** — 2 `get_data()` reads → Recipe R1. 1 FSM-pop → Recipe R6.

    **`sirius_physical_merge_sort.cpp`** — 1 FSM-pop at line ~66 → Recipe R6. No `get_data()` calls per RESEARCH.md.

    P1 discipline: scoped accessors only. `grouped_aggregate_merge`'s `mut` accessor on the freshly-created batch is held during the write block then released — DO NOT span any function call that might re-lock.

    HYG-02: zero new `rmm::cuda_stream_default`.
  </action>
  <acceptance_criteria>
    - `grep -E "pop_data_batch\(.*task_created|pop_data_batch\(.*in_transit" src/op/sirius_physical_top_n.cpp src/op/sirius_physical_grouped_aggregate_merge.cpp src/op/sirius_physical_ungrouped_aggregate.cpp src/op/sirius_physical_merge_sort.cpp` returns zero hits.
    - `grep -c "pop_next_data_batch" src/op/sirius_physical_top_n.cpp src/op/sirius_physical_grouped_aggregate_merge.cpp src/op/sirius_physical_ungrouped_aggregate.cpp src/op/sirius_physical_merge_sort.cpp` returns at least 4 (one per file).
    - `grep -E "->get_data\(\)|\.get_data\(\)" src/op/sirius_physical_top_n.cpp src/op/sirius_physical_grouped_aggregate_merge.cpp src/op/sirius_physical_ungrouped_aggregate.cpp` — every match preceded (within 5 lines) by `to_read_only()` or `to_mutable()`.
    - `grep -c "rmm::cuda_stream_default" src/op/sirius_physical_top_n.cpp src/op/sirius_physical_grouped_aggregate_merge.cpp src/op/sirius_physical_ungrouped_aggregate.cpp src/op/sirius_physical_merge_sort.cpp` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "pop_next_data_batch" src/op/sirius_physical_top_n.cpp src/op/sirius_physical_grouped_aggregate_merge.cpp src/op/sirius_physical_ungrouped_aggregate.cpp src/op/sirius_physical_merge_sort.cpp</automated>
  </verify>
  <done>4 FSM-pop sites → pop_next_data_batch; 5 read sites use to_read_only; 1 write site (merge) uses to_mutable.</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Combined deleted-FSM-symbol grep on the 8 files:
   `grep -E "pop_data_batch\(.*task_created|pop_data_batch\(.*in_transit|data_batch_processing_handle|try_to_lock_for_in_transit|wait_to_lock_for_processing" src/op/sirius_physical_table_scan.cpp src/op/sirius_physical_hash_join.cpp src/op/sirius_physical_concat.cpp src/op/sirius_physical_top_n.cpp src/op/sirius_physical_grouped_aggregate_merge.cpp src/op/sirius_physical_ungrouped_aggregate.cpp src/op/sirius_physical_merge_sort.cpp src/op/sirius_physical_nested_loop_join.cpp` → zero hits.

2. 3-arg pop-by-id check:
   `grep -cE "pop_data_batch_by_id\([^,]+,[^,]+,[^,)]+\)" src/op/sirius_physical_*.cpp` → 0.

3. HYG-02 on 8 files:
   `grep -c "rmm::cuda_stream_default" src/op/sirius_physical_table_scan.cpp src/op/sirius_physical_hash_join.cpp src/op/sirius_physical_concat.cpp src/op/sirius_physical_top_n.cpp src/op/sirius_physical_grouped_aggregate_merge.cpp src/op/sirius_physical_ungrouped_aggregate.cpp src/op/sirius_physical_merge_sort.cpp src/op/sirius_physical_nested_loop_join.cpp` → 0.

4. MCP build (likely still failing — but error count is small, single digits):
   `mcp__project-commands__run_command build`. Expected: errors only in read-only operators (plan 18-04 territory) and tests (plan 18-05).
</verification>

<success_criteria>
- 8 stateful operator files migrated.
- All FSM-pop sites use pop_next_data_batch.
- All pop_data_batch_by_id calls use 2-arg form.
- HYG-02: 0 `rmm::cuda_stream_default` introduced.
- Build error count drops to single digits in src/ (test errors persist; that's plan 18-05).
</success_criteria>

<output>
After completion, create `.planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-03-SUMMARY.md` capturing:
- Build error count delta from 18-02.
- Any P1 lock-scope concerns (especially in hash_join's build/probe path).
- Any deviation from Recipe R1/R3/R6/R7.
- Confirmation that grouped_aggregate_merge's line 209 was correctly classified as Recipe R3 (write).
</output>
