---
phase: 18-databatch-raii-migration-cucascade-117-surface
plan: 05
type: execute
wave: 4
depends_on:
  - 18-03
  - 18-04
files_modified:
  - test/cpp/data/test_convertible_data_batch.cpp
  - test/cpp/data/test_convertible_gpu_pipeline_task.cpp
  - test/cpp/operator/test_physical_order.cpp
  - test/cpp/operator/test_physical_limit.cpp
  - test/cpp/operator/test_physical_mark_join.cpp
  - test/cpp/operator/test_physical_partition.cpp
  - test/cpp/operator/test_physical_projection.cpp
  - test/cpp/operator/test_physical_table_scan.cpp
  - test/cpp/operator/test_physical_top_n.cpp
  - test/cpp/operator/test_physical_concat.cpp
  - test/cpp/operator/test_physical_merge_sort.cpp
  - test/cpp/operator/test_physical_filter.cpp
  - test/cpp/operator/test_physical_ungrouped_aggregate.cpp
  - test/cpp/operator/test_physical_grouped_aggregate.cpp
  - test/cpp/operator/test_physical_result_collector.cpp
  - test/cpp/operator/test_host_table_chunk_reader.cpp
  - test/cpp/operator/operator_test_utils.hpp
  - test/cpp/operator/test_gpu_partition_impl.cpp
  - test/cpp/operator/aggregate/test_gpu_merge_impl.cpp
  - test/cpp/scan/test_utils.hpp
  - test/cpp/expression_executor/test_gpu_expression_executor.cpp
  - test/cpp/config/test_context.cpp
  - test/cpp/downgrade/test_downgrade_executor.cpp
  - test/cpp/memory/test_host_table_utils.cpp
  - test/cpp/utils/test_validation_utility.hpp
  - test/cpp/pipeline/test_gpu_pipeline_disk_readback.cpp
  - test/cpp/pipeline/test_gpu_pipeline_task_history.cpp
autonomous: true
requirements:
  - DB-03
  - DB-04
must_haves:
  truths:
    - "All 23 test/cpp/ files migrated to RAII accessors; no test fixture uses try_to_lock_for_*, wait_to_lock_for_*, or 3-arg pop_data_batch_by_id."
    - "Bulk read-only verification pattern (`auto ro = batch->to_read_only(); ro.get_data()->cast<...>()`) applied to all 96 test get_data() sites."
    - "All 13 try_to_*_in_transit pairs replaced with scoped to_mutable() accessors per Recipe R8."
    - "All FSM-pop in tests (6 sites) replaced with pop_next_data_batch."
    - "test/cpp/operator/test_gpu_partition_impl.cpp: data_batch_processing_handle decl replaced with mutable_data_batch."
    - "Build error count reaches 0 — full mcp__project-commands__run_command build exits 0."
  artifacts:
    - path: test/cpp/operator/operator_test_utils.hpp
      provides: "Shared test helper using accessor — propagates fix to all operator tests"
    - path: test/cpp/data/test_convertible_data_batch.cpp
      provides: "Wrapper test fixture using to_mutable / to_read_only"
    - path: test/cpp/downgrade/test_downgrade_executor.cpp
      provides: "Largest test file (11 get_data + 8 try_to_*_in_transit) migrated"
  key_links:
    - from: test/cpp/operator/test_physical_*.cpp (12 files)
      to: cucascade::read_only_data_batch
      via: "Bulk-replace pattern of `auto ro = batch->to_read_only();`"
      pattern: "auto\\s+ro\\s*=\\s*batch->to_read_only"
---

<objective>
DB-03 closure (~16 tests) + DB-04 build-clean: Migrate all 23 test/cpp/ files to RAII accessors. After this plan, the entire repository compiles cleanly (build exits 0). Plan 18-06 then runs the regression gauntlet.

Purpose: The test surface is the largest mechanical migration (96 get_data + 13 try_to_*_in_transit + 5 try_to_lock_for_processing + 6 FSM-pop + 1 data_batch_processing_handle). Bulk patterns from Recipe R1, R6, R7, R8 cover all sites. The shared helper `operator_test_utils.hpp` is migrated first to propagate the fix to every operator-test consumer.
Output: 23 test files migrated; full build exits 0; ready for DB-05 regression gauntlet (plan 18-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-CONTEXT.md
@.planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-RESEARCH.md
@.planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-04-SUMMARY.md
@cucascade/include/cucascade/data/data_batch.hpp
@src/include/pipeline/batch_lock_utils.hpp
@src/include/data/data_batch_utils.hpp
@test/cpp/operator/operator_test_utils.hpp
@test/cpp/data/test_convertible_data_batch.cpp
@test/cpp/data/test_convertible_gpu_pipeline_task.cpp
@test/cpp/operator/test_physical_filter.cpp
@test/cpp/downgrade/test_downgrade_executor.cpp
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate the shared test helper (operator_test_utils.hpp) + 12 test_physical_*.cpp operator tests + result_collector + host_table_chunk_reader</name>
  <files>test/cpp/operator/operator_test_utils.hpp, test/cpp/operator/test_physical_order.cpp, test/cpp/operator/test_physical_limit.cpp, test/cpp/operator/test_physical_mark_join.cpp, test/cpp/operator/test_physical_partition.cpp, test/cpp/operator/test_physical_projection.cpp, test/cpp/operator/test_physical_table_scan.cpp, test/cpp/operator/test_physical_top_n.cpp, test/cpp/operator/test_physical_concat.cpp, test/cpp/operator/test_physical_merge_sort.cpp, test/cpp/operator/test_physical_filter.cpp, test/cpp/operator/test_physical_ungrouped_aggregate.cpp, test/cpp/operator/test_physical_grouped_aggregate.cpp, test/cpp/operator/test_physical_result_collector.cpp, test/cpp/operator/test_host_table_chunk_reader.cpp</files>
  <read_first>
    - test/cpp/operator/operator_test_utils.hpp (1 get_data — propagates to all operator tests)
    - test/cpp/operator/test_physical_filter.cpp (representative sample of bulk pattern; ~5-10 get_data)
    - test/cpp/operator/test_physical_order.cpp
    - test/cpp/operator/test_physical_limit.cpp
    - test/cpp/operator/test_physical_mark_join.cpp
    - test/cpp/operator/test_physical_partition.cpp
    - test/cpp/operator/test_physical_projection.cpp
    - test/cpp/operator/test_physical_table_scan.cpp
    - test/cpp/operator/test_physical_top_n.cpp
    - test/cpp/operator/test_physical_concat.cpp
    - test/cpp/operator/test_physical_merge_sort.cpp
    - test/cpp/operator/test_physical_ungrouped_aggregate.cpp
    - test/cpp/operator/test_physical_grouped_aggregate.cpp
    - test/cpp/operator/test_physical_result_collector.cpp (1 get_data + 2 try_to_lock_for_processing)
    - test/cpp/operator/test_host_table_chunk_reader.cpp (2 get_data + 2 try_to_lock_for_processing)
    - cucascade/include/cucascade/data/data_batch.hpp
    - .planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-RESEARCH.md (Recipe R1 + Recipe R8 + "Bulk replace pattern" reference for all test_physical_*.cpp)
  </read_first>
  <action>
    15 test files; bulk Recipe R1 pattern.

    **Step 1: Migrate `test/cpp/operator/operator_test_utils.hpp` FIRST** — it's the shared helper used by every operator test. The fix propagates.

    Single `get_data()` site: wrap with `to_read_only()` accessor. If the helper is a function returning a `cudf::table_view`, change its signature to take `const cucascade::read_only_data_batch&` (matching the `data_batch_utils.hpp::get_cudf_table_view` pattern from plan 18-01). Otherwise, take a scoped `to_read_only()` inside the helper body.

    **Step 2: Bulk-migrate the 12 `test_physical_*.cpp` operator tests + result_collector + host_table_chunk_reader.**

    Bulk pattern per RESEARCH.md Recipe R1:
    ```cpp
    // BEFORE (verification block):
    REQUIRE(batch->get_data()->cast<cucascade::gpu_table_representation>().get_table_view() == expected_view);

    // AFTER:
    auto ro = batch->to_read_only();
    REQUIRE(ro.get_data()->cast<cucascade::gpu_table_representation>().get_table_view() == expected_view);
    ```

    Each test typically has 5-10 such verification sites; some have 0-2 mutation sites. Discriminate:
    - **Read-only verification (Recipe R1):** majority. Wrap each `batch->get_data()->cast<...>().get_table_view()` access in a scoped `auto ro = batch->to_read_only();` block.
    - **`try_to_lock_for_processing(...)` sites in `test_physical_result_collector.cpp` (2) + `test_host_table_chunk_reader.cpp` (2):** Recipe R8 — replace with `to_mutable()` directly:
      ```cpp
      // BEFORE: REQUIRE(batch->try_to_lock_for_processing(space->get_id()));
      //         // ... mutate ...
      // AFTER:  auto mut = batch->to_mutable();
      //         // ... mutate via mut ...
      // mut destroyed at end of scope
      ```
      Note: `try_to_lock_for_processing` returned `lock_for_processing_result` with a status enum. The test was likely checking for a specific failure status — verify whether the new test should `REQUIRE(...has_value())` from `try_to_mutable()` (Recipe R8 alternative).

    P1 discipline: each accessor scoped to the `REQUIRE(...)` block or the verification sub-section. Never held across a function call that takes a different accessor on the same batch.

    HYG-02: every test fixture using `convert_to<T>` or `make_data_batch` passes a real stream, NOT `rmm::cuda_stream_default`. Acceptable: `rmm::cuda_stream_view{stream.value()}` from a local `rmm::cuda_stream`.
  </action>
  <acceptance_criteria>
    - `grep -E "->get_data\(\)|\.get_data\(\)" test/cpp/operator/test_physical_*.cpp test/cpp/operator/test_host_table_chunk_reader.cpp test/cpp/operator/operator_test_utils.hpp` — every match preceded (within 5 lines) by an accessor reference.
    - `grep -E "try_to_lock_for_processing|wait_to_lock_for_processing" test/cpp/operator/test_physical_*.cpp test/cpp/operator/test_host_table_chunk_reader.cpp test/cpp/operator/operator_test_utils.hpp` returns zero hits.
    - `grep -c "to_read_only\|to_mutable" test/cpp/operator/test_physical_filter.cpp` returns at least 1 (sample sanity check).
    - `grep -c "rmm::cuda_stream_default" test/cpp/operator/test_physical_*.cpp test/cpp/operator/test_host_table_chunk_reader.cpp test/cpp/operator/operator_test_utils.hpp test/cpp/operator/test_physical_result_collector.cpp` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -cE "try_to_lock_for_processing|wait_to_lock_for_processing" test/cpp/operator/test_physical_filter.cpp test/cpp/operator/test_physical_result_collector.cpp test/cpp/operator/test_host_table_chunk_reader.cpp test/cpp/operator/operator_test_utils.hpp</automated>
  </verify>
  <done>15 operator-test files migrated; helper propagates fix; deleted-FSM symbols absent.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate convertible_* tests + downgrade_executor + context + pipeline tests + handle decl in test_gpu_partition_impl + try_to_lock_for_processing in test_gpu_merge_impl</name>
  <files>test/cpp/data/test_convertible_data_batch.cpp, test/cpp/data/test_convertible_gpu_pipeline_task.cpp, test/cpp/downgrade/test_downgrade_executor.cpp, test/cpp/config/test_context.cpp, test/cpp/pipeline/test_gpu_pipeline_disk_readback.cpp, test/cpp/pipeline/test_gpu_pipeline_task_history.cpp, test/cpp/operator/test_gpu_partition_impl.cpp, test/cpp/operator/aggregate/test_gpu_merge_impl.cpp</files>
  <read_first>
    - test/cpp/data/test_convertible_data_batch.cpp (5 get_data + 4 task_created/in_transit checks + 2 try_to_lock_for_in_transit + 1 try_to_release_in_transit)
    - test/cpp/data/test_convertible_gpu_pipeline_task.cpp (2 get_data + 4 set_task_created/task_created checks; uses try_to_create_task() which is gone)
    - test/cpp/downgrade/test_downgrade_executor.cpp (largest; 11 get_data + 8 try_to_*_in_transit)
    - test/cpp/config/test_context.cpp (5 get_data + 4 try_to_*_in_transit)
    - test/cpp/pipeline/test_gpu_pipeline_disk_readback.cpp (2 get_data + 6 try_to_*_in_transit)
    - test/cpp/pipeline/test_gpu_pipeline_task_history.cpp (2 get_data + 2 try_to_*_in_transit)
    - test/cpp/operator/test_gpu_partition_impl.cpp (1 data_batch_processing_handle decl + 1 try_to_lock_for_processing)
    - test/cpp/operator/aggregate/test_gpu_merge_impl.cpp (4 try_to_lock_for_processing)
    - cucascade/include/cucascade/data/data_batch.hpp
    - .planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-RESEARCH.md (Recipe R8 + Pitfall 5 — convertible-data wrapper task_created → idle)
  </read_first>
  <action>
    8 test files involving wrappers, downgrade, context, and pipeline.

    **`test_convertible_data_batch.cpp`** — 5 get_data + 4 task_created/in_transit checks + 2+1 try_to_*_in_transit pairs:
    1. Replace 5 `get_data()` reads with R1 (scoped `to_read_only()`).
    2. Per Pitfall 5: replace `cucascade::batch_state::task_created` checks with `cucascade::batch_state::idle`. Verify via `batch->get_state()` (still public).
    3. Per Recipe R8: replace `try_to_lock_for_in_transit()` + `try_to_release_in_transit()` pairs with scoped `auto mut = batch->try_to_mutable(); REQUIRE(mut.has_value());` blocks.

    **`test_convertible_gpu_pipeline_task.cpp`** — 2 get_data + 4 set_task_created/task_created checks:
    Per Pitfall 5: the fixture used `try_to_create_task()` (gone) to set state. Under the new model `idle` is the default state on construction — the fixture can DROP the state-setting calls entirely. Replace state checks with `cucascade::batch_state::idle`.

    **`test_downgrade_executor.cpp`** — largest; 11 get_data + 8 try_to_*_in_transit:
    Bulk-replace all `try_to_lock_for_in_transit()` / `try_to_release_in_transit()` pairs with scoped `to_mutable()` (or `try_to_mutable()` for non-blocking). All 11 `get_data()` reads use scoped `to_read_only()`.

    **`test_context.cpp`** — 5 get_data + 4 try_to_*_in_transit: same R1 + R8 pattern.

    **`test_gpu_pipeline_disk_readback.cpp`** — 2 get_data + 6 try_to_*_in_transit: same.

    **`test_gpu_pipeline_task_history.cpp`** — 2 get_data + 2 try_to_*_in_transit: same.

    **`test_gpu_partition_impl.cpp`** — 1 `data_batch_processing_handle` decl + 1 `try_to_lock_for_processing`:
    ```cpp
    // BEFORE: cucascade::data_batch_processing_handle handle = batch->try_to_lock_for_processing(...);
    // AFTER:  auto mut_opt = batch->try_to_mutable();
    //         REQUIRE(mut_opt.has_value());
    //         auto& mut = *mut_opt;
    ```

    **`test_gpu_merge_impl.cpp`** — 4 `try_to_lock_for_processing` calls. Bulk-replace with `try_to_mutable()` per Recipe R8.

    P1 discipline: scoped accessors. Test fixtures often have lambda captures — verify each lambda doesn't capture an accessor by reference and outlive the capturing scope.

    HYG-02: zero new `rmm::cuda_stream_default` introduced. Test fixtures that need a stream should use `rmm::cuda_stream_view{stream.value()}` from a local `rmm::cuda_stream`.
  </action>
  <acceptance_criteria>
    - `grep -E "try_to_lock_for_in_transit|try_to_release_in_transit|try_to_lock_for_processing|wait_to_lock_for_processing" test/cpp/data/test_convertible_data_batch.cpp test/cpp/data/test_convertible_gpu_pipeline_task.cpp test/cpp/downgrade/test_downgrade_executor.cpp test/cpp/config/test_context.cpp test/cpp/pipeline/test_gpu_pipeline_disk_readback.cpp test/cpp/pipeline/test_gpu_pipeline_task_history.cpp test/cpp/operator/test_gpu_partition_impl.cpp test/cpp/operator/aggregate/test_gpu_merge_impl.cpp` returns zero hits.
    - `grep -c "data_batch_processing_handle" test/cpp/operator/test_gpu_partition_impl.cpp` returns 0.
    - `grep -E "cucascade::batch_state::task_created|cucascade::batch_state::in_transit|cucascade::batch_state::processing" test/cpp/data/test_convertible_data_batch.cpp test/cpp/data/test_convertible_gpu_pipeline_task.cpp` returns zero hits.
    - `grep -E "->get_data\(\)|\.get_data\(\)" test/cpp/downgrade/test_downgrade_executor.cpp test/cpp/data/test_convertible_data_batch.cpp test/cpp/config/test_context.cpp` — every match preceded (within 5 lines) by an accessor reference.
    - `grep -c "rmm::cuda_stream_default" test/cpp/data/test_convertible_data_batch.cpp test/cpp/data/test_convertible_gpu_pipeline_task.cpp test/cpp/downgrade/test_downgrade_executor.cpp test/cpp/config/test_context.cpp test/cpp/pipeline/test_gpu_pipeline_disk_readback.cpp test/cpp/pipeline/test_gpu_pipeline_task_history.cpp test/cpp/operator/test_gpu_partition_impl.cpp test/cpp/operator/aggregate/test_gpu_merge_impl.cpp` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -cE "try_to_lock_for_in_transit|try_to_release_in_transit|try_to_lock_for_processing" test/cpp/data/test_convertible_data_batch.cpp test/cpp/downgrade/test_downgrade_executor.cpp test/cpp/operator/test_gpu_partition_impl.cpp test/cpp/operator/aggregate/test_gpu_merge_impl.cpp</automated>
  </verify>
  <done>8 tests migrated; data_batch_processing_handle gone; FSM-state literals gone; Recipe R8 applied throughout.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Migrate misc tests (scan/test_utils, expression_executor, host_table_utils, validation_utility) + verify full build-clean</name>
  <files>test/cpp/scan/test_utils.hpp, test/cpp/expression_executor/test_gpu_expression_executor.cpp, test/cpp/memory/test_host_table_utils.cpp, test/cpp/utils/test_validation_utility.hpp</files>
  <read_first>
    - test/cpp/scan/test_utils.hpp (1 FSM-pop at line 157)
    - test/cpp/expression_executor/test_gpu_expression_executor.cpp (4 get_data — read+write fixture)
    - test/cpp/memory/test_host_table_utils.cpp (2 get_data + 1 try_to_lock_for_processing)
    - test/cpp/utils/test_validation_utility.hpp (3 get_data — cross-batch comparison helper)
    - cucascade/include/cucascade/data/data_repository.hpp
    - cucascade/include/cucascade/data/data_batch.hpp
    - .planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-RESEARCH.md (Recipe R1 + R6 + R8)
  </read_first>
  <action>
    4 small test files + a final full-build verification gate.

    **`test/cpp/scan/test_utils.hpp`** — 1 FSM-pop at line 157:
    Recipe R6: `pop_data_batch(state)` → `pop_next_data_batch(partition_idx)`.

    **`test/cpp/expression_executor/test_gpu_expression_executor.cpp`** — 4 `get_data()`:
    Per RESEARCH.md "Read + write fixture; needs careful classification":
    - Read sites: scoped `to_read_only()` (Recipe R1).
    - Write sites (if any): scoped `to_mutable()` (Recipe R3).
    Inspect each of the 4 sites in context to classify correctly.

    **`test/cpp/memory/test_host_table_utils.cpp`** — 2 `get_data()` + 1 `try_to_lock_for_processing`:
    - 2 `get_data()` reads: Recipe R1 (scoped `to_read_only()`).
    - 1 `try_to_lock_for_processing`: Recipe R8 (`auto mut_opt = batch->try_to_mutable();`).

    **`test/cpp/utils/test_validation_utility.hpp`** — 3 `get_data()` (cross-batch comparison helper):
    The helper compares two batches' contents. Recipe R1: scoped `to_read_only()` per batch. If the helper signature already takes `const data_batch&`, change to `const cucascade::read_only_data_batch&` (matching the `get_cudf_table_view` pattern from plan 18-01) — caller takes the accessor and passes it.

    **Final full-repository build-clean verification**:
    After all test edits, run MCP build:
    ```
    mcp__project-commands__run_command build
    ```
    Expected outcome: build exits 0. Capture build log line count and any warnings.

    If build still fails, the SUMMARY documents:
    - File:line of remaining errors.
    - Likely root cause (missed get_data site? include missing? signature mismatch?).
    - Plan 18-06 may need a Wave 4.5 hotfix task; flag in SUMMARY.

    **Final repo-wide DB-02/DB-03 grep gates** (per RESEARCH.md "Verification Gate Sequencing" plan 18-06 closure):
    ```bash
    grep -rn "->get_data()\|\.get_data()" src/ test/ | grep -v /legacy/ | wc -l
    ```
    Every remaining hit must be on an accessor variable (read_only_data_batch / mutable_data_batch). Manually inspect a sample of 10 hits to confirm.

    ```bash
    grep -rnE "data_batch_processing_handle|lock_for_processing_status|lock_for_processing_result|wait_to_lock_for_processing|try_to_lock_for_in_transit|try_to_release_in_transit|try_to_lock_for_processing" src/ test/ | grep -v /legacy/ | wc -l
    ```
    Expected: 0.

    ```bash
    grep -rnE "cucascade::batch_state::task_created|cucascade::batch_state::in_transit|cucascade::batch_state::processing" src/ test/ | grep -v /legacy/ | wc -l
    ```
    Expected: 0.

    ```bash
    grep -rnE "pop_data_batch_by_id\([^,]+,[^,]+,[^,)]+\)" src/ test/ | wc -l
    ```
    Expected: 0 (no 3-arg pop-by-id calls remain).

    ```bash
    grep -rnE "pop_data_batch\([^)]*task_created|pop_data_batch\([^)]*in_transit" src/ test/ | wc -l
    ```
    Expected: 0.

    HYG-02 final: `grep -rn "rmm::cuda_stream_default" src/ | grep -v /legacy/ | wc -l` → 0.
    Full src/ count: `grep -rn "rmm::cuda_stream_default" src/ | wc -l` → 40 (all in legacy/).
  </action>
  <acceptance_criteria>
    - `grep -E "->get_data\(\)|\.get_data\(\)" test/cpp/scan/test_utils.hpp test/cpp/expression_executor/test_gpu_expression_executor.cpp test/cpp/memory/test_host_table_utils.cpp test/cpp/utils/test_validation_utility.hpp` — every match preceded (within 5 lines) by an accessor reference.
    - `grep -E "pop_data_batch\(.*task_created|pop_data_batch\(.*in_transit|try_to_lock_for_processing|wait_to_lock_for_processing" test/cpp/scan/test_utils.hpp test/cpp/expression_executor/test_gpu_expression_executor.cpp test/cpp/memory/test_host_table_utils.cpp test/cpp/utils/test_validation_utility.hpp` returns zero hits.
    - `grep -c "rmm::cuda_stream_default" test/cpp/scan/test_utils.hpp test/cpp/expression_executor/test_gpu_expression_executor.cpp test/cpp/memory/test_host_table_utils.cpp test/cpp/utils/test_validation_utility.hpp` returns 0.
    - Final repo-wide deleted-FSM grep: `grep -rnE "data_batch_processing_handle|lock_for_processing_status|lock_for_processing_result|wait_to_lock_for_processing|try_to_lock_for_in_transit|try_to_release_in_transit|try_to_lock_for_processing" src/ test/ | grep -v /legacy/ | wc -l` returns 0.
    - Final repo-wide FSM-state literal grep: `grep -rnE "cucascade::batch_state::task_created|cucascade::batch_state::in_transit|cucascade::batch_state::processing" src/ test/ | grep -v /legacy/ | wc -l` returns 0.
    - Final 3-arg pop-by-id grep: `grep -rnE "pop_data_batch_by_id\([^,]+,[^,]+,[^,)]+\)" src/ test/ | wc -l` returns 0.
    - Final repo-wide HYG-02: `grep -rn "rmm::cuda_stream_default" src/ | grep -v /legacy/ | wc -l` returns 0.
    - **MCP build exits 0**: `mcp__project-commands__run_command build` succeeds. Capture exit code via `; echo "EXIT=$?"`. Expected: `EXIT=0`.
  </acceptance_criteria>
  <verify>
    <automated>grep -rnE "data_batch_processing_handle|lock_for_processing_status|try_to_lock_for_in_transit|try_to_lock_for_processing" src/ test/ | grep -v /legacy/ | wc -l</automated>
  </verify>
  <done>4 misc tests migrated; final repo-wide grep gates all 0; MCP build exits 0.</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Final repo-wide grep gate suite (all must return 0):
```bash
grep -rnE "data_batch_processing_handle|lock_for_processing_status|lock_for_processing_result|wait_to_lock_for_processing|try_to_lock_for_in_transit|try_to_release_in_transit|try_to_lock_for_processing" src/ test/ | grep -v /legacy/ | wc -l
grep -rnE "cucascade::batch_state::task_created|cucascade::batch_state::in_transit|cucascade::batch_state::processing" src/ test/ | grep -v /legacy/ | wc -l
grep -rnE "pop_data_batch_by_id\([^,]+,[^,]+,[^,)]+\)" src/ test/ | wc -l
grep -rnE "pop_data_batch\([^)]*task_created|pop_data_batch\([^)]*in_transit" src/ test/ | wc -l
```

2. HYG-02 final: `grep -rn "rmm::cuda_stream_default" src/ | grep -v /legacy/ | wc -l` → 0.

3. **MCP BUILD EXITS 0**: `mcp__project-commands__run_command build`. This is the DB-04 success gate.
</verification>

<success_criteria>
- 23 test files migrated.
- All deleted-FSM symbols (handle type, status enum, in-transit/for-processing methods) absent from src/+test/.
- All FSM-state literals (task_created, in_transit, processing) absent.
- All 3-arg pop_data_batch_by_id calls converted.
- HYG-02: 0 rmm::cuda_stream_default outside src/legacy/.
- MCP build exits 0 (DB-04 closure).
</success_criteria>

<output>
After completion, create `.planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-05-SUMMARY.md` capturing:
- Build exit status from final MCP build.
- Final repo-wide grep gate counts (all should be 0).
- Any test files that needed deviation from Recipe R1/R6/R8 (e.g., complex fixture interactions).
- Confirmation `test_convertible_gpu_pipeline_task.cpp` no longer calls `try_to_create_task()` (Pitfall 5 closure).
- Hand-off note for plan 18-06: list of test names that should now pass `[mgpu]` filter.
</output>
