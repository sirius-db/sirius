---
phase: 18-databatch-raii-migration-cucascade-117-surface
plan: 06
type: execute
wave: 5
depends_on:
  - 18-05
files_modified:
  - .planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-VERDICT.md
autonomous: true
requirements:
  - DB-04
  - DB-05
must_haves:
  truths:
    - "MCP build exits 0 (DB-04 final gate)."
    - "Repo-wide DB-02 grep gate: zero hits for `->get_data()` / `.get_data()` outside src/legacy/ that aren't on a read_only_data_batch / mutable_data_batch accessor (verified by inspection)."
    - "Repo-wide deleted-FSM grep gate: 0 hits for data_batch_processing_handle, lock_for_processing_status, lock_for_processing_result, wait_to_lock_for_processing, try_to_lock_for_in_transit, try_to_release_in_transit, try_to_lock_for_processing."
    - "HYG-02: `grep -rn rmm::cuda_stream_default src/ | wc -l` returns ≤ 40 (all in src/legacy/)."
    - "[mgpu] filter passes 16/16 (DB-05 light regression gate)."
    - "[mgpu_stress] 1-iteration smoke (NOT 500) passes — SCHED-RR survival post-migration."
    - "compute-sanitizer racecheck on [mgpu_foundation] returns 0 hazards."
  artifacts:
    - path: .planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-VERDICT.md
      provides: "Phase 18 verdict document with all 5 DB-XX requirement statuses + verification evidence"
      contains: "PASS\\|FAIL"
  key_links: []
---

<objective>
DB-04 + DB-05 closure: Run the full Phase 18 verification gauntlet and write the phase verdict. The build is already clean (plan 18-05 closure); this plan validates regression behavior against [mgpu], [mgpu_stress] 1-iter, and compute-sanitizer racecheck, plus runs all repo-wide grep gates.

Purpose: Phase 18 cannot ship without proving (a) the build is clean, (b) HYG-02 baseline preserved, (c) [mgpu] still passes 16/16, (d) SCHED-RR survives the migration via [mgpu_stress] 1-iter, (e) racecheck on multi_gpu_foundation is clean. Failure of any gate is a Phase 18 verdict FAIL and triggers a gap-closure replan.
Output: 18-VERDICT.md with all 5 DB-XX statuses; phase ready to hand off to Phase 19.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-CONTEXT.md
@.planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-RESEARCH.md
@.planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-01-SUMMARY.md
@.planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-02-SUMMARY.md
@.planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-03-SUMMARY.md
@.planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-04-SUMMARY.md
@.planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-05-SUMMARY.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run final grep-gate suite + MCP build + capture HYG-02 baseline</name>
  <files>.planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-gate-evidence.log</files>
  <read_first>
    - .planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-RESEARCH.md (section "Verification Gate Sequencing" — plan 18-06 step lists the exact greps)
    - .planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-CONTEXT.md
    - .planning/REQUIREMENTS.md (DB-01..05 expected gate values)
  </read_first>
  <action>
    Run the verification gate suite and capture results into a temp file `.planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-gate-evidence.log`.

    Specific commands (run sequentially; capture stdout + stderr to the log):

    1. **DB-04 build gate**:
       ```bash
       mcp__project-commands__run_command build
       ```
       Expected: exit 0. If exit != 0, FAIL the verdict and stop here — gap closure required.

    2. **DB-02 deleted-FSM-symbol grep gate**:
       ```bash
       grep -rnE "data_batch_processing_handle|lock_for_processing_status|lock_for_processing_result|wait_to_lock_for_processing|try_to_lock_for_in_transit|try_to_release_in_transit|try_to_lock_for_processing" src/ test/ | grep -v /legacy/
       ```
       Expected: empty output. Any hit is a P7 violation (PR #739 cherry-pick sneak-back) and a phase FAIL.

    3. **DB-02 FSM-state literal grep gate**:
       ```bash
       grep -rnE "cucascade::batch_state::task_created|cucascade::batch_state::in_transit|cucascade::batch_state::processing" src/ test/ | grep -v /legacy/
       ```
       Expected: empty. Any hit is a Pitfall 5 violation.

    4. **DB-02 3-arg pop-by-id grep gate**:
       ```bash
       grep -rnE "pop_data_batch_by_id\([^,]+,[^,]+,[^,)]+\)" src/ test/
       ```
       Expected: empty.

    5. **DB-02 FSM-pop grep gate**:
       ```bash
       grep -rnE "pop_data_batch\([^)]*task_created|pop_data_batch\([^)]*in_transit" src/ test/
       ```
       Expected: empty.

    6. **DB-04 HYG-02 gate**:
       ```bash
       grep -rn "rmm::cuda_stream_default" src/ | wc -l
       grep -rn "rmm::cuda_stream_default" src/ | grep -v /legacy/ | wc -l
       ```
       Expected: total ≤ 40, non-legacy = 0.

    7. **DB-02 get_data() repo-wide audit**:
       ```bash
       grep -rn "->get_data()\|\.get_data()" src/ test/ | grep -v /legacy/ | wc -l
       ```
       Capture count. Manually inspect a 10-sample of the output to confirm every hit is on a `read_only_data_batch` / `mutable_data_batch` accessor variable (NOT on a `data_batch` `shared_ptr` directly). If any direct-on-data_batch hit found → FAIL.

    8. **DB-02 2-arg make_data_batch gate**:
       ```bash
       grep -rnE "make_data_batch\([^,]+,\s*\*[^,)]+\)" src/
       ```
       Expected: empty (Pitfall 4 closure verified).

    Record each gate's pass/fail in the log with timestamp + command + output snippet (first 5 lines if non-empty, "EMPTY" if no output).
  </action>
  <acceptance_criteria>
    - `.planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-gate-evidence.log` exists.
    - Log contains line `MCP_BUILD_EXIT=0` (or build was already cached as exit 0 from plan 18-05).
    - Log contains line `DELETED_FSM_GREP_HITS=0`.
    - Log contains line `FSM_STATE_LITERAL_HITS=0`.
    - Log contains line `THREE_ARG_POPID_HITS=0`.
    - Log contains line `FSM_POP_HITS=0`.
    - Log contains lines `HYG02_TOTAL=N1` and `HYG02_NON_LEGACY=0` where N1 ≤ 40.
    - Log contains line `TWO_ARG_MAKE_DATA_BATCH=0`.
    - Log contains a sampled-inspection note (10-line sample of `->get_data()` hits) confirming all are on accessor variables.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "DELETED_FSM_GREP_HITS=0" .planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-gate-evidence.log</automated>
  </verify>
  <done>All static gates evidence captured; build exit + grep counts logged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run [mgpu] 16/16 + [mgpu_stress] 1-iter smoke + compute-sanitizer racecheck</name>
  <files>.planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-gate-evidence.log</files>
  <read_first>
    - .planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-RESEARCH.md (sections "Fastest [mgpu] subset for DB-05" + "compute-sanitizer racecheck (Bash + timeout, NOT MCP)")
    - .planning/STATE.md (Phase 14/15 baseline: [mgpu] 16/16 in 120.3s, 79091 assertions; SCHED-RR survival; HYG-02 = 40)
    - CLAUDE.md (testing protocol — MCP for unit/integration; Bash + timeout for compute-sanitizer)
  </read_first>
  <action>
    Run the 3 dynamic-verification gates per RESEARCH.md "Verification Gate Sequencing" plan 18-06. Capture all output to `.planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-gate-evidence.log` (append).

    Per project memory + CLAUDE.md: USE MCP for unit/integration tests; USE Bash + `timeout` for compute-sanitizer (MCP-routed compute-sanitizer hangs on this host).

    1. **DB-05 [mgpu] filter** (full 16-test suite, expected ~120s on 2 × RTX 6000 Ada):
       ```
       mcp__project-commands__run_command unit-tests --filter "[mgpu]"
       ```
       Capture: assertion count, pass/fail count (must be 16/16), wall-clock, exit code (must be 0). Budget per project memory: expected runtime × 2-3 = up to 360s. If runtime exceeds 360s, poll `nvidia-smi` separately; kill on ~3 min GPU-idle.
       Baseline reference (Phase 15 ship-gate): 16/16, 79091 assertions, 120.3s.

    2. **DB-05 [mgpu_stress] 1-iter smoke** (NOT 500-iter — that's REG-05 in Phase 21):
       The existing `[mgpu_stress]` test runs 100 iterations × 5 queries. For Phase 18 we want a 1-repetition smoke. Inspect the test source first:
       ```bash
       grep -rn "TEST_CASE.*\[mgpu_stress\]" test/cpp/
       ```
       If the test takes a `repetitions` parameter via env var or a Catch2 generator, set it to 1. If it's hard-coded at 100, run the full test (its runtime ~86s per Phase 15) — that's still acceptable budget under DB-05.
       ```
       mcp__project-commands__run_command unit-tests --filter "[mgpu_stress]"
       ```
       Capture: assertion count, pass/fail, wall-clock, exit code. Expected: exit 0; SCHED-RR disjointedness REQUIRE fires green.

    3. **DB-05 compute-sanitizer racecheck on [mgpu_foundation]** (Bash + timeout, NOT MCP):
       ```bash
       timeout 600 compute-sanitizer --tool racecheck --check-cache-control yes \
         build/release/extension/sirius/test/cpp/sirius_unittest "[mgpu_foundation]"
       ```
       Expected: 0 hazards. Cap: 10 min wall clock. If racecheck reports any hazard, that's a P1 lock-scope leak (likely from a missed accessor scope in plans 18-01..18-05) and a phase FAIL.

       Run this command via the unsandboxed Bash tool (compute-sanitizer requires real device access).

    Record each gate's pass/fail in the log:
    - `[mgpu]: PASS 16/16 in Ts assertions=N exit=0`
    - `[mgpu_stress]: PASS exit=0 ... `
    - `racecheck: PASS 0 hazards in Ts` (or `FAIL N hazards: …`).
  </action>
  <acceptance_criteria>
    - Log contains a `[mgpu] passed: 16/16` line OR equivalent indicating 16 test cases passed and 0 failed.
    - Log contains `[mgpu] exit=0`.
    - Log contains `[mgpu] assertions >= 79091` (or note if assertion count is different — degradation > 5% is a yellow flag for SUMMARY).
    - Log contains `[mgpu_stress] exit=0`.
    - Log contains `racecheck hazards=0` (or equivalent compute-sanitizer summary line).
    - Log contains the timeout-bash-tool invocation that ran racecheck (proving it ran via Bash + timeout, not MCP).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "racecheck hazards=0" .planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-gate-evidence.log</automated>
  </verify>
  <done>3 dynamic gates run; results captured.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write 18-VERDICT.md + update STATE.md + ROADMAP.md</name>
  <files>.planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-VERDICT.md, .planning/STATE.md, .planning/ROADMAP.md, .planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-06-SUMMARY.md</files>
  <read_first>
    - .planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-gate-evidence.log (post tasks 1+2)
    - .planning/REQUIREMENTS.md (DB-01..05 contract)
    - .planning/ROADMAP.md (Phase 18 entry — Success Criteria 1-5 + Pitfalls)
    - .planning/STATE.md (current Phase 17 complete; Phase 18 in progress)
    - .planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-01-SUMMARY.md through 18-05-SUMMARY.md
  </read_first>
  <action>
    Produce the phase verdict and update tracking.

    **File 1: `.planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-VERDICT.md`** (NEW)

    Structure:
    ```markdown
    ---
    phase: 18-databatch-raii-migration-cucascade-117-surface
    verdict_date: 2026-05-XX
    status: PASS|PARTIAL|FAIL
    ---

    # Phase 18 Verdict

    ## Summary
    [1-2 sentences: overall result, e.g., "Phase 18 PASS — DB-01..05 satisfied. Build clean, [mgpu] 16/16, [mgpu_stress] 1-iter clean, racecheck 0 hazards. Ready for Phase 19."]

    ## Requirement Status

    | ID | Description | Status | Evidence |
    |----|-------------|--------|----------|
    | DB-01 | batch_lock_utils.hpp rewritten | PASS|FAIL | 18-01-SUMMARY.md + grep gate |
    | DB-02 | All call sites migrated | PASS|FAIL | repo-wide grep gates (see Section "Static Gates") |
    | DB-03 | Operators + tests adapted | PASS|FAIL | 18-02..18-05 SUMMARY hand-offs |
    | DB-04 | Compile-clean + HYG-02 ≤ 40 | PASS|FAIL | MCP build exit 0; HYG-02 = N |
    | DB-05 | [mgpu] 16/16 + 1-iter stress + racecheck | PASS|FAIL | task 2 gate-evidence.log |

    ## Phase 18 Success Criteria (from ROADMAP)

    1. `grep -rn "->get_data()|\.get_data()|pop_data_batch.*task_created|data_batch_processing_handle|task_created|in_transit" src/ test/` returns zero hits → STATUS, evidence
    2. MCP build exits 0 → STATUS
    3. `grep -c "rmm::cuda_stream_default" src/` ≤ 40 → STATUS, count
    4. [mgpu] 16/16 → STATUS, assertions, runtime
    5. [mgpu_stress] 1-iter exit 0 → STATUS

    ## Static Gates (from task 1)
    [Paste log excerpts: DELETED_FSM_GREP_HITS, FSM_STATE_LITERAL_HITS, THREE_ARG_POPID_HITS, FSM_POP_HITS, HYG02 counts, TWO_ARG_MAKE_DATA_BATCH]

    ## Dynamic Gates (from task 2)
    [Paste log excerpts: [mgpu] result, [mgpu_stress] result, racecheck result]

    ## Pitfall Compliance Audit
    - **P1 (RAII lock scope):** [Did any plan SUMMARY note a P1 concern? Resolution?]
    - **P3 (pop_next_data_batch):** [TPC-H parquet correctness verified via mgpu? Or deferred to Phase 19/20?]
    - **P7 (PR #739):** [Confirm zero data_batch_processing_handle re-introductions]

    ## Hand-off to Phase 19
    - Cucascade pin still 1c1e648 (defended by Phase 17 D-G6).
    - Build clean against post-#117 RAII.
    - Phase 19 IO Framework adoption can begin: install liburing-dev as first step (per ROADMAP).
    - Open follow-ups: [list any deferred items, e.g., mark_task_created Sirius-method renaming if not done; readonly_to_mutable demotion opportunity from RESEARCH.md Open Question 1; convertible_data_batch readonly path optimization]

    ## Files Modified This Phase
    [Aggregate from 18-01..18-05 SUMMARYs: full list of files touched]

    ## Plan-by-Plan Status
    | Plan | Status | Build error count delta |
    |------|--------|-------------------------|
    | 18-01 | DONE | 63 → ~40-45 |
    | 18-02 | DONE | ~40-45 → ~15-25 |
    | 18-03 | DONE | ~15-25 → single digits |
    | 18-04 | DONE | src/ errors → 0 |
    | 18-05 | DONE | test errors → 0; build exits 0 |
    | 18-06 | THIS | verdict + dynamic gates |
    ```

    **File 2: `.planning/STATE.md` update**

    Edit the `## Phase Overview (v1.4)` table row for Phase 18 to `Complete (X/X plans, PASS|PARTIAL|FAIL)` and update the progress bar + completed_phases counter.

    Append a new bullet to `## Decisions`:
    ```
    - **[Phase 18]**: DB-01..05 verdict {PASS|PARTIAL|FAIL}. {Brief 1-line summary.} Cucascade pin 1c1e648 preserved. Phase 19 unblocked.
    ```

    Update the YAML frontmatter:
    - `stopped_at`: Updated to "Completed Phase 18 verdict"
    - `progress.completed_phases`: increment from 2 → 3 (assuming PASS).
    - `progress.completed_plans`: add 6 (plans 18-01 through 18-06).

    **File 3: `.planning/ROADMAP.md` update**

    1. Edit `## Phases` Phase 18 entry: `[ ]` → `[x]` and append completion note "**COMPLETE** (2026-05-XX): {1-line verdict}".
    2. Edit `## Progress` table row for Phase 18: `0/?` → `6/6`, `Status: Complete`, add completion date.
    3. If verdict is PASS, no further changes needed. If PARTIAL or FAIL, add a "Carry Forward" note describing what remains for Phase 19+.

    HYG-02: zero edits to any code file in this task — pure documentation.
  </action>
  <acceptance_criteria>
    - `.planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-VERDICT.md` exists.
    - File has YAML frontmatter with `status: PASS` (or PARTIAL/FAIL with explicit reason).
    - File has 5-row Requirement Status table with DB-01..05 each marked PASS|FAIL with evidence reference.
    - File has 5-criterion ROADMAP success-criteria block matching ROADMAP Phase 18 entry.
    - `.planning/STATE.md` Phase 18 row updated to `Complete` (or PARTIAL/FAIL).
    - `.planning/STATE.md` `progress.completed_phases` is now 3 (was 2).
    - `.planning/ROADMAP.md` Phase 18 entry has `[x]` checkbox.
    - `.planning/ROADMAP.md` Progress table Phase 18 row reads `6/6`.
    - 18-VERDICT.md references at least one line from `18-gate-evidence.log` (build exit, [mgpu] count, racecheck count).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "status: " .planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-VERDICT.md</automated>
  </verify>
  <done>18-VERDICT.md written with PASS/PARTIAL/FAIL; STATE.md and ROADMAP.md updated; phase ready to hand off.</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. `cat .planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-VERDICT.md` — verify all 5 DB-XX statuses are PASS (or PARTIAL/FAIL is explicitly justified).

2. `grep -A2 "## Phase Overview" .planning/STATE.md | head -10` — verify Phase 18 row says Complete.

3. `grep -A1 "Phase 18:" .planning/ROADMAP.md | head -5` — verify [x] checkbox.

4. Confirm `18-gate-evidence.log` exists and contains all expected `KEY=VALUE` lines from task 1+2.
</verification>

<success_criteria>
- 18-VERDICT.md captures all 5 DB-XX requirement statuses + ROADMAP success criteria evidence.
- STATE.md + ROADMAP.md updated to reflect Phase 18 closure.
- All static + dynamic gates evidence preserved in 18-gate-evidence.log.
- Phase 19 IO Framework adoption is unblocked (cucascade pin defended; build clean; Phase 18 hand-off documented).
</success_criteria>

<output>
After completion, create `.planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-06-SUMMARY.md` capturing:
- Final verdict (PASS|PARTIAL|FAIL).
- Any gates that flipped during the run (e.g., racecheck FAIL on first run, then re-run after a P1 hotfix).
- Hand-off note: 1 sentence to Phase 19 listing what's settled.
</output>
