---
phase: 18-databatch-raii-migration-cucascade-117-surface
plan: 04
type: execute
wave: 3
depends_on:
  - 18-02
files_modified:
  - src/op/sirius_physical_filter.cpp
  - src/op/sirius_physical_projection.cpp
  - src/op/sirius_physical_limit.cpp
  - src/op/sirius_physical_partition.cpp
  - src/op/sirius_physical_result_collector.cpp
  - src/op/sirius_physical_sort_sample.cpp
  - src/op/scan/sirius_gpu_parquet_scan_operator.cpp
  - src/op/scan/parquet_scan_task.cpp
  - src/op/scan/duckdb_scan_task.cpp
  - src/op/scan/cpu_source_task.cpp
  - src/op/scan/duckdb_scan_executor.cpp
  - src/creator/task_creator.cpp
  - src/debug_utils.cpp
autonomous: true
requirements:
  - DB-02
  - DB-03
must_haves:
  truths:
    - "All read-only operators (filter, projection, limit, partition, result_collector, sort_sample) use to_read_only() RAII accessors."
    - "All scan-task .cpp files (parquet, duckdb, cpu_source, scan_executor) use to_read_only() for size/output reads; FSM-pop replaced with pop_next_data_batch where present."
    - "All 4 known 2-arg make_data_batch sites (filter:60, table_scan:167 [done in 18-03], projection:63, gpu_parquet_scan_operator:252) use 3-arg form with explicit operator stream — Pitfall 4 closure."
    - "task_creator.cpp + debug_utils.cpp use accessors; mark_task_created(...) Sirius method left intact (out-of-scope rename)."
    - "src/-side compile reaches 0 errors after this plan; only test errors remain."
  artifacts:
    - path: src/op/sirius_physical_filter.cpp
      provides: "Read-only filter operator using accessor; 2-arg make_data_batch fixed"
    - path: src/op/scan/sirius_gpu_parquet_scan_operator.cpp
      provides: "GPU parquet scan operator with cached path migrated; line 252 make_data_batch 3-arg"
    - path: src/creator/task_creator.cpp
      provides: "task_creator size estimators using accessors"
  key_links:
    - from: src/op/sirius_physical_filter.cpp
      to: sirius::make_data_batch (3-arg with stream)
      via: "Output construction with operator stream"
      pattern: "make_data_batch\\([^,]+,[^,]+,\\s*stream"
---

<objective>
DB-02 + DB-03 (read-only operators + scan tasks + misc) + Pitfall 4 closure (4 sites of 2-arg make_data_batch). After this plan, `src/` reaches build-clean (only test errors remain) and HYG-02 risk closure on the writer_stream sneak-back sites is verified.

Purpose: These files are the long-tail of read-only operators (Recipe R1) plus the scan-task layer (Recipe R1 / R6). Plan 18-04 also closes Pitfall 4 — 4 known sites where `make_data_batch(table, *space)` is missing the `writer_stream` argument, which would re-open the Phase 13 P2 race if not fixed.
Output: 13 files migrated; src/ builds clean (test errors persist for plan 18-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-CONTEXT.md
@.planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-RESEARCH.md
@.planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-02-SUMMARY.md
@cucascade/include/cucascade/data/data_batch.hpp
@src/include/pipeline/batch_lock_utils.hpp
@src/include/data/data_batch_utils.hpp
@src/op/sirius_physical_filter.cpp
@src/op/sirius_physical_projection.cpp
@src/op/sirius_physical_limit.cpp
@src/op/sirius_physical_partition.cpp
@src/op/sirius_physical_result_collector.cpp
@src/op/sirius_physical_sort_sample.cpp
@src/op/scan/sirius_gpu_parquet_scan_operator.cpp
@src/op/scan/parquet_scan_task.cpp
@src/op/scan/duckdb_scan_task.cpp
@src/op/scan/cpu_source_task.cpp
@src/op/scan/duckdb_scan_executor.cpp
@src/creator/task_creator.cpp
@src/debug_utils.cpp
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate read-only operators (filter, projection, limit, partition, sort_sample, result_collector) + close 2-arg make_data_batch sites</name>
  <files>src/op/sirius_physical_filter.cpp, src/op/sirius_physical_projection.cpp, src/op/sirius_physical_limit.cpp, src/op/sirius_physical_partition.cpp, src/op/sirius_physical_result_collector.cpp, src/op/sirius_physical_sort_sample.cpp</files>
  <read_first>
    - .planning/phases/17-sirius-origin-dev-merge-base-layer/17-build-output.log (anchor to ACTUAL post-Phase-17 build error sites; line numbers below may have shifted after dev-merge auto-merges — use this log to confirm current sites before editing)
    - src/op/sirius_physical_filter.cpp (1 get_data, 2-arg make_data_batch at line 60 — verify against 17-build-output.log)
    - src/op/sirius_physical_projection.cpp (1 get_data, 2-arg make_data_batch at line 63 — verify against 17-build-output.log)
    - src/op/sirius_physical_limit.cpp (1 get_data — read-only get_table_view)
    - src/op/sirius_physical_partition.cpp (1 get_data — total_bytes read-only)
    - src/op/sirius_physical_result_collector.cpp (2 get_data at lines 129, 159 — clone path mixed; verify against 17-build-output.log)
    - src/op/sirius_physical_sort_sample.cpp (1 get_data — read-only)
    - src/include/data/data_batch_utils.hpp (post-18-01 — make_data_batch 3-arg signatures at lines 80, 96)
    - cucascade/include/cucascade/data/data_batch.hpp
    - .planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-RESEARCH.md (Recipe R1 + "Sirius use — read-only single-shot (R1)" code example for filter; Pitfall 4 (2-arg make_data_batch sneak-back))
  </read_first>
  <action>
    6 read-only operator files. Apply Recipe R1 (read-only single-shot) per the canonical example in RESEARCH.md.

    **`sirius_physical_filter.cpp`** — 1 `get_data()` + 1 `make_data_batch` 2-arg at line ~60. Apply the canonical example from RESEARCH.md "Sirius use — read-only single-shot (R1)":
    ```cpp
    for (auto const& batch : input_batches) {
      if (!batch) { continue; }
      auto ro = batch->to_read_only();
      auto filtered_table = gpu_expression_executor.select(
        ro.get_data()->cast<cucascade::gpu_table_representation>().get_table_view());
      output_batches.push_back(
        sirius::make_data_batch(std::move(filtered_table), *ro.get_memory_space(), stream));
    }
    ```
    Critical: `make_data_batch` MUST be 3-arg with `stream` (the operator's actual execution stream). Pitfall 4 closure.

    **`sirius_physical_projection.cpp`** — 1 `get_data()` + 1 `make_data_batch` 2-arg at line ~63. Same R1 + Pitfall 4 pattern.

    **`sirius_physical_limit.cpp`** — 1 `get_data()` read-only `get_table_view()`. R1: scoped `to_read_only()`.

    **`sirius_physical_partition.cpp`** — 1 `get_data()`: `total_bytes += batch->get_data()->get_size_in_bytes();`. R2 (size estimator):
    ```cpp
    auto ro = batch->to_read_only();
    if (ro.get_data()) { total_bytes += ro.get_data()->get_size_in_bytes(); }
    ```

    **`sirius_physical_result_collector.cpp`** — 2 `get_data()` at lines 129, 159. Per RESEARCH.md: "Mixed — clone path needs read-only accessor; clone_batch->get_data() is on a freshly cloned batch (no lock contention)." For both:
    - The pre-clone read: scoped `to_read_only()` accessor on the source batch.
    - The post-clone access on the freshly cloned batch: scoped `to_read_only()` accessor on `clone_batch` (it's a new `data_batch` with no lock contention, but `get_data()` is still private without an accessor).

    **`sirius_physical_sort_sample.cpp`** — 1 `get_data()` read-only. R1: scoped `to_read_only()`.

    P1 discipline: every accessor scoped to its access block; never spans an `execute()` call or anything that takes another accessor.

    HYG-02: every `make_data_batch` 3-arg call passes the operator's actual stream. Verify after edits with `grep -E "make_data_batch\([^,]+,[^,]+,\\s*stream" src/op/sirius_physical_filter.cpp src/op/sirius_physical_projection.cpp` returns at least 1 hit each.
  </action>
  <acceptance_criteria>
    - `grep -E "make_data_batch\([^,]+,\s*\*[^,)]+\)" src/op/sirius_physical_filter.cpp src/op/sirius_physical_projection.cpp` returns zero hits (no 2-arg form).
    - `grep -c "to_read_only" src/op/sirius_physical_filter.cpp src/op/sirius_physical_projection.cpp src/op/sirius_physical_limit.cpp src/op/sirius_physical_partition.cpp src/op/sirius_physical_result_collector.cpp src/op/sirius_physical_sort_sample.cpp` returns at least 7 (1 per filter/projection/limit/partition/sort_sample + 2 for result_collector).
    - `grep -E "->get_data\(\)|\.get_data\(\)" src/op/sirius_physical_filter.cpp src/op/sirius_physical_projection.cpp src/op/sirius_physical_limit.cpp src/op/sirius_physical_partition.cpp src/op/sirius_physical_result_collector.cpp src/op/sirius_physical_sort_sample.cpp` — every match preceded (within 5 lines) by an accessor reference.
    - `grep -c "rmm::cuda_stream_default" src/op/sirius_physical_filter.cpp src/op/sirius_physical_projection.cpp src/op/sirius_physical_limit.cpp src/op/sirius_physical_partition.cpp src/op/sirius_physical_result_collector.cpp src/op/sirius_physical_sort_sample.cpp` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -cE "make_data_batch\([^,]+,\s*\*[^,)]+\)" src/op/sirius_physical_filter.cpp src/op/sirius_physical_projection.cpp</automated>
  </verify>
  <done>6 read-only operators migrated; 2 known 2-arg make_data_batch sites fixed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate scan layer (parquet_scan_task, duckdb_scan_task, cpu_source_task, duckdb_scan_executor, sirius_gpu_parquet_scan_operator)</name>
  <files>src/op/scan/sirius_gpu_parquet_scan_operator.cpp, src/op/scan/parquet_scan_task.cpp, src/op/scan/duckdb_scan_task.cpp, src/op/scan/cpu_source_task.cpp, src/op/scan/duckdb_scan_executor.cpp</files>
  <read_first>
    - .planning/phases/17-sirius-origin-dev-merge-base-layer/17-build-output.log (anchor to ACTUAL post-Phase-17 build error sites; line numbers below may have shifted after dev-merge auto-merges — use this log to confirm current sites before editing)
    - src/op/scan/sirius_gpu_parquet_scan_operator.cpp (1 get_data at line 197 cached path; 2-arg make_data_batch at line 252; Phase 17 TODO at line ~190 — verify all line numbers against 17-build-output.log)
    - src/op/scan/duckdb_scan_executor.cpp (1 get_data at line 296 — verify against 17-build-output.log)
    - src/op/scan/duckdb_scan_task.cpp (1 get_data at line 541 — verify against 17-build-output.log)
    - src/op/scan/parquet_scan_task.cpp (1 get_data at line 826 — verify against 17-build-output.log)
    - src/op/scan/cpu_source_task.cpp (1 get_data at line 367 — verify against 17-build-output.log)
    - cucascade/include/cucascade/data/data_batch.hpp
    - src/include/data/data_batch_utils.hpp (make_data_batch 3-arg at lines 80, 96)
    - .planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-RESEARCH.md (Recipe R1 + Pitfall 4 sites)
  </read_first>
  <action>
    5 scan-layer files; mostly Recipe R1 reads + 1 Pitfall 4 site (line 252 in `sirius_gpu_parquet_scan_operator.cpp`).

    **`sirius_gpu_parquet_scan_operator.cpp`**:
    1. Line ~197 (cached path): 1 `get_data()` read — Recipe R1 (`to_read_only()`).
    2. Line ~252: 2-arg `make_data_batch(table, *space)` — Pitfall 4 closure. Add the operator's stream argument:
       ```cpp
       sirius::make_data_batch(std::move(table), *space, stream);
       ```
       Verify the local `stream` is the actual operator/task execution stream (NOT `rmm::cuda_stream_default`, NOT a default-constructed `rmm::cuda_stream_view{}`).
    3. Phase 17 TODO comment at line ~190 — remove after migration.

    **`duckdb_scan_executor.cpp`** — 1 `get_data()` at line ~296. Recipe R1.

    **`duckdb_scan_task.cpp`** — 1 `get_data()` at line ~541. Recipe R1 (likely a size estimate or output read).

    **`parquet_scan_task.cpp`** — 1 `get_data()` at line ~826. Recipe R1.

    **`cpu_source_task.cpp`** — 1 `get_data()` at line ~367. Recipe R1.

    P1 discipline: each `to_read_only()` accessor scoped to its access block.

    HYG-02: Pitfall 4 closure on line 252; no other new `rmm::cuda_stream_default` references.
  </action>
  <acceptance_criteria>
    - `grep -E "make_data_batch\([^,]+,\s*\*[^,)]+\)" src/op/scan/sirius_gpu_parquet_scan_operator.cpp` returns zero hits (no 2-arg form).
    - `grep -E "->get_data\(\)|\.get_data\(\)" src/op/scan/sirius_gpu_parquet_scan_operator.cpp src/op/scan/duckdb_scan_executor.cpp src/op/scan/duckdb_scan_task.cpp src/op/scan/parquet_scan_task.cpp src/op/scan/cpu_source_task.cpp` — every match preceded (within 5 lines) by an accessor reference.
    - `grep -c "to_read_only" src/op/scan/sirius_gpu_parquet_scan_operator.cpp src/op/scan/duckdb_scan_executor.cpp src/op/scan/duckdb_scan_task.cpp src/op/scan/parquet_scan_task.cpp src/op/scan/cpu_source_task.cpp` returns at least 5.
    - `grep -E "TODO.*Phase 17|TODO.*Phase 18" src/op/scan/sirius_gpu_parquet_scan_operator.cpp` returns zero hits.
    - `grep -c "rmm::cuda_stream_default" src/op/scan/sirius_gpu_parquet_scan_operator.cpp src/op/scan/duckdb_scan_executor.cpp src/op/scan/duckdb_scan_task.cpp src/op/scan/parquet_scan_task.cpp src/op/scan/cpu_source_task.cpp` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -cE "make_data_batch\([^,]+,\s*\*[^,)]+\)" src/op/scan/sirius_gpu_parquet_scan_operator.cpp</automated>
  </verify>
  <done>Scan layer migrated; line 252 make_data_batch fixed (Pitfall 4); TODOs cleared.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Migrate task_creator + debug_utils + verify src/ build-clean</name>
  <files>src/creator/task_creator.cpp, src/debug_utils.cpp</files>
  <read_first>
    - src/creator/task_creator.cpp (2 get_data at lines 461-462 — affinity/sizing; 4 pipeline->mark_task_created calls — Sirius method, NOT cucascade FSM, leave intact)
    - src/debug_utils.cpp (1 get_data at line 75)
    - src/include/pipeline/sirius_pipeline.hpp (mark_task_created decl at line 184 — out of scope)
    - src/pipeline/sirius_pipeline.cpp (mark_task_created at line 358 — out of scope)
    - .planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-RESEARCH.md (Recipe R1/R2 + "Sirius method, not cucascade FSM" classification)
  </read_first>
  <action>
    Two small files, then a final build-clean verification.

    **`src/creator/task_creator.cpp`**:
    1. Lines 461-462: 2 `get_data()` calls (affinity/sizing, read paths). Recipe R2:
       ```cpp
       auto ro = batch->to_read_only();
       size += ro.get_data()->get_size_in_bytes();
       ```
       Or, if the two calls are sequential reads on the same batch, share one accessor.
    2. The 4 `pipeline->mark_task_created()` calls are SIRIUS METHOD CALLS, not cucascade FSM transitions. Per RESEARCH.md "Sirius method, not cucascade FSM. Compile-clean; no migration needed." LEAVE THESE INTACT.

    **`src/debug_utils.cpp`** — 1 `get_data()` at line 75. Recipe R1: `auto ro = batch->to_read_only();` then access `ro.get_data()`.

    **Final src/-side build verification**:
    After all edits, run MCP build:
    ```
    mcp__project-commands__run_command build
    ```
    Capture output. Expected outcome:
    - 0 errors in `src/` (any remaining errors should be in `test/cpp/`).
    - If errors persist in `src/`, report them in the SUMMARY (likely a missed get_data site or include).

    If build is fully clean: that's a bonus signal but not required (test files in plan 18-05 will likely keep the build broken).

    HYG-02 final src/ check:
    ```bash
    grep -rn "rmm::cuda_stream_default" src/ | grep -v /legacy/ | wc -l
    ```
    Expected: 0.

    Combined src/-side deleted-FSM grep:
    ```bash
    grep -rn "->get_data()\|\.get_data()" src/ | grep -v /legacy/
    ```
    Every remaining hit MUST be on a `read_only_data_batch` or `mutable_data_batch` accessor reference (NOT on a `data_batch` directly). Expected: every site is accessor-mediated.
  </action>
  <acceptance_criteria>
    - `grep -E "->get_data\(\)|\.get_data\(\)" src/creator/task_creator.cpp src/debug_utils.cpp` — every match preceded (within 5 lines) by an accessor reference.
    - `grep -c "mark_task_created" src/creator/task_creator.cpp` is unchanged from baseline (Sirius method preserved).
    - `grep -c "rmm::cuda_stream_default" src/creator/task_creator.cpp src/debug_utils.cpp` returns 0.
    - Final src/-side HYG-02 gate: `grep -rn "rmm::cuda_stream_default" src/ | grep -v /legacy/ | wc -l` returns 0.
    - Final src/-side deleted-FSM gate: `grep -rnE "data_batch_processing_handle|lock_for_processing_status|lock_for_processing_result|wait_to_lock_for_processing|try_to_lock_for_in_transit|try_to_release_in_transit|try_to_lock_for_processing" src/ | grep -v /legacy/ | wc -l` returns 0.
    - Final src/-side FSM-state literal gate: `grep -rnE "cucascade::batch_state::task_created|cucascade::batch_state::in_transit|cucascade::batch_state::processing" src/ | grep -v /legacy/ | wc -l` returns 0.
    - MCP build completes; src/-side errors = 0 (test/-side errors expected to persist for plan 18-05).
  </acceptance_criteria>
  <verify>
    <automated>grep -rnE "data_batch_processing_handle|lock_for_processing_status" src/ | grep -v /legacy/ | wc -l</automated>
  </verify>
  <done>task_creator + debug_utils migrated; mark_task_created Sirius method preserved; src/-side build-clean gates pass.</done>
</task>

</tasks>

<verification>
After all tasks complete (note: all greps scoped to `src/` only — `test/` errors are expected to persist until plan 18-05 per this plan's success_criteria):

1. Combined src/ HYG-02:
   `grep -rn "rmm::cuda_stream_default" src/ | grep -v /legacy/ | wc -l` → 0.

2. Combined src/ deleted-FSM grep (src/ only — test/ excluded; test migration is plan 18-05's responsibility):
   `grep -rnE "data_batch_processing_handle|lock_for_processing_status|lock_for_processing_result|wait_to_lock_for_processing|try_to_lock_for_in_transit|try_to_release_in_transit|try_to_lock_for_processing|cucascade::batch_state::task_created|cucascade::batch_state::in_transit|cucascade::batch_state::processing" src/ | grep -v /legacy/ | wc -l` → 0.

3. Combined src/ get_data() gate (every hit must be on accessor):
   `grep -rn "->get_data()" src/ | grep -v /legacy/` — manually inspect; every line must be on a `read_only_data_batch` / `mutable_data_batch` variable, NOT a `data_batch` `shared_ptr`.

4. 2-arg make_data_batch closure (src/ only):
   `grep -rnE "make_data_batch\([^,]+,\s*\*[^,)]+\)" src/` → 0 hits.

5. MCP build:
   `mcp__project-commands__run_command build`. Expected: build still fails BUT all errors are in test/cpp/ files (plan 18-05 territory). src/ errors = 0.
</verification>

<success_criteria>
- 13 files migrated (6 read-only operators + 5 scan-layer + task_creator + debug_utils).
- 4 known 2-arg make_data_batch sites all closed (Pitfall 4 closure).
- src/ HYG-02 = 0; src/ deleted-FSM-symbol grep = 0; src/ FSM-state literal grep = 0.
- MCP build: 0 src/ errors. Test errors persist (expected — plan 18-05).
</success_criteria>

<output>
After completion, create `.planning/phases/18-databatch-raii-migration-cucascade-117-surface/18-04-SUMMARY.md` capturing:
- Confirmation src/ build-clean (0 errors in src/).
- Final src/ HYG-02 count.
- Test error count remaining (sets expectations for plan 18-05).
- Any additional sneak-back sites discovered beyond the 4 RESEARCH.md-known Pitfall 4 sites.
</output>
