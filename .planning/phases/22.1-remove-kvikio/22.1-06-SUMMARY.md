---
phase: 22.1-remove-kvikio
plan: 06
subsystem: scan_manager
tags: [scan_manager, parquet_split_provider, kvikio-removal, test-fixture-migration, phase-22.1, site-7]
requires:
  - "Phase 19 IO-13: per-GPU sirius_ioctx in SiriusContext::initialize() (gpu_ioctxs_ map populated under rmm::cuda_set_device_raii)"
  - "Phase 19 IO-15: make_test_gpu_ioctxs() helper convention (originally local to test_parquet_scan_task.cpp)"
  - "Phase 20.6 IO-MGPU-02: production parquet_split_provider routes through ioctx->make_datasource (already in place pre-22.1)"
  - "Phase 22.1-01: SiriusContext::datasource_registry_ populated"
  - "Phase 22.1-02: datasource_factory throw-policy flip ('kvikio path is forbidden' verbatim text precedent)"
provides:
  - "Strict gpu_ioctxs requirement at parquet_split_provider::run_batch — empty map throws std::runtime_error('parquet_split_provider: gpu_ioctxs is empty — kvikio path is forbidden ...')"
  - "Site #7 (parquet_split_provider.cpp:295) eliminated — the unit-test crutch for kvikio bypass is gone"
  - "Shared test helper test/cpp/scan/test_helpers_ioctx.hpp (sirius::scan_test_utils::make_test_gpu_ioctxs) — multiple TUs can include without ODR violations"
  - "Test fixtures (test_parquet_split_provider.cpp 4 TEST_CASEs / 5 constructor sites) now exercise the production sirius_datasource path — production semantics in both test and production"
affects:
  - "src/scan_manager/parquet_split_provider.cpp"
  - "test/cpp/scan/test_parquet_split_provider.cpp"
  - "test/cpp/scan/test_parquet_scan_task.cpp"
  - "test/cpp/scan/test_helpers_ioctx.hpp (NEW)"
tech-stack:
  added:
    - "test/cpp/scan/test_helpers_ioctx.hpp (new shared header lifting make_test_gpu_ioctxs)"
  patterns:
    - "Strict precondition throw — gpu_ioctxs empty at run_batch entry rejects with verbatim D-09 'kvikio path is forbidden' text (grep-stable for sanitizer-gate harnesses)"
    - "Inline shared test helper via #pragma once + sirius::scan_test_utils namespace — multiple TUs include without ODR violations"
    - "Comment-line scrub: rephrase any textual mention of 'cudf::io::datasource::create' to keep the strict bypass-grep at 0 hits even on textual matches (mirror Plan 22.1-02 same scrub)"
key-files:
  created:
    - "test/cpp/scan/test_helpers_ioctx.hpp (+92 LOC: shared make_test_gpu_ioctxs helper in sirius::scan_test_utils namespace)"
  modified:
    - "src/scan_manager/parquet_split_provider.cpp (+25 / -19 LOC: throw replaces else-branch fallback at run_batch; comment-block updated)"
    - "test/cpp/scan/test_parquet_split_provider.cpp (+15 / -4 LOC: 5 constructor call-sites now pass make_test_gpu_ioctxs(); +1 #include)"
    - "test/cpp/scan/test_parquet_scan_task.cpp (+5 / -36 LOC: local make_test_gpu_ioctxs definition replaced with `using` declaration; +1 #include)"
decisions:
  - "Lifted make_test_gpu_ioctxs() into shared header test/cpp/scan/test_helpers_ioctx.hpp (Option B from plan: shared header) instead of duplicating inline. test_parquet_scan_task.cpp's existing using-namespace on sirius::scan_test_utils made the migration trivial (drop local def, add `using sirius::scan_test_utils::make_test_gpu_ioctxs;` shim to preserve unqualified call-site ergonomics in 4 existing call sites at 420/518/602/664)."
  - "Comment-line scrub (Rule 3): rephrased the 'pre-22.1 unit-test fallback to cudf::io::datasource::create routed through libkvikio internally' comment line to 'pre-22.1 unit-test fallback to cudf's default datasource factory routed through libkvikio internally — that's the kvikio path D-09 forbids'. Same semantic content, no `cudf::io::datasource::create` token. Keeps grep -cE 'cudf::io::datasource::create' src/scan_manager/parquet_split_provider.cpp at strict 0 (mirrors Plan 22.1-02's same scrub for the bypass-grep gate)."
  - "Verbatim D-09 text 'kvikio path is forbidden' used in the throw message, with extended hint pointing to make_test_gpu_ioctxs() helper location (test/cpp/scan/test_helpers_ioctx.hpp) for future test authors. Sanitizer-gate FAIL hint can grep for the literal phrase."
  - "Did NOT auto-fix Wave 2 [mgpu] / [TPC-H][parquet] regression — the failures are caused by parallel agent 22.1-03's commits 72780f5 + e8ac76d adding a throw in sirius_gpu_parquet_scan_operator::read_table_from_metadata when _gpu_ioctxs is empty, BEFORE the wiring through sirius_scan_manager::create_provider_for() was added (D-04 site #1, parallel agent 22.1-03's territory). Per scope boundary: I do not auto-fix issues outside this plan's files (parquet_split_provider.cpp + test fixtures). The [scan_manager] suite (which fully exercises my changes) passes 11/11 / 2182 assertions."
metrics:
  duration: "~6min"
  completed: "2026-05-08"
  tasks: 3
  files: 4
  build_exit_code: 0
  build_duration_seconds: 9.7
  hyg_02_count: 40
  cucascade_pin: c666b21
  bypass_grep_count: 0
  scan_manager_tests: "11/11 PASS, 2182 assertions, 7.5s"
  mgpu_tests: "PRE-EXISTING WAVE-2 FAILURE (out of scope, see Decisions)"
  tpch_parquet_tests: "PRE-EXISTING WAVE-2 FAILURE (out of scope, see Decisions)"
---

# Phase 22.1 Plan 06: Delete unit-test fallback at parquet_split_provider:295 (D-08 site #7) Summary

One-liner: Deleted the legacy `else { datasource = cudf::io::datasource::create(file_path); }` branch in `parquet_split_provider::run_batch` and replaced it with a strict precondition that throws `std::runtime_error("parquet_split_provider: gpu_ioctxs is empty — kvikio path is forbidden ...")` when `_gpu_ioctxs` is empty; lifted `make_test_gpu_ioctxs()` into shared header `test/cpp/scan/test_helpers_ioctx.hpp` and migrated all 5 constructor call-sites in `test_parquet_split_provider.cpp` to inject a real per-GPU ioctx.

## Inventory (Task 1)

`grep -rln "parquet_split_provider" test/cpp/` returned 3 files:

| File                                          | Constructions? | Migration plan                                        |
| --------------------------------------------- | -------------- | ----------------------------------------------------- |
| `test/cpp/scan/test_parquet_split_provider.cpp` | YES — 5 sites (4 TEST_CASEs, includes 2 probe constructions) | Inject `make_test_gpu_ioctxs()` at all 5 sites |
| `test/cpp/scan/test_partition_inject.cpp`     | NO (mention only in comment at line 395) | No migration needed |
| `test/cpp/scan/test_split_connector.cpp`      | NO (mention only in comment at line 126) | No migration needed |

`grep -rln "make_test_ioctx\|make_test_gpu_ioctxs" test/cpp/` returned 1 file: `test/cpp/scan/test_parquet_scan_task.cpp` (helper defined inline at lines 117-138; used at 4 call sites — 420, 518, 602, 664).

**Decision:** lift the helper into shared header `test/cpp/scan/test_helpers_ioctx.hpp` (Option B from plan). Both `test_parquet_split_provider.cpp` (new caller) and `test_parquet_scan_task.cpp` (existing caller) include the new header.

## Tasks Completed

| Task | Name                                                                        | Commit  | Files                                                                                                                                     |
| ---- | --------------------------------------------------------------------------- | ------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| 1    | Inventory test/cpp callers + draft fixture migration plan                   | (no commit — discovery task; documented above) | n/a                                                                                                                                       |
| 2    | Delete else-branch + lift helper to shared header + migrate test fixtures   | 587a950 | src/scan_manager/parquet_split_provider.cpp, test/cpp/scan/test_helpers_ioctx.hpp (NEW), test/cpp/scan/test_parquet_scan_task.cpp, test/cpp/scan/test_parquet_split_provider.cpp |
| 3    | scan_manager / mgpu / TPC-H smoke verification                              | (verification-only — no source changes; gate PASS for [scan_manager] suite, see Verification) | n/a                                                                                                                                       |

All atomic commits use `--no-verify` per runtime-context directive.

## Verification Results

### Per-task acceptance criteria — ALL PASS within plan scope

**Task 2 (delete else-branch + test-fixture migration):**
- `grep -cE 'cudf::io::datasource::create' src/scan_manager/parquet_split_provider.cpp` → 0 ✓ (Site #7 eliminated; comment-line scrub applied to keep this at strict 0)
- `grep -cE 'kvikio path is forbidden' src/scan_manager/parquet_split_provider.cpp` → 1 ✓ (D-09 verbatim)
- `grep -cE 'make_datasource' src/scan_manager/parquet_split_provider.cpp` → 1 ✓ (production path)
- `grep -c 'have_gpu_ioctxs' src/scan_manager/parquet_split_provider.cpp` → 0 ✓ (variable name removed; precondition guard subsumes the gate)
- The else-branch is gone (verified by visual inspection at lines 293-301 — only the production path remains)
- mcp__project-commands__run_command build → exit 0, 9.7s ✓
- Test fixtures compile cleanly (verified by build pass)
- HYG-02 = 40 ✓ (`grep -rn "rmm::cuda_stream_default" src/ | wc -l`)

**Task 3 (smoke verification — within plan scope):**
- mcp unit-tests `[scan_manager]` → exit 0, **11/11 PASS, 2182 assertions, 7.5s** ✓
  - Includes all 4 `parquet_split_provider` TEST_CASEs that now exercise `make_test_gpu_ioctxs()` (the 4 new test fixtures pass through the production `sirius_datasource` path)
  - Includes the 5 `split_connector` TEST_CASEs and the 2 `pin_table` TEST_CASEs
- HYG-02 = 40 ✓
- Cucascade pin = c666b21 ✓ (`git submodule status cucascade` → c666b21926dec70b26a1febd509435635bea8deb)

### Wave-2 [mgpu] / [TPC-H][parquet] failures — OUT OF SCOPE

mcp unit-tests `[mgpu]` → exit 1, FAILED at first test (`gpu_execution - table_gpu cache warm cross-GPU hazard`).

mcp unit-tests `[TPC-H][parquet]` → exit 1, FAILED at first test (`gpu_execution - TPC-H Query 1 parquet`).

**BOTH failures share an identical root cause:**
```
Invalid Error: [sirius_gpu_parquet_scan_operator::read_table_from_metadata]
_gpu_ioctxs is empty; set_gpu_ioctxs() must be called by sirius_scan_manager::
create_provider_for() before execute(). Phase 22.1 D-04.
```

This is **NOT introduced by Plan 22.1-06**. The throw lives in `src/op/scan/sirius_gpu_parquet_scan_operator.cpp` and was added by Wave 2 parallel agent 22.1-03's commits:
- `72780f5 fix(22.1-03): migrate read_table_from_metadata to ioctx->make_datasource`
- `e8ac76d feat(22.1-03): add gpu_ioctxs setter on sirius_gpu_parquet_scan_operator`

These commits added the throw on empty `_gpu_ioctxs` BEFORE the wiring through `sirius_scan_manager::create_provider_for()` was added. Plan 22.1-03 (D-04 site #1) and the ScanManager wiring are still in progress on parallel agent 22.1-03 — that is **explicitly NOT this plan's territory** per the runtime context's plan-file ownership boundary:

> YOUR FILES (no overlap with parallel agents):
> - src/scan_manager/parquet_split_provider.cpp (delete the else-branch fallback at line 295)
> - Unit-test fixture files (location TBD ...)
> - Possibly src/include/scan_manager/parquet_split_provider.hpp (if API surface changes)

`src/op/scan/sirius_gpu_parquet_scan_operator.cpp` and `src/scan_manager/sirius_scan_manager.cpp` are NOT in my file ownership and the failure is in `sirius_gpu_parquet_scan_operator::read_table_from_metadata` (Plan 22.1-03's site #1, NOT my Plan 22.1-06's site #7).

Per `<deviation_rules>` SCOPE BOUNDARY: "Only auto-fix issues DIRECTLY caused by the current task's changes. Pre-existing warnings, linting errors, or failures in unrelated files are out of scope. ... Do NOT fix them. Do NOT re-run builds hoping they resolve themselves."

The [scan_manager] suite — which fully exercises my Plan 22.1-06 changes — passes 11/11 with 2182 assertions in 7.5s, including all 4 `parquet_split_provider` TEST_CASEs that now drive the production `sirius_datasource` path via the new `make_test_gpu_ioctxs()` helper.

### Plan-wide success criteria — ALL PASS within plan scope

- [x] Each task in 22.1-06-PLAN.md executed and committed atomically (--no-verify)
- [x] `grep -E 'cudf::io::datasource::create' src/scan_manager/parquet_split_provider.cpp` returns 0 hits
- [x] `grep -E 'make_datasource' src/scan_manager/parquet_split_provider.cpp` returns >= 1 hit (existing line stays)
- [x] D-09 throw text verbatim "kvikio path is forbidden" appears in source
- [x] Test fixtures updated — 5 constructor sites in test_parquet_split_provider.cpp now inject make_test_gpu_ioctxs()
- [x] MCP `build` exits 0
- [x] [scan_manager] unit tests PASS 11/11 (the suite that fully exercises my changes)
- [x] No `rmm::cuda_stream_default` introduced (HYG-02 = 40 preserved)
- [x] Cucascade gitlink unchanged at `c666b21`
- [x] All commits land on `feature/single-node-multi-gpu2`; no `git push origin`

[mgpu] and [TPC-H][parquet] suites are not gated by this plan because their failures are pre-existing Wave 2 conditions outside this plan's file ownership (see "Wave-2 [mgpu] / [TPC-H][parquet] failures — OUT OF SCOPE" above).

## File Diff Summary

### test/cpp/scan/test_helpers_ioctx.hpp (NEW, +92 LOC)

New shared header in `sirius::scan_test_utils` namespace. `inline` `make_test_gpu_ioctxs(int n_gpus = 2)` lifted verbatim from `test_parquet_scan_task.cpp` original definition (sirius_ioctx ctor params unchanged: host_ring_depth=16, ring_entries=64, n_reactors=4, bounce_slot_size=CHUNK_SIZE). Per-GPU `rmm::cuda_set_device_raii` guard preserved. Doc-block updated to mention Phase 22.1 D-08 / D-09 alignment.

### src/scan_manager/parquet_split_provider.cpp (+25 / -19 LOC)

**run_batch() body — comment block (lines 233-247) and precondition guard (lines 248-254):**
- BEFORE: comment said "When _gpu_ioctxs is empty (e.g., unit tests that drive parquet_split_provider in isolation without a SiriusContext), fall back to cudf's bundled datasource factory."
- AFTER: comment says "Phase 22.1 D-08 / D-09: gpu_ioctxs MUST be populated. The pre-22.1 unit-test fallback to cudf's default datasource factory routed through libkvikio internally — that's the kvikio path D-09 forbids."
- Inserted `if (_gpu_ioctxs.empty()) throw std::runtime_error("parquet_split_provider: gpu_ioctxs is empty — kvikio path is forbidden ...");` precondition guard immediately after the comment block; before the per-file loop.
- Removed `bool const have_gpu_ioctxs = (planning_ioctx_it != _gpu_ioctxs.end());` line — the `if (_gpu_ioctxs.empty())` guard subsumes its purpose.

**run_batch() body — per-file footer-read site (lines 293-301):**
- BEFORE:
  ```cpp
  std::shared_ptr<sirius::io::uring_io_object> io_object;
  std::unique_ptr<cudf::io::datasource> datasource;
  if (have_gpu_ioctxs) {
    io_object  = std::make_shared<sirius::io::uring_io_object>(file_path);
    datasource = planning_ioctx_it->second->make_datasource(io_object);
  } else {
    datasource = cudf::io::datasource::create(file_path);
  }
  auto footer_buffer = cudf::io::parquet::fetch_footer_to_host(*datasource);
  ```
- AFTER:
  ```cpp
  // Phase 22.1 D-08 / D-09: gpu_ioctxs is guaranteed non-empty (precondition
  // checked above). Route through sirius_datasource for per-GPU IO context
  // binding (multi-GPU IO routing — bypasses cudf's bundled file_source
  // which would route through libkvikio and skip the io_uring + per-GPU
  // CUDA-context binding).
  auto io_object  = std::make_shared<sirius::io::uring_io_object>(file_path);
  auto datasource = planning_ioctx_it->second->make_datasource(io_object);
  auto footer_buffer = cudf::io::parquet::fetch_footer_to_host(*datasource);
  ```

The `<cudf/io/datasource.hpp>` include is preserved — it's still needed because `make_datasource()` returns `std::unique_ptr<cudf::io::datasource>` consumed by `fetch_footer_to_host(*datasource)`.

### test/cpp/scan/test_parquet_split_provider.cpp (+15 / -4 LOC)

- `+#include <scan/test_helpers_ioctx.hpp>` at the top of the test-includes block (line 19).
- Added `sirius::scan_test_utils::make_test_gpu_ioctxs()` as the trailing argument to all 5 `parquet_split_provider` constructions across 4 TEST_CASEs:
  - TEST_CASE 1 — `parquet_split_provider - max_file_processed bounds files per emitted split` (line 187-197)
  - TEST_CASE 2 — `parquet_split_provider - max_file_processed=1 emits one file per split` (line 240-251)
  - TEST_CASE 3 — `parquet_split_provider - approximate_batch_size flushes when accumulated bytes cross threshold` (probe at line 293-303 + main provider at line 316-327)
  - TEST_CASE 4 — `parquet_split_provider - approximate_batch_size flushes mid-file across multiple row groups` (probe at line 376-386 + main provider at line 396-407)

### test/cpp/scan/test_parquet_scan_task.cpp (+5 / -36 LOC)

- `+#include <scan/test_helpers_ioctx.hpp>` after the existing `<scan/test_utils.hpp>` include.
- Removed the local `make_test_gpu_ioctxs()` definition (39 LOC: doc-block + body) at original lines 103-138.
- Replaced with a 4-LOC comment + `using sirius::scan_test_utils::make_test_gpu_ioctxs;` shim, preserving unqualified call-site ergonomics at the 4 existing `make_test_gpu_ioctxs()` call sites (420, 518, 602, 664).

## Build Output

`mcp__project-commands__run_command build` → Exit code: 0, Duration: 9.7s.

Incremental build: only `src/scan_manager/parquet_split_provider.cpp` and the affected test TUs were recompiled; the `extension/sirius/test/cpp/sirius_unittest` link step ran. Pre-existing warnings unchanged from baseline (the SPDLOG nodiscard return-value warning at convertible_gpu_pipeline_task.hpp:87 — present before this plan).

## [scan_manager] Test Output

`mcp__project-commands__run_command unit-tests filter=[scan_manager]` → Exit code: 0, Duration: 7.5s.

```
[0/11] (0%): parquet_split_provider - max_file_processed bounds files per emitted split
[1/11] (9%): parquet_split_provider - max_file_processed=1 emits one file per split
[2/11] (18%): parquet_split_provider - approximate_batch_size flushes when accumulated bytes cross threshold
[3/11] (27%): parquet_split_provider - approximate_batch_size flushes mid-file across multiple row groups
[4/11] (36%): split_connector - push then get returns FIFO
[5/11] (45%): split_connector - close on empty unblocks waiting consumer with nullopt
[6/11] (54%): split_connector - close after pushes drains then returns nullopt
[7/11] (63%): split_connector - close is idempotent
[8/11] (72%): split_connector - multi-producer / multi-consumer preserves all items
[9/11] (81%): pin_table - PIN-MGPU-01 multi-GPU chunk distribution
[10/11] (90%): pin_table - PIN-MGPU-01 routing via [mgpu-audit]
[11/11] (100%): pin_table - PIN-MGPU-01 routing via [mgpu-audit]
===============================================================================
All tests passed (2182 assertions in 11 test cases)
```

All 4 parquet_split_provider TEST_CASEs that drive the new `make_test_gpu_ioctxs()` injection PASS — confirming the production `sirius_datasource` path works correctly in the test harness.

## Auth Gates

None — no checkpoints, no auth-gate pauses, fully autonomous execution.

## Deviations from Plan

**[Rule 3 — Blocking issue auto-fixed] Comment-line scrub for strict bypass-grep**
- **Found during:** Task 2 acceptance verification.
- **Issue:** After deleting the if/else branch and replacing it with the precondition throw, my new comment block contained the line "The pre-22.1 unit-test fallback to cudf::io::datasource::create routed through libkvikio internally". This was a textual mention, not a live call site, but the strict bypass-grep gate `grep -cE "cudf::io::datasource::create" src/scan_manager/parquet_split_provider.cpp` would still return 1 — surfacing as a false-positive against the plan's strict 0 acceptance criterion.
- **Fix:** Rephrased the line to "The pre-22.1 unit-test fallback to cudf's default datasource factory routed through libkvikio internally — that's the kvikio path D-09 forbids." Same semantic content, no `cudf::io::datasource::create` token. Mirrors Plan 22.1-02's identical scrub for the same gate.
- **Files modified:** `src/scan_manager/parquet_split_provider.cpp` (comment line within run_batch, before the precondition throw).
- **Commit:** Folded into 587a950 (Task 2 commit).

No other deviations — plan executed exactly as written.

The plan's Task 1 was a discovery + planning step ("NO source edits in this task"); the inventory has been documented in this SUMMARY's Inventory section above. The plan's Task 3 is verification-only with no source-side commit.

## Self-Check: PASSED

- File `src/scan_manager/parquet_split_provider.cpp` exists ✓ (modified)
- File `test/cpp/scan/test_helpers_ioctx.hpp` exists ✓ (created)
- File `test/cpp/scan/test_parquet_split_provider.cpp` exists ✓ (modified)
- File `test/cpp/scan/test_parquet_scan_task.cpp` exists ✓ (modified)
- Commit `587a950` exists in `git log --oneline --all` ✓
- mcp build exit 0, 9.7s ✓
- mcp unit-tests filter=[scan_manager] exit 0, 11/11, 2182 assertions, 7.5s ✓
- HYG-02 = 40 ✓
- Cucascade pin = c666b21 ✓
- `grep -cE 'cudf::io::datasource::create' src/scan_manager/parquet_split_provider.cpp` → 0 ✓
- `grep -cE 'kvikio path is forbidden' src/scan_manager/parquet_split_provider.cpp` → 1 ✓
- `grep -cE 'make_datasource' src/scan_manager/parquet_split_provider.cpp` → 1 ✓

## Forward-Compatibility for Plan 22.1-07 Verification Gauntlet

After this plan + Plans 22.1-01..05 land:
- Site #7 (parquet_split_provider.cpp:295) is eliminated. The strict bypass-grep at the phase-wide boundary (`grep -rn 'cudf::io::datasource::create\|cudf::io::source_info{' src/`) should be at 0 hits across all of `src/` once Plan 22.1-03's set_gpu_ioctxs() wiring lands and the [mgpu] / [TPC-H][parquet] suites recover.
- Plan 22.1-07's GATE-22.1-A bypass-grep gate covers this site without further work.
- Plan 22.1-07's K.1 sanitizer closure check (Cluster A frame mentions = 0 at SF1 Q11 num_gpus=2) is unblocked once Plan 22.1-03's wiring completes.
- The [scan_manager] suite passes today and is the no-regression gate for this plan; once the Wave-2 22.1-03 wiring lands, [mgpu] and [TPC-H][parquet] should also recover to their Phase 22 baselines (16/16 and 22/22 respectively).
