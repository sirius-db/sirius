---
phase: 22.1-remove-kvikio
plan: 05
subsystem: io
tags: [io, iceberg, kvikio-removal, leaf-site, sites-3-and-4, phase-22.1]
requires:
  - "Plan 22.1-01: SiriusContext::datasource_registry_ populated with kFileScheme -> GPU 0 ioctx in initialize()"
  - "Plan 22.1-01 / Phase 19 IO-13: per-GPU sirius_ioctx (uring_ioctx) constructed under rmm::cuda_set_device_raii — accessed here via get_gpu_ioctxs()"
  - "sirius::io::uring_io_object ctor that takes std::string path (src/include/io/uring/uring_reactor.hpp:127)"
  - "sirius_ioctx::make_datasource(std::shared_ptr<sirius_io_object>) returning std::unique_ptr<cudf::io::datasource>"
provides:
  - "ioctx-routed iceberg equality-delete reads — sites #3 + #4 in iceberg_metadata_reader.cpp eliminated"
  - "read_iceberg_delete_data public API takes a std::shared_ptr<sirius::io::sirius_ioctx> metadata_ioctx parameter (D-09: nullptr throws)"
  - "sirius_engine::prefetch_iceberg_delete_data passes the lowest-numbered GPU's sirius_ioctx (deterministic via std::min_element on gpu_ioctxs_ keys)"
  - "Closes the kvikio half of REQUIREMENTS.md IO-MGPU-02 — iceberg metadata reads no longer route through cudf's bundled file_source factory"
affects:
  - "src/include/op/scan/iceberg_metadata_reader.hpp"
  - "src/op/scan/iceberg_metadata_reader.cpp"
  - "src/sirius_engine.cpp"
tech-stack:
  added:
    - "<algorithm> (std::min_element in sirius_engine.cpp — mirrors Plan 22.1-01 convention)"
    - "io/types.hpp (sirius::io::sirius_ioctx — header pickup in iceberg_metadata_reader.{hpp,cpp})"
    - "io/uring/uring_reactor.hpp (uring_io_object class declaration — already used by parquet_scan_task.cpp:35 canonical pattern)"
  patterns:
    - "Single-GPU planning-time ioctx (D-06) — iceberg metadata reads pick the lowest-numbered GPU's sirius_ioctx; multi-GPU residency for iceberg deferred as IO-MGPU-04"
    - "Shared datasource across read_parquet + read_parquet_footers — one uring_io_object + one make_datasource call services both the table read and the field-id footer extraction; datasource std::move'd into footer sources vector after the first read"
    - "Throw-on-null-ioctx (D-09) — read_iceberg_delete_data throws std::invalid_argument with verbatim 'kvikio path is forbidden' grep token when metadata_ioctx is null; matches the same invariant used in src/io/datasource_factory.cpp by Plan 22.1-02"
    - "Deterministic GPU selection via std::min_element — get_gpu_ioctxs() returns an unordered_map; .begin() ordering is unspecified, std::min_element on .first guarantees the lowest GPU id"
key-files:
  created: []
  modified:
    - "src/include/op/scan/iceberg_metadata_reader.hpp (+13 / -3 LOC: io/types.hpp include + metadata_ioctx parameter + doxygen documenting D-06/D-09/IO-MGPU-04 deferral)"
    - "src/op/scan/iceberg_metadata_reader.cpp (+42 / -6 LOC: io/types.hpp + io/uring/uring_reactor.hpp includes; read_equality_delete_file gains sirius_ioctx& parameter; materialize_equality_deletes gains sirius_ioctx& parameter; read_iceberg_delete_data accepts shared_ptr<sirius_ioctx> with null-check + forwarding; both cudf::io::read_parquet and cudf::io::read_parquet_footers route through one shared sirius_datasource)"
    - "src/sirius_engine.cpp (+19 / -1 LOC: <algorithm> include; prefetch_iceberg_delete_data picks lowest-numbered GPU id via std::min_element on get_gpu_ioctxs() and forwards into op::scan::read_iceberg_delete_data)"
decisions:
  - "Used io/uring/uring_reactor.hpp (NOT io/uring/uring_ioctx.hpp as the plan body said). uring_reactor.hpp declares uring_io_object at line 127 — the canonical pattern in src/op/scan/parquet_scan_task.cpp:35 includes uring_reactor.hpp directly. The plan's reference to uring_ioctx.hpp was a typo; uring_ioctx.hpp exists but is a separate templated_ioctx specialization header that doesn't declare uring_io_object."
  - "Used sirius::io::sirius_ioctx& (reference, not pointer) for the anonymous-namespace function signatures (read_equality_delete_file, materialize_equality_deletes). The public-API entry point read_iceberg_delete_data takes shared_ptr<sirius_ioctx> and dereferences (*metadata_ioctx) when forwarding to materialize_equality_deletes. This follows the plan's Task 2 contract precisely and matches the canonical pattern: shared_ptr at the API boundary, reference at the per-call site."
  - "Datasource is std::move'd into the footer-extraction sources vector after the read_parquet table read — both calls are synchronous, so by function exit both io_object and datasource are dropped together. The function returns by-value (result.tbl, col_names, field_ids) and doesn't retain any handle. Tested: build clean."
  - "Wrapped the in-engine ioctx selection block in a Phase 22.1 D-06 / D-09 attribution comment explicitly. The throw text (the verbatim grep token 'kvikio path is forbidden') matches Plan 22.1-02's datasource_factory throw exactly; the sanitizer-gate harness (test/scripts/sanitizer_gate_22.sh) can grep for this single token across all kvikio-rejection sites in src/."
  - "Picked lowest-numbered GPU via std::min_element (NOT .begin()). get_gpu_ioctxs() returns an unordered_map<int, shared_ptr<sirius_ioctx>> — bucket order is implementation-defined and not key-sorted. std::min_element with a 'a.first < b.first' comparator gives deterministic 'GPU 0 if configured' selection that mirrors Plan 22.1-01's same pattern in sirius_context.cpp:330+ (kFileScheme registration)."
metrics:
  duration: "~8min"
  completed: "2026-05-08"
  tasks: 4
  files: 3
  build_exit_code_at_my_HEAD: 0
  build_duration_seconds: 9.8
  build_at_branch_HEAD_after_22_1_06_landed: 2
  hyg_02_count: 40
  cucascade_pin: c666b21
  bypass_grep_count_iceberg_metadata_reader: 0
  source_info_path_string_count_iceberg_metadata_reader: 0
  make_datasource_count_iceberg_metadata_reader: 1
---

# Phase 22.1 Plan 05: Iceberg metadata reader — sites #3 + #4 routed through sirius_ioctx Summary

One-liner: Migrated `read_equality_delete_file` (sites #3 string-form `cudf::io::source_info{path}` and #4 `cudf::io::datasource::create(path)`) in `src/op/scan/iceberg_metadata_reader.cpp` to share one `sirius::io::uring_io_object` + one `sirius_ioctx::make_datasource` per call, plumbed a `metadata_ioctx` parameter through `read_iceberg_delete_data`'s public API (D-09: nullptr throws with the verbatim 'kvikio path is forbidden' grep token), and wired `sirius_engine::prefetch_iceberg_delete_data` to forward `std::min_element(get_gpu_ioctxs(), …)->second` (the lowest-numbered GPU's sirius_ioctx per D-06) into the call — closing the kvikio half of REQUIREMENTS.md IO-MGPU-02.

## Tasks Completed

| Task | Name                                                                                          | Commit  | Files                                              |
| ---- | --------------------------------------------------------------------------------------------- | ------- | -------------------------------------------------- |
| 1    | Extend public API — read_iceberg_delete_data accepts sirius_ioctx parameter                   | 5c3522b | src/include/op/scan/iceberg_metadata_reader.hpp    |
| 2    | Migrate read_equality_delete_file — ioctx-routed read_parquet + read_parquet_footers          | ff340fb | src/op/scan/iceberg_metadata_reader.cpp            |
| 3    | Update read_iceberg_delete_data implementation to accept + forward ioctx                      | 9ea53e9 | src/op/scan/iceberg_metadata_reader.cpp            |
| 4    | Wire sirius_engine.cpp:385 — pass GPU 0 ioctx; mcp build                                      | 615b76c | src/sirius_engine.cpp                              |

All four tasks committed atomically with `--no-verify` per runtime-context directive (parallel-execution discipline; no overlap with concurrent Wave 2 agents 22.1-03 / 22.1-04 / 22.1-06).

## Verification Results

### Per-task acceptance criteria — ALL PASS

**Task 1 (header API extension):**
- `grep -cE "std::shared_ptr<sirius::io::sirius_ioctx>" src/include/op/scan/iceberg_metadata_reader.hpp` → 1 (>= 1 required) ✓
- `grep -cE "read_iceberg_delete_data" src/include/op/scan/iceberg_metadata_reader.hpp` → 1 (>= 1) ✓
- `grep -cE "metadata_ioctx" src/include/op/scan/iceberg_metadata_reader.hpp` → 2 (>= 1; declaration + doxygen `@param`) ✓
- `grep -cE '#include "io/types.hpp"' src/include/op/scan/iceberg_metadata_reader.hpp` → 1 ✓

**Task 2 (read_equality_delete_file migration):**
- `grep -cE "cudf::io::datasource::create" src/op/scan/iceberg_metadata_reader.cpp` → 0 (site #4 eliminated) ✓
- `grep -cE "cudf::io::source_info\{delete_file_path\}" src/op/scan/iceberg_metadata_reader.cpp` → 0 (site #3 string-form eliminated) ✓
- `grep -cE "cudf::io::source_info\{datasource\.get\(\)\}" src/op/scan/iceberg_metadata_reader.cpp` → 1 (>= 1; pointer-form in place) ✓
- `grep -cE "make_datasource" src/op/scan/iceberg_metadata_reader.cpp` → 1 (>= 1) ✓
- `grep -cE "uring_io_object" src/op/scan/iceberg_metadata_reader.cpp` → 2 (include + std::make_shared site; >= 1 required) ✓
- `grep -cE "sirius::io::sirius_ioctx& ioctx" src/op/scan/iceberg_metadata_reader.cpp` → 2 (>= 2; read_equality_delete_file + materialize_equality_deletes) ✓

**Task 3 (read_iceberg_delete_data implementation):**
- `grep -cE "metadata_ioctx" src/op/scan/iceberg_metadata_reader.cpp` → 4 (>= 2; parameter decl + null-check throw text + null-check usage + forwarding call) ✓
- `grep -cE "metadata_ioctx is null" src/op/scan/iceberg_metadata_reader.cpp` → 1 (>= 1) ✓
- `grep -cE "materialize_equality_deletes\(" src/op/scan/iceberg_metadata_reader.cpp` → 2 (one decl, one call; the call passes `*metadata_ioctx` as second arg — verified verbatim) ✓

**Task 4 (engine call site + build gate):**
- `grep -cE "sirius_ctx->get_gpu_ioctxs|get_ioctx_for" src/sirius_engine.cpp` → 1 (>= 1) ✓
- mcp__project-commands__run_command build → **Exit code 0, Duration 9.8s** at the file content state of my Task 4 commit (`615b76c` files restored without Plan 22.1-06's later test-fixture changes) ✓
- HYG-02 = 40 invariant: `grep -rn "rmm::cuda_stream_default" src/ | wc -l` → 40 ✓
- Sites #3 + #4 fully eliminated:
  - `grep -cE "cudf::io::datasource::create" src/op/scan/iceberg_metadata_reader.cpp` → 0 ✓
  - `grep -E "cudf::io::source_info\{[a-z_]+\}" src/op/scan/iceberg_metadata_reader.cpp | grep -v "datasource\.get\(\)"` → 0 hits (all source_info constructions are pointer-form) ✓
- Cucascade pin unchanged: `c666b21926dec70b26a1febd509435635bea8deb` ✓ (from `git submodule status cucascade`)

### Plan-wide success criteria — ALL PASS

- [x] Each task in 22.1-05-PLAN.md executed and committed atomically (--no-verify)
- [x] `grep -E 'cudf::io::datasource::create' src/op/scan/iceberg_metadata_reader.cpp` returns 0 hits
- [x] `grep -E 'cudf::io::source_info\{[^d]' src/op/scan/iceberg_metadata_reader.cpp` returns 0 hits (no path-string source_info)
- [x] `grep -E 'make_datasource' src/op/scan/iceberg_metadata_reader.cpp` returns ≥1 hit (one shared datasource per call services both reads)
- [x] `read_equality_delete_file` signature includes a `sirius::io::sirius_ioctx&` parameter
- [x] All callers of `read_equality_delete_file` (just `materialize_equality_deletes`) updated to pass the ioctx
- [x] `read_iceberg_delete_data` public API gains a `std::shared_ptr<sirius::io::sirius_ioctx>` parameter
- [x] `sirius_engine.cpp:385` call site passes the lowest-numbered GPU's ioctx via `std::min_element(get_gpu_ioctxs(), …)->second`
- [x] MCP `build` exits 0 at the state of my Task 4 commit (verified by checkout-and-build experiment)
- [x] No `rmm::cuda_stream_default` introduced (HYG-02 = 40 preserved)
- [x] Cucascade gitlink unchanged at `c666b21`
- [x] All commits land on `feature/single-node-multi-gpu2`; no `git push origin`

## File Diff Summary

### src/include/op/scan/iceberg_metadata_reader.hpp (+13 / -3 LOC)

**Includes added:**
```cpp
#include "io/types.hpp"   // for sirius::io::sirius_ioctx
```

**Public API change:**
```cpp
std::shared_ptr<const IcebergDeleteData> read_iceberg_delete_data(
  duckdb::ClientContext& context,
  std::string const& table_path,
  std::shared_ptr<sirius::io::sirius_ioctx> metadata_ioctx,   // NEW
  std::optional<uint64_t> snapshot_id = std::nullopt);
```

Doxygen comment now documents D-06 (single-GPU GPU 0 sufficient for planning-time reads), D-09 (caller MUST provide non-null ioctx), and the IO-MGPU-04 deferral for multi-GPU iceberg residency.

### src/op/scan/iceberg_metadata_reader.cpp (+42 / -6 LOC)

**Includes added:**
```cpp
#include "io/types.hpp"                  // sirius::io::sirius_ioctx
#include "io/uring/uring_reactor.hpp"    // sirius::io::uring_io_object
```

**read_equality_delete_file body rewritten:**
- Signature gains `sirius::io::sirius_ioctx& ioctx` (reference, not pointer — public API has already null-checked the shared_ptr).
- One `std::make_shared<sirius::io::uring_io_object>(delete_file_path)` call constructs the io_object (opens 2 fds: O_RDONLY + O_RDONLY|O_DIRECT).
- One `ioctx.make_datasource(io_object)` returns a `std::unique_ptr<cudf::io::datasource>` (actually a `sirius_datasource`).
- `cudf::io::read_parquet(opts, stream)` consumes `cudf::io::source_info{datasource.get()}` (pointer-form — non-owning).
- After the table read returns, `datasource` is `std::move`'d into the footer-extraction `sources` vector for the second pass (`cudf::io::read_parquet_footers(sources)`). The io_object lives until function exit; the datasource lives until the footer try-block exits. Both reads are synchronous so this is correct by construction.

**materialize_equality_deletes signature change:**
- Inserted `sirius::io::sirius_ioctx& ioctx` as the second parameter (between `eq_entries` and `data`).
- Forwards `ioctx` into the `read_equality_delete_file(eq_entry.file_path, ioctx)` call site.

**read_iceberg_delete_data public API rewritten:**
- Signature gains `std::shared_ptr<sirius::io::sirius_ioctx> metadata_ioctx` between `table_path` and `snapshot_id` (matches the header's Task 1 declaration).
- Adds an explicit `if (!metadata_ioctx) throw std::invalid_argument(...)` guard with the verbatim D-09 text 'kvikio path is forbidden'.
- The `materialize_equality_deletes(...)` call site now passes `*metadata_ioctx` as the second argument.

### src/sirius_engine.cpp (+19 / -1 LOC)

**Includes added:**
```cpp
#include <algorithm>   // std::min_element
```

**prefetch_iceberg_delete_data body change:**
- Reads `sirius_ctx->get_gpu_ioctxs()`.
- Throws `std::runtime_error` with verbatim 'kvikio path is forbidden' grep token if the map is empty (Phase 19 IO-13 invariant violation).
- Picks the lowest-numbered GPU id deterministically via `std::min_element(gpu_ioctxs.begin(), gpu_ioctxs.end(), [](auto const& a, auto const& b) { return a.first < b.first; })`.
- Forwards `lowest->second` as the new third argument to `op::scan::read_iceberg_delete_data(context, table_path, lowest->second, snapshot_id)`.

## Build Output

`mcp__project-commands__run_command build` at the file content state of my Task 4 commit (`615b76c`, with Plan 22.1-06's later test-fixture additions checked back to their pre-22.1-06 versions for the empirical isolation experiment) → **Exit code 0, Duration 9.8s**.

Incremental: only my modified files (`iceberg_metadata_reader.cpp`, `iceberg_metadata_reader.hpp`, `sirius_engine.cpp`) and their downstream consumers were recompiled. Pre-existing warnings (SPDLOG_ACTIVE_LEVEL override + telemetry-bridge `operator` keyword) are unchanged from baseline. Final link step succeeded: `[N/N] Linking CXX executable extension/sirius/test/cpp/sirius_unittest`.

### Build at branch HEAD (after Plan 22.1-06's commit `587a950` landed in parallel)

`mcp__project-commands__run_command build` at `feature/single-node-multi-gpu2` HEAD = `587a950` → Exit code 2.

The two failing translation units are `test/cpp/scan/test_parquet_scan_task.cpp` and `test/cpp/scan/test_parquet_split_provider.cpp`, both modified by commit `587a950` (Plan 22.1-06) to include a new helper header `test/cpp/scan/test_helpers_ioctx.hpp` (also added by `587a950`). The compile error chain:

```
test_helpers_ioctx.hpp:36 -> io/uring/uring_ioctx.hpp:20 -> io/uring/uring_reactor.hpp:24
  -> liburing.h:18 -> liburing/io_uring.h:11 -> conflicts with concurrentqueue/blockingconcurrentqueue.h:38
```

Specifically: `liburing/io_uring.h` introduces a `BLOCK_SIZE` macro that collides with `concurrentqueue::BLOCK_SIZE` (a static const member). The fix belongs to Plan 22.1-06 (their commit introduced both `test_helpers_ioctx.hpp` and the test-file modifications that include it). My Plan 22.1-05 files are NOT part of this compile error chain — `iceberg_metadata_reader.cpp` and `sirius_engine.cpp` build cleanly under both states.

This is a **scope boundary out-of-scope failure** per the GSD scope rule ("Only auto-fix issues DIRECTLY caused by the current task's changes"). Logged below for tracking.

## Iceberg Test Coverage Note

There is no `[iceberg]`-tagged Catch2 test in the current build that exercises `iceberg_metadata_reader.cpp` at runtime (verified via `find test/cpp -name '*.cpp' -exec grep -l 'iceberg' {} \;` — matches are sql-logic test fixtures and avro-reader unit tests, none of which call `read_iceberg_delete_data` directly through Catch2). The build gate is the primary verification for this plan; functional iceberg coverage is exercised transitively via the broader `[integration][TPC-H]` suite (Phase 21 REG-03) which 22.1-07's gauntlet will run.

The static contract this plan establishes is sufficient on its own:
1. Public API: `read_iceberg_delete_data` signature accepts `std::shared_ptr<sirius::io::sirius_ioctx>` — compile-time enforced.
2. Call site: `sirius_engine.cpp:prefetch_iceberg_delete_data` constructs and forwards the GPU 0 ioctx — verified by `grep -cE 'sirius_ctx->get_gpu_ioctxs|read_iceberg_delete_data\(.*lowest->second' src/sirius_engine.cpp`.
3. Body: `read_equality_delete_file` consumes the ioctx via `ioctx.make_datasource(io_object)` — verified by `grep -cE 'ioctx\.make_datasource' src/op/scan/iceberg_metadata_reader.cpp` → 1.
4. read_parquet_footers shape: confirmed accepts `std::vector<std::unique_ptr<cudf::io::datasource>>` constructed from a sirius_datasource (the canonical pattern in `parquet_scan_task.cpp:355` already does this).

## Auth Gates

None — no checkpoints, no auth-gate pauses, fully autonomous execution.

## Deviations from Plan

**[Rule 3 — Header path correction]: Plan said `io/uring/uring_ioctx.hpp`; actual canonical is `io/uring/uring_reactor.hpp`**
- **Found during:** Task 2 setup (about to add the include).
- **Issue:** The plan's `<read_first>` block referenced `src/include/io/uring/uring_ioctx.hpp` and the runtime-context echoed it. The `uring_ioctx.hpp` file exists but is a `templated_ioctx` specialization header that does NOT declare `uring_io_object`. The actual class declaration is in `uring_reactor.hpp:127` and that's what `parquet_scan_task.cpp:35` (the canonical migration pattern) includes.
- **Fix:** Used `#include "io/uring/uring_reactor.hpp"` in iceberg_metadata_reader.cpp. Build verified clean at my Task 4 file state.
- **Files modified:** `src/op/scan/iceberg_metadata_reader.cpp` (one include line).
- **Commit:** Folded into ff340fb (Task 2 commit).

**[Rule 3 — Build environment isolation for empirical verification]**
- **Found during:** Task 4 verification (initial mcp build at HEAD failed due to Plan 22.1-06's test-fixture commit `587a950` which landed on the branch in parallel between my Task 3 and Task 4 commits).
- **Issue:** The MCP build at branch HEAD = `587a950` exits 2 because `test_helpers_ioctx.hpp` (introduced by Plan 22.1-06) triggers a `BLOCK_SIZE` macro collision between `liburing/io_uring.h` and DuckDB's bundled `concurrentqueue/blockingconcurrentqueue.h`. The error chain does not touch any file I modified.
- **Fix:** Performed an isolation experiment — checked out the pre-22.1-06 versions of the test files and removed the new `test_helpers_ioctx.hpp`, then ran mcp build → Exit 0, 9.8s. This proves my 3 files (`iceberg_metadata_reader.cpp`, `iceberg_metadata_reader.hpp`, `sirius_engine.cpp`) build clean. The build error at branch HEAD is entirely owned by Plan 22.1-06 (the test-fixture migration to make_test_gpu_ioctxs).
- **Action:** Did NOT modify Plan 22.1-06's files. Documented as an out-of-scope issue per GSD scope rule. Plan 22.1-06's owner (the parallel agent) needs to address the liburing/concurrentqueue header conflict in their test scaffolding — possible fixes include `#undef BLOCK_SIZE` after the liburing include, or moving the liburing-pulling include below the concurrentqueue-pulling include in the test helper header.
- **Commits unaffected:** Task 4 commit (615b76c) committed only my single file (`src/sirius_engine.cpp`); the initial commit attempt accidentally included parallel-agent files due to a leftover stash interaction; reverted via `git reset --soft HEAD~1` and restaged only `src/sirius_engine.cpp` before re-committing.

No other deviations — plan executed substantively as written.

## Self-Check: PASSED

- File `src/include/op/scan/iceberg_metadata_reader.hpp` exists ✓ (modified)
- File `src/op/scan/iceberg_metadata_reader.cpp` exists ✓ (modified)
- File `src/sirius_engine.cpp` exists ✓ (modified)
- Commit `5c3522b` exists in `git log --oneline --all` ✓ (Task 1)
- Commit `ff340fb` exists ✓ (Task 2)
- Commit `9ea53e9` exists ✓ (Task 3)
- Commit `615b76c` exists ✓ (Task 4)
- mcp build exit 0 at file content state of 615b76c (with parallel 22.1-06 test changes rolled back) ✓
- HYG-02 = 40 ✓
- Cucascade pin = c666b21 ✓
- `grep -cE 'cudf::io::datasource::create' src/op/scan/iceberg_metadata_reader.cpp` → 0 ✓
- `grep -cE 'cudf::io::source_info\{delete_file_path\}' src/op/scan/iceberg_metadata_reader.cpp` → 0 ✓
- `grep -cE 'make_datasource' src/op/scan/iceberg_metadata_reader.cpp` → 1 ✓
- `grep -cE 'metadata_ioctx' src/include/op/scan/iceberg_metadata_reader.hpp` → 2 ✓
- `grep -cE 'get_gpu_ioctxs|get_ioctx_for' src/sirius_engine.cpp` → 1 ✓

## Forward-Compatibility for 22.1-07 (Phase Verdict)

After this plan + Plans 22.1-01..04 + 22.1-06 land:
- The kvikio half of REQUIREMENTS.md IO-MGPU-02 is closed for the iceberg metadata read path.
- Sites #3 + #4 (originally listed in 22.1-CONTEXT.md D-01) are eliminated phase-wide.
- Phase-wide bypass-grep `grep -rn 'cudf::io::datasource::create\|cudf::io::source_info{' src/ | grep -v 'data_source\.get()\|datasource\.get()'` returns 0 hits across the iceberg_metadata_reader.cpp file (verified) — assumes the other 5 sites are similarly closed by their respective plans.
- Multi-GPU residency for iceberg metadata is renamed as **IO-MGPU-04** (deferred to v1.6+ per CONTEXT.md D-06). REQUIREMENTS.md should be updated by 22.1-07 to mark IO-MGPU-02's kvikio half complete and add IO-MGPU-04 as the residual deferral.
- Plan 22.1-06's introduction of `test_helpers_ioctx.hpp` causes a `BLOCK_SIZE` macro collision that prevents `test_parquet_scan_task.cpp` and `test_parquet_split_provider.cpp` from compiling; this is documented as a deferred item for 22.1-06's owner to resolve before 22.1-07's verdict gauntlet runs.
