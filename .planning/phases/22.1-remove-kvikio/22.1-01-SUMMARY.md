---
phase: 22.1-remove-kvikio
plan: 01
subsystem: io
tags: [io, datasource_registry, kFileScheme, foundation, phase-22.1]
requires:
  - "Phase 19 IO-13: per-GPU sirius_ioctx in SiriusContext::initialize() (gpu_ioctxs_ map populated under rmm::cuda_set_device_raii)"
  - "Phase 19 IO-15: sirius::io::datasource_registry class (declared in src/include/io/datasource_factory.hpp)"
provides:
  - "SiriusContext::get_datasource_registry() — const& accessor for downstream consumers (Plan 22.1-02 datasource_factory call site)"
  - "kFileScheme entry in datasource_registry_ resolving to GPU 0's sirius_ioctx (D-10 single-slot per scheme)"
  - "Ordered shutdown: datasource_registry_.clear() runs BEFORE gpu_ioctxs_.clear() in SiriusContext::terminate()"
affects:
  - "src/include/sirius_context.hpp"
  - "src/sirius_context.cpp"
tech-stack:
  added:
    - "<algorithm> (for std::min_element)"
  patterns:
    - "Single-slot-per-scheme registry — consumers needing per-GPU ioctx selection use get_ioctx_for(device_id) directly"
    - "Lifetime-safe shutdown — clear shared_ptr borrowers before clearing owners"
    - "Throw-on-precondition — empty gpu_ioctxs_ at registration time is a Phase 19 IO-13 invariant violation"
key-files:
  created: []
  modified:
    - "src/include/sirius_context.hpp (+21 LOC: 1 include, 1 accessor, 1 member with doc-block)"
    - "src/sirius_context.cpp (+37 LOC: 1 include, 1 registration block at end of init, 1 clear() call in terminate)"
decisions:
  - "Picked the lowest-numbered GPU's ioctx as the kFileScheme entry (std::min_element on gpu_ioctxs_ keys). Arbitrary but deterministic — the registry slot is a fallback for callers that don't know which GPU they want; per-GPU-aware callers (Plans 22.1-03/04/05) pass an explicit ioctx via get_ioctx_for(device_id)."
  - "Registration site placed AFTER the per-GPU ioctx loop (sirius_context.cpp:330+), BEFORE the MGPU-06 P2P enable block — registry doesn't depend on peer-access state and downstream consumers should see it ready as soon as gpu_ioctxs_ is populated."
  - "Throw-not-warn on empty gpu_ioctxs_ at registration time. This makes the Phase 19 IO-13 invariant (initialize() always produces ≥1 ioctx) explicit at the registry-population call site rather than implicit via downstream nullptr returns from lookup."
  - "Used `std::string_view kFileScheme = \"file\";` static constexpr inside the if-block (NOT a file-scope constant) to avoid TU-wide pollution and to mirror the `kFileScheme` constant in datasource_factory.cpp:90 verbatim."
metrics:
  duration: "~3min"
  completed: "2026-05-08"
  tasks: 3
  files: 2
  build_exit_code: 0
  hyg_02_count: 40
  cucascade_pin: c666b21
---

# Phase 22.1 Plan 01: Wire kFileScheme into SiriusContext datasource_registry_ Summary

One-liner: Wired a sirius::io::datasource_registry member onto SiriusContext, populated kFileScheme → GPU 0 ioctx during initialize(), and ordered teardown so registry clears before gpu_ioctxs_ — foundation for Plan 22.1-02's factory throw-policy flip.

## Tasks Completed

| Task | Name                                                                | Commit  | Files                                              |
| ---- | ------------------------------------------------------------------- | ------- | -------------------------------------------------- |
| 1    | Add datasource_registry_ member + accessor to SiriusContext header  | c76504c | src/include/sirius_context.hpp                     |
| 2    | Register kFileScheme -> GPU 0 ioctx at end of initialize()          | 4e404fb | src/sirius_context.cpp                             |
| 3    | Order shutdown — clear registry BEFORE gpu_ioctxs_ + mcp build gate | 78af57c | src/sirius_context.cpp                             |

All three tasks committed atomically with --no-verify (parallel-execution discipline; no other Wave 2 agents racing in this plan slot).

## Verification Results

### Per-task acceptance criteria — ALL PASS

**Task 1 (header):**
- `grep -cE "datasource_registry_" src/include/sirius_context.hpp` → 2 (member decl + accessor body) ✓
- `grep -cE "get_datasource_registry" src/include/sirius_context.hpp` → 1 (accessor decl/body) ✓
- `grep -cE '#include.*io/datasource_factory.hpp' src/include/sirius_context.hpp` → 1 ✓
- `grep -c "rmm::cuda_stream_default" src/include/sirius_context.hpp` → 0 ✓
- Combined-grep verify: 3 hits (>= 3) ✓

**Task 2 (registration):**
- `grep -cE 'register_ioctx' src/sirius_context.cpp` → 1 (>=1) ✓
- `grep -cE 'kFileScheme' src/sirius_context.cpp` → 6 (>=1; counts include the new comment block, log line, and constant decl) ✓
- `grep -n 'register_ioctx'` → line 334 (> 310, after IO-13 loop) ✓
- `grep -cE 'gpu_ioctxs_\.empty\(\)' src/sirius_context.cpp` → 2 (if-guard + throw message) ✓
- `grep -c 'rmm::cuda_stream_default' src/sirius_context.cpp` → 0 (unchanged from baseline) ✓

**Task 3 (teardown ordering + build):**
- `grep -cE 'datasource_registry_\.clear\(\)' src/sirius_context.cpp` → 1 ✓
- Registry-clear line 528 < gpu_ioctxs_-clear line 535 (in terminate() body) ✓
- mcp build exit 0 — `[109/109] Linking CXX executable extension/sirius/test/cpp/sirius_unittest` ✓
- `grep -rn "rmm::cuda_stream_default" src/ | wc -l` → 40 (unchanged phase-wide HYG-02 baseline) ✓

### Plan-wide success criteria — ALL PASS

- All 3 tasks pass acceptance criteria ✓
- mcp build exit 0 ✓
- Plan 22.1-02 unblocked — `sirius_ctx->get_datasource_registry().lookup("file")` will now return a non-null ioctx ✓
- HYG-02 invariant = 40 preserved ✓
- Cucascade pin unchanged at c666b21 ✓ (`git submodule status cucascade` → c666b21926dec70b26a1febd509435635bea8deb)

## File Diff Summary

### src/include/sirius_context.hpp (+21 LOC)
- Added `#include <io/datasource_factory.hpp>` next to existing `#include <io/types.hpp>` (line 38-39).
- Added public accessor `get_datasource_registry() const -> sirius::io::datasource_registry const&` after `get_gpu_ioctxs()` (lines 204-214).
- Added private member `sirius::io::datasource_registry datasource_registry_;` immediately after `gpu_ioctxs_` declaration with explanatory comment (lines 312-318).

### src/sirius_context.cpp (+37 LOC)
- Added `<algorithm>` include for `std::min_element` (line 47).
- Added kFileScheme registration block at the end of the per-GPU ioctx loop (lines 315-342, immediately before the MGPU-06 P2P enable block):
  - Guards on `!gpu_ioctxs_.empty()`
  - Picks lowest-numbered GPU via `std::min_element` lambda comparing `.first`
  - `register_ioctx(std::string{kFileScheme}, lowest_gpu->second)`
  - `spdlog::info` log line announcing the registration
  - Else-branch throws `std::runtime_error` with explicit Phase 19 IO-13 invariant message
- Added `datasource_registry_.clear()` in `terminate()` (line 528) immediately before the existing `gpu_ioctxs_.clear()` line (now line 535) with explanatory comment.

## Build Output

mcp__project-commands__run_command build → exit 0:
- 109 build steps; final step: `[109/109] Linking CXX executable extension/sirius/test/cpp/sirius_unittest`
- Only warnings: pre-existing SPDLOG_ACTIVE_LEVEL override and Rust telemetry-bridge `operator` keyword warning (both unchanged from baseline).
- No errors related to datasource_registry, sirius_context, or any modified file.

## Lookup Behavior Verification

This plan only proves compile-correctness — the runtime lookup behavior (kFileScheme entry returns a non-null sirius_ioctx) will be exercised by Plan 22.1-02 when the factory's `create()` call site invokes `registry.lookup("file")`. The static contract is now in place:
- Header declares `get_datasource_registry()` returning `sirius::io::datasource_registry const&` ✓
- Source populates `datasource_registry_.register_ioctx("file", lowest_gpu->second)` after Phase 19 IO-13 produces gpu_ioctxs_ ✓
- The registry's `lookup("file")` will return a valid `std::shared_ptr<sirius_ioctx>` post-initialize() because `register_ioctx` stores the shared_ptr under the lower-cased scheme key (per datasource_factory.cpp:52-59).

## Deviations from Plan

None — plan executed exactly as written.

The plan's `<action>` block in Task 1 specified `>= 3` for the combined-grep verify; my edit produced exactly 3 (1 accessor decl/body, 1 member decl, 1 reference in the accessor body that returns the member). All acceptance criteria pass at the spec'd thresholds.

The plan's `<action>` block in Task 2 left optional `<algorithm>` inclusion to my discretion ("If `<algorithm>` is not already included near the top..."). I added it; pre-existing includes did not transitively pull `<algorithm>` in a way that exposed `std::min_element` cleanly, and explicit inclusion is the project convention.

The plan's Task 3 `<action>` block also specified placing `datasource_registry_.clear()` "immediately BEFORE" `gpu_ioctxs_.clear()` in the same scope — I placed it 7 lines before, with the existing IO-13 comment block intact between them. The intent (registry refs released before owners) is satisfied; the visual separation is minor and preserves the Phase 19 IO-13 documentation context.

## Auth Gates

None — no checkpoints, no auth-gate pauses.

## Self-Check: PASSED

- File `src/include/sirius_context.hpp` exists ✓ (modified)
- File `src/sirius_context.cpp` exists ✓ (modified)
- Commit `c76504c` exists in `git log --oneline --all` ✓
- Commit `4e404fb` exists ✓
- Commit `78af57c` exists ✓
- mcp build exit 0 ✓
- HYG-02 = 40 ✓
- Cucascade pin = c666b21 ✓

## Forward-Compatibility for 22.1-02

Plan 22.1-02 will:
1. Call `sirius_ctx->get_datasource_registry()` from `datasource_factory::create()` (or its caller).
2. Flip the `if (p.scheme == kFileScheme) { return cudf::io::datasource::create(...); }` branch in `src/io/datasource_factory.cpp:115-125` to instead lookup the scheme in the registry and call `ioctx->make_datasource(io_object)`.
3. Add the same flip to `create_for_parquet_scan` (line 100-113) and to all 7 bypass sites enumerated in 22.1-CONTEXT.md D-01.
4. Run grep gate `grep -rn 'cudf::io::datasource::create\|cudf::io::source_info{' src/` and verify zero hits.

Without this Plan 22.1-01 landed first, the throw-policy flip in 22.1-02 would crash every parquet read because no scheme would be registered. After this plan, the kFileScheme entry is guaranteed populated by SiriusContext::initialize() before any query runs.
