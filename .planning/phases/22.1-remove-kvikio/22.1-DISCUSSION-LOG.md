# Phase 22.1: Remove kvikio - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-07
**Phase:** 22.1-remove-kvikio
**Areas discussed:** K.6 confirmation experiment, Iceberg metadata read scope, datasource_factory policy on unknown scheme, Verification gauntlet scope

---

## K.6 confirmation experiment

| Option | Description | Selected |
|--------|-------------|----------|
| Run now (Recommended) | Execute SF1 Q11 num_gpus=2 once via Bash + duckdb (sandbox disabled for GPU access). Capture row count, result diff vs 1-GPU baseline, cudaSetDevice(-1) presence in stderr, mgpu-audit per-GPU task counts. ~5 min wall-clock. | ✓ |
| Skip — verify in gauntlet | Assume K.6 is multi-GPU-rooted; verify post-migration via gauntlet's Q11 functional check. | |

**User's choice:** Run now.

**Experiment result:** SF1 Q11 num_gpus=2 returned 51 result rows + header, IDENTICAL after sort to 1-GPU baseline. Only diff: `Run Time (s): real 0.442` (1-GPU) vs `0.460` (2-GPU). 0 `cudaSetDevice(-1)` or "invalid device ordinal" hits in stderr. K.6's empty-fallback does NOT reproduce at SF1.

**Logs:** `/tmp/claude/p22_1_k6_exp/sf1_q11_{1,2}gpu.{csv,stderr,sorted}` and `/tmp/claude/p22_1_k6_exp/log_{1,2}gpu/sirius_2026-05-07.log`.

---

## K.6 conditional gate

| Option | Description | Selected |
|--------|-------------|----------|
| Yes (Recommended) | Hard gate: SF1 Q11 num_gpus=2 must return same row count + result hash as 1-GPU baseline post-22.1. | ✓ |
| Soft gate (advisory only) | Record in VERDICT.md but don't gate. | |
| Defer — only gate at SF100 | Gate only on SF100 Q11 num_gpus=2 functional. | |

**User's choice:** Yes (hard gate).

**Effective interpretation:** Since SF1 Q11 num_gpus=2 already passes today (per experiment), this becomes a no-regression gate (D-12). The K.6 closure check itself moves to SF100 Q11 num_gpus=2 (D-13) as advisory because its current broken state is data-size-bound, not multi-GPU-bound.

---

## Iceberg metadata read scope (sites #3 + #4)

| Option | Description | Selected |
|--------|-------------|----------|
| Single-GPU ioctx for now (Recommended) | Use GPU 0 sirius_ioctx for read_equality_delete_file + footer extraction. Closes the kvikio half of IO-MGPU-02; multi-GPU residency becomes a renamed follow-up. Smallest 22.1 footprint. | ✓ |
| Full per-GPU ioctx now | Plumb full gpu_ioctxs map; closes IO-MGPU-02 completely. ~1 extra plan in 22.1. | |
| Deferred entirely | Don't touch iceberg in 22.1; gate the bypass-grep to exclude these sites. Violates the kvikio-out rule. | |

**User's choice:** Single-GPU ioctx for now.

**Captured as:** D-06 in CONTEXT.md. Renamed follow-up: IO-MGPU-04 (multi-GPU iceberg residency).

---

## datasource_factory policy on unknown scheme (sites #5 + #6)

| Option | Description | Selected |
|--------|-------------|----------|
| Throw loud (Recommended) | throw std::runtime_error("datasource_factory: no ioctx registered for scheme 'X' — kvikio path is forbidden"). Makes kvikio-free invariant impossible to violate by future code. | ✓ |
| Require all schemes register at startup | Same throw + startup validation that supported schemes have ioctxs. More defensive. | |
| Warn + fall back | Loud WARN then fall back to cudf path-based create. Risk: silent kvikio reintroduction. | |

**User's choice:** Throw loud.

**Captured as:** D-07 + D-09 in CONTEXT.md. kFileScheme MUST be registered at startup (D-10) — the registration itself is part of 22.1's scope.

---

## Verification gauntlet scope

| Option | Description | Selected |
|--------|-------------|----------|
| Full v1.4 ship gate + 22.1 new gates (Recommended) | Phase 22's full gauntlet (REG-01..06) + 22.1 new gates (bypass-grep zero, sanitizer Cluster A = 0, SF1 Q11 functional, SF100 Q11 advisory). ~30-45 min. Maximum confidence given hot-path changes. | ✓ |
| Minimal-plus-mgpu | [mgpu] + [pin_mgpu] + [mgpu_stress] + bypass + sanitizer Cluster A + SF100 Q11 functional + HYG-02. ~15-20 min. | |
| Minimal | bypass + sanitizer Cluster A + SF100 Q11 + HYG-02 + [mgpu] 16/16. ~10 min. | |

**User's choice:** Full v1.4 ship gate + 22.1 new gates.

**Captured as:** D-14 in CONTEXT.md.

---

## Claude's Discretion

- Exact constructor signature change shape on `sirius_gpu_parquet_scan_operator` (whether to take `gpu_ioctxs` map directly or accept a small wrapper struct)
- How `datasource_factory::create_for_parquet_scan` should consume the per-GPU map vs a single ioctx
- Test-fixture mock-vs-real ioctx for site #7 (parquet_split_provider unit-test fallback)
- Per-site commit boundary granularity (per-site vs per-file vs per-plan)
- Order in which the 7 sites land within 22.1's plan structure — suggested ordering in CONTEXT.md is factory first (sites #5 + #6), then leaf sites in any order, then test-fallback cleanup; planner can adjust based on file collision risk

## Deferred Ideas

(See CONTEXT.md `<deferred>` section.)
