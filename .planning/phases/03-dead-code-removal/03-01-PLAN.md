---
phase: 03-dead-code-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/include/pipeline/gpu_pipeline_queue.hpp
  - src/pipeline/gpu_pipeline_queue.cpp
  - src/include/pipeline/pipeline_queue.hpp
  - src/pipeline/pipeline_queue.cpp
  - src/include/op/scan/duckdb_scan_task_queue.hpp
  - src/include/parallel/task_queue.hpp
  - CMakeLists.txt
  - test/cpp/scan/test_parquet_scan_task.cpp
  - test/cpp/pipeline/README.md
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03
  - CLEAN-04

must_haves:
  truths:
    - "gpu_pipeline_queue class no longer exists in the source tree"
    - "pipeline_queue class no longer exists in the source tree"
    - "duckdb_scan_task_queue class no longer exists in the source tree"
    - "itask_queue interface no longer exists in the source tree"
    - "No references to any removed class remain anywhere in the codebase"
    - "The project builds successfully after removal"
    - "All existing tests pass after removal"
  artifacts:
    - path: "src/include/pipeline/gpu_pipeline_queue.hpp"
      provides: "MUST NOT EXIST after plan completion"
    - path: "src/pipeline/gpu_pipeline_queue.cpp"
      provides: "MUST NOT EXIST after plan completion"
    - path: "src/include/pipeline/pipeline_queue.hpp"
      provides: "MUST NOT EXIST after plan completion"
    - path: "src/pipeline/pipeline_queue.cpp"
      provides: "MUST NOT EXIST after plan completion"
    - path: "src/include/op/scan/duckdb_scan_task_queue.hpp"
      provides: "MUST NOT EXIST after plan completion"
    - path: "src/include/parallel/task_queue.hpp"
      provides: "MUST NOT EXIST after plan completion"
    - path: "CMakeLists.txt"
      provides: "No references to removed .cpp files"
      contains: "NOT gpu_pipeline_queue.cpp, NOT pipeline_queue.cpp"
  key_links:
    - from: "CMakeLists.txt"
      to: "removed .cpp files"
      via: "source list entries must be deleted"
      pattern: "gpu_pipeline_queue\\.cpp|pipeline_queue\\.cpp"
    - from: "test/cpp/scan/test_parquet_scan_task.cpp"
      to: "duckdb_scan_task_queue.hpp"
      via: "unused #include must be deleted"
      pattern: "duckdb_scan_task_queue"
---

<objective>
Remove four legacy unused queue classes from the Sirius codebase: gpu_pipeline_queue, pipeline_queue, duckdb_scan_task_queue, and itask_queue (the base interface).

Purpose: These queue classes are dead code -- none are instantiated or referenced by production code. They have been superseded by interruptible_mpmc (used in pipeline_executor and itask_executor). Removing them simplifies the codebase before Phase 4 replaces interruptible_mpmc with inspectable_mpsc.

Output: Six source files deleted, CMakeLists.txt cleaned, stale test include removed, README updated.
</objective>

<execution_context>
@/home/william/repos2/sirius/.claude/get-shit-done/workflows/execute-plan.md
@/home/william/repos2/sirius/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete legacy queue files and clean all references</name>
  <files>
    src/include/pipeline/gpu_pipeline_queue.hpp
    src/pipeline/gpu_pipeline_queue.cpp
    src/include/pipeline/pipeline_queue.hpp
    src/pipeline/pipeline_queue.cpp
    src/include/op/scan/duckdb_scan_task_queue.hpp
    src/include/parallel/task_queue.hpp
    CMakeLists.txt
    test/cpp/scan/test_parquet_scan_task.cpp
    test/cpp/pipeline/README.md
  </files>
  <read_first>
    CMakeLists.txt
    test/cpp/scan/test_parquet_scan_task.cpp
    test/cpp/pipeline/README.md
    src/include/pipeline/gpu_pipeline_queue.hpp
    src/include/pipeline/pipeline_queue.hpp
    src/include/op/scan/duckdb_scan_task_queue.hpp
    src/include/parallel/task_queue.hpp
  </read_first>
  <action>
**Per CLEAN-01:** Delete `src/include/pipeline/gpu_pipeline_queue.hpp` and `src/pipeline/gpu_pipeline_queue.cpp`. These files define `sirius::pipeline::gpu_pipeline_queue` which inherits from `itask_queue` but is never instantiated anywhere.

**Per CLEAN-02:** Delete `src/include/pipeline/pipeline_queue.hpp` and `src/pipeline/pipeline_queue.cpp`. These files define `sirius::pipeline::pipeline_queue` which inherits from `itask_queue` but is never instantiated anywhere.

**Per CLEAN-03:** Delete `src/include/op/scan/duckdb_scan_task_queue.hpp`. This header-only file defines `sirius::op::scan::duckdb_scan_task_queue` which inherits from `itask_queue` but is never instantiated anywhere.

**Per CLEAN-04:** Delete `src/include/parallel/task_queue.hpp`. This header-only file defines the `sirius::parallel::itask_queue` abstract interface. All three concrete implementations are being deleted in this same task, so the interface is also dead code.

**Clean CMakeLists.txt:** Remove two lines from the `EXTENSION_SOURCES` list in the root `CMakeLists.txt`:
- Remove line `src/pipeline/gpu_pipeline_queue.cpp` (around line 171)
- Remove line `src/pipeline/pipeline_queue.cpp` (around line 174)
Do NOT remove any other lines. The surrounding lines (`src/pipeline/gpu_pipeline_executor.cpp`, `src/pipeline/gpu_pipeline_task.cpp`, `src/pipeline/pipeline_executor.cpp`, `src/pipeline/sirius_meta_pipeline.cpp`) must remain.

**Clean test include:** In `test/cpp/scan/test_parquet_scan_task.cpp`, remove line 24: `#include <op/scan/duckdb_scan_task_queue.hpp>`. This is an unused include -- the test file uses `itask_executor` (which has its own `interruptible_mpmc` queue), not `duckdb_scan_task_queue`. No other changes to this file.

**Clean README:** In `test/cpp/pipeline/README.md`, remove line 48 which references `./build/release/test/unittest "[pipeline_queue]"`. The `[pipeline_queue]` Catch2 tag in `test_pipeline_executor.cpp` tests `pipeline_executor` behavior (which uses `interruptible_mpmc` internally), not the `pipeline_queue` class being deleted. The tag name in the test case and its entry in the README are misleading but the test itself remains valid. Only remove the README reference line.

**Post-deletion verification:** Run `grep -rn "gpu_pipeline_queue\|pipeline_queue\|duckdb_scan_task_queue\|itask_queue\|task_queue\.hpp" src/ test/ CMakeLists.txt --include="*.hpp" --include="*.cpp" --include="*.cu" --include="*.txt" --include="*.md"` to confirm zero remaining references (excluding `.claude/worktrees/` which are separate worktrees).
  </action>
  <verify>
    <automated>bash -c 'cd /home/william/repos2/sirius && test ! -f src/include/pipeline/gpu_pipeline_queue.hpp && test ! -f src/pipeline/gpu_pipeline_queue.cpp && test ! -f src/include/pipeline/pipeline_queue.hpp && test ! -f src/pipeline/pipeline_queue.cpp && test ! -f src/include/op/scan/duckdb_scan_task_queue.hpp && test ! -f src/include/parallel/task_queue.hpp && ! grep -rn "gpu_pipeline_queue\|duckdb_scan_task_queue\|itask_queue\|task_queue\.hpp" src/ test/ CMakeLists.txt --include="*.hpp" --include="*.cpp" --include="*.cu" --include="*.txt" --include="*.md" 2>/dev/null && echo "PASS: all files deleted and no references remain"'</automated>
  </verify>
  <acceptance_criteria>
    - src/include/pipeline/gpu_pipeline_queue.hpp does not exist
    - src/pipeline/gpu_pipeline_queue.cpp does not exist
    - src/include/pipeline/pipeline_queue.hpp does not exist
    - src/pipeline/pipeline_queue.cpp does not exist
    - src/include/op/scan/duckdb_scan_task_queue.hpp does not exist
    - src/include/parallel/task_queue.hpp does not exist
    - CMakeLists.txt does NOT contain the string "gpu_pipeline_queue.cpp"
    - CMakeLists.txt does NOT contain the string "pipeline_queue.cpp" (as a standalone source entry)
    - test/cpp/scan/test_parquet_scan_task.cpp does NOT contain "duckdb_scan_task_queue"
    - grep for "gpu_pipeline_queue|duckdb_scan_task_queue|itask_queue|task_queue.hpp" across src/ test/ CMakeLists.txt returns zero matches
  </acceptance_criteria>
  <done>All six legacy queue files deleted. CMakeLists.txt source list cleaned. Stale test include removed. README cleaned. Zero references to removed classes remain in source, test, or build files.</done>
</task>

<task type="auto">
  <name>Task 2: Build project and run all tests to confirm no regressions</name>
  <files></files>
  <read_first>
    CMakeLists.txt
  </read_first>
  <action>
Build the project from scratch to confirm the removal causes no compilation or link errors:

1. Clean the build directory: `rm -rf build`
2. Build: `CMAKE_BUILD_PARALLEL_LEVEL=$(nproc) make`
3. Wait for build to complete successfully. The build produces:
   - `build/release/extension/sirius/sirius.duckdb_extension`
   - `build/release/extension/sirius/test/cpp/sirius_unittest`

If the build fails with "file not found" errors referencing any of the deleted files, it means a reference was missed in Task 1. Fix the reference and rebuild.

4. Run the unit test suite: `build/release/extension/sirius/test/cpp/sirius_unittest`
   All existing tests must pass. In particular, verify:
   - `[pipeline_executor]` tag tests still pass (they use `pipeline_executor` which uses `interruptible_mpmc`, not the deleted queues)
   - `[pipeline_queue]` tag test still passes (it tests empty-queue shutdown on `pipeline_executor`)
   - Parquet scan tests still pass (the unused include was removed but no functionality changed)

5. Run the SQL logic tests: `make test`
   All end-to-end tests must pass.
  </action>
  <verify>
    <automated>bash -c 'cd /home/william/repos2/sirius && CMAKE_BUILD_PARALLEL_LEVEL=$(nproc) make 2>&1 | tail -5 && build/release/extension/sirius/test/cpp/sirius_unittest "[pipeline_executor]" 2>&1 | tail -3 && echo "BUILD AND TESTS PASS"'</automated>
  </verify>
  <acceptance_criteria>
    - `make` exits with code 0 (build succeeds)
    - `build/release/extension/sirius/sirius.duckdb_extension` exists
    - `build/release/extension/sirius/test/cpp/sirius_unittest` exits with code 0
    - `build/release/extension/sirius/test/cpp/sirius_unittest "[pipeline_executor]"` exits with code 0
    - `make test` exits with code 0
  </acceptance_criteria>
  <done>Project builds clean with zero errors. All unit tests pass. All SQL logic tests pass. The dead code removal introduced no regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected -- this plan only deletes unused code and cleans build/test references. No new code is introduced, no interfaces change, no data flows are modified.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | build system | accept | Removing dead code from CMakeLists.txt reduces attack surface. No new code introduced. Build + test verification confirms no functionality change. |
</threat_model>

<verification>
1. `find src/ -name "*queue*" -not -path "*/exec/*"` returns zero results (only exec/interruptible_mpmc.hpp and exec/inspectable_mpsc.hpp should remain under exec/)
2. `grep -rn "itask_queue\|gpu_pipeline_queue\|pipeline_queue\|duckdb_scan_task_queue\|task_queue\.hpp" src/ test/ CMakeLists.txt` returns zero matches
3. `CMAKE_BUILD_PARALLEL_LEVEL=$(nproc) make` succeeds
4. `build/release/extension/sirius/test/cpp/sirius_unittest` passes all tests
5. `make test` passes all SQL logic tests
</verification>

<success_criteria>
- The four legacy queue classes (gpu_pipeline_queue, pipeline_queue, duckdb_scan_task_queue, itask_queue) no longer exist in the source tree
- No references to any of the removed classes remain anywhere in the codebase (headers, source, CMake, tests)
- The project builds successfully after removal
- All existing unit tests and SQL logic tests pass after removal
</success_criteria>

<output>
After completion, create `.planning/phases/03-dead-code-removal/03-01-SUMMARY.md`
</output>
