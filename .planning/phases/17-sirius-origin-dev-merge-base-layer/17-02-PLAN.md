---
phase: 17-sirius-origin-dev-merge-base-layer
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - CMakeLists.txt
  - cucascade
  - src/expression_executor/gpu_expression_executor.cpp
  - src/include/creator/task_creator.hpp
  - src/include/exec/config.hpp
  - src/include/op/scan/parquet_scan_operator_data.hpp
  - src/include/op/scan/sirius_parquet_metadata_scan_operator.hpp
  - src/op/scan/sirius_gpu_parquet_scan_operator.cpp
  - src/op/sirius_physical_table_scan.cpp
  - src/pipeline/sirius_pipeline_converter.cpp
  - src/scan_manager/parquet_split_provider.cpp
  - .planning/phases/17-sirius-origin-dev-merge-base-layer/17-MERGE-LOG.md
autonomous: true
requirements: [MERGE-01, MERGE-02, MERGE-04]
must_haves:
  truths:
    - "git merge --no-ff origin/dev was run; a single merge commit absorbs all 7 dev commits"
    - "All 11 conflict files resolved per CONTEXT.md D-D1..D-D6 and D-B1/B2 recipes"
    - "cucascade submodule pin remains 1c1e648 (defended via git checkout --ours)"
    - "PR #739's file changes are intentionally NOT applied (only the commit is absorbed bookkeeping-only)"
    - "sirius_parquet_metadata_scan_operator.hpp is removed from the working tree (deletion accepted after Phase 13 extraction completed in 17-01)"
    - "src/scan_manager/parquet_split_provider.cpp exists in the working tree (net-new from #731)"
  artifacts:
    - path: "src/scan_manager/parquet_split_provider.cpp"
      provides: "PR #731's net-new split provider; taken from origin/dev as-is"
      contains: "parquet_split_provider"
    - path: "CMakeLists.txt"
      provides: "Combined build config — multi-GPU runtime config (ours) + scan-manager dirs (theirs)"
    - path: "src/include/exec/config.hpp"
      provides: "Combined runtime config — multi-GPU + scan-manager"
    - path: "src/include/creator/task_creator.hpp"
      provides: "Combined header preserving _no_pref_rr_counter context (ours) + dev's task-creator changes"
    - path: "src/include/op/scan/parquet_scan_operator_data.hpp"
      provides: "Combined operator-data header (ours preserved + dev's structural changes)"
    - path: ".planning/phases/17-sirius-origin-dev-merge-base-layer/17-MERGE-LOG.md"
      provides: "Section A populated with per-file resolution outcomes"
  key_links:
    - from: "merge commit"
      to: "all 7 dev commits"
      via: "git merge --no-ff (single commit absorbs all)"
      pattern: "git log --oneline --merges -1"
    - from: "cucascade gitlink in tree"
      to: "1c1e648a282a06747328c78f62d2d676ce51a8ce"
      via: "git checkout --ours cucascade"
      pattern: "git ls-tree HEAD cucascade"
    - from: "sirius_gpu_parquet_scan_operator.cpp"
      to: "Phase 20 SM-01..03 TODOs"
      via: "TODO comments inserted at SCHED-RR / writer_stream / _batch_gpu_affinity sites"
      pattern: "TODO\\(v1.4 Phase 20"
---

<objective>
Execute the `git merge --no-ff origin/dev` operation and resolve all 11 conflict files mechanically per CONTEXT.md D-D1..D-D6 and D-B1/B2 recipes. Defend the cucascade submodule pin at `1c1e648`. Accept the deletion of `sirius_parquet_metadata_scan_operator.hpp` (Phase 13 hooks already extracted in 17-01). Annotate operator files with TODO comments scoped to Phase 18-20 follow-up. Create the merge commit and populate Section A of `17-MERGE-LOG.md`.

Purpose: This is the load-bearing plan for MERGE-01, MERGE-02, MERGE-04. It moves Sirius's tree to the merged shape that Phase 18 will then RAII-migrate.

Output:
- A single merge commit on `feature/single-node-multi-gpu2` absorbing the 7 origin/dev commits
- All 11 conflict files in their resolved post-merge state
- `17-MERGE-LOG.md` Section A populated with per-file resolution outcomes
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-sirius-origin-dev-merge-base-layer/17-CONTEXT.md
@.planning/phases/17-sirius-origin-dev-merge-base-layer/17-PHASE-13-EXTRACT.md
@.planning/phases/17-sirius-origin-dev-merge-base-layer/17-MERGE-LOG.md
@.planning/research/PITFALLS.md
@CLAUDE.md

<interfaces>
<!-- File shapes the executor will encounter during conflict resolution -->
<!-- These are the OURS (HEAD) and THEIRS (origin/dev) snapshots referenced by CONTEXT.md D-D1..D-D6 -->

OURS — current HEAD state (Phase 16 tip):
- src/include/op/scan/sirius_parquet_metadata_scan_operator.hpp — exists, 232 lines (Phases 5/6 + Phase 13 era)
- src/include/op/scan/parquet_scan_operator_data.hpp — exists; structural carrier
- src/include/op/scan/duckdb_scan_executor.hpp:218 — `std::unordered_map<uint64_t, int> _batch_gpu_affinity;` (Phase 9 — owns the affinity map; this file is NOT in the conflict set, but referenced by D-D4)
- src/include/pipeline/task_scheduler.hpp — `_no_pref_rr_counter` defined here (line ~210; Phase 14)
- src/pipeline/task_scheduler.cpp:156 — `// Reset SCHED-RR counter` block in prepare_for_query
- src/pipeline/task_scheduler.cpp:253 — `// SCHED-RR: distribute preference-less source tasks` block in management_eventloop
- cucascade submodule pin = `1c1e648a282a06747328c78f62d2d676ce51a8ce` (Phase 16 verified)
- src/scan_manager/ — DOES NOT exist
- src/include/scan_manager/ — DOES NOT exist
- src/include/io/cucascade_datasource.hpp — exists (will be retired in Phase 19, NOT this phase)

THEIRS — origin/dev tip (PR #739 + #675 + #731 + #721 + #733 + #734 + #735):
- src/include/op/scan/sirius_parquet_metadata_scan_operator.hpp — DELETED by #731
- src/scan_manager/parquet_split_provider.cpp — net-new from #731 (no local version)
- cucascade submodule pin = `0cd4a6a` (PR #112 tip, PRE-#117 — accepting this would lose Phase 16 work)
- 7 commits ahead per `git rev-list --count HEAD..origin/dev`

CONFLICT INVENTORY (11 files, per `git merge-tree` and CONTEXT.md):
1. CMakeLists.txt — D-D1 (combine — keep both sides' additions)
2. cucascade — D-B1/B2 (keep ours; pin must remain 1c1e648)
3. src/expression_executor/gpu_expression_executor.cpp — D-D5 (take dev's)
4. src/include/creator/task_creator.hpp — D-D4 (combine; preserve _no_pref_rr_counter context)
5. src/include/exec/config.hpp — D-D1 (combine)
6. src/include/op/scan/parquet_scan_operator_data.hpp — D-D4 (combine)
7. src/include/op/scan/sirius_parquet_metadata_scan_operator.hpp — D-D6 (accept deletion AFTER 17-01 extraction)
8. src/op/scan/sirius_gpu_parquet_scan_operator.cpp — D-D3 (take dev's; add TODOs)
9. src/op/sirius_physical_table_scan.cpp — D-D3 (take dev's; add TODOs)
10. src/pipeline/sirius_pipeline_converter.cpp — D-D3 (take dev's; add TODOs)
11. src/scan_manager/parquet_split_provider.cpp — D-D2 (take dev's net-new file)

</interfaces>
</context>

<read_first>
1. `.planning/phases/17-sirius-origin-dev-merge-base-layer/17-CONTEXT.md` — locked decisions D-A1..D-A4 (merge mechanics), D-B1/B2 (cucascade), D-D1..D-D6 (per-file recipes), D-G6 (cucascade pin gate)
2. `.planning/phases/17-sirius-origin-dev-merge-base-layer/17-PHASE-13-EXTRACT.md` — verify it was committed by 17-01
3. `.planning/phases/17-sirius-origin-dev-merge-base-layer/17-MERGE-LOG.md` — Section A skeleton to populate
4. `.planning/research/PITFALLS.md` — Pitfall 7 (PR #739 × #117 ordering — CRITICAL: do NOT apply #739's file changes here) and Pitfall 10 (Phase 13 in deleted file — extraction must be done in 17-01 BEFORE this plan runs)
5. Run `git rev-parse phase17-pre-merge-backup && git rev-parse HEAD` — both must match (otherwise 17-01 didn't run or HEAD has moved)
6. Run `git log --oneline HEAD..origin/dev` — should list 7 commits to be absorbed: PRs #739, #675, #731, #721, #733, #734, #735
7. Run `git ls-tree HEAD cucascade` — must return `1c1e648a282a06747328c78f62d2d676ce51a8ce` BEFORE merge
8. For each of the 5 "take dev's" files (D-D3 trio + D-D2 + D-D5), run `git show origin/dev:<path>` to view dev's intended content before resolving
9. For each of the 3 "combine" files (D-D1 trio: CMakeLists, config.hpp, task_creator.hpp, parquet_scan_operator_data.hpp), run BOTH `cat <path>` (ours) AND `git show origin/dev:<path>` (theirs) to understand the additive merge
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Run git merge --no-ff origin/dev + resolve cucascade pin (D-B1/B2) and Phase 13 deletion (D-C3/D-D6) first</name>
  <files>
    cucascade
    src/include/op/scan/sirius_parquet_metadata_scan_operator.hpp
  </files>
  <action>
This task runs the merge and resolves the 2 highest-stakes conflicts (cucascade pin + Phase 13 deletion) FIRST so the rest of the resolution work happens against a known-good cucascade pin.

STEP A — Pre-merge state capture (write into 17-MERGE-LOG.md "Pre-merge state" section):
```
HEAD_SHA=$(git rev-parse HEAD)
DEV_SHA=$(git rev-parse origin/dev)
PRE_HYG02=$(grep -rc "rmm::cuda_stream_default" src/ 2>/dev/null | awk -F: '{s+=$2} END {print s}')
echo "HEAD_SHA=$HEAD_SHA"
echo "DEV_SHA=$DEV_SHA"
echo "PRE_HYG02=$PRE_HYG02 (Phase 14 baseline = 40)"
```
Update `.planning/phases/17-sirius-origin-dev-merge-base-layer/17-MERGE-LOG.md` "Pre-merge state" bullets with these three values (plus the backup ref SHA from `git rev-parse phase17-pre-merge-backup`).

STEP B — Run the merge (D-A1 + D-A3):
```
git merge --no-ff origin/dev -m "merge(17-02): origin/dev into feature/single-node-multi-gpu2 (MERGE-01) — absorbs PRs #739/#675/#731/#721/#733/#734/#735; #739 is bookkeeping-only per P7 — file changes deferred to Phase 18"
```
This WILL exit non-zero with "Automatic merge failed; fix conflicts and then commit the result." That is EXPECTED — the 11 conflict files need manual resolution. DO NOT abort.

If the merge command fails for an UNEXPECTED reason (e.g., uncommitted changes, detached HEAD), abort with `git merge --abort` and surface the error to the user — do NOT continue.

STEP C — Verify the conflict surface matches CONTEXT.md inventory:
```
git diff --name-only --diff-filter=U
```
The output should be EXACTLY these 11 files (sort doesn't matter):
- CMakeLists.txt
- src/expression_executor/gpu_expression_executor.cpp
- src/include/creator/task_creator.hpp
- src/include/exec/config.hpp
- src/include/op/scan/parquet_scan_operator_data.hpp
- src/include/op/scan/sirius_gpu_parquet_scan_operator.cpp
- src/include/op/scan/sirius_parquet_metadata_scan_operator.hpp
- src/op/scan/sirius_gpu_parquet_scan_operator.cpp
- src/op/sirius_physical_table_scan.cpp
- src/pipeline/sirius_pipeline_converter.cpp
- src/scan_manager/parquet_split_provider.cpp

The cucascade submodule conflict is reported separately by `git status` (look for "modified content" or "both modified" entries on `cucascade`). Verify with:
```
git status --short | grep -E "^(UU|AU|UA|DU|UD|DD|AA) " | sort
```
Should list 11 source-file conflicts plus 1 submodule conflict.

If the count differs (more or fewer files), STOP and surface to user — the conflict surface drift indicates origin/dev has moved or our HEAD has moved since CONTEXT.md was authored.

STEP D — Resolve cucascade submodule (D-B1/B2 — defend the pin):
```
git checkout --ours cucascade
git add cucascade
```
Verify immediately:
```
git ls-tree HEAD cucascade
```
Wait — HEAD doesn't change until commit. Verify the staged index instead:
```
git ls-files --stage cucascade
```
Should output: `160000 1c1e648a282a06747328c78f62d2d676ce51a8ce 0\tcucascade`

The `0` (stage zero) means the conflict is resolved. If you see stages 1/2/3, the resolution didn't take — re-run `git checkout --ours cucascade && git add cucascade`. If the SHA is anything OTHER than `1c1e648a282a06747328c78f62d2d676ce51a8ce`, STOP — Phase 16 work is at risk.

Append to `17-MERGE-LOG.md` Section A.2 "Resolution outcome": "Resolved by `git checkout --ours cucascade && git add cucascade`. Staged index confirms `160000 1c1e648a282a06747328c78f62d2d676ce51a8ce 0 cucascade`. Phase 16 pin defended."

STEP E — Resolve sirius_parquet_metadata_scan_operator.hpp (D-D6 + D-C3):
This is the modify/delete conflict. Origin/dev (#731) deletes the file; we have local Phase 5/6/13 edits to it. Per D-C3, the extraction was already committed in 17-01 — so we accept the deletion now.

Verify the extraction file exists and is committed first:
```
git log --oneline -1 -- .planning/phases/17-sirius-origin-dev-merge-base-layer/17-PHASE-13-EXTRACT.md
```
If empty, STOP and surface — 17-01 did not run or its commit was lost. Do NOT proceed with deletion.

Accept the deletion:
```
git rm src/include/op/scan/sirius_parquet_metadata_scan_operator.hpp
```
Verify:
```
git status --short | grep "sirius_parquet_metadata_scan_operator.hpp"
```
Should output: `D  src/include/op/scan/sirius_parquet_metadata_scan_operator.hpp` (capital D = staged deletion).

Append to `17-MERGE-LOG.md` Section A.7 "Resolution outcome": "Accepted deletion via `git rm`. Phase 13 stream-lineage extraction confirmed in `17-PHASE-13-EXTRACT.md` (commit `<sha>`). Re-attachment scheduled for Phase 20 SM-03 in `src/op/scan/sirius_gpu_parquet_scan_operator.cpp::execute()`."

STEP F — Quick sanity check before moving to Task 2:
```
git diff --name-only --diff-filter=U | wc -l
```
Should now be 9 (the original 11 minus cucascade and metadata_scan_operator.hpp; cucascade isn't in `--diff-filter=U` source-file output anyway, so this should drop from 11 to 10 if cucascade was originally counted, or 11→10 if it wasn't, depending on git version — accept either 9 or 10). Goal: the 9-10 source-file conflicts that Task 2 resolves.

NO `rmm::cuda_stream_default` introduced (this task only stages existing files / runs git commands).
  </action>
  <verify>
    <automated>git status --short | grep -E "^(UU|AU|UA) " | grep -v "sirius_parquet_metadata_scan_operator.hpp" | wc -l | awk '$1 &gt;= 8 &amp;&amp; $1 &lt;= 10 {exit 0} {exit 1}' &amp;&amp; git ls-files --stage cucascade | grep -q "1c1e648a282a06747328c78f62d2d676ce51a8ce" &amp;&amp; git status --short | grep -q "^D.*sirius_parquet_metadata_scan_operator.hpp"</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files --stage cucascade` outputs `160000 1c1e648a282a06747328c78f62d2d676ce51a8ce 0\tcucascade` (stage 0 = resolved; SHA matches Phase 16 pin)
    - `git status --short | grep "sirius_parquet_metadata_scan_operator.hpp"` shows `D ` (staged deletion, no conflict markers)
    - `git diff --name-only --diff-filter=U` lists between 8 and 10 remaining unresolved source-file conflicts (the original 11 minus the 2 resolved here, give or take cucascade's reporting)
    - `17-MERGE-LOG.md` Section A.2 and A.7 "Resolution outcome" lines are populated (no longer `<filled>`)
    - The pre-merge state captured in 17-MERGE-LOG.md "Pre-merge state" section (HEAD SHA, dev SHA, pre-merge HYG-02 count, backup ref SHA all populated)
  </acceptance_criteria>
  <done>
    Merge is in progress (commit not yet created). cucascade pin defended at `1c1e648`. Phase 13 file deletion accepted (extraction was already committed by 17-01). 17-MERGE-LOG.md Section A.2 and A.7 populated. 8-10 source-file conflicts remain for Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Resolve remaining 9 conflict files per D-D1..D-D5 recipes + add Phase 18/20 TODOs</name>
  <files>
    CMakeLists.txt
    src/expression_executor/gpu_expression_executor.cpp
    src/include/creator/task_creator.hpp
    src/include/exec/config.hpp
    src/include/op/scan/parquet_scan_operator_data.hpp
    src/op/scan/sirius_gpu_parquet_scan_operator.cpp
    src/op/sirius_physical_table_scan.cpp
    src/pipeline/sirius_pipeline_converter.cpp
    src/scan_manager/parquet_split_provider.cpp
  </files>
  <action>
This task resolves the remaining 9 source-file conflicts mechanically per CONTEXT.md D-D1..D-D5 recipes and adds Phase 18/20 TODO annotations. Resolutions are processed in dependency order (build-config first, then operator files).

For each file, the resolution pattern is one of:
- **TAKE-DEV**: `git checkout --theirs <file> && git add <file>` — accept origin/dev's version verbatim, then add Phase 20 TODO comment(s) where applicable
- **COMBINE**: open the file in your editor, find each `<<<<<<< HEAD` ... `======= ` ... `>>>>>>> origin/dev` marker, manually merge so BOTH sides' additions are kept. Remove all conflict markers. `git add <file>`
- **TAKE-OURS**: not used in this task (cucascade was the only TAKE-OURS, handled in Task 1)

Process each file in this order:

### File 1 — `src/scan_manager/parquet_split_provider.cpp` (D-D2: TAKE-DEV — net-new)
```
git checkout --theirs src/scan_manager/parquet_split_provider.cpp
git add src/scan_manager/parquet_split_provider.cpp
```
Verify file exists and is staged:
```
test -f src/scan_manager/parquet_split_provider.cpp && git diff --cached --name-only | grep -q "src/scan_manager/parquet_split_provider.cpp"
```
NO TODOs needed here — this is net-new dev code; Phase 20 SM-01..03 will modify it then.

Append to 17-MERGE-LOG.md Section A.11 "Resolution outcome": "Took dev's net-new file via `git checkout --theirs`. File LOC: $(wc -l src/scan_manager/parquet_split_provider.cpp). No TODOs added (Phase 20 SM-01..03 will integrate v1.3 multi-GPU semantics)."

### File 2 — `CMakeLists.txt` (D-D1: COMBINE)
This is build configuration. Open `CMakeLists.txt`, find each `<<<<<<< HEAD` ... `>>>>>>> origin/dev` block, and merge so both sides' additions are kept. Specifically:
- If our side adds multi-GPU runtime config flags / source files, KEEP those lines
- If dev's side adds scan-manager source dirs (e.g., `src/scan_manager`), liburing detection, sirius_datasource source files, KEEP those lines
- For genuinely conflicting changes (e.g., both sides modify the same `set(...)` line), prefer the most-inclusive option (combine variables / source lists)

Run `grep -n "<<<<<<< \|======= \|>>>>>>> " CMakeLists.txt` after editing — must return zero. Then:
```
git add CMakeLists.txt
```
Append to 17-MERGE-LOG.md Section A.1 "Resolution outcome": "Combined per D-D1. Kept our multi-GPU runtime config lines [list specific anchor lines] + accepted dev's [scan_manager subdirs / liburing / sirius_datasource adds]. Markers removed."

### File 3 — `src/include/exec/config.hpp` (D-D1: COMBINE)
Same pattern as CMakeLists. Read the file's HEAD shape and dev shape:
```
git show :2:src/include/exec/config.hpp > /tmp/config_ours.hpp  # stage 2 = ours
git show :3:src/include/exec/config.hpp > /tmp/config_theirs.hpp  # stage 3 = theirs
```
Merge so both sides' configuration entries are kept. Verify zero conflict markers:
```
grep -n "<<<<<<< \|======= \|>>>>>>> " src/include/exec/config.hpp | wc -l
```
Must be 0. Then:
```
git add src/include/exec/config.hpp
```
Append to 17-MERGE-LOG.md Section A.5 "Resolution outcome".

### File 4 — `src/include/creator/task_creator.hpp` (D-D4: COMBINE; preserve _no_pref_rr_counter context)
Per D-D4: "preserve our `_no_pref_rr_counter` and `_batch_gpu_affinity` adds, accept dev's other changes."

Note: `_no_pref_rr_counter` itself lives in `src/include/pipeline/task_scheduler.hpp` (line ~210), NOT in `task_creator.hpp`. The CONTEXT.md guidance to "preserve `_no_pref_rr_counter`" in task_creator.hpp likely refers to any RELATED context (e.g., setter calls, references to the task scheduler's RR-counter API, or comments documenting the SCHED-RR contract). If you find such references in OURS but not THEIRS, KEEP them.

Process:
1. View ours: `git show :2:src/include/creator/task_creator.hpp > /tmp/tc_ours.hpp`
2. View theirs: `git show :3:src/include/creator/task_creator.hpp > /tmp/tc_theirs.hpp`
3. For each `<<<<<<< HEAD` ... `>>>>>>> origin/dev` block:
   - If our side mentions `_no_pref_rr_counter`, `_batch_gpu_affinity`, `SCHED-RR`, or has a comment block referencing Phase 14/Phase 9 — KEEP it
   - If dev's side adds new method signatures / fields for scan-manager / split-provider integration — KEEP those
   - If conflict is on the same field/method declaration — prefer the version with more context (typically dev's, which has the post-#731 type signatures)
4. Where dev's changes invalidate our adds (rare per D-D4), comment out the our-side line with: `// TODO(v1.4 Phase 20 — SM-01): re-attach SCHED-RR / affinity in scan-manager world`
5. Verify zero conflict markers and stage:
```
grep -n "<<<<<<< \|======= \|>>>>>>> " src/include/creator/task_creator.hpp
git add src/include/creator/task_creator.hpp
```
Append to 17-MERGE-LOG.md Section A.4 "Resolution outcome".

### File 5 — `src/include/op/scan/parquet_scan_operator_data.hpp` (D-D4: COMBINE)
Same combine pattern as task_creator.hpp. Note from CONTEXT.md investigation: `_batch_gpu_affinity` itself is in `duckdb_scan_executor.hpp:218`, not this file — so any "preserve our _batch_gpu_affinity" hits here are likely structural context references, not the field itself. Keep both sides' additions; comment-out only when genuinely incompatible. Add the standard TODO comment if needed:
`// TODO(v1.4 Phase 20 — SM-02): re-attach _batch_gpu_affinity recording in scan-manager-driven scan path`

Verify zero conflict markers and stage. Append to 17-MERGE-LOG.md Section A.6.

### File 6 — `src/expression_executor/gpu_expression_executor.cpp` (D-D5: TAKE-DEV)
```
git checkout --theirs src/expression_executor/gpu_expression_executor.cpp
git add src/expression_executor/gpu_expression_executor.cpp
```

Per D-D5 "flag any post-#731 stream-handling changes for Phase 18 review": Inspect the now-staged file for new patterns that may need Phase 18 RAII review. Specifically, look for:
- `batch->get_data()` calls (new occurrences) — these will fail Phase 18 RAII compile, mark with TODO
- `pop_data_batch(...)` calls — Pitfall P3 territory, mark with TODO
- New `cudaSetDevice` raw calls (Pitfall P11) — mark with TODO

Run:
```
grep -n "->get_data()\|pop_data_batch\|cudaSetDevice\b" src/expression_executor/gpu_expression_executor.cpp
```
For EACH match, insert a one-line TODO comment immediately ABOVE the matching line, in the form:
- For `get_data()`: `// TODO(v1.4 Phase 18 — DB-02): wrap in to_read_only() / to_mutable() RAII accessor (private after cucascade #117)`
- For `pop_data_batch`: `// TODO(v1.4 Phase 18 — DB-03): replace with proper wait-loop or split_connector subscriber model (Pitfall P3)`
- For raw `cudaSetDevice`: `// TODO(v1.4 Phase 19 — IO-16): wrap in rmm::cuda_set_device_raii (HYG-class, Pitfall P11)`

After editing, re-stage:
```
git add src/expression_executor/gpu_expression_executor.cpp
```
Append to 17-MERGE-LOG.md Section A.3 "Resolution outcome" with TODO count and locations.

### Files 7, 8, 9 — D-D3 trio (TAKE-DEV + Phase 20 mgpu re-integration TODOs)

`src/op/scan/sirius_gpu_parquet_scan_operator.cpp`, `src/op/sirius_physical_table_scan.cpp`, `src/pipeline/sirius_pipeline_converter.cpp`

For each of these three files, run the same TAKE-DEV + audit + TODO pattern:

```
for f in src/op/scan/sirius_gpu_parquet_scan_operator.cpp src/op/sirius_physical_table_scan.cpp src/pipeline/sirius_pipeline_converter.cpp; do
  git checkout --theirs "$f"
  git add "$f"
done
```

Then for each file, audit + insert TODOs:

A. **`src/op/scan/sirius_gpu_parquet_scan_operator.cpp`** — primary mgpu re-integration site per CONTEXT.md (per ARCHITECTURE.md Surface 5, this is where `gpu_expression_translator(stream, cudf::get_current_device_resource_ref())` will live in Phase 20):

  1. Find `execute(` definition. ABOVE it, insert as a single comment block:
```
// TODO(v1.4 Phase 20 — SM-01/SM-02/SM-03/SM-04): re-attach v1.3 multi-GPU semantics in execute():
//   - SM-01 (SCHED-RR): verify split assignment is round-robin across GPUs (or port _no_pref_rr_counter to parquet_split_provider)
//   - SM-02 (_batch_gpu_affinity): record batch_id -> device_id at scan completion (was in v1.3 sirius_gpu_parquet_scan_operator.hpp)
//   - SM-03 (writer_stream / Phase 13 stream-lineage): pass `stream` as third arg to gpu_table_representation(table, mem_space, stream) — see .planning/phases/17-sirius-origin-dev-merge-base-layer/17-PHASE-13-EXTRACT.md
//   - SM-04 (per-task filter translation): gpu_expression_translator(stream, cudf::get_current_device_resource_ref()) at task time, not plan time
```

  2. Audit for the standard patterns (get_data / pop_data_batch / cudaSetDevice) and insert per-line TODOs as in File 6's recipe.

B. **`src/op/sirius_physical_table_scan.cpp`** — table-scan operator. Audit for standard patterns + insert per-line TODOs. Above any `execute(` or `compute_task(` definition (whichever is present), insert a header TODO:
```
// TODO(v1.4 Phase 20 — SM-01..06): verify v1.3 SCHED-RR distribution survives in this operator's task-creation/dispatch path
```

C. **`src/pipeline/sirius_pipeline_converter.cpp`** — pipeline converter. Audit + insert per-line TODOs. Above the file's first relevant function, insert:
```
// TODO(v1.4 Phase 18 — DB-01): batch_lock_utils RAII migration may affect this converter — review when DB-01 lands
// TODO(v1.4 Phase 20 — SM-03): if writer_event recording happens here, ensure cudaStreamWaitEvent chain remains intact post-Scan-Manager (Phase 13 / Pitfall P2)
```

After editing each file, re-stage:
```
git add src/op/scan/sirius_gpu_parquet_scan_operator.cpp src/op/sirius_physical_table_scan.cpp src/pipeline/sirius_pipeline_converter.cpp
```
Append to 17-MERGE-LOG.md Sections A.8, A.9, A.10 "Resolution outcome" — for each file: "Took dev's version. Phase 20 mgpu re-integration TODOs inserted at <N> sites: <list line ranges>."

### Final verification before Task 3

Run:
```
git diff --name-only --diff-filter=U | wc -l
```
Must be 0 — every conflict resolved.

Run:
```
grep -rn "<<<<<<< \|======= \|>>>>>>> " src/ CMakeLists.txt 2>/dev/null | wc -l
```
Must be 0 — no leftover conflict markers anywhere in the source tree or build config.

Run:
```
git ls-files --stage cucascade | awk '{print $2}'
```
Must be `1c1e648a282a06747328c78f62d2d676ce51a8ce` (defending the pin one final time before commit).

NO `rmm::cuda_stream_default` introduced by this task. (TAKE-DEV may bring some in from origin/dev — those are documented in plan 17-03 Section B.3 HYG audit, not added by us.)
  </action>
  <verify>
    <automated>git diff --name-only --diff-filter=U | wc -l | grep -q "^0$" &amp;&amp; ! grep -rn "&lt;&lt;&lt;&lt;&lt;&lt;&lt; \|======= \|&gt;&gt;&gt;&gt;&gt;&gt;&gt; " src/ CMakeLists.txt 2&gt;/dev/null &amp;&amp; git ls-files --stage cucascade | grep -q "1c1e648a282a06747328c78f62d2d676ce51a8ce" &amp;&amp; test -f src/scan_manager/parquet_split_provider.cpp &amp;&amp; ! test -f src/include/op/scan/sirius_parquet_metadata_scan_operator.hpp &amp;&amp; grep -q "TODO(v1.4 Phase 20" src/op/scan/sirius_gpu_parquet_scan_operator.cpp</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only --diff-filter=U` returns empty (zero unresolved conflicts)
    - `grep -rn "&lt;&lt;&lt;&lt;&lt;&lt;&lt; \|======= \|&gt;&gt;&gt;&gt;&gt;&gt;&gt; " src/ CMakeLists.txt` returns nothing (no conflict markers)
    - `git ls-files --stage cucascade | awk '{print $2}'` outputs `1c1e648a282a06747328c78f62d2d676ce51a8ce`
    - `test -f src/scan_manager/parquet_split_provider.cpp` exits 0 (net-new file present)
    - `test -f src/include/op/scan/sirius_parquet_metadata_scan_operator.hpp` exits NON-ZERO (file deleted)
    - `grep -c "TODO(v1.4 Phase 20" src/op/scan/sirius_gpu_parquet_scan_operator.cpp` returns >= 1 (Phase 20 mgpu re-integration TODO inserted)
    - `grep -c "TODO(v1.4 Phase 18" src/op/scan/sirius_gpu_parquet_scan_operator.cpp src/op/sirius_physical_table_scan.cpp src/pipeline/sirius_pipeline_converter.cpp src/expression_executor/gpu_expression_executor.cpp 2&gt;/dev/null | awk -F: '{s+=$2} END {print (s &gt;= 1)? "ok": "fail"}'` outputs `ok`
    - `17-MERGE-LOG.md` Sections A.1, A.3, A.4, A.5, A.6, A.8, A.9, A.10, A.11 all populated (no `<filled>` placeholders left in Section A)
  </acceptance_criteria>
  <done>
    All 11 conflict files resolved per the CONTEXT.md recipes. cucascade pin still defended. Phase 13 file deleted. Net-new `parquet_split_provider.cpp` taken from dev. Phase 18 / Phase 20 TODO annotations inserted in operator files. Section A of 17-MERGE-LOG.md fully populated. Tree is ready for `git commit` in Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create the merge commit, verify D-G1/D-G6 gates, commit 17-MERGE-LOG.md update</name>
  <files>
    .planning/phases/17-sirius-origin-dev-merge-base-layer/17-MERGE-LOG.md
  </files>
  <action>
This task closes out the merge by creating the merge commit (which absorbs all 7 origin/dev commits per MERGE-01 / D-A1) and verifying the immediate post-commit gates D-G1 (merge commit exists) and D-G6 (cucascade pin defended).

STEP A — Pre-commit invariant checks:
```
# All conflicts resolved
test -z "$(git diff --name-only --diff-filter=U)"

# No leftover conflict markers
! grep -rn "<<<<<<< \|======= \|>>>>>>> " src/ CMakeLists.txt 2>/dev/null

# cucascade pin defended
[ "$(git ls-files --stage cucascade | awk '{print $2}')" = "1c1e648a282a06747328c78f62d2d676ce51a8ce" ]

# Phase 13 file deleted
[ ! -f src/include/op/scan/sirius_parquet_metadata_scan_operator.hpp ]

# Net-new file present
[ -f src/scan_manager/parquet_split_provider.cpp ]
```
If any of these fail, STOP — Task 1 or Task 2 didn't complete cleanly. Do NOT commit.

STEP B — Create the merge commit (D-A1: single commit absorbs all 7 dev commits):

The merge is currently in MERGE_HEAD state from Task 1's `git merge --no-ff`. Create the commit using a HEREDOC for clean multi-line message:
```
git commit --no-verify -m "$(cat <<'EOF'
merge(17-02): origin/dev into feature/single-node-multi-gpu2 (MERGE-01, MERGE-02, MERGE-04)

Absorbs 7 origin/dev commits as a single merge commit per Phase 17 D-A1:
- PR #739 (Compat/update cucascade gpu table) — bookkeeping-only absorption per P7;
  file changes deferred to Phase 18 DB-03 (post-#117 RAII shape)
- PR #675 (Sirius IO/Prefetching/Caching Framework)
- PR #731 (Sirius Scan Manager) — deletes sirius_parquet_metadata_scan_operator.hpp;
  Phase 13 stream-lineage hooks extracted to 17-PHASE-13-EXTRACT.md per MERGE-03
- PR #721 (Pin tables in GPU memory)
- PR #733 (Dedup get_next_ports_after_sink)
- PR #734 (CI: Remove GHA Runner Spot)
- PR #735 (CI: Disable GHA sccache on self-hosted)

Conflict resolution (11 files) per Phase 17 CONTEXT.md D-D1..D-D6 + D-B1/B2:
- cucascade — keep ours (1c1e648, Phase 16 pin); D-B1/B2
- src/include/op/scan/sirius_parquet_metadata_scan_operator.hpp — accept deletion;
  Phase 13 hooks extracted to 17-PHASE-13-EXTRACT.md (MERGE-03)
- src/scan_manager/parquet_split_provider.cpp — take dev's net-new file (D-D2)
- src/op/scan/sirius_gpu_parquet_scan_operator.cpp,
  src/op/sirius_physical_table_scan.cpp,
  src/pipeline/sirius_pipeline_converter.cpp — take dev's; Phase 20 TODOs inserted (D-D3)
- src/include/creator/task_creator.hpp,
  src/include/op/scan/parquet_scan_operator_data.hpp — combine; preserve our SCHED-RR / affinity context (D-D4)
- src/expression_executor/gpu_expression_executor.cpp — take dev's; Phase 18 TODOs inserted (D-D5)
- CMakeLists.txt, src/include/exec/config.hpp — combine (D-D1)

Expected post-merge state per D-F1: Sirius build will FAIL with 26+ batch->get_data() is private errors; closure deferred to Phase 18 (DB-01..05). Audit log: 17-MERGE-LOG.md.

cucascade pin still 1c1e648 (D-G6 verified pre-commit).
EOF
)"
```

If commit fails (e.g., pre-commit hook violation that --no-verify doesn't bypass), surface the error and STOP — do not amend or force.

STEP C — Verify D-G1 (merge commit exists) and D-G6 (cucascade pin) immediately:
```
# D-G1
git log --oneline --merges -1
# Should be the just-created merge commit "merge(17-02): origin/dev into feature/single-node-multi-gpu2..."
git log -1 --pretty=%P
# Should output two SHAs (the two parents): our pre-merge HEAD + origin/dev tip

# D-G6
git ls-tree HEAD cucascade
# Should output: 160000 commit 1c1e648a282a06747328c78f62d2d676ce51a8ce\tcucascade
```

If `git log --oneline --merges -1` does NOT show the new merge commit, OR if `git ls-tree HEAD cucascade` shows any SHA other than `1c1e648a282a06747328c78f62d2d676ce51a8ce`, STOP and surface.

STEP D — Update 17-MERGE-LOG.md with merge commit SHA + first two D-G gate results:

Edit `17-MERGE-LOG.md` Section D table rows for D-G1 and D-G6 to fill the "Actual" column:
- D-G1 row: replace `<filled>` with the merge commit SHA + summary line (output of `git log --oneline --merges -1`)
- D-G6 row: replace `<filled>` with `1c1e648a282a06747328c78f62d2d676ce51a8ce` (matches expected)

Stage and commit:
```
git add -f .planning/phases/17-sirius-origin-dev-merge-base-layer/17-MERGE-LOG.md
git commit --no-verify -m "docs(17-02): populate 17-MERGE-LOG.md Section A (per-file resolution outcomes) + D-G1/D-G6 gates"
```

STEP E — Final state check:
```
# Two new commits since plan 17-01: the merge + the docs update
git log --oneline phase17-pre-merge-backup..HEAD | wc -l
# Should output a number >= 3 (17-01's two commits plus this plan's two commits = 4, OR if 17-01 was squashed, fewer; main check is the merge commit)

# The merge commit must be among the new commits
git log --oneline --merges phase17-pre-merge-backup..HEAD | wc -l
# Should output 1
```

NO `rmm::cuda_stream_default` introduced. Total source-line additions to src/ from this task: 0 (we only ran git commands and edited a markdown file).
  </action>
  <verify>
    <automated>git log --oneline --merges -1 | grep -q "17-02" &amp;&amp; git ls-tree HEAD cucascade | grep -q "1c1e648a282a06747328c78f62d2d676ce51a8ce" &amp;&amp; test ! -f src/include/op/scan/sirius_parquet_metadata_scan_operator.hpp &amp;&amp; test -f src/scan_manager/parquet_split_provider.cpp &amp;&amp; git log -1 --pretty=%P | awk '{print NF}' | grep -q "^2$" &amp;&amp; ! grep -q "&lt;filled&gt;" .planning/phases/17-sirius-origin-dev-merge-base-layer/17-MERGE-LOG.md || grep -c "&lt;filled&gt;" .planning/phases/17-sirius-origin-dev-merge-base-layer/17-MERGE-LOG.md | awk '$1 &lt; 30 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline --merges -1` shows the new merge commit, message starts with `merge(17-02): origin/dev into feature/single-node-multi-gpu2`
    - `git log -1 --pretty=%P` outputs exactly two SHAs (the merge has two parents)
    - One of those parent SHAs equals `git rev-parse phase17-pre-merge-backup` (the pre-merge HEAD); the other equals `git rev-parse origin/dev` (the dev tip)
    - `git ls-tree HEAD cucascade | awk '{print $3}'` outputs `1c1e648a282a06747328c78f62d2d676ce51a8ce` (D-G6 satisfied post-commit)
    - `test ! -f src/include/op/scan/sirius_parquet_metadata_scan_operator.hpp` exits 0 (file deleted in tree)
    - `test -f src/scan_manager/parquet_split_provider.cpp` exits 0 (net-new file in tree)
    - `git log --oneline --grep "Compat/update cucascade" phase17-pre-merge-backup..HEAD` outputs >= 1 line (PR #739 absorbed per MERGE-04)
    - 17-MERGE-LOG.md Section D rows D-G1 and D-G6 no longer contain `<filled>`
    - `git status --short` is clean (no untracked / unstaged after both commits)
  </acceptance_criteria>
  <done>
    Merge commit exists with two parents (MERGE-01 satisfied). All 11 conflict files resolved (MERGE-02 satisfied — full audit in 17-MERGE-LOG.md Section A). PR #739 absorbed bookkeeping-only — file changes deferred to Phase 18 (MERGE-04 satisfied). Cucascade pin defended at `1c1e648` (D-G6). Phase 13 file deleted post-extraction (D-C3 closed). Tree is in the post-merge intermediate state — Sirius build will fail in plan 17-03, which is EXPECTED per D-F1.
  </done>
</task>

</tasks>

<verification>
Phase boundary verification (after Task 3):
- Merge commit exists: `git log --oneline --merges -1` -- non-empty
- Merge has 2 parents: `git log -1 --pretty=%P | awk '{print NF}'` returns 2
- One parent is pre-merge HEAD: parent matches `git rev-parse phase17-pre-merge-backup`
- Other parent is origin/dev tip: parent matches `git rev-parse origin/dev`
- Cucascade pin still 1c1e648: `git ls-tree HEAD cucascade | awk '{print $3}'` returns `1c1e648a282a06747328c78f62d2d676ce51a8ce`
- 7 dev commits absorbed: `git log --oneline phase17-pre-merge-backup..HEAD --first-parent` shows the merge commit; `git log --oneline phase17-pre-merge-backup..origin/dev` shows 7 commits (all reachable from merge)
- No conflict markers anywhere: `grep -rn "<<<<<<< \|======= \|>>>>>>> " src/ CMakeLists.txt` returns nothing
- 17-MERGE-LOG.md Section A fully populated (no `<filled>` in any A.* subsection)

Pitfall coverage:
- P7 (PR #739 × #117): merge commit message explicitly notes "#739 bookkeeping-only"; D-D1..D-D5 recipes resolve operator files against post-#117 shape; the commit absorbs #739 but its file edits are deferred
- P10 (Phase 13 in deleted file): 17-PHASE-13-EXTRACT.md was committed by 17-01 BEFORE this plan accepted the deletion (D-C3); verifiable via `git log --oneline -- 17-PHASE-13-EXTRACT.md` predating this plan's merge commit
</verification>

<success_criteria>
- MERGE-01: A merge commit exists absorbing all 7 origin/dev commits as a single commit (D-A1)
- MERGE-02: All 11 conflict files resolved per CONTEXT.md recipes; 17-MERGE-LOG.md Section A documents per-file resolution outcomes with explicit decision attribution (D-D1..D-D6, D-B1/B2)
- MERGE-04: PR #739's commit is absorbed (`git log --grep "Compat/update cucascade"` non-empty) but its file changes are NOT applied — they remain TODO until Phase 18 (commit message + 17-MERGE-LOG.md Section E document this explicitly)
- D-G1 (merge commit) verified post-commit
- D-G6 (cucascade pin still 1c1e648) verified post-commit
- D-C3 (deletion accepted only AFTER extraction) verified by extraction file's commit predating the merge commit
- Tree is at the expected intermediate state: `parquet_split_provider.cpp` exists, `sirius_parquet_metadata_scan_operator.hpp` deleted; build will fail with batch->get_data() errors per D-F1 (closure in plan 17-03)
</success_criteria>

<output>
After completion, create `.planning/phases/17-sirius-origin-dev-merge-base-layer/17-02-SUMMARY.md` documenting:
1. Merge commit SHA + parent SHAs
2. Conflict resolution outcomes per file (cross-reference 17-MERGE-LOG.md Section A)
3. Cucascade pin verification (D-G6 actual value)
4. Confirmation Phase 13 extraction file predates the merge commit (D-C3)
5. List of TODO comments inserted (count per file)
6. Diff stat: `git diff phase17-pre-merge-backup..HEAD --stat | tail -3`
</output>
