---
phase: 17-sirius-origin-dev-merge-base-layer
plan: 03
type: execute
wave: 3
depends_on: [17-02]
files_modified:
  - .planning/phases/17-sirius-origin-dev-merge-base-layer/17-MERGE-LOG.md
  - .planning/phases/17-sirius-origin-dev-merge-base-layer/17-build-output.log
autonomous: true
requirements: [MERGE-05]
must_haves:
  truths:
    - "33 auto-merged files have been audited for FSM enum names (zero hits in src/ AND test/) and HYG-02 deltas"
    - "SCHED-RR survival is verified: _no_pref_rr_counter exists in src/include/pipeline/task_scheduler.hpp; SCHED-RR block exists in src/pipeline/task_scheduler.cpp"
    - "Sirius build was attempted; build output captured to .planning/phases/17-.../17-build-output.log"
    - "Build error count is bounded — expected categories (get_data() private, RAII, pop_data_batch removed, data_batch_processing_handle removed) tallied; unrelated errors == 0 OR explicitly investigated"
  artifacts:
    - path: ".planning/phases/17-sirius-origin-dev-merge-base-layer/17-MERGE-LOG.md"
      provides: "Sections B and C populated — auto-merge audit results, SCHED-RR survival, HYG-02 delta, build error inventory"
    - path: ".planning/phases/17-sirius-origin-dev-merge-base-layer/17-build-output.log"
      provides: "Captured Sirius build output for traceability"
  key_links:
    - from: "src/include/pipeline/task_scheduler.hpp"
      to: "_no_pref_rr_counter field (Phase 14 SCHED-RR)"
      via: "grep gate D-G2"
      pattern: "_no_pref_rr_counter"
    - from: "src/pipeline/task_scheduler.cpp"
      to: "SCHED-RR distribution block (Phase 14)"
      via: "grep gate D-G2"
      pattern: "SCHED-RR"
    - from: "src/ AND test/"
      to: "zero old FSM enum value names"
      via: "grep gate D-G3"
      pattern: "task_created\\|in_transit\\|data_batch_processing_handle\\|idata_batch_probe"
---

<objective>
Audit the 33 auto-merged files (per CONTEXT.md D-E1/D-E2) for two regressions: (1) re-introduction of pre-#117 FSM enum names that #117 deleted (Pitfall P7), (2) HYG-02 increases (`rmm::cuda_stream_default` count). Verify SCHED-RR distribution semantics survived the merge (Pitfall P6 / D-G2). Run the Sirius build (knowing it WILL fail per D-F1) and bound the error count by category — expected vs unrelated (D-F2/D-F3 / MERGE-05). Populate Sections B and C of `17-MERGE-LOG.md`.

Purpose: This is the load-bearing plan for MERGE-05. It produces the documented evidence that the post-merge intermediate state has only the EXPECTED breakage (~26+ `batch->get_data() is private` errors plus RAII compile errors) and zero unrelated errors. Without this audit, plan 17-04 cannot validate D-F1/D-F3.

Output:
- Build output log at `.planning/phases/17-sirius-origin-dev-merge-base-layer/17-build-output.log`
- 17-MERGE-LOG.md Section B (auto-merge audit) populated with FSM grep results, HYG-02 delta, SCHED-RR survival evidence, TODO insertion list
- 17-MERGE-LOG.md Section C (build error bounding) populated with per-category counts and verdict
- Two new commits (one for any TODO insertions discovered by the audit, one for the docs update)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-sirius-origin-dev-merge-base-layer/17-CONTEXT.md
@.planning/phases/17-sirius-origin-dev-merge-base-layer/17-MERGE-LOG.md
@.planning/phases/17-sirius-origin-dev-merge-base-layer/17-PHASE-13-EXTRACT.md
@.planning/research/PITFALLS.md
@CLAUDE.md
@.claude/skills/build-errors/SKILL.md

<interfaces>
<!-- Post-merge tree state: cucascade pinned at 1c1e648 with #117 RAII; Sirius src/ has dev's post-#731 -->
<!-- operator file shapes but still calls many `batch->get_data()` (now private). Build EXPECTED to fail with: -->
<!--   - 26+ "is a private member" errors on batch->get_data() -->
<!--   - "no member named 'pop_data_batch'" -->
<!--   - "no member named 'data_batch_processing_handle'" -->
<!--   - Possibly missing liburing-dev headers (IO-12 territory; documented but does not block) -->
<!-- "use of undeclared identifier 'task_created'/'in_transit'" errors must be 0 (P7 regression check) -->

Project skill — build-errors:
The .claude/skills/build-errors/ directory contains documented patterns for classifying build errors. Use it to bound which errors are "expected intermediate" vs "unrelated/unexpected" (D-F3).

Per CLAUDE.md: "Use MCP for build/test" — use `mcp__project-commands__run_command build` if available. If MCP aborts on intentional non-zero exit (D-F2 caveat), fall back to direct pixi invocation with `tee` + `; exit 0` to capture log even on failure.
</interfaces>
</context>

<read_first>
1. `.planning/phases/17-sirius-origin-dev-merge-base-layer/17-CONTEXT.md` — locked decisions D-E1, D-E2 (auto-merge audit), D-F1, D-F2, D-F3 (build error bounding), D-G2 (SCHED-RR survival), D-G3 (FSM names absent)
2. `.planning/phases/17-sirius-origin-dev-merge-base-layer/17-MERGE-LOG.md` — Section B and C skeletons; verify Section A is fully populated by 17-02 (no `<filled>` placeholders in Section A subsections)
3. `.planning/research/PITFALLS.md` Pitfall 6 (SCHED-RR counter port stale), Pitfall 7 (PR #739 × #117 — FSM names absent gate is the regression test for this), Pitfall 11 (HYG-02 raw cudaSetDevice in uring_reactor.cpp — expected from #675 IO Framework)
4. `.claude/skills/build-errors/SKILL.md` (or equivalent skill index) for build-error categorization patterns
5. Run `git log --oneline phase17-pre-merge-backup..HEAD --merges` — must show the 17-02 merge commit; if missing, plan 17-02 didn't complete
6. Run `cat .planning/phases/17-sirius-origin-dev-merge-base-layer/17-MERGE-LOG.md | grep -c "<filled>"` — note baseline count of remaining `<filled>` placeholders (expected: only Sections B/C/D unfilled)
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Auto-merge audit (33 files) — FSM/HYG-02 grep gates + SCHED-RR survival; populate 17-MERGE-LOG.md Section B</name>
  <files>
    .planning/phases/17-sirius-origin-dev-merge-base-layer/17-MERGE-LOG.md
  </files>
  <action>
This task runs the D-E1 / D-E2 audit on the auto-merged files and the D-G2 / D-G3 verification gates, populating Section B of 17-MERGE-LOG.md.

STEP A — Inventory the auto-merged files (Section B.1):

The merge commit has two parents. The 11 files resolved manually in 17-02 are listed in 17-MERGE-LOG.md Section A. Everything else origin/dev touched (relative to our HEAD) is auto-merged.

```
# All files origin/dev changed since the merge-base
git diff --name-only "$(git merge-base phase17-pre-merge-backup origin/dev)..origin/dev" > /tmp/17-all-touched.txt

# The 11 files resolved manually
cat <<'EOF' > /tmp/17-manual-conflicts.txt
CMakeLists.txt
cucascade
src/expression_executor/gpu_expression_executor.cpp
src/include/creator/task_creator.hpp
src/include/exec/config.hpp
src/include/op/scan/parquet_scan_operator_data.hpp
src/include/op/scan/sirius_parquet_metadata_scan_operator.hpp
src/op/scan/sirius_gpu_parquet_scan_operator.cpp
src/op/sirius_physical_table_scan.cpp
src/pipeline/sirius_pipeline_converter.cpp
src/scan_manager/parquet_split_provider.cpp
EOF

# Auto-merged = touched - manually resolved
sort /tmp/17-all-touched.txt > /tmp/17-all-touched-sorted.txt
sort /tmp/17-manual-conflicts.txt > /tmp/17-manual-sorted.txt
comm -23 /tmp/17-all-touched-sorted.txt /tmp/17-manual-sorted.txt > /tmp/17-auto-merged.txt
AUTO_COUNT=$(wc -l < /tmp/17-auto-merged.txt)
echo "Auto-merged file count: $AUTO_COUNT"
```

CONTEXT.md says "33 auto-merges" — actual count may differ. Document the actual count. Append the file list to 17-MERGE-LOG.md Section B.1 in a fenced code block.

STEP B — FSM grep audit (Section B.2; D-E1 step 1; D-G3 / Pitfall P7 gate):

Per-file FSM grep on auto-merged files only:
```
FSM_PATTERN='task_created\|in_transit\|data_batch_processing_handle\|idata_batch_probe'
TOTAL_FSM_HITS=0
> /tmp/17-fsm-hits.txt
while IFS= read -r f; do
  if [ -f "$f" ]; then
    HITS=$(grep -n "$FSM_PATTERN" "$f" 2>/dev/null || true)
    if [ -n "$HITS" ]; then
      echo "=== $f ===" >> /tmp/17-fsm-hits.txt
      echo "$HITS" >> /tmp/17-fsm-hits.txt
      TOTAL_FSM_HITS=$((TOTAL_FSM_HITS + $(echo "$HITS" | wc -l)))
    fi
  fi
done < /tmp/17-auto-merged.txt
echo "Per-file FSM hits in auto-merged: $TOTAL_FSM_HITS"
cat /tmp/17-fsm-hits.txt
```

Project-wide FSM gate (D-G3):
```
SRC_FSM_HITS=$(grep -rn "$FSM_PATTERN" src/ 2>/dev/null | wc -l)
TEST_FSM_HITS=$(grep -rn "$FSM_PATTERN" test/ 2>/dev/null | wc -l)
echo "src/ FSM hits: $SRC_FSM_HITS (expected: 0)"
echo "test/ FSM hits: $TEST_FSM_HITS (expected: 0)"
```

Both MUST be 0 for D-G3 to pass. Any non-zero count means dev re-introduced an old FSM enum value — annotate per D-E2.

If non-zero, for each hit insert a TODO comment ABOVE the offending line:
`// TODO(v1.4 Phase 18 — DB-02): origin/dev auto-merge re-introduced pre-#117 batch_state name; replace with RAII accessor pattern`

If FSM hits == 0 for both src/ and test/ (the expected case): Section B.2 records "0 hits".

Append B.2 contents to 17-MERGE-LOG.md:
- "Per-file FSM hits: $TOTAL_FSM_HITS" + the per-file breakdown from /tmp/17-fsm-hits.txt
- "src/ FSM hits: $SRC_FSM_HITS"
- "test/ FSM hits: $TEST_FSM_HITS"
- D-G3 verdict: PASS if both 0, INVESTIGATE if non-zero

STEP C — HYG-02 audit (Section B.3; D-E1 step 2):

Pre-merge HYG-02:
```
PRE_HYG02=$(git grep -c "rmm::cuda_stream_default" phase17-pre-merge-backup -- "src/*" 2>/dev/null | awk -F: '{s+=$NF} END {print s+0}')
echo "PRE_HYG02 (from phase17-pre-merge-backup) = $PRE_HYG02"
```

Post-merge HYG-02:
```
POST_HYG02=$(grep -rc "rmm::cuda_stream_default" src/ 2>/dev/null | awk -F: '{s+=$2} END {print s+0}')
DELTA=$((POST_HYG02 - PRE_HYG02))
echo "Pre-merge HYG-02: $PRE_HYG02"
echo "Post-merge HYG-02: $POST_HYG02"
echo "Net delta: $DELTA (expected: positive — #675 IO Framework adds new sites; Phase 19 IO-16 cleans up)"
```

Per-file HYG-02 hits in auto-merged files (only show non-zero):
```
> /tmp/17-hyg-hits.txt
while IFS= read -r f; do
  if [ -f "$f" ]; then
    HITS=$(grep -c "rmm::cuda_stream_default" "$f" 2>/dev/null || echo 0)
    if [ "$HITS" -gt 0 ]; then
      echo "$HITS  $f" >> /tmp/17-hyg-hits.txt
    fi
  fi
done < /tmp/17-auto-merged.txt
sort -rn /tmp/17-hyg-hits.txt | head -20
```

Append Section B.3 to 17-MERGE-LOG.md:
- Pre-merge HYG-02 count, post-merge count, delta
- Top files with HYG-02 hits in the auto-merged set
- Note: any cudaSetDevice raw calls in `src/io/uring_reactor.cpp` are deferred per Pitfall P11 / Phase 19 IO-16 — DO NOT introduce TODOs for these now (#675 is a coherent addition; Phase 19 owns the cleanup)

STEP D — SCHED-RR survival (Section B.4; D-G2 / Pitfall P6):

```
RR_HPP_HITS=$(grep -c "_no_pref_rr_counter" src/include/pipeline/task_scheduler.hpp 2>/dev/null || echo 0)
echo "_no_pref_rr_counter in task_scheduler.hpp: $RR_HPP_HITS (expected: >= 1; Phase 14 baseline = 3)"

RR_CPP_HITS=$(grep -n "SCHED-RR" src/pipeline/task_scheduler.cpp 2>/dev/null)
echo "SCHED-RR mentions in task_scheduler.cpp:"
echo "$RR_CPP_HITS"
RR_CPP_COUNT=$(echo "$RR_CPP_HITS" | grep -c "SCHED-RR" || echo 0)
echo "Count: $RR_CPP_COUNT (expected: >= 2; line ~156 reset comment + line ~253 distribution block)"
```

Both gates must pass. If `RR_HPP_HITS == 0` OR `RR_CPP_COUNT == 0`, the SCHED-RR machinery was lost in the merge — STOP and surface to user per D-A4 (potentially abort to `phase17-pre-merge-backup`).

Append Section B.4 to 17-MERGE-LOG.md with both numeric counts and the verbatim grep output for `SCHED-RR`.

STEP E — Final TODO annotation count (Section B.5):

If Steps B/C inserted any TODO comments, list affected files with line counts. If no TODOs were inserted (FSM hits = 0; HYG raw-cudaSetDevice deferred to Phase 19), Section B.5 records "No TODO annotations added in this audit (FSM grep gate green; HYG-02 deltas attributed to known #675 IO Framework code, deferred to Phase 19 IO-16 sweep)."

STEP F — Commit:

If TODO comments were inserted into source files:
```
git add -f .planning/phases/17-sirius-origin-dev-merge-base-layer/17-MERGE-LOG.md
git add src/ test/  # only if files received TODO insertions
git commit --no-verify -m "docs(17-03): populate 17-MERGE-LOG.md Section B (auto-merge audit) — D-G2 SCHED-RR + D-G3 FSM gates"
```

If only the markdown file changed:
```
git add -f .planning/phases/17-sirius-origin-dev-merge-base-layer/17-MERGE-LOG.md
git commit --no-verify -m "docs(17-03): populate 17-MERGE-LOG.md Section B (auto-merge audit, SCHED-RR survival)"
```

NO `rmm::cuda_stream_default` introduced. Audit is read-only on src/ except potentially adding TODO comments above existing FSM-name lines (which themselves stay until Phase 18 deletes them).
  </action>
  <verify>
    <automated>SRC_FSM=$(grep -rn 'task_created\|in_transit\|data_batch_processing_handle\|idata_batch_probe' src/ 2>/dev/null | wc -l); TEST_FSM=$(grep -rn 'task_created\|in_transit\|data_batch_processing_handle\|idata_batch_probe' test/ 2>/dev/null | wc -l); RR_HPP=$(grep -c "_no_pref_rr_counter" src/include/pipeline/task_scheduler.hpp 2>/dev/null || echo 0); test "$SRC_FSM" = "0" && test "$TEST_FSM" = "0" && test "$RR_HPP" -ge 1 && grep -q "SCHED-RR" src/pipeline/task_scheduler.cpp</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_no_pref_rr_counter" src/include/pipeline/task_scheduler.hpp` returns >= 1 (D-G2 SCHED-RR field survival)
    - `grep "SCHED-RR" src/pipeline/task_scheduler.cpp` returns at least one line (D-G2 SCHED-RR block survival)
    - `grep -rn "task_created\|in_transit\|data_batch_processing_handle\|idata_batch_probe" src/ | wc -l` returns 0 (D-G3 / P7 / MERGE-02 partial)
    - `grep -rn "task_created\|in_transit\|data_batch_processing_handle\|idata_batch_probe" test/ | wc -l` returns 0 (D-G3 test/ extension)
    - 17-MERGE-LOG.md Section B subsections (B.1, B.2, B.3, B.4, B.5) all populated (no `<filled>` in Section B)
    - 17-MERGE-LOG.md records pre-merge HYG-02 count + post-merge count + delta in Section B.3
    - Commit exists with message starting `docs(17-03):` documenting Section B population
  </acceptance_criteria>
  <done>
    Auto-merge audit complete. SCHED-RR survival verified (D-G2 PASS). FSM names absent from src/ AND test/ (D-G3 PASS / MERGE-02 partial / P7 regression check PASS). HYG-02 delta documented (expected to increase from #675; deferred to Phase 19 IO-16). Section B of 17-MERGE-LOG.md fully populated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run Sirius build, capture output, classify errors per D-F1/D-F2/D-F3 — populate 17-MERGE-LOG.md Section C</name>
  <files>
    .planning/phases/17-sirius-origin-dev-merge-base-layer/17-MERGE-LOG.md
    .planning/phases/17-sirius-origin-dev-merge-base-layer/17-build-output.log
  </files>
  <action>
This task runs the build (knowing it WILL fail per D-F1) and bounds the failure surface per category — closing MERGE-05 / D-F1/D-F2/D-F3.

Reference the `build-errors` Claude Code skill for error classification patterns:
```
ls .claude/skills/build-errors/ 2>/dev/null
cat .claude/skills/build-errors/SKILL.md 2>/dev/null | head -50
```

STEP A — Run the build, capturing output to a log:

Per CLAUDE.md: prefer MCP. Per CONTEXT.md D-F2 caveat: MCP may abort on exit-code-nonzero from intentional build failures.

Try MCP first:
```
mcp__project-commands__run_command build 2>&1 | tee .planning/phases/17-sirius-origin-dev-merge-base-layer/17-build-output.log
MCP_EXIT=$?
```
(MCP exit code will be non-zero — that's fine; the log is what matters.)

If MCP refuses to run (returns immediately with an error before invoking cmake), fall back to direct pixi invocation:
```
pixi run -- bash -c '
  set +e
  cmake -S . -B build -DCMAKE_BUILD_TYPE=Release 2>&1
  CMAKE_EXIT=$?
  if [ $CMAKE_EXIT -ne 0 ]; then
    echo "=== CMAKE CONFIG FAILED (exit=$CMAKE_EXIT) ==="
    exit 0
  fi
  CMAKE_BUILD_PARALLEL_LEVEL=8 cmake --build build 2>&1
  echo "=== BUILD EXIT: $? ==="
' 2>&1 > .planning/phases/17-sirius-origin-dev-merge-base-layer/17-build-output.log

# Verify log size:
LOG_LINES=$(wc -l < .planning/phases/17-sirius-origin-dev-merge-base-layer/17-build-output.log)
echo "Build log lines: $LOG_LINES"
```

Log file should be ≥ 100 lines (full build attempt captured).

STEP B — Classify errors per D-F1 buckets (Section C.2):

Use grep on the captured log to count errors in each expected category. Exact regex depends on compiler (gcc/clang/nvcc) and may need adjustment based on actual output:

```
LOG=.planning/phases/17-sirius-origin-dev-merge-base-layer/17-build-output.log

# Bucket 1: get_data() private — Expected 26+ per D-F1
PRIV_GETDATA=$(grep -E "'get_data'.*private|get_data.*is private|cannot access private member" "$LOG" 2>/dev/null | wc -l)

# Bucket 2: pop_data_batch removed
POP_DB=$(grep -E "no member named 'pop_data_batch'|use of undeclared identifier 'pop_data_batch'" "$LOG" 2>/dev/null | wc -l)

# Bucket 3: data_batch_processing_handle removed
DBPH=$(grep "data_batch_processing_handle" "$LOG" 2>/dev/null | grep -E "error|undefined|undeclared|no member" | wc -l)

# Bucket 4: FSM enum names — Expected 0 per D-F3 + Pitfall P7
FSM_ERR=$(grep -E "task_created|in_transit|idata_batch_probe" "$LOG" 2>/dev/null | grep -E "error|undeclared|undefined" | wc -l)

# Bucket 5: missing liburing header
LIBURING=$(grep -E "liburing.h.*No such file|cannot find -luring|fatal error: liburing" "$LOG" 2>/dev/null | wc -l)

# Bucket 6: RAII compile errors
RAII=$(grep -E "to_read_only|to_mutable|read_only_data_batch|mutable_data_batch" "$LOG" 2>/dev/null | grep -E "error|no matching|cannot convert" | wc -l)

# Total error lines
TOTAL_ALT=$(grep -cE "error:" "$LOG" 2>/dev/null)

echo "Bucket 1 (get_data private):        $PRIV_GETDATA  (expected: >= 26 per D-F1)"
echo "Bucket 2 (pop_data_batch removed):  $POP_DB        (expected: any non-zero)"
echo "Bucket 3 (data_batch_processing):   $DBPH          (expected: any non-zero)"
echo "Bucket 4 (FSM enum names):          $FSM_ERR       (expected: 0 — Pitfall P7)"
echo "Bucket 5 (missing liburing):        $LIBURING      (expected: 0 if dev preinstalled, else IO-12 territory)"
echo "Bucket 6 (RAII):                    $RAII          (expected: any non-zero)"
echo "Total error lines (alt grep):       $TOTAL_ALT"

EXPECTED_SUM=$((PRIV_GETDATA + POP_DB + DBPH + LIBURING + RAII))
UNRELATED=$((TOTAL_ALT - EXPECTED_SUM - FSM_ERR))
if [ "$UNRELATED" -lt 0 ]; then UNRELATED=0; fi
echo "Estimated unrelated errors:         $UNRELATED  (expected: 0 — D-F3 gate)"
```

If `Bucket 4 (FSM_ERR)` > 0: contradicts the FSM grep gate from Task 1 — surface as a regression and STOP.

If `UNRELATED > 0`: per D-F3 "investigate before proceeding" — examine build log around any error not matched by the buckets. Common cases:
- A header include that origin/dev added but our manual conflict resolution forgot — diagnose by reading the offending line and recent merge surface
- A CMake target missing because a `target_link_libraries(...)` line was lost in CMakeLists conflict resolution — fix in 17-02 if minor, or escalate
- A missing system dependency (e.g., liburing-dev) — document in C.2 Bucket 5 instead of "unrelated"

If `UNRELATED == 0` (the expected case): Section C verdict = PASS.

STEP C — Populate Section C of 17-MERGE-LOG.md:

For each Bucket row in Section C.2 table, replace `<filled>` with count from STEP B. Replace verdict cell in C.3 with PASS or INVESTIGATE based on UNRELATED count.

Also fill C.1 "Build log location" with: `.planning/phases/17-sirius-origin-dev-merge-base-layer/17-build-output.log`.

STEP D — Edge-case handling: build succeeded:

If the build EXITED 0 (very unlikely given D-F1 expectation), this means either: (a) we already have a separate Phase 18 baseline merged (no — Phase 18 is Not started), OR (b) origin/dev's auto-merge was so thorough that no batch->get_data() callers remain. Document this surprise in Section C.3 with verdict "UNEXPECTED-PASS — escalate to user; Phase 18 may already be partially complete in dev".

STEP E — Commit:
```
git add -f .planning/phases/17-sirius-origin-dev-merge-base-layer/17-MERGE-LOG.md \
           .planning/phases/17-sirius-origin-dev-merge-base-layer/17-build-output.log
git commit --no-verify -m "docs(17-03): populate 17-MERGE-LOG.md Section C (build error bounding) + capture 17-build-output.log (MERGE-05 / D-F1..F3)"
```

NO `rmm::cuda_stream_default` introduced. This task only runs read commands and edits markdown.
  </action>
  <verify>
    <automated>test -f .planning/phases/17-sirius-origin-dev-merge-base-layer/17-build-output.log && test -s .planning/phases/17-sirius-origin-dev-merge-base-layer/17-build-output.log && git log --oneline -1 -- .planning/phases/17-sirius-origin-dev-merge-base-layer/17-build-output.log | grep -q "17-03"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/17-sirius-origin-dev-merge-base-layer/17-build-output.log` exists and is non-empty (>= 100 lines)
    - 17-MERGE-LOG.md Section C.1 (Build log location) is populated
    - 17-MERGE-LOG.md Section C.2 (error bucket table) all rows have numeric counts (no `<filled>`)
    - 17-MERGE-LOG.md Section C.3 has Total / Expected sum / Unrelated count + verdict (PASS / INVESTIGATE / UNEXPECTED-PASS)
    - Bucket 4 (FSM enum errors) reads 0 — if not, plan blocks on investigation per D-F3 + P7
    - Commit exists with message starting `docs(17-03):` documenting Section C + log capture
  </acceptance_criteria>
  <done>
    Sirius build attempted; output captured to `17-build-output.log`. Errors classified by category. Bucket 4 (FSM names) is 0 (P7 regression check passes). Unrelated count documented (expected 0; if non-zero, recorded with investigation notes). Section C of 17-MERGE-LOG.md fully populated. MERGE-05 evidence in place for plan 17-04 to validate.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
- `test -f .planning/phases/17-sirius-origin-dev-merge-base-layer/17-build-output.log && test -s ...` (log captured)
- `grep -c "<filled>" .planning/phases/17-sirius-origin-dev-merge-base-layer/17-MERGE-LOG.md` returns only count for Section D rows that 17-04 will fill (Sections A, B, C all populated)
- `grep -rn "task_created\|in_transit\|data_batch_processing_handle\|idata_batch_probe" src/ test/ | wc -l` returns 0 (D-G3 / P7 regression check)
- `grep -c "_no_pref_rr_counter" src/include/pipeline/task_scheduler.hpp` returns >= 1 (D-G2 SCHED-RR field)
- `grep "SCHED-RR" src/pipeline/task_scheduler.cpp` returns at least one line (D-G2 SCHED-RR block)
- Build log shows expected error categories dominate; "unrelated" count == 0 OR explicitly investigated
- Two new commits exist on top of 17-02's tip with `docs(17-03):` prefix

Pitfall coverage:
- P6 (SCHED-RR counter port stale): D-G2 grep gates fired in Task 1 STEP D; both must be non-zero
- P7 (PR #739 × #117 ordering): D-G3 FSM grep gate (Task 1 STEP B) + Bucket 4 build error count (Task 2 STEP B) — both should be 0
- P11 (HYG-02 raw cudaSetDevice in uring_reactor): documented in Section B.3 as expected from #675; cleanup deferred to Phase 19 IO-16 (do NOT add TODOs here)
</verification>

<success_criteria>
- MERGE-05: Build error count is bounded and documented in 17-MERGE-LOG.md Section C; Bucket 1 ≥ 26 (the expected `get_data() private` errors); Bucket 4 == 0 (no re-introduced FSM names); Unrelated == 0 (or explicitly investigated)
- D-G2 verified: `_no_pref_rr_counter` and `SCHED-RR` both still present in their respective files
- D-G3 verified: zero hits of FSM enum names in src/ AND test/
- D-F1 confirmed: build fails with the expected category mix (not unexplained errors)
- D-F3 satisfied: zero unrelated errors, OR each unrelated error has an investigation note in Section C
- Pre/post-merge HYG-02 delta documented (expected positive due to #675; deferred to Phase 19)
</success_criteria>

<output>
After completion, create `.planning/phases/17-sirius-origin-dev-merge-base-layer/17-03-SUMMARY.md` documenting:
1. Auto-merged file count (actual vs CONTEXT.md's "33")
2. FSM grep results: src/ count, test/ count (both should be 0)
3. HYG-02: pre-merge count, post-merge count, delta, top contributing files
4. SCHED-RR survival: `_no_pref_rr_counter` count in task_scheduler.hpp; SCHED-RR mention count in task_scheduler.cpp
5. Build error counts per Bucket (1-6) + Unrelated count
6. Verdict: PASS (expected D-F1 state) / INVESTIGATE (unrelated > 0) / UNEXPECTED-PASS (build succeeded)
7. Build log path + size
</output>
