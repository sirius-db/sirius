---
phase: 16-cucascade-submodule-rebase-pin-recovery
plan: 03
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - cucascade/src/memory/common.cpp
  - cucascade/src/data/representation_converter.cpp
autonomous: true
requirements: [CC-02]
must_haves:
  truths:
    - "After rebasing Group 2 onto the 16-02 tip, HEAD is 3 commits above 73d00c4 (Group 1, Group 3, Group 2 — note Group 2 is applied AFTER Group 3, but the squashed-group commit messages preserve original chronology)"
    - "p2p_dma_supported() probe block exists in cucascade/src/memory/common.cpp with the empirical peer-DMA test (Section A 'Conflict File 5' — ~120 lines)"
    - "convert_host_to_gpu (cudf::pack path) constructs gpu_table_representation with a stream argument (provisional 3-arg form before Group 4 finalizes the writer_stream contract)"
    - "convert_host_fast_to_gpu constructs gpu_table_representation with a stream argument"
    - "convert_disk_to_gpu constructs gpu_table_representation with a stream argument"
    - "p2p_dma_supported is referenced (called) from src/data/representation_converter.cpp inside convert_gpu_to_gpu (P2P routing logic — provisional version that Group 4 will rewrite)"
  artifacts:
    - path: cucascade/src/memory/common.cpp
      provides: "P2P peer-DMA probe block (Group 2's main contribution to common.cpp)"
      contains: "p2p_dma_supported"
    - path: cucascade/src/data/representation_converter.cpp
      provides: "Stream-bound converter construction sites + provisional convert_gpu_to_gpu P2P routing"
      contains: "p2p_dma_supported"
  key_links:
    - from: "convert_gpu_to_gpu"
      to: "p2p_dma_supported"
      via: "function call to decide host-staging vs peer-DMA"
      pattern: "p2p_dma_supported"
    - from: "convert_host_to_gpu / convert_host_fast_to_gpu / convert_disk_to_gpu"
      to: "gpu_table_representation ctor"
      via: "stream argument passed at construction site"
      pattern: "gpu_table_representation\\(.*stream"
---

<objective>
Cherry-pick the squashed Group 2 commit (P2P override — target-bound stream + DMA probe at init) on top of 16-02's HEAD. Resolve the conflict in `src/data/representation_converter.cpp` per RESEARCH.md Section A "Conflict File 3", and re-apply Group 2's content to `src/memory/common.cpp` (the `p2p_dma_supported` probe block, which is Group 2's contribution beyond Group 1's `enable_pool_peer_access_for_all_visible_devices` helper).

**Important constraint:** This plan re-applies Group 2's stream-wiring across `convert_host_to_gpu` / `convert_host_fast_to_gpu` / `convert_disk_to_gpu`, but does NOT yet finalize `convert_gpu_to_gpu`'s column-tree-walk + `cudaStreamWaitEvent`. That is Group 4's job (16-04). At the end of 16-03, `convert_gpu_to_gpu` may still use the `cudf::pack` path (from 73d00c4) augmented with Group 2's P2P routing logic — i.e., a provisional shape that will be fully rewritten by Group 4.

Per RESEARCH.md Section D Round 2:
> "Note: Group 4 will later fully rewrite convert_gpu_to_gpu, so at this group it's acceptable to have a partial/provisional convert_gpu_to_gpu as long as the P2P routing logic compiles."

Purpose: Land Group 2's content (P2P probe + stream wiring) before the dual-group Group 4 round. Keeps 16-04's scope to the genuinely-novel Phase 13 surgery (writer_event API + column-tree walk).
Output: 3-commit cucascade branch (Group 1, Group 3, Group 2) on top of 73d00c4. Build does NOT need to be green yet — Group 4 in 16-04 will close the build gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-cucascade-submodule-rebase-pin-recovery/16-CONTEXT.md
@.planning/phases/16-cucascade-submodule-rebase-pin-recovery/16-RESEARCH.md
@.planning/phases/16-cucascade-submodule-rebase-pin-recovery/16-rebase-log.md
@.planning/research/PITFALLS.md
@.planning/phases/16-cucascade-submodule-rebase-pin-recovery/16-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cherry-pick Group 2 + resolve common.cpp + representation_converter.cpp conflicts</name>
  <files>
    cucascade/src/memory/common.cpp
    cucascade/src/data/representation_converter.cpp
  </files>
  <read_first>
    - .planning/phases/16-cucascade-submodule-rebase-pin-recovery/16-RESEARCH.md Section A "Conflict File 3: src/data/representation_converter.cpp" (HIGHEST RISK — full re-implementation guidance)
    - .planning/phases/16-cucascade-submodule-rebase-pin-recovery/16-RESEARCH.md Section A "Conflict File 5: src/memory/common.cpp" (P2P probe block detail)
    - .planning/phases/16-cucascade-submodule-rebase-pin-recovery/16-CONTEXT.md (D-D2 — re-implement against new shape; D-D3 — combine signature-change conflicts)
    - .planning/research/PITFALLS.md P2 (writer_stream survival in representation_converter.cpp — though full survival is Group 4's job, the stream-wiring of OTHER converters is Group 2's job)
    - cucascade/src/memory/common.cpp at Group 2: `git -C cucascade show phase16-squashed-group2:src/memory/common.cpp`
    - cucascade/src/memory/common.cpp at current HEAD (post-16-02): `git -C cucascade show HEAD:src/memory/common.cpp`
    - cucascade/src/data/representation_converter.cpp at Group 2: `git -C cucascade show phase16-squashed-group2:src/data/representation_converter.cpp`
    - cucascade/src/data/representation_converter.cpp at 73d00c4 (no Group changes yet): `git -C cucascade show 73d00c4:src/data/representation_converter.cpp`
    - cucascade/include/cucascade/data/gpu_data_representation.hpp at 73d00c4: `git -C cucascade show 73d00c4:include/cucascade/data/gpu_data_representation.hpp` — to confirm at 73d00c4 the ctor is 2-arg (no writer_stream); we are passing a stream as a PROVISIONAL 3rd arg that 16-04 will formalize when it adds writer_stream as REQUIRED to the header
  </read_first>
  <action>
**CRITICAL CAVEAT for this round:** At the end of 16-02, the `gpu_table_representation` header (`include/cucascade/data/gpu_data_representation.hpp`) is still at the `73d00c4` shape — the 2-arg ctor (no writer_stream). 16-04 will modify the header to require writer_stream as a 3rd ctor argument. So when we cherry-pick Group 2's converter changes that "add a stream argument to construction sites", we have two options:

**Option A (preferred per D-D2):** Don't yet add the 3rd arg in this plan. Instead, add the `cudaStream_t` / `rmm::cuda_stream_view` *variable declarations* at the converter call sites (so the stream is in scope) but call the 2-arg ctor. Then in 16-04, after the header is changed to 3-arg, edit each site to pass the already-in-scope stream as the 3rd arg.

**Option B (per RESEARCH.md Section D Round 2 note):** Add the 3rd arg now, accepting that the build is BROKEN at the end of 16-03 because the header doesn't yet have the 3-arg ctor. 16-04 fixes the build by updating the header.

**Choose Option B** per RESEARCH.md's explicit Round 2 note (build green is not required between rounds). It minimizes churn — 16-04 won't have to revisit converter call sites.

```bash
cd /home/felipe/sirius/.worktrees/ws-9aa781df-6d8c-4395-9329-737a67e8e272/cucascade

git cherry-pick phase16-squashed-group2
```

Conflicts expected in:
1. `src/memory/common.cpp` — Group 2 adds the P2P probe block on top of 16-02's `enable_pool_peer_access_for_all_visible_devices` helper.
2. `src/data/representation_converter.cpp` — Group 2 adds stream args to converter call sites + adds a P2P-probe-routing branch in `convert_gpu_to_gpu` (the cudf::pack path at 73d00c4).

### Resolving `src/memory/common.cpp`:

After 16-02, `common.cpp` has:
- The `enable_pool_peer_access_for_all_visible_devices` helper (from 16-02 Group 1)
- 73d00c4's base content

Group 2's patch ADDS:
- `#include <mutex>` (if not already there from Group 1)
- `kMaxDevices` constant
- `g_p2p_supported`, `g_p2p_probed` static atomics
- `p2p_probe_mutex()` helper
- `run_p2p_probe_locked()` helper
- `p2p_dma_supported(int src, int dst)` entry point

Per D-D1 (additive, prefer ours), copy the entire P2P probe block into the anonymous namespace inside `cucascade::memory`. The helper from 16-02 should remain — co-locate the probe block alongside it.

Reference: extract Group 2's exact text via `git -C cucascade show phase16-squashed-group2:src/memory/common.cpp` and copy the lines that are NOT in `git -C cucascade show HEAD:src/memory/common.cpp`. The diff between those two trees IS Group 2's `common.cpp` contribution.

A practical extraction recipe:
```bash
git -C cucascade show phase16-squashed-group2:src/memory/common.cpp > "$TMPDIR/g2_common.cpp"
git -C cucascade show HEAD:src/memory/common.cpp > "$TMPDIR/cur_common.cpp"
diff -u "$TMPDIR/cur_common.cpp" "$TMPDIR/g2_common.cpp" > "$TMPDIR/g2_common_delta.diff"
cat "$TMPDIR/g2_common_delta.diff"
```

The `diff` output shows the lines Group 2 adds. Apply them manually to the conflicted `src/memory/common.cpp`. After resolution:

```bash
grep -c "p2p_dma_supported\|run_p2p_probe_locked\|g_p2p_probed" src/memory/common.cpp
# Expected: >= 3 (definition site of each)
grep -c "enable_pool_peer_access_for_all_visible_devices" src/memory/common.cpp
# Expected: >= 1 (still present from 16-02)
git add src/memory/common.cpp
```

### Resolving `src/data/representation_converter.cpp`:

This is where it gets complex. Per RESEARCH.md Section A Conflict File 3:

The `73d00c4` version uses:
- `get_table_view()` (NOT `get_table()`)
- 2-arg ctor for `gpu_table_representation`
- `cudf::pack`-based `convert_gpu_to_gpu`

Group 2's contribution:
- Stream-wiring in `convert_host_to_gpu` (~line 246 at HEAD)
- Stream-wiring in `convert_host_fast_to_gpu` (~line 853 at HEAD)
- Stream-wiring in `convert_disk_to_gpu` (~line 1453 at HEAD)
- A P2P-routing branch in `convert_gpu_to_gpu`: call `p2p_dma_supported(src_dev, dst_dev)` and decide host-staging vs peer-DMA path

Resolution steps (per D-D2 — re-implement against new shape):

1. Start from `73d00c4`'s `representation_converter.cpp` (what 16-02 had at HEAD; same content since 16-02 didn't touch this file).
2. For each of `convert_host_to_gpu`, `convert_host_fast_to_gpu`, `convert_disk_to_gpu`:
   - Locate the `gpu_table_representation` construction call (typically a `std::make_unique<gpu_table_representation>(table, mem_space)` or similar).
   - Convert it to `std::make_unique<gpu_table_representation>(std::move(table), mem_space, stream)` — passing the function's local `stream` (or `target_stream`) as the 3rd arg.
   - **Note:** at this point the `gpu_data_representation.hpp` header still has only the 2-arg ctor — the build will FAIL at the end of this plan. That is acceptable per RESEARCH.md Section D Round 2 note. 16-04 fixes the header in the next round.
3. For `convert_gpu_to_gpu`:
   - Keep `73d00c4`'s `cudf::pack`-based implementation as the base (Group 4 in 16-04 will replace it).
   - At the entry of the function, add the P2P routing call: `if (p2p_dma_supported(source_device_id, target_device_id)) { /* peer-DMA path */ } else { /* host-staging path */ }`.
   - Pull the exact branching shape from Group 2's version: `git -C cucascade show phase16-squashed-group2:src/data/representation_converter.cpp` and look for the body of `convert_gpu_to_gpu`.
   - Update the `gpu_table_representation` construction at the end of `convert_gpu_to_gpu` to use 3-arg form (passing `target_stream`).
4. For all 4 converter functions, `gpu_source.get_table()` calls must become `gpu_source.get_table_view()` (per RESEARCH.md Section A "Key API rename").

Reference Section A's per-call-site table:
| Function | 3rd arg variable name |
|----------|-----------------------|
| `convert_gpu_to_gpu` | `target_stream` |
| `convert_host_to_gpu` | `stream` |
| `convert_host_fast_to_gpu` | `stream` |
| `convert_disk_to_gpu` | `stream` |

After resolution, verify:

```bash
# All converter call sites should use 3-arg ctor with stream:
grep -nE "make_unique<gpu_table_representation>\(.*,.*,.*stream" src/data/representation_converter.cpp
# Expected: at least 3 lines (convert_host_to_gpu, convert_host_fast_to_gpu, convert_disk_to_gpu, convert_gpu_to_gpu)

# get_table() should be replaced by get_table_view() everywhere:
grep -c "\.get_table()" src/data/representation_converter.cpp
# Expected: 0

# p2p_dma_supported should be referenced inside convert_gpu_to_gpu:
grep -c "p2p_dma_supported" src/data/representation_converter.cpp
# Expected: >= 1

git add src/data/representation_converter.cpp
git -c core.editor=true cherry-pick --continue
```

If `cherry-pick --continue` fails because the editor is opened for commit-message editing, the `core.editor=true` should suppress the prompt and accept the existing message. If still fails, manually run:

```bash
git -c core.editor=true commit -C phase16-squashed-group2 --allow-empty
```

Verify the resulting commit:

```bash
git log -1 --format=%s
# Expected: fix(representation_converter): P2P override — target-bound stream, DMA probe at init
git log --oneline 73d00c4..HEAD
# Expected: 3 commits (Group 1, Group 3, Group 2) — note: Group 2 is now the most recent, applied AFTER Group 3
```

**About the order**: The original chronological order was Group 1 → Group 2 → Group 3 → Group 4. We've applied Group 1 → Group 3 → Group 2 (since 16-02 deliberately reordered Group 3 before Group 2 to land easy fixes first). Per D-A1 the GROUPS are still self-consistent commits; the order within the rebased branch is permitted to differ from the original chronology. This is acceptable per "preserves the carry as 4 group commits" in 16-01's CC-02 evidence.
  </action>
  <verify>
    <automated>cd /home/felipe/sirius/.worktrees/ws-9aa781df-6d8c-4395-9329-737a67e8e272/cucascade && [ "$(git rev-list --count 73d00c4..HEAD)" = "3" ] && grep -q "p2p_dma_supported" src/memory/common.cpp && grep -q "p2p_dma_supported" src/data/representation_converter.cpp && [ "$(grep -c '\.get_table()' src/data/representation_converter.cpp)" = "0" ] && [ "$(grep -cE 'make_unique<gpu_table_representation>\(.*,.*,.*stream' src/data/representation_converter.cpp)" -ge "3" ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `git -C cucascade rev-list --count 73d00c4..HEAD` returns `3`
    - `git -C cucascade log -1 --format=%s` returns `fix(representation_converter): P2P override — target-bound stream, DMA probe at init`
    - `grep -c "p2p_dma_supported\|run_p2p_probe_locked" cucascade/src/memory/common.cpp` returns >= 2
    - `grep -c "enable_pool_peer_access_for_all_visible_devices" cucascade/src/memory/common.cpp` returns >= 1 (16-02's helper still present)
    - `grep -c "p2p_dma_supported" cucascade/src/data/representation_converter.cpp` returns >= 1 (called inside convert_gpu_to_gpu)
    - `grep -c "\.get_table()" cucascade/src/data/representation_converter.cpp` returns `0` (all renamed to get_table_view)
    - `grep -cE "make_unique<gpu_table_representation>\(.*,.*,.*stream" cucascade/src/data/representation_converter.cpp` returns >= 3 (3-arg ctor sites)
    - `grep -c "MUST be last" cucascade/src/data/pipeline_io_backend.cpp` still returns >= 1 (Group 3 from 16-02 not regressed)
  </acceptance_criteria>
  <done>Group 2 cherry-pick applied. P2P probe block in common.cpp. Stream args wired at 3 converter sites. p2p_dma_supported called inside convert_gpu_to_gpu. get_table() → get_table_view() rename complete in converter.</done>
</task>

<task type="auto">
  <name>Task 2: Update 16-rebase-log.md Round 3 section</name>
  <files>.planning/phases/16-cucascade-submodule-rebase-pin-recovery/16-rebase-log.md</files>
  <read_first>
    - .planning/phases/16-cucascade-submodule-rebase-pin-recovery/16-rebase-log.md (existing audit log; section "Round 3")
  </read_first>
  <action>
Use Edit tool to update the Round 3 (Group 2 — stream/converter) section. Per the existing scaffolding the section header is "### Round 3 (Group 2 — stream/converter) — 16-03".

Replace `Status: pending` → `Status: applied (provisional convert_gpu_to_gpu — finalized in 16-04)`.

Replace `Resolution notes: (filled by 16-03)` → A 6-10 line summary covering:
- Files: `src/memory/common.cpp` (P2P probe block re-applied alongside 16-02's helper), `src/data/representation_converter.cpp` (3 converter sites wired with stream + P2P routing in convert_gpu_to_gpu)
- API rename: `get_table()` → `get_table_view()` across the file (per #117 surface)
- Provisional convert_gpu_to_gpu: still uses cudf::pack-based path from 73d00c4 augmented with `p2p_dma_supported` routing — Group 4 (16-04) will replace it with the column-tree walk
- Build state at end of 16-03: NOT compile-clean (header still has 2-arg ctor; 3 converter sites pass 3 args) — 16-04 fixes by adding writer_stream as REQUIRED 3rd ctor arg
- Per D-A1 the apply order on the rebased branch is Group 1 → Group 3 → Group 2 (different from original chronological 1 → 2 → 3 → 4); acceptable per CC-02 "preserves carry as 4 group commits"
- Group 2's resulting commit SHA (run `git -C cucascade rev-parse HEAD`)

Verify:
```bash
grep -A2 "### Round 3" .planning/phases/16-cucascade-submodule-rebase-pin-recovery/16-rebase-log.md | grep -q "applied"
```
  </action>
  <verify>
    <automated>grep -A4 "### Round 3" /home/felipe/sirius/.worktrees/ws-9aa781df-6d8c-4395-9329-737a67e8e272/.planning/phases/16-cucascade-submodule-rebase-pin-recovery/16-rebase-log.md | grep -q "applied" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Round 3 section status reads "applied" (with optional "provisional" qualifier)
    - Round 3 section mentions both `src/memory/common.cpp` and `src/data/representation_converter.cpp`
    - Round 3 section mentions `p2p_dma_supported` and `get_table_view`
    - Round 3 section explicitly notes the build is NOT compile-clean at end of 16-03 (will be in 16-04)
  </acceptance_criteria>
  <done>Audit log Round 3 section recorded with concrete resolution notes + provisional state caveat.</done>
</task>

</tasks>

<verification>
- 3 commits on top of `73d00c4` (Group 1, Group 3, Group 2 in apply-order on the rebased branch)
- P2P probe block (`p2p_dma_supported`, `run_p2p_probe_locked`, etc.) in `src/memory/common.cpp` co-located with 16-02's helper
- All 3 of `convert_host_to_gpu`, `convert_host_fast_to_gpu`, `convert_disk_to_gpu` construct `gpu_table_representation` with a 3rd stream argument
- `convert_gpu_to_gpu` calls `p2p_dma_supported` for routing
- `get_table()` API removed everywhere in `representation_converter.cpp` (renamed to `get_table_view()` per #117)
- 16-02's invariants preserved: `_thread` last in io_worker; `cudaHostAllocPortable` in pinned-allocation files; pool-priming arg removed from `cuda_async_memory_resource` ctor
- 16-rebase-log.md Round 3 section marked "applied (provisional)"
- Build is NOT yet compile-clean — that's 16-04's gate
</verification>

<success_criteria>
- `git -C cucascade rev-list --count 73d00c4..HEAD` returns 3
- `grep -c "p2p_dma_supported" cucascade/src/memory/common.cpp` returns >= 1 (definition)
- `grep -c "p2p_dma_supported" cucascade/src/data/representation_converter.cpp` returns >= 1 (call site)
- `grep -c "\.get_table()" cucascade/src/data/representation_converter.cpp` returns 0
- Plan completes within ~50% context (~30-40 min — this is the trickiest non-Group-4 round)
</success_criteria>

<output>
After completion, create `.planning/phases/16-cucascade-submodule-rebase-pin-recovery/16-03-SUMMARY.md`
</output>
