---
phase: 03-operator-sweep-and-clean-build
plan: 03
type: execute
wave: 1
depends_on: ["03-02"]
files_modified:
  # Task 1: Operator clone/to_idle fixes (9 files)
  - src/op/sirius_physical_hash_join.cpp
  - src/op/sirius_physical_table_scan.cpp
  - src/op/sirius_physical_concat.cpp
  - src/op/sirius_physical_grouped_aggregate.cpp
  - src/op/sirius_physical_grouped_aggregate_merge.cpp
  - src/op/sirius_physical_merge_sort.cpp
  - src/op/sirius_physical_order.cpp
  - src/op/sirius_physical_partition.cpp
  - src/op/sirius_physical_ungrouped_aggregate.cpp
  # Task 1: Additional source fixes (4 files)
  - src/cuda/print.cu
  - src/include/print.hpp
  - src/downgrade/downgrade_executor.cpp
  - src/op/scan/sirius_gpu_parquet_scan_operator.cpp
  # Task 1: Test file fixes (17 files)
  - test/cpp/downgrade/test_downgrade_executor.cpp
  - test/cpp/expression_executor/test_gpu_expression_executor.cpp
  - test/cpp/operator/aggregate/test_physical_grouped_aggregate.cpp
  - test/cpp/operator/operator_test_utils.hpp
  - test/cpp/operator/test_physical_filter.cpp
  - test/cpp/operator/test_physical_limit.cpp
  - test/cpp/operator/test_physical_mark_join.cpp
  - test/cpp/operator/test_physical_merge_sort.cpp
  - test/cpp/operator/test_physical_order.cpp
  - test/cpp/operator/test_physical_partition.cpp
  - test/cpp/operator/test_physical_projection.cpp
  - test/cpp/operator/test_physical_table_scan.cpp
  - test/cpp/operator/test_physical_top_n.cpp
  - test/cpp/operator/test_physical_ungrouped_aggregate.cpp
  - test/cpp/pipeline/test_gpu_pipeline_disk_readback.cpp
  - test/cpp/pipeline/test_gpu_pipeline_task_history.cpp
  - test/cpp/scan/test_utils.hpp
autonomous: true
gap_closure: true
requirements: [OPER-01, OPER-02, OPER-03, OPER-04, ACCS-01, ACCS-02, ACCS-03, ACCS-04, BILD-01]

must_haves:
  truths:
    - "All 9 operator files use batch.clone(id, stream) directly — no to_idle() wrapper around clone() calls"
    - "sirius_physical_table_scan.cpp passes (sirius::get_next_batch_id(), stream) to clone() — no zero-arg clone() call"
    - "All uncommitted source and test fixes are committed to git HEAD"
    - "CMAKE_BUILD_PARALLEL_LEVEL=$(nproc) make completes with zero errors from committed code at HEAD"
  artifacts:
    - path: "src/op/sirius_physical_hash_join.cpp"
      provides: "Hash join clone without to_idle wrapper"
      contains: "build_batch_ro.clone(sirius::get_next_batch_id(), stream)"
    - path: "src/op/sirius_physical_table_scan.cpp"
      provides: "Table scan clone with required arguments"
      contains: "batch_ref_ptr->clone(sirius::get_next_batch_id(), stream)"
    - path: "build/release/extension/sirius/sirius.duckdb_extension"
      provides: "Clean build artifact compiled from committed HEAD"
  key_links:
    - from: "src/op/sirius_physical_*.cpp"
      to: "cucascade/include/cucascade/data/data_batch.hpp"
      via: "read_only_data_batch::clone(uint64_t, rmm::cuda_stream_view) returning shared_ptr<data_batch>"
      pattern: "\\.clone\\(sirius::get_next_batch_id\\(\\)"
---

<objective>
Commit the 30 uncommitted working-tree fixes that close the final BILD-01 gap, then verify the project compiles cleanly from committed code at HEAD.

Purpose: The verification report found that the build artifact on disk was produced from uncommitted working-tree changes. The committed code at HEAD has 9 operator files calling `data_batch::to_idle(batch.clone(id, stream))` which is a type mismatch (clone() returns `shared_ptr<data_batch>`, to_idle() requires `read_only_data_batch&&` or `mutable_data_batch&&`). Additionally, `sirius_physical_table_scan.cpp` calls clone() with zero arguments where two are required. There are also 4 additional source file fixes (print.cu, print.hpp, downgrade_executor.cpp, sirius_gpu_parquet_scan_operator.cpp) and 17 test file fixes in the working tree that are needed for a clean build. All fixes are already correct on disk but were never committed.

Output: All fixes committed to git; clean build verified from committed HEAD.
</objective>

<execution_context>
@/home/william/repos2/sirius/.claude/get-shit-done/workflows/execute-plan.md
@/home/william/repos2/sirius/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-operator-sweep-and-clean-build/03-VERIFICATION.md
@.planning/phases/03-operator-sweep-and-clean-build/03-01-SUMMARY.md
@.planning/phases/03-operator-sweep-and-clean-build/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and commit all uncommitted working-tree API fixes</name>
  <files>
    src/op/sirius_physical_hash_join.cpp
    src/op/sirius_physical_table_scan.cpp
    src/op/sirius_physical_concat.cpp
    src/op/sirius_physical_grouped_aggregate.cpp
    src/op/sirius_physical_grouped_aggregate_merge.cpp
    src/op/sirius_physical_merge_sort.cpp
    src/op/sirius_physical_order.cpp
    src/op/sirius_physical_partition.cpp
    src/op/sirius_physical_ungrouped_aggregate.cpp
    src/cuda/print.cu
    src/include/print.hpp
    src/downgrade/downgrade_executor.cpp
    src/op/scan/sirius_gpu_parquet_scan_operator.cpp
    test/cpp/downgrade/test_downgrade_executor.cpp
    test/cpp/expression_executor/test_gpu_expression_executor.cpp
    test/cpp/operator/aggregate/test_physical_grouped_aggregate.cpp
    test/cpp/operator/operator_test_utils.hpp
    test/cpp/operator/test_physical_filter.cpp
    test/cpp/operator/test_physical_limit.cpp
    test/cpp/operator/test_physical_mark_join.cpp
    test/cpp/operator/test_physical_merge_sort.cpp
    test/cpp/operator/test_physical_order.cpp
    test/cpp/operator/test_physical_partition.cpp
    test/cpp/operator/test_physical_projection.cpp
    test/cpp/operator/test_physical_table_scan.cpp
    test/cpp/operator/test_physical_top_n.cpp
    test/cpp/operator/test_physical_ungrouped_aggregate.cpp
    test/cpp/pipeline/test_gpu_pipeline_disk_readback.cpp
    test/cpp/pipeline/test_gpu_pipeline_task_history.cpp
    test/cpp/scan/test_utils.hpp
  </files>
  <read_first>
    src/op/sirius_physical_hash_join.cpp
    src/op/sirius_physical_table_scan.cpp
    src/op/sirius_physical_concat.cpp
    src/op/sirius_physical_ungrouped_aggregate.cpp
    cucascade/include/cucascade/data/data_batch.hpp
  </read_first>
  <action>
    The fixes are already present in the working tree. This task verifies correctness and commits them.

    **Step 1: Verify the 9 operator clone/to_idle fixes are correct on disk.**

    For each of these 9 files, confirm the pattern `data_batch::to_idle(*.clone(` does NOT appear,
    and instead `*.clone(sirius::get_next_batch_id(), stream)` is used directly (without to_idle wrapper):

    - src/op/sirius_physical_hash_join.cpp (~line 848): Should be `_build_table = build_batch_ro.clone(sirius::get_next_batch_id(), stream);`
    - src/op/sirius_physical_concat.cpp (~line 187): Should be `valid_batches.push_back(batch.clone(sirius::get_next_batch_id(), stream));`
    - src/op/sirius_physical_grouped_aggregate.cpp (~line 180): Should be `auto idle_batch = input_batch.clone(sirius::get_next_batch_id(), stream);`
    - src/op/sirius_physical_grouped_aggregate_merge.cpp (~line 208): Should be `idle_batches.push_back(batch.clone(sirius::get_next_batch_id(), stream));`
    - src/op/sirius_physical_merge_sort.cpp (~line 91): Should be `valid_batches.push_back(batch.clone(sirius::get_next_batch_id(), stream));`
    - src/op/sirius_physical_order.cpp (~line 79): Should be `auto idle_batch = batch.clone(sirius::get_next_batch_id(), stream);`
    - src/op/sirius_physical_partition.cpp (~line 183): Should be `auto input_batch = input_batch_ro.clone(sirius::get_next_batch_id(), stream);`
    - src/op/sirius_physical_ungrouped_aggregate.cpp (~line 500): Should be `valid_batches.push_back(batch.clone(sirius::get_next_batch_id(), stream));`
    - src/op/sirius_physical_table_scan.cpp (~line 180): Should be `output_batch = batch_ref_ptr->clone(sirius::get_next_batch_id(), stream);` (NOT zero-arg clone)

    **Semantic rationale:** `read_only_data_batch::clone(uint64_t, rmm::cuda_stream_view)` returns `shared_ptr<data_batch>` which is already in idle state. Wrapping in `data_batch::to_idle()` is wrong because `to_idle()` accepts `read_only_data_batch&&` or `mutable_data_batch&&` (accessor rvalue refs), not `shared_ptr<data_batch>`.

    **Step 2: Verify the 4 additional source fixes are correct on disk.**

    - src/cuda/print.cu: `print_data_batch_contents` signature changed from `const data_batch&` to `data_batch&` (non-const) because `to_read_only()` is non-const. Includes NOLINTNEXTLINE comment.
    - src/include/print.hpp: Matching declaration change from `const data_batch&` to `data_batch&` with NOLINTNEXTLINE comment.
    - src/downgrade/downgrade_executor.cpp: `get_repositories()` replaced with `for_each_repository()` lambda pattern (API change in cucascade d9dc331).
    - src/op/scan/sirius_gpu_parquet_scan_operator.cpp: Removed explicit `input_batch_ro = {};` comment line — RAII destructor handles cleanup automatically.

    **Step 3: Verify the 17 test file changes are present on disk.**

    Run `git diff HEAD --stat -- test/` to confirm all 17 test files have modifications. These are compilation fixes for the new cucascade API (removing old batch_state references, replacing try_to_create_task, using to_read_only/to_mutable accessors, etc.).

    **Step 4: Stage and commit all 30 files.**

    Stage all modified source and test files (the exact set from `git diff HEAD --name-only -- src/ test/`).
    Commit with message: `fix(03-03): commit uncommitted API fixes for clean build against cucascade d9dc331`

    Do NOT stage any files outside src/ and test/ (e.g., .planning/, log/, tools/, etc.).
  </action>
  <verify>
    <automated>
      cd /home/william/repos2/sirius && git diff HEAD -- src/ test/ --stat | wc -l
    </automated>
    After commit, the command above should output 0 (no uncommitted changes in src/ or test/).
    Additionally verify: `git log -1 --oneline` shows the new commit.
    Additionally verify: `grep -r 'to_idle.*clone' src/op/ | wc -l` returns 0.
    Additionally verify: `grep -rn 'clone()' src/op/sirius_physical_table_scan.cpp | grep -v get_next_batch_id | wc -l` returns 0 (no zero-arg clone).
  </verify>
  <acceptance_criteria>
    - `git diff HEAD -- src/ test/` produces empty output (all changes committed)
    - `grep -r 'to_idle.*\.clone(' src/op/` returns zero matches
    - `grep -rn '->clone()' src/op/sirius_physical_table_scan.cpp` returns zero matches (no zero-arg clone)
    - `git log -1 --format=%s` contains "fix(03-03)"
    - All 30 files appear in the commit's diff stat
  </acceptance_criteria>
  <done>All 30 uncommitted working-tree fixes (9 operator clone/to_idle, 4 additional source, 17 test files) are committed to git HEAD</done>
</task>

<task type="auto">
  <name>Task 2: Verify clean build from committed HEAD against cucascade d9dc331</name>
  <files>build/release/extension/sirius/sirius.duckdb_extension</files>
  <read_first>
    src/op/sirius_physical_hash_join.cpp
  </read_first>
  <action>
    Run a full build from the committed code at HEAD to verify BILD-01 is satisfied.

    **Step 1: Confirm no uncommitted source/test changes remain.**

    Run `git diff HEAD --name-only -- src/ test/` and verify empty output. If any changes remain, STOP and investigate — Task 1 should have committed everything.

    **Step 2: Clean stale build artifacts and rebuild.**

    Run: `rm -rf build && CMAKE_BUILD_PARALLEL_LEVEL=$(nproc) make`

    This performs a full clean rebuild from the committed tree. The build must complete with zero errors.

    **Step 3: Verify build artifacts exist.**

    Confirm these files exist after build:
    - `build/release/extension/sirius/sirius.duckdb_extension`
    - `build/release/extension/sirius/test/cpp/sirius_unittest`

    **Step 4: Final grep verification of all BILD-01 blockers.**

    Run these checks to confirm no type-mismatch patterns remain in committed code:
    - `grep -r 'to_idle.*\.clone(' src/` should return 0 matches
    - `grep -rn '->clone()' src/op/` should return 0 matches (no zero-arg clone anywhere in operators)
  </action>
  <verify>
    <automated>
      ls -la /home/william/repos2/sirius/build/release/extension/sirius/sirius.duckdb_extension && ls -la /home/william/repos2/sirius/build/release/extension/sirius/test/cpp/sirius_unittest
    </automated>
    Both files must exist with a timestamp after the commit from Task 1.
  </verify>
  <acceptance_criteria>
    - `build/release/extension/sirius/sirius.duckdb_extension` exists with timestamp after Task 1 commit
    - `build/release/extension/sirius/test/cpp/sirius_unittest` exists with timestamp after Task 1 commit
    - Build log contains no error lines (grep for "error:" in build output returns 0)
    - `grep -r 'to_idle.*\.clone(' src/` returns 0 matches
    - `grep -rn '->clone()' src/op/` returns 0 matches
  </acceptance_criteria>
  <done>Project compiles cleanly against cucascade d9dc331 from committed code at HEAD — BILD-01 satisfied</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This is a commit-and-build verification task on existing code in the working tree.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Uncommitted working tree | accept | All changes are developer-authored API fixes already reviewed in VERIFICATION.md; no external input |
</threat_model>

<verification>
After both tasks complete:
1. `git diff HEAD -- src/ test/` is empty (all changes committed)
2. `grep -r 'to_idle.*\.clone(' src/` returns 0 matches
3. `grep -rn '->clone()' src/op/` returns 0 matches
4. `build/release/extension/sirius/sirius.duckdb_extension` exists and was built after the commit
5. `build/release/extension/sirius/test/cpp/sirius_unittest` exists and was built after the commit
</verification>

<success_criteria>
- All 30 uncommitted API migration fixes are committed to git HEAD
- `CMAKE_BUILD_PARALLEL_LEVEL=$(nproc) make` completes with zero errors from committed code
- Both build artifacts (extension + test binary) exist with post-commit timestamps
- BILD-01 requirement is fully satisfied: project compiles cleanly against cucascade d9dc331
</success_criteria>

<output>
After completion, create `.planning/phases/03-operator-sweep-and-clean-build/03-03-SUMMARY.md`
</output>
