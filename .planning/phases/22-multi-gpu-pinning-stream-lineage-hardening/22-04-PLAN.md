---
phase: 22-multi-gpu-pinning-stream-lineage-hardening
plan: 04
type: execute
wave: 2
depends_on:
  - 22-03
files_modified:
  - cucascade
autonomous: true
requirements:
  - fu17-cluster-b
must_haves:
  truths:
    - "Sirius parent repo cucascade submodule pin advances from 1c1e648 to the Plan 03 fix commit SHA"
    - "Sirius MCP build exit 0 with the new pin (compile chain still clean)"
    - "[mgpu] 16/16 PASS on the new pin (no regression vs Phase 21 baseline 79091 assertions / 106.3s)"
    - "HYG-02 invariant preserved at 40 across src/ post-pin-bump"
  artifacts:
    - path: "cucascade"
      provides: "Submodule pin advanced to Plan 03 Cluster B fix commit"
      contains: "Gitlink to the new cucascade SHA"
  key_links:
    - from: ".gitmodules cucascade entry"
      to: "Plan 03 fix commit on local cucascade fork"
      via: "git submodule update + git add cucascade + commit"
      pattern: "Subproject commit"
---

<objective>
Advance the Sirius parent repository's `cucascade/` submodule pin from `1c1e648` to the Plan 03 fix commit, then verify integration smoke (MCP build + `[mgpu]` 16/16) so that subsequent plans (05 routing test, 06 sanitizer gate, 07 v1.4 ship gauntlet) execute against the Cluster B fix.

Purpose: Plan 03 commits the cucascade fix on the cucascade submodule's local fork. The Sirius parent must record that pin advance as a gitlink change to actually use the fix at link time. Per D-08 / D-14 / CC-UPSTREAM-01 carry pattern: two commit chains (cucascade + Sirius parent), one upstream PR deferred to v1.6+.

Output: Sirius parent commit recording the new gitlink; MCP build exit 0; `[mgpu]` 16/16 PASS unchanged from Phase 21 baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-CONTEXT.md
@.planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-RESEARCH.md
@.planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-03-SUMMARY.md
@CLAUDE.md

<interfaces>
Submodule state pre-Plan-04: `git -C cucascade rev-parse HEAD` returns the cucascade fix commit SHA (recorded in 22-03-SUMMARY.md). Sirius parent's gitlink still points at `1c1e648`.

Submodule state post-Plan-04: Sirius parent gitlink advances to the new cucascade SHA. Verify via:
- `git ls-tree HEAD cucascade` (in Sirius parent root) shows the new SHA.
- `git diff HEAD~1 cucascade` shows the gitlink advance.

History reference (Phase 16 SUMMARY): Phase 16 advanced the pin from `995bf4e -> 1c1e648` via the same workflow — see `16-04-SUMMARY.md` and `16-05-SUMMARY.md` for canonical pin-advance pattern.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Advance Sirius parent submodule pin to Plan 03 cucascade fix commit</name>
  <files>cucascade</files>
  <read_first>
    - .planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-03-SUMMARY.md (the Plan 03 fix commit SHA on cucascade fork)
    - .planning/milestones/v1.4-phases/16-cucascade-submodule-rebase-pin-recovery/16-05-SUMMARY.md (canonical pin-advance workflow from Phase 16)
    - .gitmodules at repo root (verify cucascade submodule path)
    - CLAUDE.md (project guidelines)
  </read_first>
  <action>
    1. From the Sirius parent repo root (`/home/felipe/sirius/.worktrees/ws-9aa781df-6d8c-4395-9329-737a67e8e272/`):
       - Confirm cucascade HEAD is on the Plan 03 fix commit:
         `git -C cucascade log --oneline -1` should show the same SHA recorded in `22-03-SUMMARY.md`.
       - Confirm Sirius parent's current gitlink:
         `git ls-tree HEAD cucascade` should still show `1c1e648` (the pre-Phase-22 pin).

    2. Stage the gitlink advance:
       - `git add cucascade` (this stages the new gitlink — NOT the cucascade tree contents, which are managed by the cucascade repo itself).

    3. Verify the staged change:
       - `git diff --cached --submodule=log cucascade` should show a one-line submodule advance with the Plan 03 fix commit message.
       - `git status` should show `cucascade` as a staged "modified content, new commits" entry.

    4. Commit on the Sirius parent repo:
       - Commit message: `chore(22): bump cucascade pin for Cluster B same-stream fix` followed by a body block citing the Plan 03 commit SHA and the 22-CONTEXT.md D-08 / CC-UPSTREAM-01 carry pattern.
       - Include the plan-checker signature trailer (per gsd-tools commit pattern).

    5. Verify the commit landed:
       - `git log --oneline -1` should show the new commit on the Sirius parent.
       - `git ls-tree HEAD cucascade` should show the new cucascade SHA (NOT `1c1e648`).

    6. Smoke verification: build + [mgpu] regression.
       - `mcp__project-commands__run_command build` — must exit 0.
       - `mcp__project-commands__run_command unit-tests filter='[mgpu]'` — must PASS 16/16, exit 0, ≥79091 assertions, ≤130s wall-clock.

    7. Phase-wide HYG-02 grep gate:
       - `grep -rn "rmm::cuda_stream_default" src/ | wc -l` must equal 40 (no regression).

    Implementation invariants:
    - D-08: cucascade fork pin only — DO NOT push cucascade fork to upstream cucascade.
    - D-14: track diff in `22-CUCASCADE-DIFF.md` (Plan 07 authors that doc; Plan 04 just records the SHA in its SUMMARY).
    - D-15: HYG-02 = 40 unchanged.
    - Don't push to Sirius parent's `dev` or `main` — work stays on `feature/single-node-multi-gpu2` per project memory `feedback_feature_branches`.
  </action>
  <verify>
    <automated>git ls-tree HEAD cucascade | grep -v "1c1e648" &amp;&amp; git log --oneline -1 | grep -E "(bump cucascade pin|Cluster B)"</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-tree HEAD cucascade` shows a SHA different from `1c1e648`.
    - `git ls-tree HEAD cucascade` shows the SAME SHA as `git -C cucascade rev-parse HEAD`.
    - `git log --oneline -3` shows a Sirius parent commit with subject containing "cucascade pin" or "Cluster B".
    - `git -C cucascade log --oneline -2` shows the Plan 03 fix commit on top of `1c1e648`-descendant ancestry.
    - `mcp__project-commands__run_command build` exits 0.
    - `mcp__project-commands__run_command unit-tests filter='[mgpu]'` exits 0; output reports 16/16 test cases passed and ≥79091 assertions; runtime ≤130s.
    - `grep -rn "rmm::cuda_stream_default" src/ | wc -l` returns 40 (HYG-02 invariant unchanged).
    - Branch is still `feature/single-node-multi-gpu2`: `git rev-parse --abbrev-ref HEAD` returns `feature/single-node-multi-gpu2`.
  </acceptance_criteria>
  <done>Sirius parent gitlink advances to the Plan 03 fix; build green; [mgpu] 16/16 unchanged; HYG-02 = 40; cucascade fork NOT pushed upstream.</done>
</task>

</tasks>

<verification>
- `git submodule status cucascade` reports the new SHA (not `1c1e648`).
- `mcp__project-commands__run_command build` exits 0.
- `mcp__project-commands__run_command unit-tests filter='[mgpu]'` PASSES 16/16 with ≥79091 assertions in ≤130s.
- HYG-02 invariant preserved: `grep -rn "rmm::cuda_stream_default" src/ | wc -l` returns 40.
- The Sirius parent commit is on `feature/single-node-multi-gpu2` (NOT `dev`).
</verification>

<success_criteria>
- Sirius parent submodule pin advances to the Plan 03 cucascade fix commit.
- MCP build exit 0.
- `[mgpu]` 16/16 PASS unchanged from Phase 21 baseline.
- HYG-02 = 40 invariant preserved.
- Plans 06 and 07 unblocked (sanitizer gate runs against the bumped pin; v1.4 gauntlet runs against the bumped pin).
</success_criteria>

<output>
After completion, create `.planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-04-SUMMARY.md` documenting:
1. Old pin SHA: `1c1e648`.
2. New pin SHA (from Plan 03).
3. Sirius parent commit SHA recording the gitlink advance.
4. MCP build result (exit code, wall-clock).
5. `[mgpu]` 16/16 result (test count, assertions, runtime, exit code).
6. HYG-02 grep result post-bump: 40.
7. Branch confirmation: `feature/single-node-multi-gpu2`.
</output>
