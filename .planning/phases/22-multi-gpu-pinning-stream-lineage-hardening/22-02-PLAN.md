---
phase: 22-multi-gpu-pinning-stream-lineage-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - src/sirius_extension.cpp
  - src/scan_manager/cached_split_provider.cpp
autonomous: true
requirements:
  - PIN-MGPU-01
must_haves:
  truths:
    - "PinTableFunction iterates parquet files with a per-call local counter, NOT a global atomic, and binds each file's reads to a target GPU via cuda_set_device_raii"
    - "Each emitted scan_cached_operator_data carries the chunk's memory_space* (read from pinned_entry.chunk_memory_spaces[i]) instead of an entry-level shared pointer"
    - "Round-robin counter is per-pin_table-call (D-02) — `std::size_t chunk_idx = 0` local to PinTableFunction, not std::atomic"
    - "MCP build now exits 0 — Plan 01's intentional break is closed"
  artifacts:
    - path: "src/sirius_extension.cpp"
      provides: "PinTableFunction round-robin chunk distribution + per-file rmm::cuda_set_device_raii guard around chunked_parquet_reader"
      contains: "chunk_idx % gpu_spaces.size()"
    - path: "src/scan_manager/cached_split_provider.cpp"
      provides: "Per-chunk memory_space* read from pinned_entry.chunk_memory_spaces[i] when emitting each scan_cached_operator_data"
  key_links:
    - from: "PinTableFunction chunk loop"
      to: "sirius_scan_manager::insert_pinned_entry"
      via: "std::vector<cucascade::memory::memory_space*> chunk_memory_spaces moved as last argument"
      pattern: "insert_pinned_entry.*chunk_memory_spaces"
    - from: "cached_split_provider chunk emission"
      to: "pinned_entry.chunk_memory_spaces[chunk_idx]"
      via: "per-chunk memory_space* lookup in start()"
      pattern: "chunk_memory_spaces\\["
---

<objective>
Plumb round-robin GPU distribution through `PinTableFunction` and `cached_split_provider` so that `pin_table` actually places parquet chunks on alternating GPUs (PIN-MGPU-01) and the cached-split consumer reads each chunk's actual `memory_space*` from the per-chunk vector introduced in Plan 01.

Purpose: This is the user-visible behavior change for PIN-MGPU-01. After this plan, `CALL pin_table('multi-file-table', tier='gpu', ...)` on `num_gpus=2` distributes files across GPUs 0 and 1, and SCHED-01 routing in `task_creator.cpp:494-501` automatically fans the cached-scan tasks across GPUs because each emitted batch carries the correct memory_space.

Output: Build is green again; `[mgpu]` 16/16 + `[TPC-H][parquet]` 22/22 still pass (regression baseline); a multi-file pin produces alternating-GPU chunk placement (assertion proven in Plan 05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-CONTEXT.md
@.planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-RESEARCH.md
@.planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-01-SUMMARY.md
@CLAUDE.md
@src/sirius_extension.cpp
@src/scan_manager/cached_split_provider.cpp
@src/include/scan_manager/sirius_scan_manager.hpp

<interfaces>
PinTableFunction current shape — src/sirius_extension.cpp:680-825. Current single-GPU pin (line 749 hardcodes [0]):
```
auto& mem_space = const_cast<cucascade::memory::memory_space&>(*gpu_spaces[0]);
```

Target shape (Phase 22 D-01/D-02/D-05):
```
std::size_t chunk_idx = 0;  // per-call local counter (D-02); NOT std::atomic
std::vector<cucascade::memory::memory_space*> chunk_memory_spaces;
for (auto const& path : file_paths) {
  auto* target_space = const_cast<cucascade::memory::memory_space*>(
    gpu_spaces[chunk_idx % gpu_spaces.size()]);
  int target_gpu_id = target_space->get_device_id();
  rmm::cuda_set_device_raii guard{rmm::cuda_device_id{target_gpu_id}};
  // ... build opts, construct chunked_parquet_reader inside this scope ...
  while (reader.has_next()) {
    auto chunk = reader.read_chunk();
    if (chunk.tbl->num_rows() == 0) break;
    tables.emplace_back(std::move(chunk.tbl));
    chunk_memory_spaces.push_back(target_space);
  }
  ++chunk_idx;
}
sirius_ctx->get_scan_manager().insert_pinned_entry(
    data.args.name, std::move(read_column_names), std::move(file_paths),
    std::move(tables), std::move(chunk_memory_spaces));
```

cached_split_provider current consumer — src/scan_manager/cached_split_provider.cpp.
Current single-pointer (constructor stores entry-level _memory_space at lines 39-43):
```
cached_split_provider::cached_split_provider(const pinned_entry& entry, ...)
  : _entry(&entry), _memory_space(&entry.memory_space), ... {}
// Line 99-100 reads the entry-level pointer when wrapping:
view, std::move(owner), alloc_size, *_memory_space, no_writer_stream);
```

Target — per-chunk lookup (D-04):
```
// Constructor no longer stores _memory_space; remove the field.
// In start() / chunk emission loop, look up the per-chunk pointer:
auto* chunk_space = _entry->chunk_memory_spaces.at(chunk_idx);
// pass chunk_space (dereferenced) into make_data_batch:
view, std::move(owner), alloc_size, *chunk_space, no_writer_stream);
```
</interfaces>

Must-read helpers:
- `rmm::cuda_set_device_raii` usage pattern: `src/op/scan/duckdb_scan_executor.cpp:95` (canonical).
- Memory space accessor `get_device_id()`: cucascade `cucascade::memory::memory_space::get_device_id()` (already used in Phase 19).
- `chunked_parquet_reader` ctor: `src/sirius_extension.cpp:798` (already in current code).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PinTableFunction round-robin distribution + per-file cuda_set_device_raii</name>
  <files>src/sirius_extension.cpp</files>
  <read_first>
    - src/sirius_extension.cpp lines 680-830 (PinTableBind + PinTableFunction full bodies — round-robin loop, gpu_spaces lookup at ~745, single-GPU pin at ~749, chunked_parquet_reader at ~798)
    - src/op/scan/duckdb_scan_executor.cpp:90-110 (canonical rmm::cuda_set_device_raii pattern + acquire of target_gpu_id from a memory_space)
    - .planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-RESEARCH.md "Code Examples - PinTableFunction round-robin" (lines 355-426; the verbatim recommended-shape transformation)
    - .planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-CONTEXT.md D-01, D-02, D-05 (locked decisions: idx % num_gpus simple counter; per-call counter; cuda_set_device_raii around reader)
    - .planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-01-SUMMARY.md (Plan 01's new insert_pinned_entry signature)
    - .planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-RESEARCH.md "Common Pitfalls - Pitfall 1 + Pitfall 2" (research recommends per-file binding, NOT per-chunk-within-a-file binding; document the choice)
  </read_first>
  <behavior>
    - Test 1 (compile-only): PinTableFunction no longer references `gpu_spaces[0]` — the literal `[0]` index is replaced with `[chunk_idx % gpu_spaces.size()]`.
    - Test 2 (compile-only): insert_pinned_entry is called with the new last argument `std::move(chunk_memory_spaces)` (a `std::vector<cucascade::memory::memory_space*>`).
    - Test 3 (compile-only): the chunk loop is wrapped in an `rmm::cuda_set_device_raii guard{rmm::cuda_device_id{target_gpu_id}}` block (per D-05).
    - Test 4 (compile-only): chunk_idx is declared `std::size_t chunk_idx = 0;` (NOT std::atomic) — D-02 lock.
    - Test 5 (runtime — covered by Plan 05): a 4-file pin under num_gpus=2 produces chunk_memory_spaces with at least 2 distinct device_ids.
  </behavior>
  <action>
    1. In src/sirius_extension.cpp, locate the PinTableFunction body. The current single-GPU pin around line 749 reads:
         auto& mem_space = const_cast<cucascade::memory::memory_space&>(*gpu_spaces[0]);
       DELETE this line. Replace with a `std::size_t chunk_idx = 0;` declaration AND a `std::vector<cucascade::memory::memory_space*> chunk_memory_spaces;` declaration BEFORE the file loop. The exact target shape is verbatim from 22-RESEARCH.md lines 355-426.

    2. Inside the `for (auto const& path : file_paths)` loop:
       - At the loop body start, compute the target memory_space pointer:
           auto* target_space = const_cast<cucascade::memory::memory_space*>(gpu_spaces[chunk_idx % gpu_spaces.size()]);
           int target_gpu_id = target_space->get_device_id();
           rmm::cuda_set_device_raii device_guard{rmm::cuda_device_id{target_gpu_id}};
       - Inside the `while (reader.has_next())` chunk loop, AFTER `tables.emplace_back(std::move(chunk.tbl));`, push the parallel space:
           chunk_memory_spaces.push_back(target_space);
       - At the end of the file loop body (just before the closing brace of `for`), increment `++chunk_idx;`. This implements PER-FILE binding (research-recommended; see Pitfall 2 - "one reader per file binds to one device"). Add a comment block above the increment explaining the choice:
           // Phase 22 PIN-MGPU-01 (D-01/D-02): per-call local counter, increments
           // per file. All chunks within a single chunked_parquet_reader stay on
           // the same GPU (per D-03 chunks-at-index-i invariant). Cross-file
           // alternation produces the round-robin. Multi-chunk single-file
           // distribution requires multi-file fixtures (see Plan 05 distribution
           // gate test).
           ++chunk_idx;

    3. After the file loop, replace the existing insert_pinned_entry call to pass the new last argument:
         sirius_ctx->get_scan_manager().insert_pinned_entry(
             data.args.name,
             std::move(read_column_names),
             std::move(file_paths),
             std::move(tables),
             std::move(chunk_memory_spaces));
       Verify the old call passed `mem_space` as the last arg and remove that. The local `mem_space` reference is gone from step 1.

    4. Add `#include <rmm/cuda_device.hpp>` and `#include <rmm/cuda_stream.hpp>` if not already present (search for them at the top of the file). `rmm::cuda_set_device_raii` and `rmm::cuda_device_id` are declared there.

    5. Per CLAUDE.md: run `/module-context` for cudf chunked_parquet_reader + rmm::cuda_set_device_raii BEFORE editing if any signature uncertainty remains. The shapes used here are already canonical in the codebase; this is a recommendation for safety.

    Implementation invariants:
    - D-15: NO `rmm::cuda_stream_default`. The `rmm::cuda_set_device_raii` guard binds device only — it does NOT touch streams.
    - D-02: counter is `std::size_t` (NOT `std::atomic<size_t>`). PinTableFunction is single-threaded.
    - Pitfall 1: `++chunk_idx` is at file-loop end, NOT inside the chunk loop (research-locked: per-file binding).
  </action>
  <verify>
    <automated>grep -nE "chunk_idx % gpu_spaces" src/sirius_extension.cpp &amp;&amp; grep -nE "rmm::cuda_set_device_raii" src/sirius_extension.cpp &amp;&amp; grep -nE "std::size_t chunk_idx = 0" src/sirius_extension.cpp &amp;&amp; ! grep -E "gpu_spaces\[0\]" src/sirius_extension.cpp &amp;&amp; ! grep -E "std::atomic.*chunk_idx" src/sirius_extension.cpp</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "chunk_idx % gpu_spaces" src/sirius_extension.cpp` returns at least 1 line inside PinTableFunction.
    - `grep -nE "rmm::cuda_set_device_raii.*device_guard" src/sirius_extension.cpp` returns at least 1 line within PinTableFunction's body.
    - `grep -nE "std::size_t chunk_idx = 0" src/sirius_extension.cpp` returns exactly 1 line.
    - `grep -E "gpu_spaces\[0\]" src/sirius_extension.cpp` returns 0 lines (hardcoded zero index gone).
    - `grep -E "std::atomic.*chunk_idx" src/sirius_extension.cpp` returns 0 lines (D-02 counter is plain size_t).
    - `grep -nE "insert_pinned_entry.*chunk_memory_spaces" src/sirius_extension.cpp` shows the call site passing chunk_memory_spaces (could span multiple lines; alternative: `grep -A 5 "get_scan_manager().insert_pinned_entry" src/sirius_extension.cpp` shows `chunk_memory_spaces` in the next 5 lines).
    - `grep -c "rmm::cuda_stream_default" src/sirius_extension.cpp` returns 0 (no new HYG-02 introductions).
    - `grep -nE "Phase 22|D-01|D-02|D-05|PIN-MGPU-01" src/sirius_extension.cpp` returns at least 1 line (traceability comment present).
  </acceptance_criteria>
  <done>PinTableFunction round-robins chunks across gpu_spaces under per-file cuda_set_device_raii guard; insert_pinned_entry called with the new chunk_memory_spaces vector argument; no atomic counter; no `[0]` literal index; no rmm::cuda_stream_default introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cached_split_provider per-chunk memory_space lookup</name>
  <files>src/scan_manager/cached_split_provider.cpp</files>
  <read_first>
    - src/scan_manager/cached_split_provider.cpp full file (37-111) — current ctor at 39-46 stores _memory_space; line 86-100 contains "no_writer_stream" rationale (load-bearing, do NOT modify per Anti-Patterns); line 100 wraps with default-constructed cuda_stream_view{} (NOT cuda_stream_default — preserves HYG-02)
    - src/include/scan_manager/cached_split_provider.hpp (full file — header to flip the _memory_space member)
    - src/include/scan_manager/sirius_scan_manager.hpp (POST-Plan 01 state — confirm pinned_entry now has chunk_memory_spaces)
    - .planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-RESEARCH.md "Anti-Patterns to Avoid" (do NOT modify the no_writer_stream rationale doc-block at lines 86-100)
    - .planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-CONTEXT.md D-04 (consumer reads per-chunk memory_space)
  </read_first>
  <behavior>
    - Test 1 (compile-only): cached_split_provider's constructor no longer stores an entry-level _memory_space pointer; the member is removed from both .cpp and .hpp.
    - Test 2 (compile-only): the chunk-emission site (around line 99-100) reads `_entry->chunk_memory_spaces.at(chunk_idx)` (or equivalent index from the local emission counter) and passes the dereferenced pointer to make_data_batch as the memory_space argument.
    - Test 3 (compile-only): the no_writer_stream rationale comment at lines 86-100 is preserved verbatim.
    - Test 4 (runtime — covered by Plan 05 routing test): cached scan tasks for a multi-file pin under num_gpus=2 dispatch on both GPU 0 and GPU 1 (visible via [mgpu-audit] log).
  </behavior>
  <action>
    1. In src/scan_manager/cached_split_provider.cpp:
       - DELETE the `_memory_space` member initialization in the constructor (current line 42-43: `_memory_space(&entry.memory_space)` or similar pattern).
       - REMOVE the `_memory_space` member from the class declaration in src/include/scan_manager/cached_split_provider.hpp (search for `_memory_space` in the header).

    2. In the chunk-emission body (where `make_data_batch` or `scan_cached_operator_data` is constructed — currently around line 99-100 with `view, std::move(owner), alloc_size, *_memory_space, no_writer_stream`):
       - Replace `*_memory_space` with `*_entry->chunk_memory_spaces.at(chunk_idx)`. The `chunk_idx` here is the local emission counter that walks chunks within `start()` (executor MUST identify the existing local counter or introduce one with the same name `chunk_idx` — read the function body at lines 55-105 to find the existing index variable; it is likely an iteration index for `data_batches_by_column` per chunk).
       - Use `.at()` (NOT `[]`) so an out-of-range index throws — this catches Plan 01's invariant violation if it ever fires at runtime.

    3. PRESERVE the no_writer_stream rationale comment at lines 86-100 verbatim. Do NOT modify it. Per Anti-Patterns: "That comment is load-bearing — changing it might cause a future reviewer to think the cached path needs writer-event recording (it does not, per the doc-block)."

    4. PRESERVE the `rmm::cuda_stream_view{}` (default-constructed null stream view, NOT rmm::cuda_stream_default) at line 100 unchanged.

    5. Add a one-line comment above the changed call site:
       // Phase 22 D-04: per-chunk memory_space lookup. Replaces entry-level
       // _memory_space (now gone post-PIN-MGPU-01); each chunk carries the
       // memory_space its data lives on so SCHED-01 routing fans tasks correctly.

    6. Run MCP build via `mcp__project-commands__run_command build` after the change. Expected: exit 0. If any other src/ site references the old `pinned_entry.memory_space` single-pointer member, fix it inline (Plan 01 Open Q3 requires this grep before commit). Run:
         grep -rn "\.memory_space\b" src/scan_manager/ src/include/scan_manager/ src/sirius_extension.cpp src/op/scan/
       If any non-comment hit references the old single-pointer member name, that site must be flipped here too (or determined to be unrelated — e.g., a method call on the cucascade memory_space type, NOT a member access on pinned_entry).

    Implementation invariants:
    - D-15: no rmm::cuda_stream_default introduction. Existing `rmm::cuda_stream_view{}` at line 100 is NOT rmm::cuda_stream_default; preserve verbatim.
    - HYG-02: `grep -c "rmm::cuda_stream_default" src/scan_manager/cached_split_provider.cpp` MUST remain 0.
  </action>
  <verify>
    <automated>grep -nE "_entry->chunk_memory_spaces" src/scan_manager/cached_split_provider.cpp &amp;&amp; ! grep -nE "_memory_space\(&amp;entry\.memory_space\)" src/scan_manager/cached_split_provider.cpp &amp;&amp; grep -c "rmm::cuda_stream_default" src/scan_manager/cached_split_provider.cpp | grep -qE "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "_entry->chunk_memory_spaces\.at\(" src/scan_manager/cached_split_provider.cpp` returns at least 1 line.
    - `grep -E "_memory_space\(&entry\.memory_space\)" src/scan_manager/cached_split_provider.cpp` returns 0 lines (old constructor init gone).
    - `grep -E "_memory_space" src/include/scan_manager/cached_split_provider.hpp` returns 0 lines (member declaration gone from header).
    - `grep -nE "no_writer_stream" src/scan_manager/cached_split_provider.cpp` STILL returns at least 1 line (load-bearing rationale preserved).
    - `grep -c "rmm::cuda_stream_default" src/scan_manager/cached_split_provider.cpp` returns 0.
    - `grep -nE "rmm::cuda_stream_view\{\}" src/scan_manager/cached_split_provider.cpp` STILL returns at least 1 line (existing null stream view preserved).
    - MCP build via `mcp__project-commands__run_command build` exits 0 (Plan 01's intentional break is closed).
    - HYG-02 invariant phase-wide: `grep -rn "rmm::cuda_stream_default" src/ | wc -l` returns 40 (unchanged baseline).
    - `[TPC-H][parquet]` smoke regression via `mcp__project-commands__run_command unit-tests filter='[TPC-H][parquet]'` — 22/22 PASS, exit 0 (proves the round-robin + per-chunk space changes do NOT break the cached-path or the single-GPU integration baseline).
  </acceptance_criteria>
  <done>cached_split_provider reads per-chunk memory_space from pinned_entry.chunk_memory_spaces; old single-pointer member removed from header and impl; no_writer_stream comment block preserved; build green; [TPC-H][parquet] 22/22 unchanged.</done>
</task>

</tasks>

<verification>
- `grep -rn "\.memory_space\b" src/sirius_extension.cpp src/include/scan_manager/ src/scan_manager/ src/op/scan/` returns 0 hits referencing the OLD `pinned_entry.memory_space` single-pointer member (Plan 01 Open Q3 closure).
- `grep -nE "chunk_memory_spaces" src/sirius_extension.cpp src/scan_manager/cached_split_provider.cpp src/include/scan_manager/sirius_scan_manager.hpp src/scan_manager/sirius_scan_manager.cpp | wc -l` returns at least 8 (declarations + usages across the 4 files).
- HYG-02 invariant: `grep -rn "rmm::cuda_stream_default" src/ | wc -l` returns 40 (no Phase 22 introductions).
- MCP build exits 0; `[mgpu]` 16/16 PASS at single-GPU baseline (no test-fixture changes here yet — distribution + routing gates are Plan 05).
- `[TPC-H][parquet]` 22/22 PASS (single-GPU integration smoke regression — unchanged from Phase 21 baseline).
</verification>

<success_criteria>
- PinTableFunction iterates files with a per-call `std::size_t chunk_idx = 0` counter (D-02), binds each file's chunk-reads to a target GPU via `rmm::cuda_set_device_raii` (D-05), and emits a parallel `chunk_memory_spaces` vector consumed by Plan 01's new insert_pinned_entry signature.
- cached_split_provider reads per-chunk memory_space from `_entry->chunk_memory_spaces.at(chunk_idx)` instead of an entry-level shared pointer.
- Build is green (`mcp__project-commands__run_command build` exits 0).
- HYG-02 = 40 invariant preserved.
- `[TPC-H][parquet]` 22/22 smoke regression PASS — single-GPU integration baseline unchanged.
- Plans 03 and 05 unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-02-SUMMARY.md` documenting:
1. Exact file:line of round-robin counter site in PinTableFunction.
2. Exact file:line of `cuda_set_device_raii` guard placement.
3. Exact file:line of insert_pinned_entry call passing chunk_memory_spaces.
4. Exact file:line of cached_split_provider per-chunk lookup.
5. Per-file vs per-chunk binding rationale (per-file recommended in research; document why this satisfies D-03).
6. HYG-02 grep result post-change: must equal 40.
7. `[TPC-H][parquet]` smoke regression result: 22/22 PASS, runtime, assertion count.
</output>
