---
phase: 22-multi-gpu-pinning-stream-lineage-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - cucascade/src/data/representation_converter.cpp
autonomous: true
requirements:
  - fu17-cluster-b
must_haves:
  truths:
    - "alloc_and_peer_copy_async issues rmm::device_buffer::allocate_async (line 600) and BOTH cudaMemcpyAsync legs on the same stream argument (target_stream)"
    - "The local rmm::cuda_stream src_stream construction at line 615 is REMOVED"
    - "DtoH leg is issued under cuda_set_device_raii(src_device) but on target_stream (per D-07 same-stream invariant)"
    - "cudaFreeHost ordering preserved: a cudaStreamSynchronize(target_stream) MUST run before cudaFreeHost(host_buf)"
    - "Cucascade ctest still passes after the change (per CC-04 invariant from v1.4)"
    - "SF1 Q11 num_gpus=2 sanitizer reports 0 stream-ordered races at alloc_and_peer_copy_async frames (Cluster A frames may still appear; advisory)"
  artifacts:
    - path: "cucascade/src/data/representation_converter.cpp"
      provides: "Same-stream invariant fix to alloc_and_peer_copy_async at lines 593-625"
      contains: "cudaMemcpyAsync(host_buf, src_ptr, size, cudaMemcpyDeviceToHost, target_stream.value())"
  key_links:
    - from: "rmm::device_buffer buf(size, target_stream, target_mr) (line 600)"
      to: "cudaMemcpyAsync DtoH (was line 617, post-fix on target_stream)"
      via: "Same target_stream argument shared by allocator and both memcpy legs"
      pattern: "cudaMemcpyAsync.*cudaMemcpyDeviceToHost.*target_stream"
    - from: "Cluster B race shape (16/21 in 20-05 sanitizer trace)"
      to: "0 races post-fix (Phase 22 D-12 sanitizer gate)"
      via: "Single-stream timeline eliminates allocator vs memcpy stream split"
      pattern: "alloc_and_peer_copy_async"
---

<objective>
Close fu17 Cluster B (16/21 of the SF1 Q11 num_gpus=2 sanitizer races) by collapsing `alloc_and_peer_copy_async`'s host-staging fallback path onto a single CUDA stream so that `rmm::device_buffer::allocate_async` (line 600) and both `cudaMemcpyAsync` legs (DtoH + HtoD) observe a single timeline.

Purpose: Per `20-05-INVESTIGATION.md` and CONTEXT.md D-07, the host-staging fallback in `alloc_and_peer_copy_async` constructs a fresh local `rmm::cuda_stream src_stream` (line 615) for the DtoH leg, but the device buffer was allocated on `target_stream` (line 600). This allocator-vs-memcpy stream split is the root cause of Cluster B race shape A. Fix shape per D-07: drop the local `src_stream`, use `target_stream` for both legs, bind src_device context via `cuda_set_device_raii(src_device)` for the DtoH issue.

Output: A single cucascade fork commit on the local pin descended from `1c1e648`. Plan 04 advances the Sirius parent's submodule pin to this commit and verifies the integration smoke. Plan 06 builds the sanitizer gate that empirically asserts Cluster B = 0 post-fix.

Open question per research: does `cudaMemcpyAsync(host_buf, src_ptr, ..., DtoH, target_stream)` from within `cuda_set_device_raii(src_device)` produce a sanitizer-clean stream-ordered copy when target_stream lives on a different device? Task 2 (sanitizer micro-validation) gates the Plan 04 submodule bump on a positive answer; if negative, fall back to event-bridge shape per CONTEXT.md D-07's allowed alternatives.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-CONTEXT.md
@.planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-RESEARCH.md
@.planning/milestones/v1.4-phases/20-scan-manager-pin-tables-port-pr-731-pr-721/20-05-INVESTIGATION.md
@CLAUDE.md
@cucascade/CLAUDE.md
@cucascade/src/data/representation_converter.cpp

<interfaces>
Current broken code at cucascade/src/data/representation_converter.cpp:593-625 (verbatim):

```
static rmm::device_buffer alloc_and_peer_copy_async(const void* src_ptr,
                                                    int src_device,
                                                    std::size_t size,
                                                    int dst_device,
                                                    rmm::cuda_stream_view target_stream,
                                                    rmm::device_async_resource_ref target_mr)
{
  rmm::device_buffer buf(size, target_stream, target_mr);
  if (size == 0 || src_ptr == nullptr) { return buf; }

  if (memory::probe_peer_dma_works(src_device, dst_device)) {
    CUCASCADE_CUDA_TRY(cudaMemcpyPeerAsync(
      buf.data(), dst_device, src_ptr, src_device, size, target_stream.value()));
    return buf;
  }

  // Peer DMA broken — explicitly stage through pinned host memory.
  void* host_buf = nullptr;
  CUCASCADE_CUDA_TRY(cudaMallocHost(&host_buf, size));
  {
    rmm::cuda_set_device_raii src_guard{rmm::cuda_device_id{src_device}};
    rmm::cuda_stream src_stream;                                    // <-- LINE 615 (BUG)
    CUCASCADE_CUDA_TRY(
      cudaMemcpyAsync(host_buf, src_ptr, size, cudaMemcpyDeviceToHost, src_stream.view().value()));
    src_stream.synchronize();                                       // <-- LINE 618
  }
  CUCASCADE_CUDA_TRY(
    cudaMemcpyAsync(buf.data(), host_buf, size, cudaMemcpyHostToDevice, target_stream.value()));
  CUCASCADE_CUDA_TRY(cudaStreamSynchronize(target_stream.value()));
  cudaFreeHost(host_buf);
  return buf;
}
```

Reference (working) peer-DMA path at lines 605-606 — single target_stream for cudaMemcpyPeerAsync. The Cluster B fix must replicate this discipline for the host-staging fallback.

Target shape post-fix (D-07 same-stream invariant):

```
static rmm::device_buffer alloc_and_peer_copy_async(const void* src_ptr,
                                                    int src_device,
                                                    std::size_t size,
                                                    int dst_device,
                                                    rmm::cuda_stream_view target_stream,
                                                    rmm::device_async_resource_ref target_mr)
{
  rmm::device_buffer buf(size, target_stream, target_mr);
  if (size == 0 || src_ptr == nullptr) { return buf; }

  if (memory::probe_peer_dma_works(src_device, dst_device)) {
    CUCASCADE_CUDA_TRY(cudaMemcpyPeerAsync(
      buf.data(), dst_device, src_ptr, src_device, size, target_stream.value()));
    return buf;
  }

  // Peer DMA broken — host-stage through pinned host memory.
  // Phase 22 D-07: collapse to target_stream so allocate_async (line above)
  // and both memcpy legs share a single timeline. Closes Cluster B sanitizer
  // race shape (16/21 of SF1 Q11 num_gpus=2 races per 20-05-INVESTIGATION.md).
  void* host_buf = nullptr;
  CUCASCADE_CUDA_TRY(cudaMallocHost(&host_buf, size));
  {
    // src_device context required for DtoH issue; stream lives on target device.
    rmm::cuda_set_device_raii src_guard{rmm::cuda_device_id{src_device}};
    CUCASCADE_CUDA_TRY(
      cudaMemcpyAsync(host_buf, src_ptr, size, cudaMemcpyDeviceToHost, target_stream.value()));
    CUCASCADE_CUDA_TRY(cudaStreamSynchronize(target_stream.value()));
    // Sync required: cudaFreeHost(host_buf) below is host-synchronous and must
    // not race with the DtoH read; also ensures host_buf is fully populated
    // before the HtoD enqueue.
  }
  CUCASCADE_CUDA_TRY(
    cudaMemcpyAsync(buf.data(), host_buf, size, cudaMemcpyHostToDevice, target_stream.value()));
  CUCASCADE_CUDA_TRY(cudaStreamSynchronize(target_stream.value()));
  cudaFreeHost(host_buf);
  return buf;
}
```
</interfaces>

Cucascade conventions (per cucascade/CLAUDE.md):
- snake_case for everything; member underscore prefix.
- Use `CUCASCADE_CUDA_TRY(call)` macro for all CUDA runtime calls (NOT raw `cudaXXX` returns).
- clang-format v20.1.4 enforced via pre-commit; 2-space indent, 100-char column limit, WebKit brace style.
- `CUCASCADE_WARNINGS_AS_ERRORS=ON` — all warnings are errors.
- `pre-commit run -a` style cleanup expected before commit.
- Standard test: `ctest` inside `cucascade/build` (per CC-04).

Pitfall 4 (research): DO NOT remove the `cudaStreamSynchronize(target_stream.value())` calls — `cudaFreeHost(host_buf)` is host-synchronous and must not race with in-flight memcpys. The current code's sync-then-free ordering is correct; the fix changes which stream is synced, not whether sync happens.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply same-stream invariant fix to alloc_and_peer_copy_async</name>
  <files>cucascade/src/data/representation_converter.cpp</files>
  <read_first>
    - cucascade/src/data/representation_converter.cpp lines 580-630 (current alloc_and_peer_copy_async; the function above and below for context; alloc_and_peer_copy_sync at 631-643 which calls alloc_and_peer_copy_async — confirm it still works after the change)
    - cucascade/src/data/representation_converter.cpp lines 823-870 (working peer-DMA path in convert_gpu_to_gpu — reference for the same-stream pattern; lines 840-847 in particular for `target_stream = target_memory_space->acquire_stream()` and the entry-level cudaStreamWaitEvent on writer_event)
    - cucascade/src/data/representation_converter.cpp full file context — see how `target_stream` flows through the function chain (callers pass in a target-device-bound stream)
    - cucascade/CLAUDE.md (snake_case, CUCASCADE_CUDA_TRY macro requirement, clang-format v20.1.4 conventions)
    - .planning/milestones/v1.4-phases/20-scan-manager-pin-tables-port-pr-731-pr-721/20-05-INVESTIGATION.md "Recommended Fix Shape" Section 1 (the authoritative race taxonomy and fix-shape rationale)
    - .planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-CONTEXT.md D-07 + D-08 (locked decisions: same-stream invariant; cucascade fork+bump local pin)
    - .planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-RESEARCH.md "Pattern 2: Stream-lineage via writer-event" + "Pitfall 4: Cluster B fix accidentally serializes host-staging path" (lines 178-235 + 326-334)
  </read_first>
  <behavior>
    - Test 1 (compile-only): `rmm::cuda_stream src_stream;` is REMOVED from inside the host-staging block.
    - Test 2 (compile-only): the DtoH `cudaMemcpyAsync` (was line 617) takes `target_stream.value()` as its stream argument (NOT `src_stream.view().value()`).
    - Test 3 (compile-only): a `cudaStreamSynchronize(target_stream.value())` call is present immediately after the DtoH `cudaMemcpyAsync`, INSIDE the `cuda_set_device_raii(src_device)` block scope.
    - Test 4 (compile-only): `src_stream.synchronize()` is REMOVED.
    - Test 5 (compile-only): the `cuda_set_device_raii src_guard{rmm::cuda_device_id{src_device}}` line is PRESERVED — only the local `rmm::cuda_stream` construction is removed.
    - Test 6 (compile-only): the existing HtoD `cudaMemcpyAsync` and trailing `cudaStreamSynchronize(target_stream.value()) + cudaFreeHost(host_buf)` ordering at the end of the function is preserved verbatim (per Pitfall 4).
    - Test 7 (build): cucascade builds clean via `mcp__project-commands__run_command build` (parent build chain includes cucascade as submodule).
    - Test 8 (cucascade unit tests): `ctest` inside `cucascade/build` passes (CC-04 invariant from v1.4 — must not regress).
  </behavior>
  <action>
    1. In `cucascade/src/data/representation_converter.cpp`, locate the `alloc_and_peer_copy_async` function at lines 593-625. Inside the `// Peer DMA broken` block (lines 610-624 of the current file), apply the surgical edit:

       DELETE these exact lines (currently 615-619):
       ```
           rmm::cuda_stream src_stream;
           CUCASCADE_CUDA_TRY(
             cudaMemcpyAsync(host_buf, src_ptr, size, cudaMemcpyDeviceToHost, src_stream.view().value()));
           src_stream.synchronize();
         }
       ```

       REPLACE with:
       ```
           // Phase 22 D-07: same-stream invariant. Issue DtoH on target_stream
           // (matching rmm::device_buffer::allocate_async at the top of this
           // function) under cuda_set_device_raii(src_device) for src-side
           // context. Closes Cluster B sanitizer race shape A
           // (16/21 of SF1 Q11 num_gpus=2 races per 20-05-INVESTIGATION.md).
           CUCASCADE_CUDA_TRY(cudaMemcpyAsync(
             host_buf, src_ptr, size, cudaMemcpyDeviceToHost, target_stream.value()));
           CUCASCADE_CUDA_TRY(cudaStreamSynchronize(target_stream.value()));
           // Sync inside the src_guard scope: cudaFreeHost (after the closing
           // brace below) is host-synchronous and must not race with the DtoH
           // read; the sync also ensures host_buf is fully populated before
           // the HtoD enqueue executes on target_stream.
         }
       ```

    2. Verify the closing brace of `alloc_and_peer_copy_async` and the trailing HtoD + sync + cudaFreeHost block (currently lines 620-624) are preserved verbatim:
       ```
         CUCASCADE_CUDA_TRY(
           cudaMemcpyAsync(buf.data(), host_buf, size, cudaMemcpyHostToDevice, target_stream.value()));
         CUCASCADE_CUDA_TRY(cudaStreamSynchronize(target_stream.value()));
         cudaFreeHost(host_buf);
         return buf;
       }
       ```
       Per Pitfall 4: this block is correct as written; do NOT modify ordering or remove syncs.

    3. Confirm `cuda_set_device_raii src_guard{rmm::cuda_device_id{src_device}};` (currently line 614) is PRESERVED — the src-device context binding is required for `cudaMemcpyAsync(..., DtoH, target_stream)` to identify the source pointer's device.

    4. Run the cucascade pre-commit hooks: from `cucascade/` run `pre-commit run -a` (per cucascade/CLAUDE.md). If clang-format adjusts indentation/spacing, accept the formatting changes. Codespell may flag — fix any project-specific terms.

    5. Build via `mcp__project-commands__run_command build` (the parent build chain rebuilds cucascade as a submodule). Expected: exit 0.

    6. Run cucascade ctest (CC-04 invariant): from `cucascade/build/` run `ctest --output-on-failure`. Expected: 100% PASS, exit 0. If ANY cucascade test regresses, the fix shape must be reconsidered (escalate to fall-back shape 2 event-bridge per CONTEXT.md D-07).

    7. Commit on the cucascade fork (NOT the Sirius parent repo):
       - Working dir: `cucascade/` (the submodule)
       - Branch: ensure HEAD is on the local fork branch descended from `1c1e648` (verify with `git -C cucascade log --oneline -5` — Phase 16 SUMMARY says current pin is `1c1e648`).
       - Commit message: `fix(p22): same-stream invariant in alloc_and_peer_copy_async (Cluster B)` followed by a body citing the Phase 22 plan and `20-05-INVESTIGATION.md`.
       - DO NOT push to upstream cucascade — D-08: local pin advance only, CC-UPSTREAM-01 carry pattern.
       - Record the new commit SHA. Plan 04 will bump the Sirius submodule pin to this SHA.

    Implementation invariants:
    - D-07: same-stream invariant; one stream argument shared by allocator + both memcpy legs.
    - D-08: cucascade fork commit; no upstream PR.
    - D-15: no `rmm::cuda_stream_default` introduced.
    - Pitfall 4: preserve sync-then-free ordering; the change is which stream is synced, not whether sync happens.
    - cucascade conventions: CUCASCADE_CUDA_TRY for all CUDA calls; snake_case; pre-commit run -a clean.
  </action>
  <verify>
    <automated>cd cucascade &amp;&amp; grep -nE "cudaMemcpyAsync\(host_buf, src_ptr, size, cudaMemcpyDeviceToHost, target_stream\.value\(\)\)" src/data/representation_converter.cpp &amp;&amp; ! grep -nE "rmm::cuda_stream src_stream" src/data/representation_converter.cpp &amp;&amp; ! grep -nE "src_stream\.synchronize\(\)" src/data/representation_converter.cpp &amp;&amp; grep -nE "cuda_set_device_raii src_guard\{rmm::cuda_device_id\{src_device\}\}" src/data/representation_converter.cpp</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "cudaMemcpyAsync\(host_buf, src_ptr, size, cudaMemcpyDeviceToHost, target_stream\.value\(\)\)" cucascade/src/data/representation_converter.cpp` returns exactly 1 line (the new DtoH on target_stream).
    - `grep -E "rmm::cuda_stream src_stream" cucascade/src/data/representation_converter.cpp` returns 0 lines (local stream gone).
    - `grep -E "src_stream\.synchronize\(\)" cucascade/src/data/representation_converter.cpp` returns 0 lines (local sync gone).
    - `grep -nE "cuda_set_device_raii src_guard.*src_device" cucascade/src/data/representation_converter.cpp` returns at least 1 line (src-device context binding preserved).
    - `grep -nE "cudaMemcpyAsync\(buf\.data\(\), host_buf, size, cudaMemcpyHostToDevice, target_stream\.value\(\)\)" cucascade/src/data/representation_converter.cpp` returns at least 1 line (HtoD on target_stream — preserved verbatim).
    - `grep -cE "cudaStreamSynchronize\(target_stream\.value\(\)\)" cucascade/src/data/representation_converter.cpp` inside the host-staging block: count is 2 (one inside src_guard scope before cudaFreeHost; one at function exit pre-existing). Verify by reading the function body.
    - `grep -nE "Phase 22|D-07|Cluster B" cucascade/src/data/representation_converter.cpp` returns at least 1 line (traceability).
    - `grep -c "rmm::cuda_stream_default" cucascade/src/data/representation_converter.cpp` returns 0 (no HYG-02-class introductions).
    - `mcp__project-commands__run_command build` exits 0.
    - cucascade ctest: from `cucascade/build/` run `ctest --output-on-failure` — exits 0, 100% PASS, no regressions vs CC-04 baseline.
    - `git -C cucascade log --oneline -1` shows the new fix commit on top of `1c1e648`-descendant ancestry.
    - `pre-commit run -a` (run from `cucascade/`) exits 0 (clang-format + cmake-format + codespell + black all clean).
  </acceptance_criteria>
  <done>alloc_and_peer_copy_async issues both memcpy legs and the allocator on a single target_stream; sync ordering preserved; cucascade ctest passes; committed on cucascade fork; cucascade SHA recorded for Plan 04.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Pre-bump sanitizer micro-validation (Open Q1 closure)</name>
  <files>cucascade/src/data/representation_converter.cpp</files>
  <read_first>
    - .planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-RESEARCH.md "Open Questions - Q1" (lines 626-631 — explicit recommendation to verify with sanitizer locally before assuming correctness)
    - .planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-CONTEXT.md D-07 (allows fall-back to event-bridge shape if same-stream shape fails)
    - .planning/milestones/v1.4-phases/21-v1-4-ship-gate-full-v1-3-gauntlet-on-rebased-branch/21-VERDICT.md Section F (compute-sanitizer command shape — verbatim)
    - MEMORY: feedback_sanitizer_via_bash_not_mcp (compute-sanitizer MUST run via Bash + timeout, NOT MCP)
    - MEMORY: feedback_test_runtime_caps (cap test runtimes; budget = expected runtime × 2-3)
  </read_first>
  <behavior>
    - Test 1: SF1 Q11 num_gpus=2 sanitizer log post-fix shows 0 races whose backtrace contains `alloc_and_peer_copy_async`.
    - Test 2: If Test 1 fails, this task DOES NOT mark the plan complete; instead it surfaces the failure so Plan 03 can be re-attempted with shape 2 (event-bridge per CONTEXT.md D-07).
  </behavior>
  <action>
    1. Build is already green from Task 1. Run a SF1 Q11 num_gpus=2 sanitizer pass to validate Open Question 1 (does target_stream-from-src_device produce a sanitizer-clean stream-ordered copy?). Use Bash + timeout (NOT MCP — per project memory `feedback_sanitizer_via_bash_not_mcp`).

    2. Use this verbatim command shape (matches Phase 21 21-VERDICT.md Section F sanitizer pattern):
       ```
       CUDA_BIN=$(ls -1 /usr/local/cuda*/bin/compute-sanitizer 2>/dev/null | head -1)
       LOG=/tmp/p22_03_t2_sanitizer.log
       timeout 600 "$CUDA_BIN" \
         --tool memcheck \
         --track-stream-ordered-races=all \
         --show-backtrace=yes \
         --launch-timeout=600 \
         --log-file "$LOG" \
         --print-limit 100 \
         build/release/extension/sirius/test/cpp/sirius_unittest "gpu_execution - TPC-H Query 11 parquet" || true
       ```

    3. Parse the log:
       - `CLUSTER_B=$(grep -cE 'Host Frame:.*alloc_and_peer_copy_async' "$LOG" || true)` — gate.
       - `CLUSTER_A=$(grep -cE 'Host Frame:.*(read_column_chunks_async|posix_device_io)' "$LOG" || true)` — advisory only.
       - `TOTAL_RACES=$(grep -cE 'Use-before-alloc on allocation' "$LOG" || true)` — context.

    4. If `CLUSTER_B == 0`: Task 2 PASSES; Plan 03 is complete; Plan 04 unblocked to bump the submodule pin.

    5. If `CLUSTER_B > 0`: Task 2 FAILS. Do NOT proceed to Plan 04. Document the failure in `22-03-SUMMARY.md`; the Plan 03 SUMMARY must mark the plan as PARTIAL/FAILED and recommend escalation to fix shape 2 (event-bridge) per CONTEXT.md D-07. The orchestrator will need to re-plan Task 1 with the alternative shape.

    6. Cluster A count is recorded for the advisory log in 22-VERDICT.md (Plan 07) but does NOT affect this gate (D-09).

    Implementation invariants:
    - Sanitizer via Bash + timeout, NOT MCP (project memory rule).
    - Cap test runtime: 600s timeout (per Q11 SF1 historical runtime ~30s + sanitizer overhead 10x = 300s + safety margin).
    - Don't poll nvidia-smi mid-run; let the timeout act as the cap.
  </action>
  <verify>
    <automated>test -f /tmp/p22_03_t2_sanitizer.log &amp;&amp; CB=$(grep -cE 'Host Frame:.*alloc_and_peer_copy_async' /tmp/p22_03_t2_sanitizer.log || echo 0) &amp;&amp; test "$CB" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/p22_03_t2_sanitizer.log` exists and contains compute-sanitizer output.
    - `grep -cE 'Host Frame:.*alloc_and_peer_copy_async' /tmp/p22_03_t2_sanitizer.log` returns 0.
    - The Cluster A count (advisory) is recorded as a number in the Plan 03 SUMMARY (any value is acceptable; this is informational only per D-09).
    - The total race count (informational) is recorded in the Plan 03 SUMMARY.
    - If `CLUSTER_B > 0`: SUMMARY explicitly marks Plan 03 status as PARTIAL/FAILED with a recommendation to escalate to event-bridge shape (D-07 alternative). Subsequent plans cannot proceed.
  </acceptance_criteria>
  <done>SF1 Q11 num_gpus=2 sanitizer log shows 0 races at alloc_and_peer_copy_async frames; Plan 04 unblocked.</done>
</task>

</tasks>

<verification>
- cucascade/ commit on local fork: `git -C cucascade log --oneline -1` shows the Phase 22 fix commit.
- cucascade ctest passes from `cucascade/build/`: `ctest --output-on-failure` exits 0.
- Phase-wide HYG-02 invariant unchanged: `grep -c "rmm::cuda_stream_default" cucascade/src/data/representation_converter.cpp` returns 0.
- Cluster B sanitizer race count at SF1 Q11 num_gpus=2 is 0 (recorded in /tmp/p22_03_t2_sanitizer.log).
- Cluster A frames may persist (advisory only per D-09); recorded in 22-VERDICT.md by Plan 07.
- pre-commit hooks clean from `cucascade/`.
- Sirius parent repo's submodule pin is NOT yet advanced (Plan 04 does that).
</verification>

<success_criteria>
- alloc_and_peer_copy_async (lines 593-625 of cucascade/src/data/representation_converter.cpp) is patched per D-07 same-stream invariant: rmm::device_buffer::allocate_async (line 600) and BOTH cudaMemcpyAsync legs share `target_stream` as their stream argument; the local `rmm::cuda_stream src_stream` is gone; sync-then-free ordering at function tail is preserved.
- cucascade builds clean and `ctest` passes 100% (CC-04 invariant from v1.4 unchanged).
- SF1 Q11 num_gpus=2 sanitizer log post-fix shows 0 races whose backtrace contains `alloc_and_peer_copy_async`.
- New cucascade commit on the local fork (descendant of `1c1e648`); commit SHA is recorded for Plan 04.
- HYG-02 invariant preserved across cucascade/.
</success_criteria>

<output>
After completion, create `.planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-03-SUMMARY.md` documenting:
1. Exact pre-fix vs post-fix diff in alloc_and_peer_copy_async (function lines 593-625, with line numbers).
2. New cucascade commit SHA on the local fork (record for Plan 04 submodule bump).
3. Cucascade ctest result (PASS, runtime, count).
4. Sanitizer micro-validation result: Cluster B count (must be 0), Cluster A advisory count, total races.
5. Confirmation that pre-commit hooks ran cleanly.
6. If Task 2 fails: explicit FAILED status with recommendation to switch to fix shape 2 (event-bridge per D-07 alternative).
</output>
