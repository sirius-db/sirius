---
phase: 22-multi-gpu-pinning-stream-lineage-hardening
plan: 05
type: tdd
wave: 2
depends_on:
  - 22-01
  - 22-02
files_modified:
  - test/cpp/scan_manager/test_pin_table_multi_gpu.cpp
  - test/cpp/CMakeLists.txt
autonomous: true
requirements:
  - PIN-MGPU-01
must_haves:
  truths:
    - "[pin_mgpu] distribution test asserts pinned_entry.chunk_memory_spaces has chunks on at least 2 distinct GPU device_ids on num_gpus=2 hosts"
    - "[pin_mgpu][mgpu-audit] routing test asserts at least 1 task ran on each of the 2 GPU executors when SELECT-ing from a pinned multi-file table"
    - "Multi-file fixture (>=2 parquet files) is required because per-file binding from Plan 02 means single-file pins land all chunks on one GPU"
    - "Both tests gate on require_two_gpus() and silently skip on 1-GPU hosts"
    - "Both tests are registered in the unit-test build"
  artifacts:
    - path: "test/cpp/scan_manager/test_pin_table_multi_gpu.cpp"
      provides: "Two Catch2 TEST_CASEs tagged [pin_mgpu] (distribution + routing)"
      contains: "TEST_CASE(\"pin_table - PIN-MGPU-01"
    - path: "test/cpp/CMakeLists.txt"
      provides: "TEST_SOURCES entry registering the new test file"
      contains: "scan_manager/test_pin_table_multi_gpu.cpp"
  key_links:
    - from: "Distribution test"
      to: "sirius_scan_manager::get_pinned_entries() (Plan 01 accessor)"
      via: "REQUIRE on chunk_memory_spaces[i]->get_device_id() distinct count >= 2"
      pattern: "get_pinned_entries\\(\\)"
    - from: "Routing test"
      to: "[mgpu-audit] log emission via parse_audit_log()"
      via: "SIRIUS_LOG_DIR swap + counts[0]/counts[1] scan_ids assertions"
      pattern: "parse_audit_log"
---

<objective>
Author the two new Catch2 tests (`[pin_mgpu]` distribution gate + `[pin_mgpu][mgpu-audit]` routing gate) that prove PIN-MGPU-01 works end-to-end on `num_gpus=2`, and register them in the unit-test build.

Purpose: Per CONTEXT.md D-12, two new gates are required:
1. Distribution gate: pin a multi-file parquet table on `num_gpus=2`, then via the public accessor from Plan 01 assert `pinned_entry.chunk_memory_spaces` has chunks on both GPU 0 and GPU 1.
2. Routing gate: pin a multi-file table, run a SELECT, capture `[mgpu-audit]` INFO logs via SIRIUS_LOG_DIR swap, parse via `parse_audit_log()`, assert ≥1 task ran on each GPU's executor.

Output: A new test file under `test/cpp/scan_manager/`, registered in the unit-test CMake target, both tests PASS via MCP on the 2-GPU host.

Interface-first ordering: The test file is the consumer of the public API surface introduced by Plans 01-02 (`get_pinned_entries()`, `chunk_memory_spaces`, the round-robin behavior). Plans 01+02 must land first.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-CONTEXT.md
@.planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-RESEARCH.md
@.planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-01-SUMMARY.md
@.planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-02-SUMMARY.md
@CLAUDE.md
@test/cpp/operator/mgpu_test_utils.hpp
@test/cpp/integration/test_gpu_execution_tpch_mgpu_audit.cpp
@test/cpp/integration/test_table_gpu_cache_warm_mgpu.cpp
@test/cpp/integration/test_gpu_execution_tpch.cpp

<interfaces>
Reusable test infrastructure from `test/cpp/operator/mgpu_test_utils.hpp` (lines 54-300+). Executor MUST read the full file before writing the new test:
- `mgpu_env_params` (struct configuring per-test env: num_gpus, GPU memory size, etc.)
- `write_mgpu_yaml(path, params)` — writes a sirius config yaml for `scoped_mgpu_env`
- `require_two_gpus()` — returns false (and WARN+returns from the test) if `cudaGetDeviceCount() < 2`
- `scoped_mgpu_env{yaml_path}` — RAII env that loads the config, exposes `pause()`/`resume()`/`make_connection()`
- `generate_parquet_surface(dir, sql, num_files)` — generates N parquet files via DuckDB COPY
- `parquet_glob(dir)` — returns the glob pattern for the surface
- `parse_audit_log(log_dir)` — returns `std::map<int gpu_id, audit_counts>` where audit_counts has `pipeline_ids` and `scan_ids` sets

Reference test pattern for SIRIUS_LOG_DIR-swap (verbatim from `test/cpp/integration/test_gpu_execution_tpch_mgpu_audit.cpp:166-235`):
```
env->pause();
auto tmp_log_dir = fs::temp_directory_path() / ("sirius-pin-mgpu-" + std::to_string(::getpid()));
fs::remove_all(tmp_log_dir, ec); fs::create_directories(tmp_log_dir);
setenv("SIRIUS_LOG_DIR", tmp_log_dir.c_str(), 1);
setenv("SIRIUS_LOG_LEVEL", "info", 1);
env->resume();
{
  auto con = std::make_unique<duckdb::Connection>(env->make_connection());
  // ... CALL pin_table; SELECT ... ;
}
env->pause();   // destroys DuckDB; flushes spdlog
auto counts = parse_audit_log(tmp_log_dir);
```

Reference test pattern for `[pin_table]` SQL DDL (from `test/cpp/integration/test_gpu_execution_tpch.cpp:4370-4389`):
```
auto pin_sql = "CALL pin_table('" + glob_pattern + "', tier='gpu', name='multi_chunk');";
auto pin = con.Query(pin_sql);
REQUIRE(pin); REQUIRE_FALSE(pin->HasError());
// ... use the pinned table in a SELECT ...
auto unpin = con.Query("CALL unpin_table('multi_chunk');");
REQUIRE(unpin); REQUIRE_FALSE(unpin->HasError());
```

CMakeLists.txt registration target: `test/cpp/CMakeLists.txt` lines 363-438 (per research). Find the `TEST_SOURCES` list and add the new file path in alphabetical order: `scan_manager/test_pin_table_multi_gpu.cpp`.

NEW directory `test/cpp/scan_manager/` does not exist. The executor must `mkdir -p` and place the test there. Alternative discussed in research: place in existing `test/cpp/integration/` next to `test_table_gpu_cache_warm_mgpu.cpp`. Plan 05 locks `test/cpp/scan_manager/` per research recommendation; if CMake glob-style TEST_SOURCES requires the file to be co-located with sibling tests for a reason, the executor may pivot to integration/ but MUST document the choice in the SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author [pin_mgpu] distribution + routing tests</name>
  <files>test/cpp/scan_manager/test_pin_table_multi_gpu.cpp</files>
  <read_first>
    - test/cpp/operator/mgpu_test_utils.hpp (FULL file — confirm exact signatures of mgpu_env_params, write_mgpu_yaml, require_two_gpus, scoped_mgpu_env, generate_parquet_surface, parquet_glob, parse_audit_log)
    - test/cpp/integration/test_gpu_execution_tpch_mgpu_audit.cpp lines 60-300 (canonical SIRIUS_LOG_DIR-swap pattern — Test 2 must mirror it)
    - test/cpp/integration/test_table_gpu_cache_warm_mgpu.cpp lines 1-240 (multi-file parquet generation pattern — fu17 sentinel test reuses generate_parquet_surface; Test 1 mirrors this)
    - test/cpp/integration/test_gpu_execution_tpch.cpp lines 4370-4389 (existing [pin_table] SQL pattern — CALL pin_table + SELECT + CALL unpin_table)
    - src/include/scan_manager/sirius_scan_manager.hpp (post-Plan-01 — confirm get_pinned_entries() signature)
    - src/include/sirius_context.hpp or wherever SiriusContext is registered as DuckDB session state — to confirm how to access the SiriusContext from the test connection. Likely pattern: `con.context->registered_state->Get<duckdb::SiriusContext>("sirius_state")` per research line 535.
    - .planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-RESEARCH.md "Distribution gate Catch2 test" (lines 478-558 — verbatim test skeleton)
    - .planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-CONTEXT.md D-12 (locked decisions for both gates)
  </read_first>
  <behavior>
    - Test 1 (distribution gate): name `pin_table - PIN-MGPU-01 multi-GPU chunk distribution`, tags `[pin_mgpu][scan_manager]`.
      - Skip via `if (!sirius::test::mgpu::require_two_gpus()) return;` on 1-GPU hosts.
      - Generate 4 parquet files via `generate_parquet_surface(tmp_dir, "SELECT range AS k, range * 2 AS v FROM range(100000)", /*num_files=*/4)`.
      - Write mgpu yaml via `write_mgpu_yaml(yaml, {})` (default num_gpus=2 from mgpu_test_utils).
      - Construct `scoped_mgpu_env env{yaml}`; `auto con = env.make_connection();`.
      - CALL `pin_table('<glob>', tier='gpu', name='multi_chunk')` — REQUIRE no error.
      - Acquire SiriusContext from connection state; call `sirius_ctx->get_scan_manager().get_pinned_entries()` (the Plan 01 accessor).
      - Find the entry by name `multi_chunk`; iterate its `chunk_memory_spaces`; collect `get_device_id()` into `std::set<int>`.
      - REQUIRE distinct device_ids.size() >= 2 (at least 2 GPUs received chunks).
      - CALL `unpin_table('multi_chunk')`; REQUIRE no error.
    - Test 2 (routing gate): name `pin_table - PIN-MGPU-01 routing via [mgpu-audit]`, tags `[pin_mgpu][scan_manager][mgpu-audit]`.
      - Skip on 1-GPU hosts.
      - Same 4-file fixture.
      - SIRIUS_LOG_DIR-swap pattern verbatim from `test_gpu_execution_tpch_mgpu_audit.cpp:166-235`:
        - env->pause(); set SIRIUS_LOG_DIR + SIRIUS_LOG_LEVEL=info; env->resume();
        - CALL pin_table; SELECT * FROM multi_chunk WHERE k % 2 = 0; CALL unpin_table.
        - env->pause() to flush spdlog before parsing.
      - `auto counts = parse_audit_log(tmp_log_dir);` — counts is `std::map<int, audit_counts>`.
      - REQUIRE counts[0].scan_ids.size() >= 1 (GPU 0 ran at least one cached scan task).
      - REQUIRE counts[1].scan_ids.size() >= 1 (GPU 1 ran at least one cached scan task).
      - Cleanup tmp dirs.
  </behavior>
  <action>
    1. Create directory `test/cpp/scan_manager/` if it does not exist (executor must `mkdir -p test/cpp/scan_manager/` from repo root).

    2. Create `test/cpp/scan_manager/test_pin_table_multi_gpu.cpp`. Include header order per CLAUDE.md / project conventions (research lines 482-498 verbatim include order). The skeleton from research lines 478-558 is the recommended starting point; the executor MUST verify each header path exists by reading the corresponding file (e.g., `operator/mgpu_test_utils.hpp` may need to be `mgpu_test_utils.hpp` if the project's include path roots differ — confirm via existing `#include` lines in `test_gpu_execution_tpch_mgpu_audit.cpp` and `test_table_gpu_cache_warm_mgpu.cpp`).

    3. Write the distribution TEST_CASE first (RED-GREEN cycle):
       - RED: First commit only the TEST_CASE skeleton with REQUIRE statements that will FAIL on the post-Plan-02 code only because of fixture errors (e.g., parquet glob path issues). Run via `mcp__project-commands__run_command unit-tests filter='[pin_mgpu]'` and verify it RUNS (not "test not found"). Note that since Plans 01+02 are already in HEAD before Plan 05 starts, the production assertion (chunks on 2 GPUs) is expected to PASS — RED-GREEN-REFACTOR for this plan reduces to "test fails before fixture is wired correctly; passes after".
       - GREEN: Wire the fixture, the SQL, and the assertions. Run again; expect 1/1 PASS.

    4. Write the routing TEST_CASE second (same RED-GREEN cycle).
       - Use `parse_audit_log()` exactly as `test_gpu_execution_tpch_mgpu_audit.cpp` does. The function returns `std::map<int gpu, audit_counts>` where keys 0 and 1 may not always be present — REQUIRE first that both keys exist:
         ```
         REQUIRE(counts.count(0) == 1);
         REQUIRE(counts.count(1) == 1);
         REQUIRE(counts.at(0).scan_ids.size() >= 1);
         REQUIRE(counts.at(1).scan_ids.size() >= 1);
         ```
       - The exact name of the audit_counts member (`scan_ids`, `scan_batch_ids`, etc.) MUST be confirmed by reading `mgpu_test_utils.hpp` lines 266-287 BEFORE the test is written.

    5. Commit message convention: `test(p22): add [pin_mgpu] distribution + routing tests` followed by body citing PIN-MGPU-01 + D-12.

    Implementation invariants:
    - D-15: no `rmm::cuda_stream_default` usage in the test.
    - Catch2 v2 syntax (NOT v3 — cucascade fetches v2.13.10 per research line 95; sirius likely matches).
    - require_two_gpus() check at the TOP of every TEST_CASE (silent skip on 1-GPU).
    - Use `std::filesystem` (namespace `fs = std::filesystem;`) for tmp dirs; clean up before and after with `fs::remove_all(...)`.
    - Tag both tests `[pin_mgpu]` per D-12; routing test ALSO carries `[mgpu-audit]` tag.
  </action>
  <verify>
    <automated>test -f test/cpp/scan_manager/test_pin_table_multi_gpu.cpp &amp;&amp; grep -cE "TEST_CASE.*pin_table.*PIN-MGPU-01" test/cpp/scan_manager/test_pin_table_multi_gpu.cpp | grep -qE "^2$" &amp;&amp; grep -cE "\[pin_mgpu\]" test/cpp/scan_manager/test_pin_table_multi_gpu.cpp | grep -qE "^[2-9]" &amp;&amp; grep -nE "require_two_gpus" test/cpp/scan_manager/test_pin_table_multi_gpu.cpp</automated>
  </verify>
  <acceptance_criteria>
    - File `test/cpp/scan_manager/test_pin_table_multi_gpu.cpp` exists.
    - `grep -cE "TEST_CASE.*PIN-MGPU-01" test/cpp/scan_manager/test_pin_table_multi_gpu.cpp` returns 2 (both gates present).
    - `grep -cE "\[pin_mgpu\]" test/cpp/scan_manager/test_pin_table_multi_gpu.cpp` returns at least 2 (one per TEST_CASE).
    - `grep -nE "require_two_gpus" test/cpp/scan_manager/test_pin_table_multi_gpu.cpp` returns at least 2 lines (one guard per TEST_CASE).
    - `grep -nE "get_pinned_entries\(\)" test/cpp/scan_manager/test_pin_table_multi_gpu.cpp` returns at least 1 line (Plan 01 accessor used).
    - `grep -nE "chunk_memory_spaces" test/cpp/scan_manager/test_pin_table_multi_gpu.cpp` returns at least 1 line (Plan 01 vector traversed).
    - `grep -nE "parse_audit_log" test/cpp/scan_manager/test_pin_table_multi_gpu.cpp` returns at least 1 line (routing test).
    - `grep -nE "generate_parquet_surface.*4" test/cpp/scan_manager/test_pin_table_multi_gpu.cpp` returns at least 1 line (multi-file fixture, num_files=4).
    - `grep -nE "REQUIRE.*scan_ids.*size.*>= 1" test/cpp/scan_manager/test_pin_table_multi_gpu.cpp` returns at least 2 lines (one per GPU in routing test).
    - `grep -c "rmm::cuda_stream_default" test/cpp/scan_manager/test_pin_table_multi_gpu.cpp` returns 0.
  </acceptance_criteria>
  <done>Test file authored with both TEST_CASEs, multi-file fixture, both gates' assertions, require_two_gpus guards, no HYG-02 violations.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Register test in CMakeLists.txt and verify both tests PASS via MCP</name>
  <files>test/cpp/CMakeLists.txt</files>
  <read_first>
    - test/cpp/CMakeLists.txt lines 360-450 (TEST_SOURCES list — find the alphabetical insertion point for `scan_manager/test_pin_table_multi_gpu.cpp`)
    - test/cpp/CMakeLists.txt full file (smaller than 800 lines per research — confirm there is no separate scan_manager/ subdirectory that needs a `add_subdirectory()` call; the file likely uses a flat TEST_SOURCES list)
  </read_first>
  <action>
    1. Locate the `TEST_SOURCES` list in `test/cpp/CMakeLists.txt` (research says lines 363-438). The list is likely alphabetical by directory then file. Insert this entry in the appropriate alphabetical position:
       ```
         scan_manager/test_pin_table_multi_gpu.cpp
       ```
       If the existing list groups by directory (e.g., all `integration/*` together), follow that grouping (insert `scan_manager/` between `scan/` and other entries alphabetically).

    2. Verify the build picks up the new file:
       - `mcp__project-commands__run_command build` — exit 0.
       - `ls build/release/extension/sirius/test/cpp/sirius_unittest` exists.
       - `build/release/extension/sirius/test/cpp/sirius_unittest --list-tests | grep "PIN-MGPU-01"` returns at least 2 lines (both new TEST_CASEs registered).

    3. Run the new tests via MCP:
       - `mcp__project-commands__run_command unit-tests filter='[pin_mgpu]'`.
       - Expected: 2/2 PASS, exit 0, runtime <60s (small fixture).
       - Expected output: 2 test cases, ≥4 assertions per test (REQUIRE counts on at least 2 distinct GPUs + REQUIRE >=1 per GPU + REQUIRE no error on pin/unpin).

    4. Run `[mgpu]` smoke regression to confirm the new test does not destabilize the existing suite:
       - `mcp__project-commands__run_command unit-tests filter='[mgpu]'`.
       - Expected: 16/16 PASS, exit 0, ≥79091 assertions.

    5. Run `[TPC-H][parquet]` smoke regression for completeness:
       - `mcp__project-commands__run_command unit-tests filter='[TPC-H][parquet]'`.
       - Expected: 22/22 PASS, exit 0, ≥36256 assertions.

    6. Phase-wide HYG-02 check: `grep -rn "rmm::cuda_stream_default" src/ | wc -l` returns 40.

    Implementation invariants:
    - All test runs via MCP per project memory `feedback_use_mcp_build` and `feedback_mcp_tests_scope` (unit AND integration/GPU all via MCP).
    - Cap test runtime: Catch2 unit suite has historical bounds in `[mgpu]` and `[TPC-H][parquet]`; per `feedback_test_runtime_caps` budget = expected × 2-3, monitor for stalls.
    - Don't introduce `rmm::cuda_stream_default` in CMakeLists.txt (impossible, but check post-edit grep).
  </action>
  <verify>
    <automated>grep -nE "scan_manager/test_pin_table_multi_gpu\\.cpp" test/cpp/CMakeLists.txt &amp;&amp; ls build/release/extension/sirius/test/cpp/sirius_unittest 2&gt;/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "scan_manager/test_pin_table_multi_gpu\.cpp" test/cpp/CMakeLists.txt` returns exactly 1 line.
    - `mcp__project-commands__run_command build` exits 0.
    - `build/release/extension/sirius/test/cpp/sirius_unittest --list-tests | grep -c "PIN-MGPU-01"` returns 2.
    - `mcp__project-commands__run_command unit-tests filter='[pin_mgpu]'` exits 0 and reports 2/2 PASS, runtime ≤60s.
    - `mcp__project-commands__run_command unit-tests filter='[mgpu]'` exits 0 and reports 16/16 PASS, ≥79091 assertions, ≤130s.
    - `mcp__project-commands__run_command unit-tests filter='[TPC-H][parquet]'` exits 0 and reports 22/22 PASS, ≥36256 assertions, ≤90s.
    - `grep -rn "rmm::cuda_stream_default" src/ | wc -l` returns 40.
  </acceptance_criteria>
  <done>New test file registered in CMakeLists.txt; both [pin_mgpu] tests pass via MCP; [mgpu] 16/16 + [TPC-H][parquet] 22/22 unchanged; HYG-02 = 40.</done>
</task>

</tasks>

<verification>
- Two new TEST_CASEs exist: `pin_table - PIN-MGPU-01 multi-GPU chunk distribution` (`[pin_mgpu][scan_manager]`) and `pin_table - PIN-MGPU-01 routing via [mgpu-audit]` (`[pin_mgpu][scan_manager][mgpu-audit]`).
- `mcp__project-commands__run_command unit-tests filter='[pin_mgpu]'` — 2/2 PASS, exit 0.
- `mcp__project-commands__run_command unit-tests filter='[mgpu]'` — 16/16 PASS, exit 0 (no destabilization from new test).
- `mcp__project-commands__run_command unit-tests filter='[TPC-H][parquet]'` — 22/22 PASS, exit 0.
- HYG-02 invariant: `grep -rn "rmm::cuda_stream_default" src/ | wc -l` returns 40.
</verification>

<success_criteria>
- Distribution gate: TEST_CASE asserts pinned_entry.chunk_memory_spaces has chunks on at least 2 distinct GPU device_ids on a 4-file × num_gpus=2 pin.
- Routing gate: TEST_CASE asserts at least 1 [mgpu-audit] scan task was dispatched on each of GPU 0 and GPU 1 after CALL pin_table + SELECT.
- Both tests PASS via MCP on the 2-GPU host.
- Existing `[mgpu]` 16/16 + `[TPC-H][parquet]` 22/22 unchanged.
- HYG-02 = 40 invariant preserved.
- Plans 06 and 07 unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-05-SUMMARY.md` documenting:
1. Final test file path (test/cpp/scan_manager/test_pin_table_multi_gpu.cpp; document if pivoted to integration/).
2. Two TEST_CASE names + tags + assertion counts.
3. CMakeLists.txt insertion line + position (alphabetical bucket).
4. MCP test run output: [pin_mgpu] 2/2 PASS with runtime + assertion count.
5. [mgpu] 16/16 + [TPC-H][parquet] 22/22 unchanged confirmation.
6. HYG-02 grep result post-Plan-05: 40.
</output>
