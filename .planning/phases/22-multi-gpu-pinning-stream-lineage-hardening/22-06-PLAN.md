---
phase: 22-multi-gpu-pinning-stream-lineage-hardening
plan: 06
type: execute
wave: 3
depends_on:
  - 22-04
files_modified:
  - test/scripts/sanitizer_gate_22.sh
autonomous: true
requirements:
  - fu17-cluster-b
must_haves:
  truths:
    - "Bash script `test/scripts/sanitizer_gate_22.sh` runs SF1 Q11 num_gpus=2 under compute-sanitizer via Bash + timeout (NOT MCP, per project memory)"
    - "Cluster B race count (matches `Host Frame:.*alloc_and_peer_copy_async`) MUST be 0 — exit 1 otherwise"
    - "Cluster A race count (matches `read_column_chunks_async` or `posix_device_io`) is recorded on stdout but DOES NOT affect exit code (D-09 advisory)"
    - "Script is executable (chmod +x), uses `set -euo pipefail`, and has bounded timeout (600s)"
    - "Filter logic literal: gate is grep -cE on stack-frame strings, NOT grep -v"
  artifacts:
    - path: "test/scripts/sanitizer_gate_22.sh"
      provides: "Executable bash script gating Cluster B = 0 with Cluster A advisory"
      contains: "alloc_and_peer_copy_async"
  key_links:
    - from: "Sanitizer log /tmp/p22_sanitizer.log"
      to: "Cluster B race count via grep -cE 'Host Frame:.*alloc_and_peer_copy_async'"
      via: "Stack-frame regex filter (Pitfall 5: literal grep -cE NOT grep -v)"
      pattern: "Host Frame:.*alloc_and_peer_copy_async"
---

<objective>
Author the Phase 22 D-12 sanitizer gate script that runs SF1 Q11 num_gpus=2 under `compute-sanitizer --tool memcheck --track-stream-ordered-races=all` via Bash + timeout, parses the log, and exits 0 only if 0 Cluster B races (`alloc_and_peer_copy_async` frames) are present. Cluster A frames (cudf+kvikio internal) are recorded on stdout but do NOT affect the exit code per D-09.

Purpose: Per CONTEXT.md D-12, the third new gate added by Phase 22 is the fu17 Cluster B sanitizer gate. This script provides a reproducible CI/local-dev gate that proves the Plan 03 + Plan 04 fix landed correctly. Match Phase 21 21-VERDICT.md Section F sanitizer command shape verbatim.

Output: `test/scripts/sanitizer_gate_22.sh` (new), executable, with bounded timeout, literal filter logic, and Phase 21-verbatim sanitizer invocation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-CONTEXT.md
@.planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-RESEARCH.md
@.planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-04-SUMMARY.md
@.planning/milestones/v1.4-phases/21-v1-4-ship-gate-full-v1-3-gauntlet-on-rebased-branch/21-VERDICT.md
@CLAUDE.md

<interfaces>
Phase 21 sanitizer command shape (verbatim from 21-VERDICT.md:295-317; Phase 22 must match exactly except for the test filter and log path):

```
CUDA_BIN=$(ls -1 /usr/local/cuda*/bin/compute-sanitizer 2>/dev/null | head -1)
LOG=/tmp/p22_sanitizer.log

timeout 600 "${CUDA_BIN}" \
  --tool memcheck \
  --track-stream-ordered-races=all \
  --show-backtrace=yes \
  --launch-timeout=600 \
  --log-file "${LOG}" \
  --print-limit 100 \
  build/release/extension/sirius/test/cpp/sirius_unittest "<TEST_NAME>" \
  || true   # sanitizer non-zero exit is expected; we gate on log content
```

Filter regexes (Pitfall 5 literal — do NOT invert):
- Cluster B (gate): `grep -cE 'Host Frame:.*alloc_and_peer_copy_async' "$LOG"` MUST be 0.
- Cluster A (advisory only): `grep -cE 'Host Frame:.*(read_column_chunks_async|posix_device_io)' "$LOG"` (no exit gating).
- Total race blocks: `grep -cE 'Use-before-alloc on allocation' "$LOG"` (informational).

SF1 Q11 num_gpus=2 test name: `gpu_execution - TPC-H Query 11 parquet` per research line 576. Executor MUST verify exact name via:
  build/release/extension/sirius/test/cpp/sirius_unittest --list-tests | grep -i "Query 11"
The test must be num_gpus=2 — Catch2 GENERATE may produce two flavors (1-GPU, 2-GPU). The flavor pinned to num_gpus=2 is the target. Verify the exact case selector by reading `test_gpu_execution_tpch.cpp` Q11 TEST_CASE.

Project memory: `feedback_sanitizer_via_bash_not_mcp` — compute-sanitizer hangs on this host when invoked via MCP; ALWAYS use Bash + timeout (this script complies by being a standalone Bash script, not an MCP command).

Project memory: `feedback_test_runtime_caps` — budget = expected × 2-3. SF1 Q11 baseline ~30s; under sanitizer 10x = 300s; safety margin → 600s timeout.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author sanitizer_gate_22.sh and validate against bumped pin</name>
  <files>test/scripts/sanitizer_gate_22.sh</files>
  <read_first>
    - .planning/milestones/v1.4-phases/21-v1-4-ship-gate-full-v1-3-gauntlet-on-rebased-branch/21-VERDICT.md Section F (lines 280-330 — verbatim sanitizer command shape; the exact compute-sanitizer flags Phase 22 must match)
    - .planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-RESEARCH.md "Sanitizer gate bash script (recommended skeleton)" (lines 561-611 — recommended script body, gate logic, exit codes)
    - .planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-RESEARCH.md "Common Pitfalls - Pitfall 5" (lines 336-348 — Cluster B count via grep -cE, NOT grep -v)
    - .planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-CONTEXT.md D-12 (sanitizer gate spec) + D-09 (Cluster A advisory only) + D-16 (sanitizer via Bash + timeout)
    - test/cpp/integration/test_gpu_execution_tpch.cpp Q11 TEST_CASE — confirm exact Catch2 test name and which case selector pins num_gpus=2 (typically `[1]` or similar Catch2 GENERATE selector). Pattern source: `test_gpu_execution_tpch_mgpu_audit.cpp` for how multi-GPU TEST_CASEs are filtered.
  </read_first>
  <action>
    1. Create `test/scripts/` directory if it does not exist (executor must `mkdir -p test/scripts/` from repo root). Verify with `ls -la test/scripts/` after creation.

    2. Author `test/scripts/sanitizer_gate_22.sh`. The exact body (use research lines 561-611 verbatim as starting point; adjust per below):
       ```
       #!/usr/bin/env bash
       # Phase 22 D-12 Cluster B sanitizer gate.
       # Exits 0 only if Cluster B race count == 0; logs Cluster A as advisory.
       # Per project memory feedback_sanitizer_via_bash_not_mcp: compute-sanitizer
       # MUST run via Bash + timeout (NOT MCP) due to host-specific incompatibility.
       set -euo pipefail

       CUDA_BIN=$(ls -1 /usr/local/cuda*/bin/compute-sanitizer 2>/dev/null | head -1)
       [[ -n "${CUDA_BIN}" ]] || { echo "compute-sanitizer not found"; exit 2; }

       LOG="${P22_SANITIZER_LOG:-/tmp/p22_sanitizer.log}"
       UNIT="${P22_UNITTEST_BIN:-build/release/extension/sirius/test/cpp/sirius_unittest}"
       SF1_QUERY="${P22_QUERY:-gpu_execution - TPC-H Query 11 parquet}"

       [[ -x "${UNIT}" ]] || { echo "unittest binary not found at ${UNIT}"; exit 2; }

       echo "[p22-sanitizer-gate] starting compute-sanitizer on '${SF1_QUERY}'"
       timeout 600 "${CUDA_BIN}" \
         --tool memcheck \
         --track-stream-ordered-races=all \
         --show-backtrace=yes \
         --launch-timeout=600 \
         --log-file "${LOG}" \
         --print-limit 100 \
         "${UNIT}" "${SF1_QUERY}" \
         || true

       [[ -s "${LOG}" ]] || { echo "[p22-sanitizer-gate] FAIL: log not produced"; exit 1; }

       CLUSTER_B=$(grep -cE 'Host Frame:.*alloc_and_peer_copy_async' "${LOG}" || true)
       CLUSTER_A=$(grep -cE 'Host Frame:.*(read_column_chunks_async|posix_device_io)' "${LOG}" || true)
       TOTAL_RACES=$(grep -cE 'Use-before-alloc on allocation' "${LOG}" || true)

       echo "[p22-sanitizer-gate] cluster_B=${CLUSTER_B} (gate: must be 0)"
       echo "[p22-sanitizer-gate] cluster_A=${CLUSTER_A} (advisory; D-09)"
       echo "[p22-sanitizer-gate] total_races=${TOTAL_RACES}"

       if [[ "${CLUSTER_B}" -ne 0 ]]; then
         echo "[p22-sanitizer-gate] FAIL: ${CLUSTER_B} Cluster B races found"
         grep -B 1 -A 30 'alloc_and_peer_copy_async' "${LOG}" | head -200
         exit 1
       fi

       echo "[p22-sanitizer-gate] PASS: Cluster B = 0"
       exit 0
       ```

    3. Make executable: `chmod +x test/scripts/sanitizer_gate_22.sh`.

    4. Verify the test name resolves. Run:
       `build/release/extension/sirius/test/cpp/sirius_unittest --list-tests 2>&1 | grep -iE "TPC-H Query 11"`
       The output should show the exact TEST_CASE name. If the name in the script (`gpu_execution - TPC-H Query 11 parquet`) does not exactly match, update the script's `SF1_QUERY` default. If Catch2 GENERATE produces a parameterized variant, the bash script may need to pass a Catch2 tag filter like `'[Q11][parquet][mgpu]'` instead of a name; pivot accordingly and document in the SUMMARY.

    5. Run the script locally to validate: `bash test/scripts/sanitizer_gate_22.sh`.
       - Expected: exit 0, output shows `cluster_B=0`, `cluster_A=<some positive number — advisory>`, `total_races=<some number>`.
       - This is a real run against the bumped pin (Plan 04 already advanced submodule + verified [mgpu] 16/16 unchanged).
       - If Cluster B != 0: Plan 06 reports a FAIL state in its SUMMARY. Plan 03 must be re-attempted with fix shape 2 (event-bridge per CONTEXT.md D-07). DO NOT attempt to "tune" the filter regex to pass — that breaks Pitfall 5.

    6. Confirm exit code:
       - Re-run with `bash test/scripts/sanitizer_gate_22.sh; echo "exit=$?"` — should print `exit=0`.

    7. Negative-test the gate (sanity check the filter):
       - Manually inject a fake Cluster B frame into a copy of the log: `cp /tmp/p22_sanitizer.log /tmp/p22_sanitizer_negative.log; echo "Host Frame:.*alloc_and_peer_copy_async fake frame" >> /tmp/p22_sanitizer_negative.log`.
       - Run: `P22_SANITIZER_LOG=/tmp/p22_sanitizer_negative.log bash test/scripts/sanitizer_gate_22.sh; echo "exit=$?"`.
       - Expected: exit 1, output reports `cluster_B=1`. Confirms the gate fires correctly on Cluster B frames.

    8. Cleanup negative-test files: `rm -f /tmp/p22_sanitizer_negative.log`.

    Implementation invariants:
    - D-12: Cluster B = 0 gates exit 0; Cluster A is advisory only.
    - D-09: Cluster A persists post-fix (cudf+kvikio internal); does NOT affect exit code.
    - Pitfall 5: literal `grep -cE` on stack-frame strings; NEVER invert with `-v`.
    - Project memory `feedback_sanitizer_via_bash_not_mcp`: this is a Bash script, executed directly (NOT via MCP). The orchestrator + verify-work agent calls this script via Bash, never via mcp__project-commands__run_command.
    - Project memory `feedback_test_runtime_caps`: timeout 600s.
    - Script uses `set -euo pipefail` for safety; uses `|| true` after compute-sanitizer (sanitizer non-zero exit is expected and is gated on log content).
    - Script accepts environment overrides for log path / unittest binary / query name (so future automation can reuse it).
  </action>
  <verify>
    <automated>test -x test/scripts/sanitizer_gate_22.sh &amp;&amp; grep -nE "alloc_and_peer_copy_async" test/scripts/sanitizer_gate_22.sh &amp;&amp; grep -nE "track-stream-ordered-races=all" test/scripts/sanitizer_gate_22.sh &amp;&amp; grep -nE "set -euo pipefail" test/scripts/sanitizer_gate_22.sh &amp;&amp; ! grep -E "grep -v.*alloc_and_peer_copy_async" test/scripts/sanitizer_gate_22.sh</automated>
  </verify>
  <acceptance_criteria>
    - File `test/scripts/sanitizer_gate_22.sh` exists and is executable (`test -x test/scripts/sanitizer_gate_22.sh`).
    - `grep -nE "set -euo pipefail" test/scripts/sanitizer_gate_22.sh` returns exactly 1 line (safety mode set).
    - `grep -nE "track-stream-ordered-races=all" test/scripts/sanitizer_gate_22.sh` returns exactly 1 line (D-12 sanitizer flag).
    - `grep -nE "Host Frame:.*alloc_and_peer_copy_async" test/scripts/sanitizer_gate_22.sh` returns at least 2 lines (filter + display).
    - `grep -nE "read_column_chunks_async|posix_device_io" test/scripts/sanitizer_gate_22.sh` returns at least 1 line (Cluster A advisory regex).
    - `grep -E "grep -v.*alloc_and_peer_copy_async" test/scripts/sanitizer_gate_22.sh` returns 0 lines (Pitfall 5: NO inverted filter).
    - `grep -nE "exit 0" test/scripts/sanitizer_gate_22.sh` returns exactly 1 line (success exit).
    - `grep -nE "exit 1" test/scripts/sanitizer_gate_22.sh` returns at least 1 line (failure exit on Cluster B != 0).
    - `grep -nE "timeout 600" test/scripts/sanitizer_gate_22.sh` returns at least 1 line (per `feedback_test_runtime_caps`).
    - Bash run on the bumped pin: `bash test/scripts/sanitizer_gate_22.sh` exits 0 and reports `cluster_B=0` on stdout.
    - Negative-test: `P22_SANITIZER_LOG=/tmp/p22_sanitizer_negative.log bash test/scripts/sanitizer_gate_22.sh` exits 1 (gate fires on injected Cluster B line).
    - HYG-02 invariant unchanged: `grep -rn "rmm::cuda_stream_default" src/ | wc -l` returns 40.
  </acceptance_criteria>
  <done>Script authored, executable, exit-0 on bumped pin, exit-1 on injected failure; PIN-MGPU-01 sanitizer gate proven live; Pitfall 5 filter shape literal.</done>
</task>

</tasks>

<verification>
- `test/scripts/sanitizer_gate_22.sh` exists and is executable.
- Live run on bumped pin: exit 0, `cluster_B=0` reported.
- Negative-test: exit 1, `cluster_B=1` reported with injected fake frame.
- HYG-02 invariant: 40.
- Cluster A count is RECORDED on stdout but does NOT affect exit code.
- Sanitizer command shape matches Phase 21 21-VERDICT.md Section F verbatim (memcheck + track-stream-ordered-races=all + show-backtrace + log-file + print-limit 100).
</verification>

<success_criteria>
- The Phase 22 D-12 sanitizer gate is reproducible: any developer or CI invocation of `bash test/scripts/sanitizer_gate_22.sh` produces a deterministic pass/fail outcome.
- Gate fires on Cluster B = 0 (proven live + via injection).
- Cluster A is advisory only; does NOT cause exit 1 even when present.
- Project memory `feedback_sanitizer_via_bash_not_mcp` is honored: script invoked via Bash, NOT MCP.
- Plan 07 unblocked (uses this script as one of the new gates in the v1.4 ship gauntlet rerun).
</success_criteria>

<output>
After completion, create `.planning/phases/22-multi-gpu-pinning-stream-lineage-hardening/22-06-SUMMARY.md` documenting:
1. Final script path: `test/scripts/sanitizer_gate_22.sh`.
2. Live run output on bumped pin: cluster_B=0, cluster_A=<n>, total_races=<n>, exit 0, runtime.
3. Negative-test confirmation (injection produces exit 1).
4. SF1 Q11 num_gpus=2 test name resolution (verbatim TEST_CASE name used).
5. HYG-02 grep result: 40.
6. Confirmation that script is executable + uses set -euo pipefail + timeout 600 + Pitfall 5 literal filter.
</output>
