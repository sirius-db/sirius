---
phase: 06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter
plan: 04
type: execute
wave: 2
depends_on: ["06-01", "06-02", "06-03"]
files_modified:
  - .planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-04-VALIDATION.md
  - .planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-SUMMARY.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [MGPU-01, MGPU-02, MGPU-03, MGPU-04, MGPU-05]

must_haves:
  truths:
    - "Full build green via mcp__project-commands__run_command on current HEAD with Plans 06-01..03 applied"
    - "Full unit-tests green (>=974 cases, pass rate 100%) on current HEAD"
    - "compute-sanitizer --tool memcheck --require-cuda-init reports 0 errors on [multi_gpu_foundation] tag on N=2 host"
    - "compute-sanitizer --tool memcheck --require-cuda-init reports 0 errors on [integration][gpu_execution][parquet][join] subset on N=2 host"
    - "MGPU-04 [mgpu_04_round_trip] hidden test PASSES on N=2 host (forward leg only)"
    - "MGPU-01 sweep grep evidence captured: zero hits in src/ outside src/cuda/ legacy path"
    - "MGPU-02 SF10 wall-clock captured via run_tpch_parquet.sh sirius 10 on 1-GPU config; compared against Phase-5 SF10 baseline from 05-06-MULTIGPU-VALIDATION.md (NOT performance_test.py — that hits legacy path per RESEARCH.md Pitfall 1)"
    - "MGPU-05 evidence: /proc/PID/numa_maps spot-check recorded + SiriusContext host-space log line shows expected count==num_numa_nodes on N=2 host"
    - "Phase 6 SUMMARY written; STATE.md + ROADMAP.md + REQUIREMENTS.md mark MGPU-01..05 as Complete; human sign-off checkpoint cleared"
  artifacts:
    - path: ".planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-04-VALIDATION.md"
      provides: "Evidence artifact aggregating MGPU-01 sweep output + MGPU-02 SF10 timings + MGPU-03 sanitizer runs + MGPU-04 round-trip result + MGPU-05 numa_maps spot-check"
      contains: "MGPU-01"
    - path: ".planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-SUMMARY.md"
      provides: "Phase 6 sign-off document following templates/summary.md"
      contains: "MGPU-01"
    - path: ".planning/STATE.md"
      provides: "Updated Phase 6 status Complete"
      contains: "Phase 6"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 6 progress row marked Complete with plan count"
      contains: "06. Multi-GPU Gap Closure"
    - path: ".planning/REQUIREMENTS.md"
      provides: "MGPU-01..05 marked [x]"
      contains: "MGPU-01"
  key_links:
    - from: "06-04-VALIDATION.md"
      to: "compute-sanitizer memcheck N=2 run logs"
      via: "verbatim stderr/stdout paste + exit code"
      pattern: "compute-sanitizer"
    - from: "06-SUMMARY.md"
      to: "06-01-SUMMARY.md / 06-02-SUMMARY.md / 06-03-SUMMARY.md"
      via: "Plans table referencing each plan's commits + requirements"
      pattern: "Plan 06-01"
---

<objective>
Phase sign-off. Consumes outputs of Plans 06-01 (MGPU-01 + MGPU-05 log/assert), 06-02 (MGPU-03 device-guard), 06-03 (MGPU-04 tests) and runs them through the Phase-6 validation gates on the N=2 verification host (`6f7e4c9-lcedt`, 2 × RTX 6000 Ada, driver 595.58.03, CUDA 13.2 — same host used by Plan 05-06).

Scope per planning_context:
- **MGPU-01**: verify grep sweep gate still clean; capture the startup topology log from a live Sirius load; write into VALIDATION.md.
- **MGPU-02**: re-measure TPC-H SF10 wall-clock on current HEAD using `run_tpch_parquet.sh sirius 10` (correct tool per RESEARCH.md Pitfall 1 — NOT `performance_test.py` which hits legacy `gpu_processing`). Compare against the Phase-5 SF10 baseline in `05-06-MULTIGPU-VALIDATION.md` as the pragmatic baseline (per planning_context interpretation 2 which explicitly authorizes reuse of Phase-5 timings if full dev@484db35 re-measurement is prohibitive). Document the choice in VALIDATION.md + SUMMARY.
- **MGPU-03**: run `compute-sanitizer --tool memcheck --require-cuda-init` against `[multi_gpu_foundation]` and `[integration][gpu_execution][parquet][join]` subsets. Expect zero "invalid device" / "context mismatch" errors. The Phase-4-deferred `[.][multi_gpu_transfer]` / `[.][mem_04_p2p_transfer]` hidden tests remain hidden — per planning_context interpretation 4 the return-leg bug stays Phase 7 scope.
- **MGPU-04**: run the new `[.][multi_gpu_foundation][mgpu_04_round_trip]` test from Plan 06-03 Task 2 explicitly on the N=2 host and capture the pass/fail outcome. The non-hidden `[mgpu_04_registration]` test runs under the default invocation.
- **MGPU-05**: capture `/proc/PID/numa_maps` spot-check for a live Sirius process on the N=2 host; grep for `N0=` / `N1=` markers on pinned allocations; record in VALIDATION.md. Also record the `SiriusContext: X host memory space(s) created for Y NUMA node(s)` log line from Plan 06-01 Task 1's assertion block.

After evidence is captured and the human sign-off checkpoint passes, write the phase SUMMARY, update STATE / ROADMAP / REQUIREMENTS, and close the phase.

Purpose: Single plan concentrating ALL validation + documentation gates so the human checkpoint reviews one evidence artifact, not five.
Output: VALIDATION.md, SUMMARY.md, STATE.md/ROADMAP.md/REQUIREMENTS.md updates, commit chain closing the phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-CONTEXT.md
@.planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-RESEARCH.md
@.planning/phases/05-cucascade-backed-parquet-i-o-migration/05-SUMMARY.md
@.planning/phases/05-cucascade-backed-parquet-i-o-migration/05-06-MULTIGPU-VALIDATION.md
@.planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-01-PLAN.md
@.planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-02-PLAN.md
@.planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-03-PLAN.md

<interfaces>
<!-- Tool contracts for this plan — exact commands to invoke. -->

Build + unit-tests (must succeed BEFORE sanitizer or SF10 runs):
```bash
mcp__project-commands__run_command build        # returns exit 0 on success
mcp__project-commands__run_command unit-tests   # returns exit 0 on success
```

MGPU-01 sweep gate (grep; expect empty output and exit 1 with the final || echo guard):
```bash
grep -rn 'cudaGetDeviceCount\|numa_node_of_cpu\|numa_available' src/ \
  --include='*.cpp' --include='*.hpp' --include='*.cu' --include='*.cuh' \
  | grep -v '^src/cuda/' \
  || echo "SUPER_SIRIUS_CLEAN"
# Expected output: "SUPER_SIRIUS_CLEAN"

grep -rn 'cudaGetDeviceCount\|numa_node_of_cpu' src/cuda/
# Expected output: exactly one line — src/cuda/allocator.cu:70:  err = cudaGetDeviceCount(&nDevices);
```

MGPU-03 compute-sanitizer invocations (run on N=2 host):
```bash
SIRIUS_CONFIG_FILE=/tmp/phase5-validation/sirius-2gpu.yaml \
compute-sanitizer --tool memcheck --require-cuda-init \
  build/release/extension/sirius/test/cpp/sirius_unittest \
  "[multi_gpu_foundation]"
# Expected: exit 0, sanitizer summary "ERROR SUMMARY: 0 errors"

SIRIUS_CONFIG_FILE=/tmp/phase5-validation/sirius-2gpu.yaml \
compute-sanitizer --tool memcheck --require-cuda-init \
  build/release/extension/sirius/test/cpp/sirius_unittest \
  "[integration][gpu_execution][parquet][join]"
# Expected: exit 0, "ERROR SUMMARY: 0 errors"
```

MGPU-04 hidden round-trip on N=2 host (explicit tag invocation):
```bash
build/release/extension/sirius/test/cpp/sirius_unittest \
  "[mgpu_04_round_trip]"
# Expected: PASS on N=2 host (forward leg only — return leg is Phase 7 scope)
```

MGPU-02 SF10 measurement (run on N=2 host with CUDA_VISIBLE_DEVICES or 1-GPU YAML config):
```bash
SIRIUS_CONFIG_FILE=/tmp/phase5-validation/sirius-sf10.yaml \
  test/tpch_performance/run_tpch_parquet.sh sirius 10 $(seq 1 22)
# Expected: 22 queries return correct row counts; wall-clock per-query captured; median of 3 runs within 5% of Phase-5 SF10 baseline (1-GPU: Q1=1.273s, Q6=0.233s, Q12=0.717s)
```

MGPU-05 /proc/PID/numa_maps evidence (capture during a live Sirius run):
```bash
# While Sirius is loaded and processing queries on the N=2 host:
pgrep -f sirius_unittest | head -1 | xargs -I PID cat /proc/PID/numa_maps \
  | grep -E 'N0=|N1=' | head -20
numactl --show
# Also: pull the "SiriusContext: X host memory space(s) created for Y NUMA node(s)"
# log line from build/release/extension/sirius/test/cpp/log/sirius_2026-*.log
```

Phase 5 baseline for MGPU-02 comparison (from `05-06-MULTIGPU-VALIDATION.md`):
- 1-GPU SF10: Q1=1.273s, Q6=0.233s, Q12=0.717s
- 2-GPU SF10: Q1=1.047s, Q6=0.302s, Q12=0.724s
- Regression threshold: <=5% per CONTEXT lock
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture phase validation evidence on N=2 host</name>
  <files>.planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-04-VALIDATION.md</files>

  <read_first>
    - .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-06-MULTIGPU-VALIDATION.md (shape to mirror + baseline SF10 timings)
    - .planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-01-PLAN.md, 06-02-PLAN.md, 06-03-PLAN.md (plan outputs Task 1 must aggregate)
    - .planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-RESEARCH.md (§"Finding 5" for the run_tpch_parquet.sh vs performance_test.py distinction)
  </read_first>

  <action>
    Create `.planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-04-VALIDATION.md` as the single evidence artifact for all Phase-6 validation gates.

    **Required sections** (in this order):

    1. **Verification host + HEAD**
       - Record hostname, driver version, CUDA toolkit, sirius HEAD SHA (`git rev-parse HEAD`), cucascade submodule HEAD SHA. Mirror Plan 05-06 Task 2a's format.

    2. **Build + Unit-Tests (all Plans applied)**
       - Invoke `mcp__project-commands__run_command build` on the N=2 host; record exit code + duration.
       - Invoke `mcp__project-commands__run_command unit-tests` and record: exit code, total test cases, assertions, new test count delta (expect +1 from Plan 06-03 Task 1 non-hidden test = 974 or more).

    3. **MGPU-01 — Topology discovery fail-hard + startup log + sweep gate**
       - Run the sweep grep commands from Plan 06-01 Task 2 verbatim; paste output.
       - Pull the 3 startup log lines from `build/release/extension/sirius/test/cpp/log/sirius_*.log` matching `SiriusContext: topology summary` + `GPU {n}: ...`. Paste verbatim with timestamps.
       - Confirm the fail-hard path is reachable by static inspection (no test run on the live Sirius path — the gate is `num_gpus == 0` which the N=2 host will never hit; write "path verified by grep" and cite the grep from Plan 06-01 acceptance criteria).

    4. **MGPU-02 — Single-GPU SF10 regression gate**
       - Tool: `test/tpch_performance/run_tpch_parquet.sh sirius 10 $(seq 1 22)` (NOT `performance_test.py` — document the rejection of `performance_test.py` citing RESEARCH.md Pitfall 1).
       - Run 3x with `SIRIUS_CONFIG_FILE=/tmp/phase5-validation/sirius-sf10.yaml` on the N=2 host (1-GPU config via the YAML). Record per-query wall-clocks; compute median per query across the 3 runs.
       - Compare against Phase-5 SF10 baseline (1-GPU column) from `05-06-MULTIGPU-VALIDATION.md`:
         - Q1: baseline 1.273s
         - Q6: baseline 0.233s
         - Q12: baseline 0.717s
       - Compute percent delta per query. PASS if all queries within 5%; WARN if 5-10%; FAIL if >10% (document the threshold choice in the artifact).
       - **Baseline choice note**: Per planning_context interpretation 2, reuse Phase-5 SF10 timings as baseline IF a fresh build at dev@484db35 would take >30 min; document which path was taken. Per user directive 2026-04-21 ("we don't need to run any comparisons, let's just make sure everything is working"), if the measurement is prohibitive even on Phase-5 HEAD reuse, record absolute Phase-6 timings only and defer the comparison — state this choice verbatim.

    5. **MGPU-03 — Device-guard audit**
       - Invocation 1: `compute-sanitizer --tool memcheck --require-cuda-init build/release/extension/sirius/test/cpp/sirius_unittest "[multi_gpu_foundation]"` on N=2 host with 2-GPU YAML. Record: exit code, "ERROR SUMMARY" line verbatim, test cases run, assertions, "new" vs "Phase-5" error baseline.
       - Invocation 2: same sanitizer, tag `"[integration][gpu_execution][parquet][join]"`. Record same fields.
       - **Gate**: zero "invalid device" + zero "context mismatch" errors. Any hits must be documented with a file/line attribution.
       - **Phase-4 deferred hidden tests stay hidden**: do NOT invoke `[.][multi_gpu_transfer]` or `[.][mem_04_p2p_transfer]` during this plan — per planning_context interpretation 4, the GPU1->GPU0 return-leg bug is Phase 7 scope. Note this exclusion verbatim in the artifact.

    6. **MGPU-04 — Converter registration + round-trip**
       - Non-hidden registration test: part of the Invocation-1 `[multi_gpu_foundation]` run above. Confirm `converter_registry exposes gpu_to_gpu converter after initialize() (MGPU-04)` appears in the test output as PASS.
       - Hidden round-trip test: explicit invocation `build/release/extension/sirius/test/cpp/sirius_unittest "[mgpu_04_round_trip]"` on the N=2 host. Record: test name, pass/fail, assertions, any sanitizer output if sanitizer is piped.
       - **Expected outcome on N=2 host**: PASS (forward leg only). Document the deliberate omission of the return leg per the Plan 06-03 Task 2 action.

    7. **MGPU-05 — Per-NUMA host memory spaces**
       - Capture `SiriusContext: X host memory space(s) created for Y NUMA node(s)` log line from the N=2 host's sirius log. Assert `X == Y` (or document a warn-only mismatch per the Plan 06-01 assertion block).
       - Run the numa_maps spot-check while the unit-tests executable is live:
         ```bash
         # Start test binary in background, capture PID, wait a beat for allocation, inspect
         build/release/extension/sirius/test/cpp/sirius_unittest "[multi_gpu_foundation]" &
         SIRIUS_PID=$!; sleep 2
         cat /proc/$SIRIUS_PID/numa_maps | grep -E 'N0=|N1=' | head -20
         wait $SIRIUS_PID
         ```
       - Paste output verbatim. Evidence is successful if `N0=` and `N1=` annotations appear on pinned allocations, consistent with the NUMA layout reported by `numactl --show`.

    8. **Summary table** at bottom:

       | Requirement | Gate | Result | Evidence anchor |
       |-------------|------|--------|-----------------|
       | MGPU-01 | Sweep grep + fail-hard in code + startup log | PASS/FAIL | §3 |
       | MGPU-02 | SF10 median within 5% of Phase-5 baseline (or absolute timings + deferral note) | PASS/WARN/DEFERRED | §4 |
       | MGPU-03 | compute-sanitizer 0 errors on 2 tags | PASS/FAIL | §5 |
       | MGPU-04 | registration test PASS + round-trip forward-leg PASS | PASS/FAIL | §6 |
       | MGPU-05 | host_spaces.size == num_numa_nodes + numa_maps evidence | PASS/FAIL | §7 |

    Commit VALIDATION.md with message `docs(06-04): capture Phase 6 validation evidence`. Do NOT write the SUMMARY yet — that's Task 3 after the human sign-off checkpoint.

    **Hardware dependency**: This task runs on the N=2 host. If the current host is the GPU-less CI worktree, execute the commands on the N=2 orchestrator (the same host Plan 05-06 Task 2a used). If the N=2 host is unreachable, halt the task and write a HALT block into VALIDATION.md per the Plan 05-06 halt pattern (commit `8b2115e`).
  </action>

  <verify>
    <automated>
      test -f .planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-04-VALIDATION.md
      grep -c 'MGPU-01' .planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-04-VALIDATION.md
      grep -c 'MGPU-02' .planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-04-VALIDATION.md
      grep -c 'MGPU-03' .planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-04-VALIDATION.md
      grep -c 'MGPU-04' .planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-04-VALIDATION.md
      grep -c 'MGPU-05' .planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-04-VALIDATION.md
      grep -c 'run_tpch_parquet.sh' .planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-04-VALIDATION.md
      grep -c 'compute-sanitizer' .planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-04-VALIDATION.md
      grep -c 'SUPER_SIRIUS_CLEAN\|cudaGetDeviceCount' .planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-04-VALIDATION.md
      grep -c 'performance_test.py' .planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-04-VALIDATION.md
      grep -c 'Requirement | Gate | Result' .planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-04-VALIDATION.md
    </automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-04-VALIDATION.md` exists on disk
    - File contains all 5 requirement sections (MGPU-01 through MGPU-05) with evidence each
    - MGPU-02 section uses `run_tpch_parquet.sh` as the driver (grep hit) and explicitly rejects `performance_test.py` with a reference to RESEARCH.md Pitfall 1
    - MGPU-03 section shows 2 compute-sanitizer invocations with `ERROR SUMMARY: 0 errors` captured verbatim
    - MGPU-04 section references both `[mgpu_04_registration]` and `[mgpu_04_round_trip]` explicitly
    - MGPU-05 section includes the `SiriusContext: X host memory space(s)` log line + a `/proc/PID/numa_maps` spot-check
    - Summary table (§8) present with one row per requirement and Result column populated
    - File is committed to git on the current branch
  </acceptance_criteria>

  <done>
    VALIDATION.md is the single artifact a human reviewer consults for Phase-6 sign-off. All 5 requirements have evidence with explicit anchors. Any deferrals (e.g. MGPU-02 per user directive) are recorded verbatim with the quote.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human sign-off checkpoint — Phase 6 ship decision</name>
  <files>.planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-04-VALIDATION.md</files>

  <read_first>
    - .planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-04-VALIDATION.md (the evidence artifact from Task 1)
  </read_first>

  <what-built>
    Phase 6 evidence captured in `06-04-VALIDATION.md`:
    - MGPU-01 topology fail-hard + startup log + sweep grep = 0 hits in Super Sirius
    - MGPU-02 SF10 measurement on current HEAD vs Phase-5 baseline (or absolute timings + documented deferral)
    - MGPU-03 compute-sanitizer memcheck 0 errors on `[multi_gpu_foundation]` + `[integration][gpu_execution][parquet][join]`
    - MGPU-04 registration test PASS + forward-leg round-trip test PASS on N=2 host
    - MGPU-05 per-NUMA host-space count matches topology.num_numa_nodes + numa_maps evidence captured
  </what-built>

  <action>
    Pause execution. Present `06-04-VALIDATION.md` contents to the user for review. Do NOT proceed to Task 3 (SUMMARY + state updates) until the user replies with an explicit approval signal (`approved`, `halt: <reason>`, or `defer <requirement-id>: <reason>`).

    The reviewer's job: confirm that every one of the 5 Phase-6 requirement gates in the VALIDATION.md summary table shows an acceptable outcome (PASS, or a documented WARN/DEFERRED with the user's directive quoted verbatim). Sanitizer `ERROR SUMMARY` lines must read `0 errors` or explain any non-zero hits with file/line attribution.
  </action>

  <how-to-verify>
    1. Read `.planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-04-VALIDATION.md` end-to-end.
    2. Spot-check the compute-sanitizer section: `ERROR SUMMARY: 0 errors` must appear verbatim for both invocations. If any hits are present, they must have file/line attribution and not be "invalid device" / "context mismatch" on Plan 06-02 files.
    3. Verify the MGPU-02 baseline decision is documented: either (a) 3-run median within 5% of Phase-5 SF10 baseline, or (b) absolute timings only with a verbatim quote of the user directive "we don't need to run any comparisons, let's just make sure everything is working, we can optimize later".
    4. Confirm the MGPU-04 hidden round-trip test ran on N=2 hardware and passed on the forward leg; confirm the return leg is NOT exercised.
    5. Confirm the MGPU-01 sweep produced `SUPER_SIRIUS_CLEAN` and the documented `src/cuda/allocator.cu:70` is the only legacy-path hit.
    6. Confirm `/proc/PID/numa_maps` evidence shows per-NUMA annotation on pinned allocations for MGPU-05.
    7. Reply with one of:
       - `approved` — all 5 gates PASS; proceed to Task 3 (SUMMARY + state updates).
       - `halt: <reason>` — evidence insufficient or a gate failed; describe what's missing so Task 1 can be re-run.
       - `defer <requirement-id>: <reason>` — a specific gate's evidence is reused from Phase 5 or flagged for future optimization; document the deferral.
  </how-to-verify>

  <verify>
    <automated>
      # No automated gate — this is a human checkpoint. Executor must pause and surface VALIDATION.md to the user.
      test -f .planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-04-VALIDATION.md
    </automated>
  </verify>

  <acceptance_criteria>
    - User replies with one of the three resume signals (`approved`, `halt: ...`, `defer MGPU-0X: ...`)
    - User response is recorded verbatim in the Phase 6 SUMMARY (Task 3) under a "Human sign-off" section mirroring Plan 05-06's Task 2b checkpoint capture
  </acceptance_criteria>

  <done>
    User sign-off received and recorded. Task 3 proceeds only on `approved` (or `defer` with explicit scope note).
  </done>

  <resume-signal>Type "approved", "halt: &lt;reason&gt;", or "defer MGPU-0X: &lt;reason&gt;"</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write Phase 6 SUMMARY + update STATE / ROADMAP / REQUIREMENTS</name>
  <files>.planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-SUMMARY.md, .planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>

  <read_first>
    - .planning/phases/05-cucascade-backed-parquet-i-o-migration/05-SUMMARY.md (template shape to mirror)
    - $HOME/.claude/get-shit-done/templates/summary.md (frontmatter schema)
    - .planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-04-VALIDATION.md (evidence to cite)
    - .planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-01-SUMMARY.md (Plan 1 outputs)
    - .planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-02-SUMMARY.md (Plan 2 outputs)
    - .planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-03-SUMMARY.md (Plan 3 outputs)
    - .planning/STATE.md (current Phase-5 state to progress forward)
    - .planning/ROADMAP.md (Phase 6 progress row)
    - .planning/REQUIREMENTS.md (MGPU-01..05 lines)
  </read_first>

  <action>
    **A. Write 06-SUMMARY.md** following `templates/summary.md` schema. Mirror the structure of `05-SUMMARY.md`:
    - Frontmatter: `phase`, `status: COMPLETE`, `subsystem: multi-gpu-gap-closure`, `tags: [topology, device-guard, gpu-gpu-converter, per-numa-host, mgpu-01, mgpu-02, mgpu-03, mgpu-04, mgpu-05, phase-exit]`, dependency graph (`requires: [05-cucascade-backed-parquet-i-o-migration]`, `provides: [MGPU-01..05 closure + startup topology log + checked device-guard callbacks + forward-leg round-trip test]`, `affects: [Phase 7 MGPU-06 P2P direct; Phase 7 MGPU-07 adaptive scan]`), tech-stack/key-files/key-decisions, metrics (duration, started, completed), requirements-completed: `[MGPU-01, MGPU-02, MGPU-03, MGPU-04, MGPU-05]`.
    - Body sections (mirror 05-SUMMARY): Phase 6 Outcome, Requirements Satisfied table with evidence anchors to VALIDATION.md, Commits Landed, Deviations from Plan, TODO Markers (carry forward Phase-4 `TODO(MGPU-06)` at `test_downgrade_executor.cpp:813` and `TODO(MGPU-07)` at `:883` — Phase 6 does NOT remove them), Test Results (pull from VALIDATION.md), Cucascade API Usage Notes (`topology_discovery`, `numa_region_pinned_host_memory_resource` default, `convert_gpu_to_gpu` built-in consumer), Open Questions Resolved (reference RESEARCH.md §Open Questions — all 4 resolved via planning_context interpretations), Issues Encountered, Next Phase Prep (Phase 7 substrate: registered GPU<->GPU converter + device-guard convention + topology single-source-of-truth), Deferred Items table.

    **Deferred Items table** (explicit per planning_context):
    | Item | Deferred to | Anchor |
    |------|-------------|--------|
    | GPU1->GPU0 converter return-leg bug | Phase 7 (MGPU-06) | test_downgrade_executor.cpp:813 TODO(MGPU-06) |
    | P2P direct cudaMemcpyPeerAsync policy + fallback | Phase 7 (MGPU-06) | RESEARCH.md Finding 2 |
    | Adaptive scan partitioning by free memory | Phase 7 (MGPU-07) | test_downgrade_executor.cpp:883 TODO(MGPU-07) |
    | `src/cuda/allocator.cu:70` cudaGetDeviceCount | Out-of-scope v1.1 (legacy namespace duckdb) | Phase 5 SUMMARY §Deferred |
    | Phase-4 SF10 regression comparison (originally IO-10) | Future optimization | 05-06-MULTIGPU-VALIDATION.md baseline anchors |

    **B. Update STATE.md**:
    - `milestone` unchanged (`v1.1`).
    - `status: in_progress` -> `stopped_at: Completed Phase 6 — all 5 MGPU requirements closed on N=2 hardware; Phase 6 SUMMARY written; ready for Phase 7 planning (MGPU-06 P2P + MGPU-07 adaptive scan).`
    - `last_updated` bumped to today.
    - `progress.completed_phases: 3`, `progress.total_plans`: prior 11 + Phase 6 plan count, `progress.completed_plans`: same.
    - Current Position: Phase 6 COMPLETE; Plan 4 of 4.
    - Add Phase 6 Shipped State section mirroring Phase 5 Shipped State. List all 5 requirements closed with evidence pointers to VALIDATION.md.
    - Resume Pointer -> `/gsd:plan-phase 7`.
    - Performance Metrics: add Phase 6 row; update totals.
    - Decisions: add Phase 6 decisions (topology is audit-only; cucascade peer-async converter consumed as-is; MGPU-01 sweep scoped to Super Sirius; MGPU-02 baseline choice).
    - Blockers / Concerns: remove MGPU-* Phase-6 scope entries; retain MGPU-06 / MGPU-07 hardware gating for Phase 7; retain Q4 flake + per-file open/close carry-overs.

    **C. Update ROADMAP.md**:
    - Phase 6 row in the Progress table: `Plans Complete: 4/4`, `Status: Complete`, `Completed: <today>`.
    - Phase 6 plan list: change all 4 entries from `[ ]` to `[x]`.

    **D. Update REQUIREMENTS.md**:
    - `MGPU-01` through `MGPU-05`: change `[ ]` to `[x]`.
    - Traceability table: Phase 6 rows marked `Complete`.
    - Last-updated footer line bumped to today with the Phase 6 closure note.

    **E. Commit**:
    ```bash
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit \
      "docs(06): Phase 6 SUMMARY + close MGPU-01..05 on N=2 hardware" \
      --files \
      .planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-SUMMARY.md \
      .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md
    ```

    Do NOT introduce `rmm::cuda_stream_default` anywhere (not that these are code files, but HYG-02 vigilance is habit).
  </action>

  <verify>
    <automated>
      test -f .planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-SUMMARY.md
      grep -c 'requirements-completed: \[MGPU-01, MGPU-02, MGPU-03, MGPU-04, MGPU-05\]' .planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-SUMMARY.md
      grep -c 'status: COMPLETE' .planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-SUMMARY.md
      grep -c 'Phase 7' .planning/phases/06-multi-gpu-gap-closure-topology-device-safety-host-memory-gpu-gpu-converter/06-SUMMARY.md
      grep -c 'Completed Phase 6' .planning/STATE.md
      grep -c '/gsd:plan-phase 7' .planning/STATE.md
      grep -c '\[x\] \*\*MGPU-01\*\*' .planning/REQUIREMENTS.md
      grep -c '\[x\] \*\*MGPU-02\*\*' .planning/REQUIREMENTS.md
      grep -c '\[x\] \*\*MGPU-03\*\*' .planning/REQUIREMENTS.md
      grep -c '\[x\] \*\*MGPU-04\*\*' .planning/REQUIREMENTS.md
      grep -c '\[x\] \*\*MGPU-05\*\*' .planning/REQUIREMENTS.md
      grep -c 'Multi-GPU Gap Closure .* 4/4' .planning/ROADMAP.md
    </automated>
  </verify>

  <acceptance_criteria>
    - `06-SUMMARY.md` exists with frontmatter `status: COMPLETE` and `requirements-completed: [MGPU-01, MGPU-02, MGPU-03, MGPU-04, MGPU-05]`
    - SUMMARY body cites `06-04-VALIDATION.md` as the evidence anchor for each of the 5 requirements
    - STATE.md `progress.completed_phases` incremented; Resume Pointer updated to `/gsd:plan-phase 7`
    - REQUIREMENTS.md has `[x]` marks on lines for MGPU-01, MGPU-02, MGPU-03, MGPU-04, MGPU-05
    - ROADMAP.md Phase 6 row shows 4/4 plans complete; plan-list shows all 4 plans with `[x]`
    - Git commit present: `docs(06): Phase 6 SUMMARY + close MGPU-01..05 on N=2 hardware`
    - Deferred Items table references `test_downgrade_executor.cpp:813 TODO(MGPU-06)` and `:883 TODO(MGPU-07)` verbatim
  </acceptance_criteria>

  <done>
    Phase 6 SHIPS. Phase SUMMARY is the single canonical document for Phase 6 closure; STATE/ROADMAP/REQUIREMENTS reflect the new reality; next orchestrator action is `/gsd:plan-phase 7` to decompose MGPU-06 + MGPU-07.
  </done>
</task>

</tasks>

<verification>
- `06-04-VALIDATION.md` exists with evidence for all 5 requirements
- Human sign-off checkpoint cleared (Task 2)
- `06-SUMMARY.md` written following the template
- STATE.md Resume Pointer moved to `/gsd:plan-phase 7`
- ROADMAP.md Phase 6 shows 4/4 plans Complete
- REQUIREMENTS.md shows `[x]` on MGPU-01..05
- Git commit closes the phase with the standardized message shape
</verification>

<success_criteria>
- All 5 MGPU-01..05 requirements closed with evidence on the N=2 verification host, OR explicitly deferred with the user's verbatim directive cited in the SUMMARY.
- Compute-sanitizer reports 0 errors on both invocation tags (`[multi_gpu_foundation]`, `[integration][gpu_execution][parquet][join]`).
- MGPU-04 forward-leg round-trip PASSES on N=2; the return leg is untouched.
- STATE.md + ROADMAP.md + REQUIREMENTS.md are self-consistent with the new completion state; the orchestrator can run `/gsd:plan-phase 7` immediately after this plan without additional manual edits.
</success_criteria>

<output>
No new per-plan SUMMARY file (the phase SUMMARY is this plan's output). The artifact chain is:
1. `.planning/phases/06-.../06-04-VALIDATION.md` (evidence)
2. `.planning/phases/06-.../06-SUMMARY.md` (phase sign-off)
3. Updated `.planning/STATE.md` + `ROADMAP.md` + `REQUIREMENTS.md`
</output>
