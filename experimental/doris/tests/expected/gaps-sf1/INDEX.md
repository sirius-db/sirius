# Expected results: sql/gaps on tpch-sf1

Generated by `scripts/validate_tpch_results.py expected` with DuckDB 1.5.5 running
`sql/gaps/<query>.sql` over `<data>/<table>/*.parquet` (tpchgen-rs SF1 parquet; `revenue0` as in
`sql/tpch-views.sql`). One `<query>.tsv` per query (`.tsv.gz` above 256 KB): header row, then
one row per tuple, `NULL` for nulls, decimals with their scale, doubles as shortest round-trip.
Compared by `validate_tpch_results.py validate|consume` (see its docstring for the tolerance
and ordering rules); `scripts/run-tpch.sh` validates against this directory after each run.
The NULL probes use only the `nation` and `region` dimension rows, which are the same in the
SF0.1 dataset used to generate their baselines and in SF1.

| query | rows | columns |
|---|---|---|
| g13-distinct | 5 | n_regionkey |
| g13-distinct-topn | 3 | n_regionkey, n_name |
| null-not-in | 0 | n_nationkey |
| null-left-join | 2 | n_nationkey |
| null-not-exists | 1 | n_nationkey |
