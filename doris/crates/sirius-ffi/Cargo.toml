[package]
name = "sirius-ffi"
version = "0.1.0"
edition = "2021"

[dependencies]
duckdb.workspace = true
# Match duckdb's arrow version for IPC serialization (workspace uses 54).
arrow = { version = "58", features = ["ipc"] }
libloading = "0.8"
thiserror.workspace = true
tracing.workspace = true
