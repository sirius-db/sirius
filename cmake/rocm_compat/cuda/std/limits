// CCCL <cuda/std/limits> redirect for ROCm.
// Try hipDF's CCCL first via #include_next; fall back to std + namespace alias.
#ifndef SIRIUS_ROCM_COMPAT_CUDASTD_LIMITS
#define SIRIUS_ROCM_COMPAT_CUDASTD_LIMITS

#if __has_include_next(<cuda/std/limits>)
  #include_next <cuda/std/limits>
  #define CUDA_STD_REDIRECT_ALREADY_DEFINED
#else
  #include <limits>
#endif
#include "cuda/std/_fallback.hpp"
#endif
