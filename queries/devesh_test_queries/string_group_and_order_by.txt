Columns to Cache:
```
call gpu_caching("customer.c_nationkey");
call gpu_caching("nation.n_nationkey");
call gpu_caching("nation.n_comment");
```

Queries to Run:
```
call gpu_processing("SELECT n_nationkey, n_comment, n_regionkey, COUNT(*) FROM customer, nation WHERE customer.c_nationkey = nation.n_nationkey GROUP BY n_nationkey, n_comment, n_regionkey;");
```