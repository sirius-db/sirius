# feat(exchange): copy received frames into pool-backed tickets

Status: proposed Draft PR P16; not opened. This package is extracted from
`bench/sf500-2-mig-gpus` at `7610840c03f9086edfa072be72a0eb4c96e03d60`. Source commit identity is preserved below.

## Motivation and resulting behavior

Move inbound payload out of short-lived arena leases; claim sequence before copy and clean up tickets on refusal. Include receive-side replay fixes.

## Dependencies and extraction

P02, P03, P06, P08

Take receive claim/copy/cleanup changes from b00334cb (compute_node_service.rs receive handler, local_exchange.rs, engine.rs and receive docs). The multicast sink-validation hunks from that same commit belong to P17. Do not cherry-pick b00334cb wholesale into both PRs.

For dependent work use the repository's `gh-stack` workflow and `stacked/` branch
prefix. Merge bottom-up through individual PR queue entries. Cross-stack
prerequisites must be present before declaring the package ready. Do not submit a
synthetic integration base just to hide those dependencies.

## Validation and remaining gate

Fix InboundStore close/stage lifetime race. Replay must not touch a reused lease. Exercise cancel and failed push; document unadmitted ingress capacity.

The source review did not execute new GPU tests for this package. Existing test
code and commit-message claims do not count as a fresh validation of the extracted
PR head. Keep the draft until its relevant checks and review findings are resolved.

## Existing labels to apply

`memory`, `rust`, `starrocks`

## Source commits and files

### [698312a1](https://github.com/aocsa/sirius/commit/698312a111d3202f1dfda91e08a39b148e77fde1)

- `experimental/starrocks/docs/TUNABLES.md`
- `experimental/starrocks/src/compute_node_service.rs`
- `experimental/starrocks/src/engine.rs`
- `experimental/starrocks/src/fragment_executor.rs`
- `experimental/starrocks/src/local_exchange.rs`
- `rust/crates/sirius-sys/src/lib.rs`
- `rust/crates/sirius/src/lib.rs`
- `src/include/sirius_ffi.hpp`
- `src/sirius_ffi.cpp`

### [b00334cb](https://github.com/aocsa/sirius/commit/b00334cb55f257358aa94d34de1250235b0d7ab2)

- `experimental/starrocks/docs/TUNABLES.md`
- `experimental/starrocks/src/compute_node_service.rs`
- `experimental/starrocks/src/engine.rs`
- `experimental/starrocks/src/local_exchange.rs`
