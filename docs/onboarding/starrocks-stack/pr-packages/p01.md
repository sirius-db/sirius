# feat(scan): carry byte ranges through Substrait into GPU scans

Status: proposed Draft PR P01; not opened. This package is extracted from
`bench/sf500-2-mig-gpus` at `7610840c03f9086edfa072be72a0eb4c96e03d60`. Source commit identity is preserved below.

## Motivation and resulting behavior

Preserve per-file split offsets through the Rust translator, serialized plan and C++ scan registration.

## Dependencies and extraction

#1696 → #1700; #1708 translation conventions; #1697 lowering

Keep feature tests and repairs with the capability. Rebase/cherry-pick onto its actual prerequisites once available; do not include integration merge commits.

For dependent work use the repository's `gh-stack` workflow and `stacked/` branch
prefix. Merge bottom-up through individual PR queue entries. Cross-stack
prerequisites must be present before declaring the package ready. Do not submit a
synthetic integration base just to hide those dependencies.

## Validation and remaining gate

CPU ownership cases, range extraction and end-to-end disjoint-split row counts.

The source review did not execute new GPU tests for this package. Existing test
code and commit-message claims do not count as a fresh validation of the extracted
PR head. Keep the draft until its relevant checks and review findings are resolved.

## Existing labels to apply

`IO`, `starrocks`, `rust`

## Source commits and files

### [92e91adf](https://github.com/aocsa/sirius/commit/92e91adf95d951568b060e1947c43e89baf5d7f3)

- `CMakeLists.txt`
- `rust/crates/sirius/src/lib.rs`
- `src/include/planner/substrait_scan_ranges.hpp`
- `src/planner/sirius_physical_plan_generator.cpp`
- `src/planner/substrait_scan_ranges.cpp`
- `src/sirius_ffi.cpp`

### [d88265aa](https://github.com/aocsa/sirius/commit/d88265aa3c73cfa3e1af7fe1658511ef5ca4ba3a)

- `experimental/starrocks/crates/starrocks-plan-translator/src/error.rs`
- `experimental/starrocks/crates/starrocks-plan-translator/src/node_translator.rs`
- `experimental/starrocks/crates/starrocks-plan-translator/src/scan_paths.rs`
- `experimental/starrocks/crates/starrocks-plan-translator/tests/translate.rs`
