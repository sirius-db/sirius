# fix(expression): round floating decimal casts consistently

Status: proposed Draft PR P14; not opened. This package is extracted from
`bench/sf500-2-mig-gpus` at `7610840c03f9086edfa072be72a0eb4c96e03d60`. Source commit identity is preserved below.

## Motivation and resulting behavior

Match DuckDB's FP64-to-DECIMAL rounding behavior in GPU expression evaluation.

## Dependencies and extraction

C++ expression base

Keep feature tests and repairs with the capability. Rebase/cherry-pick onto its actual prerequisites once available; do not include integration merge commits.

For dependent work use the repository's `gh-stack` workflow and `stacked/` branch
prefix. Merge bottom-up through individual PR queue entries. Cross-stack
prerequisites must be present before declaring the package ready. Do not submit a
synthetic integration base just to hide those dependencies.

## Validation and remaining gate

Positive/negative halfway values, scale, overflow and GPU comparison with DuckDB.

The source review did not execute new GPU tests for this package. Existing test
code and commit-message claims do not count as a fresh validation of the extracted
PR head. Keep the draft until its relevant checks and review findings are resolved.

## Existing labels to apply

`bug`, `test`

## Source commits and files

### [c39da5eb](https://github.com/aocsa/sirius/commit/c39da5eb853f54755af81b420f50560200e4c848)

- `CMakeLists.txt`
- `src/expression_evaluator/specializations/cast.cpp`
- `src/expression_evaluator/specializations/round_to_scale.cu`
- `src/include/expression_evaluator/round_to_scale.hpp`
- `test/cpp/expression_evaluator/test_expression_evaluator.cpp`
