# fix(cn): serve concurrent PRPC requests on one connection

Status: opened as [Draft PR #1739](https://github.com/sirius-db/sirius/pull/1739) (P20). This package is extracted from
`bench/sf500-2-mig-gpus` at `7610840c03f9086edfa072be72a0eb4c96e03d60`. Source commit identity is preserved below.

## Motivation and resulting behavior

Avoid serialized per-connection request handling that blocks unrelated RPC responses.

## Dependencies and extraction

CN BRPC server and generated service baseline; independent of P04 peer client. P19 failure/cancel scenarios provide useful integration coverage.

Extract the source commits on the actual prerequisites; keep tests with the feature. Use gh-stack for dependent work.

## Validation and remaining gate

Out-of-order completion with correct correlation, framing integrity and disconnect handling.

GPU/runtime tests were not rerun for this extraction proposal. Keep Draft until the relevant checks and review gates pass.

## Existing labels to apply

`rust`, `starrocks`, `perf`

## Source commits and files

### [5e71059c](https://github.com/aocsa/sirius/commit/5e71059cf0817285dfe01dcc3f77c62f10055c1b)

- `experimental/starrocks/src/brpc.rs`
